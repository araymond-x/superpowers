# Deviations Register

> Auto-maintained by controller during subagent-driven-development execution.
> Review all entries before merge.

| Task | Type | Description | Disposition |
|------|------|-------------|-------------|
| Ingestion | ToolFalsePositive | `controller-checkpoint.py` pre-execution `source_contracts` check FAILs on `Source Contracts: None`. The `writing-plans` skill REQUIRES this field present, and `validate-plan.py` (authoritative plan-time gate) PASSES on `None`. Documented false positive (CLAUDE.md "Hook Development Gotchas"). Not fixed here: out of Task 1's write-scope (archive-aware lookups only) and would break the writing-plans contract. Tool-improvement candidate: treat `None`/empty as valid when a plan legitimately has no source contracts. | Accepted |
| Ingestion | ToolFalsePositive | `controller-checkpoint.py` pre-execution `stale_artifacts` WARNING fired on the freshly-created `deviations.md` template (mistakes the template's section boilerplate for prior-session content). Workspace verified clean before ingestion (no `reports/`, `deviations.md`, or `.sdd-session.json` existed). Tool-improvement candidate: ignore empty-template content in the stale scan. | Accepted |
| Task 0 | IndependentDecision | Implementer rewrote the hook's header exit-codes comment (was "0 — Always (advisory ..., never blocks)") to document the new 0=allow/2=block codes + bypass. Beyond the literal Step 4 text but required: leaving the old comment would be a false claim after promotion to blocking. Guardrail-mandated (no-false-claims). | Accepted |
| Task 0 | IndependentDecision | Implementer removed the now-orphaned `CONTEXT_MSG` and `ENCODED` variables when replacing the advisory heredoc block. Beyond the literal Step 4 text but required by guardrail #5 / architectural-principles "dead code must be removed." Verified clean (`grep CONTEXT_MSG\|ENCODED` returns nothing). | Accepted |
| Task 0 | ScopeChange | C1 (Critical, found by quality review + controller-verified): the plan-specified hook still used the pre-existing `grep '"role":"user"' \| grep -qiE` pipe under `set -o pipefail`, which SIGPIPEs on transcripts >~64KB and made the PROMOTED hook FAIL TO BLOCK in every real session (verified exit 0 at 200KB/1MB/4MB). De-piped via here-string (`grep -qiE "P" <<< "$USER_LINES"`); now blocks (exit 2) at 200KB/1MB/4MB. Beyond the plan's literal steps but required to achieve the plan's goal ("promote to blocking"). Pre-existing bug → also affects main's advisory hook (inform/BACKLOG, not patched here, out of scope). +1 >64KB regression test. | Accepted |
| Task 0 | ScopeChange | I1 (Important, found by quality review + controller-verified, USER-APPROVED): the plan's exact regex `(invoke\|use\|...)\b.{0,20}(subagent-driven-development\|sdd)` — which the plan claimed was "verified to discriminate correctly" — false-blocks `reuse the sdd module`/`misuse sdd here`/`assddata` (no `\b` before verb group / after sdd group). User chose to tighten: `\b(...)\b.{0,20}\b(...)\b`. Diverges from a plan-specified, "verified"-claimed regex; surfaced to user per CLAUDE.md before changing. Semantic false-positives (e.g. "run the sdd tests") remain an inherent, documented heuristic limitation (bypass env var recovers). +3 false-positive tests. | Accepted |
| Task 1 | ProcessNote | The Task 1 implementer subagent completed the work and committed (d8cf7e9) but its final report message was lost to an API socket error (12 tool calls had executed). Controller reconstructed `task-001-implementer-report.md` from the verified committed diff + an independent clean test run (46 passed). Spec/quality reviews dispatched independently against the code, so the lost self-report does not weaken the review chain. No code impact. | Accepted |
| Task 2 | Inform | By-design cross-task pairing (flagged by Task 2 implementer): the N3a Check 4c skip-guard (`PREV < MANIFEST_TASK_START` → skip) intentionally delegates module-boundary provenance re-verification to `transition-module.py:validate_module_completion` (Task 3). Controller is tracking the pairing — Task 3 (next) implements the sibling transition-time enforcement (N3b). Not a divergence; this is the plan's intended split (spec D1=M2). | Accepted |
| Task 2 | Inform | Quality review (APPROVED-WITH-MINOR) flagged that the N3a skip-guard comment (`sdd-pre-dispatch-hook.sh:509-510`) claims `validate_module_completion` re-verifies boundary provenance, which is NOT yet true at fe52b67 (current transition-module.py checks report files only). Controller decision: KEEP the comment — it forward-references the N3b design that Task 3 implements; the reviewer's suggested reword would become wrong after Task 3. Task 3 dispatch instructed to confirm the comment's claim holds post-change; Task 4 SSOT test pins the hook↔transition agreement. | Resolved (Task 3 004ba75 added provenance checks to validate_module_completion — comment now accurate) |
| Task 3 | IndependentDecision | Implementer strengthened `test_blocks_when_provenance_missing` from the plan's single `"not provenance-logged"` assertion into TWO assertions naming both the spec-review and quality-review error messages, with empirically-verified discrimination (removing the quality `elif` fails only the quality assertion). Additive, per CLAUDE.md "Test Coverage as a Deliverable"; the plan's single assertion would have left the new quality-provenance branch untested. | Accepted |
| Task 3 | ProcessNote | Implementer noted the dispatch's RED-count framing was imprecise: at RED the failing set was test_blocks_when_provenance_missing + test_verification_task_exempt_from_reviews + the modified test_manifest_updated_after_transition (N11 assertion); test_minimum_tier_file_waives_quality_provenance passed at RED because pre-change code already accepted the minimum-tier file and didn't yet enforce quality provenance. No code/test altered to accommodate; RED→GREEN substance matched expectations. No deliverable impact. | Accepted |
| Task 3 | FollowUp | Spec review flagged a latent gate divergence: `validate_module_completion` gates provenance on `process_requirements.{spec,quality}_review_mode != "skip"`, while the hook gates on `enforcement.dispatch_provenance`. For micro+modules (dispatch_provenance=False but modes=self_review≠skip), transition would require provenance the hook never wrote (over-enforcement). Only reachable in micro+modules (which validate-plan.py already WARNs against). Task 3 is spec-compliant (spec MANDATED `!= "skip"`). Follow-up candidate (NOT this feature): align transition gate with enforcement.dispatch_provenance, or document the intent. To be added to BACKLOG in Task 6. | Accepted (tracked follow-up) |
| Task 3 | Inform | E2E integration suite (sdd-e2e-test.sh) is RED at HEAD by DESIGN after N3b: e2e Step 4 doesn't yet write dispatch-log provenance, so the new transition validation correctly fails. Task 5 (next-but-one) adds provenance to e2e Step 4, restoring green. Expected per plan:106. Pre-completion full-suite check must run AFTER Task 5. | Accepted |
| Task 4 | ToolFix | Implementer found the plan's verbatim Task 4 test code is un-runnable: it passes nonexistent subdirs `tmp_path/"hook"` + `tmp_path/"trans"` to the two drivers, which `git init` BEFORE mkdir-ing them → all 4 cases errored with FileNotFoundError during setup. Fixed minimally by adding `(tmp_path/"hook").mkdir()` + `(tmp_path/"trans").mkdir()` at the top of `test_minimum_signal_agreement`. Load-bearing semantics (both needles, truth table, decision comparison) byte-identical to plan; driver bodies pristine. 4/4 pass; test-only commit db7e25f. | Accepted |
| Task 4 | FollowUp | Plan-hygiene: the canonical plan.md Task 4 snippet is un-runnable as-written (missing the two mkdir lines above); the partner verified the needles but did not run the test pre-dispatch. The SHIPPED test is correct (4/4). Follow-up: correct the plan snippet / note it so a future re-run from plan.md doesn't re-hit FileNotFoundError. To be noted in BACKLOG (Task 6). Not ship-blocking. | Accepted (tracked follow-up) |
| Task 5 | ProcessNote | Implementer authored report frontmatter to match the actual ImplementerReport Pydantic model (list-of-objects files_changed/contract_compliance, integer test counts) rather than the dispatch's looser prose; validate-report.py COMPLETE. Also appended the worktree-required `Co-Authored-By` commit trailer per CLAUDE.md (subject line = exact dispatch text). Both benign; e2e GREEN at 11 steps; commit 82e344f e2e-only. | Accepted |
| Task 6 | IndependentDecision | N10 had no pre-existing BACKLOG row (feature-internal ID, spec→plan without an open row). Implementer added it as a DONE row mirroring N11, with a parenthetical audit-trail note. Reasonable — keeps the BACKLOG complete. | Accepted |
| Task 6 | IndependentDecision | Manifest "Test Suites" table rows left stale by design (351 unit / 8 steps — two features old, each carries its own narrative). Per the ADD-don't-rewrite guardrail, implementer added a real-count note (405/11) above the table rather than rewriting it; authoritative running counts live in CLAUDE.md "Testing". Flagged so the staleness isn't mistaken for an oversight. | Accepted |
| Task 6 | ProcessNote | Implementer's first draft pasted the PRE-I1 regex into CLAUDE.md while annotating it as I1-tightened (self-contradictory + divergent from the manifest). Caught mid-task (its own advisor), fixed by reading the hook source and using the verbatim `\b(...)\b.{0,20}\b(...)\b` pattern. Controller verified CLAUDE.md:296 now matches sdd-skill-enforcement-hook.sh:76 exactly — no false claim shipped. | Accepted |
| Task 7 | ScopeChange | Task 7 (verification) surfaced a PLAN GAP: no task included a hook-baseline re-capture step, but Tasks 0+2 legitimately edited 2 hooks → `check-hooks.sh` (sha256 pinning) FAILed. Additionally baseline.txt was stale on main (5 other hooks drifted in prior merged features, verified byte-identical to main). USER chose fix-now: ran `--capture` (regenerates all 7 hashes — 2 feature + 5 deployed-reality-from-main; registration unchanged), committed baseline.txt alone (52f130f), then re-dispatched Task 7 so Check 9's `--after=<latest task=7 ts>` window excludes the baseline commit. Hook baseline now PASSES. | Accepted (user-approved) |
| Task 7 | ProcessNote | Task 7 was dispatched twice: first run returned BLOCKED (hook-baseline FAIL, above); after the baseline fix, re-dispatched (clean window). Check 9 keys on the LATEST task=7 implementer timestamp (dict-overwrite), so the earlier dispatch + the baseline commit fall outside the verification window. First-ever live exercise of the task_type:verification flow. | Accepted |
| Task 7 | ToolGap | First live verification run surfaced a MODEL GAP: `validate-report.py` / `ImplementerReport.files_changed_non_empty_for_done` (implementer_report.py:48-53) rejects a verification-task report because status is completed (DONE/DONE_WITH_CONCERNS) AND files_changed is empty — but verification tasks legitimately change ZERO files. task-007-implementer-report.md is honest (`files_changed: []` is correct) and was NOT gamed with a fake file. Not a mechanical blocker: task-007 is the last task (no hook Check 4b validates it; pre-completion runs no per-report validation). Pipeline-flexibility introduced verification tasks but never updated the report model. Out of THIS feature's scope → BACKLOG N16. Fix candidate: add task_type to ImplementerReport + exempt verification, or relax the rule when files_changed empty + tests.result PASS. | Accepted (tracked follow-up — BACKLOG N16) |
| Task 4 | ToolFix | Verbatim plan test code passed nonexistent subdirs (`tmp_path / "hook"`, `tmp_path / "trans"`) to drivers that `git init` their root BEFORE mkdir-ing it (`setup_manifest_workspace` helper:425; transition driver line 81). pytest creates only the base `tmp_path`, so all 4 cases errored with `FileNotFoundError` in setup, before any hook↔transition comparison ran. Per architectural-principles "satisfy the gate's structural requirement AND log the fix": added two `(tmp_path / "hook").mkdir()` / `(tmp_path / "trans").mkdir()` lines at the top of `test_minimum_signal_agreement` — touches NONE of the load-bearing semantics (both needles, the truth table, the decision comparison are byte-identical to plan). Both driver bodies kept pristine. After fix: all 4 cases PASS, including (False,False) which proves the transition driver's hand-built manifest loads and reaches `validate_module_completion`. Upstream candidate: the plan's verbatim Task 4 code needs the same two mkdir lines (partner verified the needles but the test was never run pre-dispatch). | Accepted (test-only setup fix) |

## Deferred Work
[Items deferred from plan scope]

## Independent Decisions
[Decisions made by subagents without plan guidance]

## Scope Changes
[Requirements that changed during execution]
