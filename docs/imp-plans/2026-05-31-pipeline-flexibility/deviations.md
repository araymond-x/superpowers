# Deviations Register

> Auto-maintained by controller during subagent-driven-development execution.
> Review all entries before merge.

| Task | Type | Description | Disposition |
|------|------|-------------|-------------|
| Ingestion | ScopeChange | Parent `plan.md` frontmatter had `tasks: []` (tasks defined only in module files). `materialize-manifest.py` computes `total_tasks=len(parent.tasks)` and hard-fails at 0, so it rejected the plan. Aggregated all 10 task declarations (id/title/module_id/depends_on/pattern_references/review_tier) from the module files into the parent frontmatter, mirroring them verbatim. Matches the established multi-module convention (`2026-05-17-adaptive-enforcement-tiers`, `2026-05-28-sdd-hook-improvements` both list full task arrays in the parent). No semantic change — task IDs/titles/deps identical to module files; manifest now reports `total_tasks=10`. | Accepted |
| Ingestion | IndependentDecision | `controller-checkpoint.py --phase pre-execution` reports `source_contracts: FAIL` ("section present but contains 'None'"). Documented known false positive (project CLAUDE.md, Hook Development Gotchas): the check cannot distinguish a legitimately empty Source Contracts (no Task 0, no external interface to verify — correct for this extension feature; spec-distilled.md supplies Contract Constraints, not a verifiable interface) from a malformed section. `validate-plan.py` passes with 0 FAIL; plan-validation-gate already passed. Accepted per documented guidance; not reworking reviewed plan prose to satisfy a tool false-positive. Tool-improvement note: the pre-execution check should treat prose "None" as valid-absent (or read frontmatter `source_contracts`) — candidate finding for the Task 10 SSOT audit / BACKLOG. | Accepted |

| Ingestion | ScopeChange | **Task renumbering 1-10 → 0-9 (user-approved).** The running pre-dispatch hook (main checkout) assumes the first task is Task 0 (exempt via `TASK_NUMBER -gt 0`); its Check 4c (dispatch provenance) is intentionally outside the first-task skip, so a plan starting at Task 1 was BLOCKED looking for a non-existent "Task 0" review. Escalated to user → chose renumber. Applied via section-aware script (frontmatter task `id`/`depends_on`/module `task_ids`; body `Task N`/headers/write-scope), preserving module ids (1/2/3) and fixture numbers (91-95). Mapping: 1→0, 2→1, 3→2, 4→3, 5→4, 6→5, 7→6, 8→7, 9→8, 10→9. Modules now: M1 [0,1], M2 [2,3,4,5], M3 [6,7,8,9]. Manifest re-materialized (task_range [0,1], total 10). Hook probe confirms new Task 0 dispatches with only a checkpoint requirement. **Also a finding for the Task 9 SSOT audit: the hook cannot accept a plan that legitimately starts at Task 1 (no Source Contracts → no Task 0).** | Accepted |

| Task 1 | IndependentDecision | Implementer note (flagged by validate-report as non-empty Deviations): the Edit tool matched the shared 3-line assertion snippet 3× while inserting the new test class; implementer re-anchored on the unique `test_full_tier_never_warns` body to insert after the correct final occurrence. Pure tooling mechanics — delivered code matches the plan verbatim (function @379, call site @655, 5 tests), no functional change. | Accepted |

| Module 1→2 | ScopeChange | **Module transition done via MANUAL manifest advance, NOT `transition-module.py`.** Two reasons, both verified: (1) `transition-module.py` Step 5 truncates the live dispatch log — which makes the next module's first-task **Check 4c** (dispatch provenance for the *previous* task) BLOCK, since the genuinely-dispatched prior-task reviews vanish from the live log (the current 3-stage hook's module-boundary Check 4c is a new, untested path — both prior multi-module features ran against older hooks). (2) `transition-module.py` Step 3 archives the completed module's reports to `archive-<module>/`, but the pre-completion gate is NOT archive-aware (known pending follow-up), so they'd read as missing at the final gate. **Manual advance:** active_module 1→2, task_range [0,1]→[2,5], midpoint→4, context_summary_at→4, completed_modules+=["Model and Validation"]. Reports kept in `reports/` (not archived); dispatch log kept intact (provenance preserved — empirically confirmed Task 2 hook probe no longer Check-4c-blocks). Both quirks (log-truncation breaks next-module first-task Check 4c; non-archive-aware pre-completion) are findings for the Task 9 SSOT audit. | Accepted |

| Task 2 | IndependentDecision | Implementer added an explicit `[ "$IS_IMPLEMENTER" = true ]` guard to the Stage-2 implementer-logging condition (plan snippet used only `[ -n "$TASK_NUMBER" ]`). Behaviorally equivalent today (`TASK_NUMBER` is only set by Stage-2 implementer detection; reviewers exit at Stage 1), but more self-documenting and robust against future reordering. Verified by reviews. | Accepted |
| Task 2 | IndependentDecision | Implementer merged plan Steps 3 and 5 into a single Edit; resolution block still placed after the `get_task_type` definition (the plan's required bash function-before-call placement). Same final file layout, no semantic difference. | Accepted |
| Task 2 | DeferredWork | `CURRENT_TASK_TYPE`/`PREV_TASK_TYPE` are defined in the hook but not yet consumed — Task 3 wires them into verification-aware check skipping. Dead-but-harmless until Task 3 lands (next task). Expected per module sequencing. | Accepted |

| Task 2 | IndependentDecision | Quality-review Minor accepted as-is: `get_task_type` comment (hook line 255) says it returns "implementation or verification" but actually returns the raw YAML `task_type` string (not a closed set). Cosmetic; Task 3's only callers compare strictly against `"verification"`, so the closed-set assumption holds in practice. Not worth a churn commit — reword if the hook is touched again. | Accepted |

| Task 3 | IndependentDecision | Two quality-review Minors accepted as-is (documentation niceties, reviewer approved merge): (1) `test_previous_verification_skips_review_reports` intentionally retains the `partner-review task=1` log line (Check 4c ignores it) — an inline comment could prevent future over-pruning; (2) the verification-before-tier-gate ordering (Check 5d/4c) is deliberate (verification exemption is tier-independent) but uncommented. Both are correct as implemented; comments deferred to next hook edit. | Accepted |

| Task 4 | IndependentDecision | Quality-review single-source-of-truth recommendation, dispositioned **Accepted + tracked** (NOT done now): `_verification_task_ids` mirrors `_declared_minimum_task_ids` (near-verbatim frontmatter parser). Reviewer recommends extracting `_task_ids_where(plan_contents, field, value) -> (set, parsed_any)`. Controller deferred because (a) the plan deliberately specified mirroring `_declared_minimum_task_ids`, (b) the copy is faithful + both ratio paths fully tested, (c) extracting requires refactoring the existing tested `_declared_minimum_task_ids` (out of Task 4 scope, with a `parsed_any` consumer @976) mid-execution. Low-risk (~20 lines). **BACKLOG candidate for Task 9 / flagged to user for a call.** Also noted (harmless): bool ids slip past `isinstance(int)` (intersection with `### Task N` headers filters them; identical to the mirrored helper). | Accepted |

| Task 5 | Resolved | Quality-review Minors applied via focused fix subagent: **#1** thread the resolved `git_root=_gr` into the git-reality helper call in manifest mode (the `git_root` param existed + tests exercised it, but the production caller didn't pass it → `git log` ran in CWD; harmless today since SDD runs from repo root, but a real robustness gap); **#2** provenance comment linking the reader regex to its writer (`sdd-pre-dispatch-hook.sh` ~191/194) — single-source-of-truth across the Python reader / Bash writer. Tests 380 pass, regression 145/0/3; amended onto Task 5 commit (97302b4 → **13cf2e1**). Minors #3 (inclusive window boundaries — rare 1s over-attribution, fails toward review) + #4 (`test_clean_window_passes` isolation not strictly load-bearing for its window; the detection test IS isolation-guarded) accepted note-only. | Resolved |

| Module 2→3 | ScopeChange | Module transition via MANUAL manifest advance (same approach + rationale as Module 1→2): active_module 2→3, task_range [2,5]→[6,9], midpoint→8, context_summary_at→8, completed_modules+=["Enforcement"]. Reports kept in `reports/`, dispatch log intact (preserves Task 5 review provenance for Task 6's Check 4c — empirically confirmed by hook probe). Module 3 tasks 6-9 are all `review_tier: minimum` → reduced ceremony: spec review still dispatched (required for Check 4c provenance), partner + quality reviews controller-written minimum-tier. | Accepted |

| Task 6 | Resolved | The verbatim plan Context block removed the literal substring "invoked directly", which the regression **F6 check** (`validate-all-skills.py:569`) greps for to assert standalone-invocation guidance → caused a regression FAIL. Implementer made a minimal phrasing fix ("Invoke this skill directly with a spec" → "This skill can be invoked directly with a spec") to satisfy F6 while preserving the prescribed meaning; restored 145 PASS / 0 FAIL. Honors architectural-principles "automated gate FAILs are never expected — fix the input." Only the wording of the prescribed block changed. | Resolved |
| Task 6 | IndependentDecision | Concern flagged (advisory): the F6 regression check keys on a hardcoded literal substring (`"invoked directly"`/`"skipping brainstorming"`) rather than semantic intent — brittle; a future edit to the writing-plans Context block that drops "invoked directly" will silently FAIL F6. Candidate for the Task 9 SSOT audit / BACKLOG. | Accepted |

| Task 9 | IndependentDecision | SSOT audit DONE_WITH_CONCERNS — two open structural gaps surfaced (findings, not deliverable defects; captured as BACKLOG rows N3-N9): (A) `transition-module.py` truncates the live dispatch log → next-module first-task Check 4c blocks (the manual manifest-advance workaround used this execution stands until N3 ships); the pre-completion gate is not archive-aware (N4). (B) the pre-completion gate is enforced only by the UNREGISTERED `sdd-stop-hook.sh` + controller discipline → effectively advisory at the live PreToolUse layer. Audit result is healthier than the C6 finding implied: 8 prescriptions, 0 retire / 1 strengthen / 7 keep, 0 live threshold drift. All tracked in BACKLOG for Sprint 3. | Accepted |
| Task 9 | IndependentDecision | Implementer made small additional BACKLOG.md edits beyond the literal new-row list (ID legend, I2 note, recommended-sequencing, Sources) to keep the living ledger internally consistent now that B6/P1/N2 are done. Presentational; in keeping with BACKLOG's stated living-ledger purpose. | Accepted |

| Task 9 | IndependentDecision | Spec-review ADVISORY citation imprecisions in the audit doc, accepted (substance verified-correct; precise values recorded in `task-009-spec-review.md`): (1) N5 cites "validate-plan.py L58" — the fence-blind regex is at validate-plan.py:48 (L58 is controller-checkpoint.py's identical `TASK_HEADER_PATTERN`); (2) the ≤50% minimum-tier anchor "§336" is dispatch guidance, not the ratio (which lives in controller-checkpoint.py @1234, hedged in the doc). Trivial line/anchor pairings in a Sprint-3 findings doc; conclusions sound. Not worth a churn commit. | Accepted |

| Honesty | Uncertainty | **The feature's end-to-end verification-task flow has never run in a LIVE SDD session.** Running enforcement hooks resolve to the MAIN checkout (no `task_type` yet); this feature modified the WORKTREE copies. So "dispatch a verification task → hook skips reviews → checkpoint ratio/git-reality fire" is verified by unit tests (against the worktree hook/checkpoint) + e2e validate-plan steps, but NOT by a live verification-task SDD session. First live exercise = first real post-merge use. Inherent to the self-referential setup. **User disposition (2026-05-31): Accept** — unit+e2e coverage is sufficient; the live verification-task run is the natural first post-merge use (the running hook can't see `task_type` until merged). Structural gaps N3/N4/no-Task-0 also accepted as deferred to Sprint 3 (BACKLOG). | Accepted |

| Pre-completion | IndependentDecision | Pre-completion checkpoint `all_tasks_have_reports` FAILed — DEFINITIVELY the **N5 fence-blind `TASK_HEADER_PATTERN`** false-positive: it counts 4 fixture `### Task 91/93/94/95` headers INSIDE fenced code blocks (module-1 validate-plan fixture @202; module-3 e2e fixtures @247/250/296 — all verified `in_fence=True`) as real plan tasks and demands non-existent reports for them. The GENUINE requirement is met: all 10 real tasks (0-9) have implementer reports ≥50 bytes (verified), and `all_reports_complete` PASSED. All OTHER pre-completion checks PASS (checkboxes, no-pending, honesty, trace audit, minimum-tier ratios, verification_ratio, verification_git_reality). Accepted as a confirmed tool false-positive; root cause = BACKLOG N5 (fence-aware TASK_HEADER_PATTERN), deferred to Sprint 3 per user. The methodology Pre-Completion Gate (all real tasks have reports) is satisfied. | Accepted |

| Final review | Resolved | Final whole-feature code review = **Ready to merge: Yes** (all 4 test layers green; dispatch-log writer/reader contract airtight + test-pinned; implementation-task-unchanged invariant proven by positive control; all cross-task wiring WIRED; contract trace PASS). Two Minors: **#1 APPLIED** — corrected the N5 finding in the audit doc + BACKLOG to name BOTH fence-blind regex sites (`TASK_HEADER_RE` @validate-plan.py:48 + `TASK_HEADER_PATTERN` @controller-checkpoint.py:58) and add the `all_tasks_have_reports` manifestation (committed). **#2 ACCEPTED** — `entry_mode` is write-only by design (informational audit-trail field, explicitly out-of-manifest per spec; satisfies `extra="forbid"` by being declared) — not an orphaned wire. Reviewer confirmed N5's runtime effect is benign (inflated denominator fails toward PASS, never false-FAIL; numerator reads only top frontmatter). | Resolved |

## Deferred Work
- **Extract `_task_ids_where(plan_contents, field, value)` shared helper** to collapse the duplicated frontmatter-parsing in `_declared_minimum_task_ids` + `_verification_task_ids` (controller-checkpoint.py). Single-source-of-truth cleanup; flagged by Task 4 quality review; ~20 lines; behavior-preserving (both ratio paths have test coverage). Candidate for BACKLOG.md via Task 9.

## Independent Decisions
[Decisions made by subagents without plan guidance]

## Scope Changes
[Requirements that changed during execution]
