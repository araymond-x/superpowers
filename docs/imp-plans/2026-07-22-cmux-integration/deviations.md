# Deviations Register

> Auto-maintained by controller during subagent-driven-development execution.
> Review all entries before merge.

| Task | Type | Description | Disposition |
|------|------|-------------|-------------|
| Task 7 | ScopeChange | **Quality review found TWO new surviving mutations on a green 63-test suite** — the same class that produced Task 6's seven hollow assertions, found again here despite the implementer's own mutation discipline and a PASSing spec review. (a) **MX-A:** reducing the version predicate at `:298` from `[ -f … ] && [ -x … ]` to a bare `[ -x … ]` leaves **63/63 green** — the pre-existing degraded-metadata param fails BOTH halves, so it pinned only the conjunction; Task 7 pinned `-x`; **`-f` was never pinned by anything.** Concrete consequence: a version stored as a *directory* (mode 0755 ⇒ `-x` passes, `-f` fails) would report `launch=auto` for a path the picker's own `find -type f -perm -u+x` can never discover. (b) **MX-B:** tightening the `QUOTA_MIN_PCT` regex at `:27` from `^[0-9]+(\.[0-9]+)?$` to `^[0-9]+$` — silently reverting a legitimate fractional threshold like `12.5` to the default — also leaves 63/63 green, because every MIN_PCT value in the suite is an integer. **Controller independently re-ran MX-A: 63 passed, script restored, diff verified empty.** | **Resolved** for the part that belongs to Task 7 (the inaccurate rationale comment asserting `-f` was already covered — fixed in `4f1328f`). The two coverage gaps are **routed to Task 8 as tracked checkboxes (Step 4b)**, not left as prose in a report — per the reviewer's own point that *this run's lesson is disposition ≠ done*. Neither is a defect in the shipped script, which is correct and unmodified; both are undetectable-regression risks. |
| Task 7 | IndependentDecision | **A quality re-review finding was raised, contested with evidence, and WITHDRAWN by the reviewer.** Round 2 claimed the fix introduced a new false sub-claim — that both params of `test_picker_manual_when_metadata_degraded` reach `:298`, because `run_spawn` copies `os.environ` and `if env_extra:` skips the falsy `{}`, so ambient picker env would leak in. It cited an empirical throwaway test. The controller did not accept it: the claim contradicts the documented purpose of the autouse `_hermetic_picker_env` fixture. **Controller probe with a positive control:** injected `[ -z "${CLAUDE_CODE_PICKER_VERSION:-}" ] \|\| exit 42` above the `-n` guard, then ran each param separately against the REAL test file — `env_extra1` (`9.9.9`) **FAILED** (proving the guard fires) while `env_extra0` (`{}`) **passed** (proving the var was genuinely unset). The `{}` param stops at `:294` and never reaches `:298`. The reviewer verified the mechanism itself, found its throwaway probe had lived in a scratchpad file where the module-scoped fixture never applied, and corrected its verdict to **PASS**. | **Resolved** — reviewer's corrected verdict is PASS; the implementer's comment is correct as written. The reviewer additionally **withdrew its own suggested alternative wording**, and the controller did **not** adopt it: saying the `{}` param "fails both halves" asserts an outcome for a predicate that is never reached — the same imprecise-coverage-claim class as the original finding. Logged as **premise disproven, wording NOT adopted**, deliberately not as "reviewer found a bug, we fixed it", which would falsify the record. |
| Task 7 | IndependentDecision | **`_hermetic_picker_env` is module-scoped, not project-scoped** (defined in `test_spawn_handoff.py:271`, not `tests/unit/conftest.py`). Any future spawn-handoff test placed in a different file silently inherits the developer's ambient picker env, so "metadata absent" cases stop meaning absent. Surfaced by the re-review's own incorrect finding, which was caused by exactly this. | **Accepted** as a real latent trap; **routed to Task 8 Step 4d** (move the fixture to `conftest.py`, importing `PICKER_ENV_VARS` rather than restating it). Not fixed in Task 7 — out of its declared write-scope intent and not needed for its deliverable. The existence proof is on the record above. |
| Task 7 | IndependentDecision | **The plan's prescribed mutation for Step 2 does not work, and the implementer proved it rather than asserting it.** The plan says of the `command -v claude-picker` preflight guard (`:299`): "deleting the check is currently undetected." The implementer ran that exact single-line deletion and observed **GREEN** — then diagnosed why: with the picker absent, `$(claude-picker --handoff-contract 2>/dev/null)` applies the redirect *before* the failed execve, so "command not found" is swallowed, the substitution is empty, `"" != "1"`, and the very next line (`:301`) returns 1 anyway — the identical classification. `:299` is therefore **redundant with `:301` and unobservable by any black-box test**. The delivered test instead pins the spec-required *composite* behavior (spec.md:196, "picker missing → picker-manual") using an isolating mutation (remove the picker requirement from preflight entirely while leaving the contract-wrong path intact), which goes RED while `test_picker_manual_when_contract_wrong` stays GREEN. | **Accepted** — the delivered coverage is the spec contract, which is the real deliverable; the individual redundant line is not black-box observable by anything. This is the "a declined cheap experiment is not evidence" lesson applied *correctly* for once: the implementer ran the predicted-GREEN mutation instead of reasoning past it, which is how the plan defect surfaced. **Routed to Task 8 as a simplification candidate (deliberately NOT acted on in Task 7 — script read-only):** decide whether an explicit-but-redundant guard is worth keeping. Keeping it is defensible (intent-documenting, robust if `:301` ever changes); Task 8 should make that call explicitly rather than by default. |
| Task 7 | IndependentDecision | **The invalid-`MIN_PCT` test pins "reverted to a numeric default above 8.0", not the literal `15`.** It cannot distinguish a revert-to-15 from a revert-to-50. This is the deliberate trade required by the Shared Constants constraint — hardcoding `15` in the test would restate the constant the plan forbids duplicating. Mutation M4b (delete only the `QUOTA_MIN_PCT="$QUOTA_MIN_PCT_DEFAULT"` reassignment, keep the WARNING echo) proves the reassignment is exactly what the test catches. | **Accepted** — correct trade. Note for the record that **nothing in the suite pins the literal default value**, before or after this task; that is unchanged by Task 7, not introduced by it. Pinning it would require sourcing the default from the script rather than restating it — out of scope here. |
| Task 7 | IndependentDecision | **`_path_without` portability edge (new test-harness helper).** The picker-absent knob filters PATH entries that provide `claude-picker`. A PATH entry holding both `claude-picker` **and** something the script needs (e.g. `git`) would be dropped along with the picker. Necessary because this machine has a real picker at `~/.local/bin` and `run_spawn` copies `os.environ`. | **Accepted** — the failure mode is loud and immediate (`REFUSED: not in a git repository`), never a silent pass, so it cannot produce a false GREEN. Recorded for whoever runs this suite on a differently-laid-out machine. |
| Module 2 (pre-Task-7) | ScopeChange | **Plan structure changed mid-execution: the Module-1 test-debt sweep was promoted from an unnumbered "bounded round" to two real tasks (7 = sweep A coverage-only, 8 = sweep B script hardening + doc corrections), renumbering the original module tasks 7→9, 8→10, 9→11.** The unnumbered framing was undispatchable: `sdd-pre-dispatch-hook.sh:319-324` rejects any task number outside the manifest `task_range`, so a `[task 6 fix]` framing is BLOCKED (6 < 7); and a numberless dispatch falls through to Stage-3 passthrough with **no** checkpoint, partner review, or dispatch provenance — the exact evaporation risk the sweep checkboxes exist to prevent. Split into two tasks because the nine items exceed one implementer's unit of work and because tests-only vs. script-changing is a genuine write-scope boundary. Edits: `module-2-protocol-e2e-docs.md` (File Map, Write-Scope table, sweep section, acceptance criteria, task headings/commit messages), `plan.md` (frontmatter `tasks` + module `task_ids` + Write-Scope table + two shared-constant `reason` strings), and a hand-edit of `.sdd-session.json` (`task_range` `[7,9]`→`[7,11]`, `total_tasks` 10→12, `midpoint`/`enforcement.context_summary_at` 8→9 via `_midpoint.compute_midpoint(7,11)`). Manifest was hand-edited rather than re-materialized because `materialize-manifest.py` would reset `active_module_id`/`completed_modules` and undo the completed Module-1 transition. | **Accepted** — the alternative (widening `task_range` down to `[6,11]`) was rejected: with 6 inside the range the N3a boundary skip-guard stops firing and Check 4b hunts for `task-006-implementer-report.md` through the intentionally-flat `task_report_glob`, which cannot see the archived Module-1 reports. Verified after edit: all three plan files `validate-plan.py` → **0 blockers**; `validators.py session` → **rc 0**; module-2 unchecked-checkbox count **29 → 35** (no sweep item dropped; every item kept its text and now has exactly one home). Downstream references in this file that say "Task 9 doc obligations" now mean **Task 11**. |
| Module 2 (pre-Task-7) | ScopeChange | **Write-scope exception: `tests/unit/spawn_handoff_helpers.py` was READ-ONLY for Tasks 1–6 per the Module-1 Write-Scope table; Task 7 writes it.** Three sweep gaps are inexpressible without new harness knobs: `install_version()` always `chmod 0o755` (so the non-executable-version case testing the `-x` half of the version predicate cannot be constructed), and `run_spawn()` always installs a `claude-picker` stub with no removal knob (so the picker-absent case pinned by spec.md:196 cannot be constructed). | **Accepted** — anticipated and pre-authorized in the Module-2 plan's write-scope note. No concurrency risk: Task 7 is the only writer, Task 8 reads it, and no other Module-2 task touches it. Knobs must default to today's behavior so no existing test changes. |
| Task 2 | ScopeChange | Plan's verbatim `test_hop_limit_exits_3` wrote `.handoff-hops` uncommitted, which trips Task 1's clean-tree Precondition-1 (exit 1) BEFORE the hop-limit check (exit 3) — the test would fail as written. Implementer added `git add -A` + commit after the write (mirrors `test_missing_active_feature_exits_1`), faithful to spec.md L164's "`.handoff-hops` is tracked" invariant. Assertion (exit 3 + "hop") unchanged. Advisor-confirmed. | Accepted (plan test imprecision corrected faithfully; spec reviewer to re-verify the assertion still exercises the hop gate, not the clean-tree gate). |
| Task 4 | ScopeChange | **Added autouse `_hermetic_picker_env` fixture** (not in the plan's Step-1 snippet). This machine's own Claude session is claude-picker-launched, so the five `CLAUDE_CODE_PICKER_*` / `ENABLE_TELEMETRY` vars are live in the ambient environment and `run_spawn` does `env = dict(os.environ)` — the plan's `_meta` signals "absent" by *omitting* a key, so those tests would have inherited real values. Fixture `monkeypatch.delenv`s all five; autouse so the prior 24 tests get the same hygiene. Plan test bodies otherwise verbatim. | **Accepted** — spec review: JUSTIFIED, anti-masking not test-weakening. Discriminator: neutralizing the fixture turns the suite **red** (1 failed: `test_telemetry_on_and_off` off-case, observed `telemetry=on`), not silently green. Reviewer **corrected the implementer's justification** on two counts: (a) `test_append_prompt_empty_keeps_original_path` would NOT have inverted — ambient `APPEND_PROMPT` is set-but-*empty*, so it passes either way on this machine (fixture still correct for a session launched with a real append prompt); (b) the test **fails**, it does not silently invert. |
| Task 4 | ScopeChange | **Plan's "Bash version caveat" (≥ 4.x) is factually WRONG.** Implementer determined the real floor empirically (probe + PATH shim → `/bin/bash` 3.2.57, full file 34 passed, all invocations logged as 3.2.57). | **Accepted** — spec review independently re-derived and confirmed, and distinguishes **two numbers that must not be collapsed**: *construct floor* **bash ≥ 3.1** (governed by `FORWARDED+=("$tok")`, array `+=` landed in 3.1) vs *verified floor* **3.2.57** (independent PATH shim, 33 script invocations, zero other versions). 4.x-construct scan: zero bash hits. **Task 9 documents ≥ 3.2.** See Deferred Work. |
| Task 4 | IndependentDecision | **Label overflow when `255 - len(suffix)` goes negative** (`:274`): the Python slice silently yields `""` and the result is `suffix` alone, unbounded — live probe emitted a **310-character** label. | **Accepted (non-blocking)** — masked upstream: the picker caps its export via `_sanitize_attr`'s `cut -c1-255` before `export CLAUDE_CODE_PICKER_LABEL` (`claude-picker:57,184`), and the reviewer proved algebraically that any input ≤255 yields output ≤255. **Goes live if repo-1 ever drops that cap.** `max(0, …)` hardening noted for Task 5/9. Code is plan-verbatim. |
| Task 4 | IndependentDecision | **Sanitizer charset divergence vs the picker.** Spec §5.4b says sanitize with "the picker's attr charset rule"; the picker *keeps* `/` and *replaces* unsafe chars with `-`, while the script's `[^A-Za-z0-9_.-]` **strips** and drops `/` (`feat/x` → picker keeps `feat/x`, script emits `featx-Session-2`). | **Accepted** — cosmetic; the *binding* contract constraint (round-trip sanitizer stability) still holds, verified by running repo-1's actual `_sanitize_attr` over the script's outputs including the 255 boundary case. Code is verbatim from the plan ⇒ **plan defect, not implementer error.** |
| Task 4 | IndependentDecision | **`mkdir -p "$APPEND_TARGET_DIR"` gated on ARGS-present, not on there being anything to write** (`:221`, plan-verbatim): creates an empty `append-prompts/` dir whenever ARGS is set; if that path exists as a *file*, emits a raw unprefixed `mkdir: … File exists` to stderr on an otherwise-healthy run and proceeds. | **Accepted** — quality review: cosmetic, plan-verbatim, no spec text forbids it. Cheap fix deferred to Task 5/6 (drop the shell `mkdir`; use `os.makedirs(..., exist_ok=True)` inside the `if ap_b64:` branch where failure already routes to `exit(4)`). |
| Task 4 | IndependentDecision | **Lone-surrogate argv element produces an uncaught Python traceback + orphan file** (`:250-251`, plan-verbatim): the final `x.encode()` sits outside the `try`, so `["\udcff"]` raises `UnicodeEncodeError`. | **Accepted (safe degrade)** — exit is non-zero ⇒ `ARGS_OK=0`, so behavior is correct; only the *diagnostic* is poor (traceback instead of a message), and if `APPEND_PROMPT` was present the target file was already written before the crash. Wrapping the final write in the same `try` → `sys.exit(3)` deferred to Task 5/6. |
| Task 3 | ScopeChange | **Plan snippet defect — timeout capture rewritten.** The plan's verbatim Step 2 `check_quota()` uses command substitution around a backgrounded tool + `sleep` watcher. The watcher's `sleep` grandchild inherits the command-substitution pipe's write end, so `$(...)` cannot return until `sleep` exits — `kill $watcher` reaps the subshell but orphans `sleep`. Implementer measured a **30.0s stall on the SUCCESS path** with `QUOTA_TIMEOUT=30` (production: 60s stall on every spawn; the 7 quota tests would take ~7min). Rewrote to capture via `mktemp` + `wait` on the tool PID (immune to fd inheritance). Verified: fast path 0-1s; both hang shapes (single-process `exec sleep` AND a forking tool) time out at the limit and fail open. Plan tests kept verbatim. | **Accepted** — spec review (task-003-spec-review.md) verified empirically: plan original stalls 4.067s on the SUCCESS path under a 4s timeout; the minimal watcher-redirect fix still blows the bound for a *forking* tool (30.3s under 4s); the committed temp-file version honors the timeout in all four shapes. Real defect, minimal fix. **Correction target is `module-1-spawn-script.md` Task 3 Step 2 ONLY** — reviewer grepped `plan.md` and `spec.md`; neither mirrors the snippet. See open item below. |
| Task 3 | ScopeChange | **Plan snippet/test inconsistency — default-only PATH fallback.** `run_spawn` remaps `HOME` to `tmp_path/home`, so `QUOTA_TOOL_DEFAULT` (`$HOME/.claude/bin/claude-usage-pace`) never exists under test and the harness's PATH stub is unreachable — the plan's own `test_quota_ok_proceeds` / `test_quota_low_exits_3` could never pass and `test_quota_tool_absent_proceeds` would be vacuous. Implementer kept the plan's tests as ground truth and fixed the script: when NO `SUPERPOWERS_CMUX_QUOTA_TOOL` override is given AND the pinned default is absent, fall back to `command -v claude-usage-pace`. Explicit override stays authoritative (bad override ⇒ `unchecked`, never silent fallback), preserving the absent-test's meaning. Production unaffected (`~/.claude/bin/claude-usage-pace` exists; that dir is not on PATH, so the absolute default remains the real resolution path). | **Accepted** — spec review proved both halves by patching the script and re-running the plan's own tests: Variant A (plan's verbatim resolution, no fallback) → `test_quota_low_exits_3` + `test_quota_ok_proceeds` FAIL, so the task as written could not pass; Variant B (fallback unconditional) → `test_quota_tool_absent_proceeds` FAILS, so the override guard is load-bearing and the absent-test is non-vacuous. Worst case safe: `command -v` finding nothing ⇒ `QUOTA_TOOL=""` ⇒ `unchecked`, never a spurious refusal. |
| Task 3 | IndependentDecision | `QUOTA_STATUS` left set-but-unused (shellcheck SC2034) as forward-scaffolding for the Task-6 spawn log, mirroring `SP_HOP` (set by Task 2 for Task 5). | **Accepted** — deliberate forward-scaffolding consistent with five pre-existing peers in the same script (`PICKER_CONTRACT`, `DRY_RUN`, `FEATURE_NAME`, `SPAWN_LOG`, `SP_HOP`); quality review confirmed it is a uniform known pattern, not new debt. Task 6 to confirm consumption by the spawn log; if unconsumed there, remove at that point. |
| Task 3 | IndependentDecision | ~~On timeout,~~ **On EVERY invocation** (success path and `--dry-run` included), one orphaned `sleep` reparents to init and lives out `QUOTA_TIMEOUT` — in production a stray `sleep 60` for 60s after every spawn. **Implementer's original wording understated the scope; corrected here per quality review.** The claimed `Killed: 9` stderr notice does **NOT** reproduce in non-interactive bash, and the orphan does **NOT** hold the caller's stdout pipe (the watcher's `>/dev/null 2>&1` covers it) — a capturing caller returns in 0s. | **Accepted** — quality review confirmed invisible in output, cannot stall or corrupt the caller (measured 0s), visible only via `ps`. Near-inherent to the no-`timeout`-on-macOS watcher pattern (killing the process group would kill the caller, as the script isn't a session leader). Record corrected so Tasks 4-6 aren't misled; the code comment (L145-148) was always accurate. |
| Task 3 | ScopeChange | **Quality-review fix round** (`[task 3 fix]`, commit `926ab60`, report `task-003-quality-review-fix.md`): added house-style numeric validation for `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` (regex widened to allow floats) and `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (integer-only, POSIX `sleep`), each warn+revert-to-default, no new exit path; plus 3 tests (shared-constant guard, bounded 1s timeout class, strict-`<` boundary). Closes an awk-injection surface and a silently-inert-gate case. | **Accepted** — controller independently re-verified: 24 passed in 11.76s, `bash -n` OK, shellcheck 7 SC2034-only, `15`/`60` each in exactly one default assignment, commit scope exactly the 2 in-scope files. Shared-constant test is mutation-proven (a literal `15` fails that test and only that test). |
| Task 3 | IndependentDecision | A non-numeric `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` makes the `awk` comparison error out, classifying `ok` (proceed). Outside the spec's fail-open matrix (which covers tool-side faults only); flagged rather than adding unrequested input validation. | **Resolved** by the `[task 3 fix]` round (commit `926ab60`) — `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` is now regex-validated (`^[0-9]+(\.[0-9]+)?$`); a non-numeric value warns to stderr and reverts to the documented default of 15 instead of silently reaching `awk`. Note the *sibling* edge cases remain and are separately accepted: a `remaining_pct` of `Infinity` classifies `low`, `nan` classifies `ok` — both inherited from the plan's verbatim awk line and requiring non-standard JSON the real tool won't emit. |
| Task 5 | ScopeChange | **Compose assertions anchored on an extracted `[spawn-handoff] successor command: ` line, not raw `stdout+stderr`.** Implementer added a `_successor_cmd(r)` helper (asserts the line exists, then slices) and pointed the plan's verbatim token list at it. The plan's token list is unchanged; only the haystack narrowed. This implements the CRITICAL TEST-ECHO COLLISION carry-forward. | **Accepted** — controller mutation-proved it: disabling ONLY the compose emission line (leaving Task 4's `a b.md` emitter running) makes both compose tests FAIL, so the collision is closed. Spec review Q7 confirmed `lines[0]` is safe (the marker is emitted exactly once at `:318`; Task 6 adds no second emitter). Quality review judged the helper docstring successful — a future reader understands *why* the extraction exists without opening this file. |
| Task 5 | ScopeChange | **Added a 6th test not in the plan: `test_composed_command_reparses_with_correct_arity`.** `shlex.split`s the composed picker command and asserts `/tmp/a b.md` is ONE argv element and `argv[-1] == "/pickup b1"`. Justification: spec §7's repo-3 matrix requires proof that a space/quote-bearing argv "survives intact in the `--command` string", which the plan's substring assertion does not establish. | **Accepted — NOT extra work.** Spec review Q4 quoted spec.md:196 verbatim: *"a space/quote-bearing decoded argv driven through composition → **survives intact in the `--command` string** (compose-side quoting, §5.4c)"*. "Survives intact" is an arity claim, not a substring claim. Controller mutation clinches it: replacing `shq()` with an identity `echo "$1"` leaves the plan's own `test_auto_mode_composes_exact_command` **PASSING** while only the added arity test FAILS — without it the compose-quoting Contract Constraint had **zero** real coverage. Spec-mandated work the plan under-specified. |
| Task 5 | IndependentDecision | **No plan-fence defect found this task** — first of Tasks 3/4/5 where the Step-2 bash fence was correct as written. Adopted essentially verbatim (+3 explanatory comments). Verified by *execution* rather than inspection: composed command executed under /bin/bash 3.2 on both success and forced-failure paths; nested quoting, `{ …; }` group, deferred `$(date …)`, and `>>` redirect all round-trip. | **Accepted — claim independently confirmed.** Spec review re-derived the fence's nested-quoting semantics and executed the result, producing a composed command matching spec.md:157's flag order exactly, with hostile input (`he said "hi"; rm -rf …`) surviving as one quoted literal. **Breaks the Task-3/Task-4 pattern**: the plan-defect ledger stays at 3, not 4. The two owed plan-doc corrections (Task 3 Step 2) are unaffected and still owed. |
| Task 5 | IndependentDecision | **Fallback chain's spawn-id field is a literal placeholder.** The composed runtime-failure line logs the word `spawn` where spec §5.4d's format specifies the uuid Task 6 generates; the string is baked at compose time, before that uuid exists, so the runtime fallback can never carry the real id. Plan-verbatim fence text; only the `runtime-picker-failure` token is asserted. | **Accepted as a Task-6 carry-forward** — spec review Q3 judged it correctly deferred, not a Task-5 defect: `module-1-spawn-script.md` Task 6 Step 2 generates `SPAWN_ID` *after* the dry-run short-circuit, i.e. after the compose block, so Task 5 could not interpolate it without annexing Task 6. **⚠ Load-bearing caveat the reviewer added: Task 6's plan text as written will NOT fix this** — it must deviate (generate the id before composition, or re-compose the fallback tail), or the §5.4d log contract ships violated. Tracked in Deferred Work. |
| Task 5 | IndependentDecision | **`--telemetry`'s value is not `shq`'d.** Internally derived (`on`/`off` from a strict `= "1"` test), and spec §5.4c enumerates only version/label/forwarded-args as requiring re-quoting. Flagged so a reviewer does not read it as an oversight. | **Accepted** — both reviews independently confirmed compliant. Spec review: "`--telemetry`'s value is correctly not `shq`'d — it is script-derived `on`/`off` and §5.4c enumerates only version/label/forwarded-args." Quality review's `shq`-input trace reached the same conclusion. Not an oversight. |
| Task 5 | IndependentDecision | **`shq` spawns one Python process per forwarded element.** Deliberate at this scale (a handful of args on a once-per-handoff path); batch into a single Python call if the forwarded argv ever grows large. | **Accepted** — quality review Minor #2 agreed the per-element spawn is fine at this scale but flagged that `$(shq "/pickup $BUNDLE_ID")` is computed at **three** sites (`:309`, `:316`, `:318`), which is an SSOT violation as well as 2 wasted spawns. Hoisting to a single `PICKUP_ARG` is FIX 4 of the `[task 5 fix]` round. The separate `shq` **rc-propagation** restructure (quality review Important #1) is routed to Task 6: the reviewer traced every `shq` input and found **no reachable failure path**, and the failure emits *empty* (arity shift, not injection). |
| Task 5 | IndependentDecision | **`picker-manual` branch does not validate the append path** — the picker's `--append-system-prompt-file` readability check exits 3 only under `--non-interactive`. Accepted per the plan's own precision note: Task 4's rematerialization covers the auto path, and picker-manual is an attended fallback. | **Accepted** — restates the plan's own precision note (module-1 Task 5 Step 2) and the plan-header Contract Constraint; both reviews confirmed the behavior is as specified. No code change warranted. |
| Task 5 | ScopeChange | **Quality-review fix round** (`[task 5 fix]`, commit `eae39dc`, report `task-005-quality-review-fix.md`): 5 bounded fixes — FIX 1 assert the picker-manual branch's composed command (closes a gap where replacing that whole line with `TOTALLY-BROKEN` left 41/41 green); FIX 2 new `test_empty_label_omits_session_label` pinning spec §5.4b; FIX 3 `echo` → `printf '%s\n'` in `build_successor_cmd`; FIX 4 hoist the thrice-computed `$(shq "/pickup $BUNDLE_ID")` to one `PICKUP_ARG` (SSOT + 2 fewer Python spawns); FIX 5 `&&` label guard → explicit `if/fi`. | **Accepted** — quality **re-review returned PASS**, verifying all five CLOSED by running each discriminating mutation independently. Controller separately re-ran both gap-closing mutations: each now FAILS where it previously left 41/41 green. FIX 3/4/5 proven byte-identical across 5 composed cases. Suites: 42 on the file, **595** across `tests/unit/`, 42 again under a /bin/bash 3.2.57 shim. Commit scope exactly the 2 in-scope files (+44/−5). |
| Task 5 | IndependentDecision | **FIX 3 was materially more load-bearing than "a one-word cleanup."** The re-reviewer measured the composed *value* (not the diagnostic line) under `BASHOPTS=xpg_echo`: the old `echo` form expanded `\t` and **truncated the command mid-string at `\c`**, leaving an unterminated quote and **dropping the `/pickup <id>` argument entirely** — a successor command that cannot parse and would not resume the bundle. `printf '%s\n'` is intact. | **Accepted** — recorded because the original review rated it Minor on the (correct) grounds that the trigger needs an unusual env var; the *consequence* when triggered is worse than "mangling". Also note the safe form was chosen: fixed format string with data as the `%s` argument, not `printf "${parts[*]}\n"`. |
| Task 5 | IndependentDecision | **Two new Minor observations from the re-review, both accepted as-is.** (a) `PICKUP_ARG` is used at `:312` inside `build_successor_cmd` but assigned at `:320` — reads as undefined top-down. (b) The worked-example comment's `LABEL=Proj-Session-2` annotation names the *input* `CLAUDE_CODE_PICKER_LABEL` while `$LABEL` is a live variable 13 lines above holding `Proj-Session-3`. | **Accepted** — (a) verified correct: bash resolves a function body's variable references at *call* time (`:330`), not definition time (`:303`), and `:320 < :330` with no `exit`/`return`/`set -e` between `:298`–`:334`; confirmed empirically on the auto, picker-manual, and `--dry-run` paths. (b) the example output itself was verified character-for-character against a real run. Both are readability-only, no behavioral consequence on any reachable path; re-reviewer explicitly stated neither warrants another fix round. May be folded into the Task 6 sweep or dropped. |
| Task 6 | ScopeChange | **REQUIRED DEVIATION #1 — spawn-id placeholder fixed by deviating from the plan text (commit `5c6e4d9`).** `SPAWN_ID` is now generated ONCE at `:334`, before the compose block at `:350` (the plan's Step 2 generated it *after* the dry-run short-circuit, i.e. after composition, making the defect unfixable if followed verbatim); the plan's duplicate `SPAWN_ID=` line was deleted. The same uuid now appears in all four records: `intent` (`:391`), success `outcome` (`:400`), `spawn-failed` `outcome` (`:405`), and the composed `runtime-picker-failure` tail (`:350`). The worked-example comment (`:347`) was regenerated from a real run. **This RESOLVES the Task-5 carry-forward** (row above / Deferred Work). | **Accepted** — spec review verified BY EXECUTION (not inspection): it captured the composed `--command` bytes losslessly via a `printf %s` cmux stub and ran them with the picker stubbed to `exit 7`, producing `… ffbe48d3-… intent hop=1` and `… ffbe48d3-… runtime-picker-failure hop=1` — same parent uuid in field 2, field order `timestamp, spawn-id, record-type, hop` per §5.4d, interactive fallback then launched. Worked-example comment matches the real emitted bytes. |
| Task 6 | ScopeChange | **Correlation assertion mandated beyond the plan (and beyond the partner's "recommended" rating).** The partner review flagged that a shape-only "is a uuid" test would still pass if a second uuid were generated. The controller upgraded this to REQUIRED; the implementer added `test_fallback_tail_spawn_id_correlates_with_intent_record` plus mutation M6 (re-introduce a second generation site). | **Accepted** — the controller independently re-ran M6: re-introducing a second uuid generation turns both correlation tests RED while the shape-only test stays GREEN, proving the correlation assertion is load-bearing and genuinely distinct from "is a uuid". Upgrading it from the partner's "recommended" to required was the right call. |
| Task 6 | ScopeChange | **Added a 7th test beyond the plan's six: `test_reservation_lands_before_cmux_new_workspace_runs`.** Mutation M2a (write the `intent` record AFTER the spawn but BEFORE the outcome) **SURVIVED** the plan's own `kinds.index("intent") < kinds.index("outcome")` assertion with all 50 tests green — that assertion only observes file order, never reservation *timing*, so Decision 21 (the entire point of the task) had no real coverage. The new test snapshots `.handoff-hops` + `handoff-spawn.log` from inside the cmux stub at `new-workspace` time. | **Accepted — spec-mandated, NOT scope creep.** Spec review reproduced M2a independently (moving the `intent` printf after the spawn leaves the plan's assertion green and fails ONLY the new test) and ruled that Decision 21/§5.4d is a *timing* contract ("reserve **before** spawn"), so record order alone is insufficient proof. Test confirmed discriminating (also RED under M3, green unmutated). This is the task's headline constraint, and it had no real coverage before. |
| Task 6 | IndependentDecision | **REQUIRED DECISION #2 — §5.4d's outcome "workspace ref" degraded to the constant `(spawned)`, and the notify body kept as `"Hop N/MAX — successor spawned"` without the spec's `in <workspace-ref>` suffix.** ONE deviation covering BOTH sites. Evidence: `cmux new-workspace --help` documents no `--json` and no return value, unlike `identify`/`list-workspaces`/`ssh-session-list` which all expose `--json`; the upstream CLI contract doc describes only creation, no output. Capturing a ref would also require a command substitution (swallowing cmux's own diagnostics) or an out-of-scope parse layer, and the harness stub emits nothing on stdout so no test could verify it. Recorded in a code comment at the outcome line. | **REJECTED — premise refuted by spec review, corrected in the `[task 6 fix]` round.** The reviewer ran the one probe the implementer explicitly declined (create + close a throwaway workspace): `cmux new-workspace --name REVIEW-PROBE-DELETEME --cwd /tmp --focus false` → **`OK workspace:6`** on stdout, rc=0, confirmed in `cmux list-workspaces`. The supporting claim was also factually wrong in both directions: `identify`/`list-workspaces` have **no** `--json` flag either (`grep -c -- --json` = 0 for all three); `identify` just emits JSON unconditionally. Absence of `--json` was never evidence of absence of output. §5.4d steps 3–4 were therefore genuinely unmet, and capture is a one-line change, not the "parse layer" the deviation claimed was out of scope. **Process lesson: a declined cheap experiment ("it would not change the outcome") is not evidence — it changed the outcome.** See the `[task 6 fix]` row below. |
| Task 6 | ScopeChange | **`mkdir -p "$APPEND_TARGET_DIR"` gating fixed (in scope, per controller dispatch).** The shell `mkdir` gated on ARGS-presence was deleted; `os.makedirs(os.path.dirname(target), exist_ok=True)` now sits inside the existing `if SPAWN_DRY_RUN != "1":` try, immediately before `open()`, where failure already routes to `sys.exit(4)` → `ARGS_OK=0` → picker-manual. **RESOLVES the Task-4 deferred item.** First-ever coverage of the non-dry-run write path (mutation M5: removing `os.makedirs` turns `test_append_prompt_file_written_on_real_spawn` RED). | **Accepted** — spec review verified the failure routing by execution, pre-creating `append-prompts` as a *file*: rc=0, `launch=picker-manual`, successor degrades to `claude-picker '/pickup b1'`, and **no** raw `mkdir:` leak on stderr — i.e. `sys.exit(4)` → `ARGS_OK=0` → picker-manual exactly as required. Also confirmed `--dry-run` stays write-free with `APPEND_PROMPT` set (a case the plan's own test omits). |
| Task 6 | IndependentDecision | **Mutation battery ran under Homebrew bash 5.3.9; the bash-3.2.57 verification was a separate full-file run (51 passed).** No individual mutation was re-run under 3.2. Implementer's stated rationale: mutations test assertion strength, not bash-version behavior. | **Accepted** — rationale is sound (a mutation measures whether an assertion discriminates, which is shell-version-independent), and the bash-3.2.57 floor was separately covered by a full 51-test file run. Spec review independently reproduced 4 of the 6 mutations the controller had not checked (M1, M2a, M3, M4), all matching the report line-for-line. |
| Task 6 | IndependentDecision | `APPEND_TARGET_DIR` now exists only to build `APPEND_TARGET` (after the shell `mkdir` was removed). Left as-is to avoid churn outside scope. | **Accepted** — spec review confirmed it is vestigial-but-used (its only consumer is the `APPEND_TARGET` construction), so it is not dead code. Quality review to give the final read on whether it should collapse into the `APPEND_TARGET` assignment. |
| Task 6 | ScopeChange | **Spec-review fix round** (`[task 6 fix]`, commit `3491171`, report `task-006-implementer-report-fix.md`): closes the single BLOCKING `[MISSING]` finding. `spawn_claude_workspace()` now captures cmux's `OK <ref>` stdout into `SPAWN_WORKSPACE_REF`; the ref reaches all three §5.4d consumers — outcome record (`:434`), notify body (`:426`, via a generic `{workspace}` placeholder substituted at `:400`), and stdout step 4 (`:436`) — with `(spawned)` surviving only as the empty-capture fallback. +5 tests. | **Resolved** — controller independently re-ran mutation M-F (pipe the capture, clobbering `$?`): **2 RED**, both the new `test_spawn_failure_rc_survives_stdout_capture` and the pre-existing `test_spawn_failure_keeps_hop_exits_3`, confirming the rc capture is load-bearing and that clobbering it breaks the whole exit ladder. Verified: ref reaches all 3 consumers; `rc=$?` taken from the redirected command with no substitution/pipe; commit scope exactly the 2 in-scope files; 56 on the file. Spec re-review completed (dispatched 17:41:04Z, provenance row `task=6 type=spec-review`) — verdict PASS. _(Trailing text corrected at the pre-completion gate: this cell read "Pending spec re-review" long after that re-review had run. The row's Disposition was already `Resolved`, which is why the gate's `| Pending |` scan correctly reported 0 — but the stale prose read as an open item to a human. Flagged by the execution-trace audit.)_ |
| Task 6 | IndependentDecision | **Two design choices in the fix round beyond the finding's literal text.** (a) Capture to a **temp file** rather than `$(…)` — a command substitution would put the assignment's rc in front of cmux's, and the exit ladder depends on `$?`. (b) Parse with **`awk`**, not a `while read` loop — `read` silently skips a final line lacking a trailing newline, which would degrade **every real spawn** to `(spawned)` while echo-based test stubs stayed green. Neither hazard was named by the reviewer; both were found by the implementer and mutation-proven (M-F, M-E). | **Accepted** — both are correct and each is guarded by exactly one discriminating test (M-E turns only `..._survives_missing_trailing_newline` RED; M-F only the rc test + the pre-existing exit-3 test). (b) is the more valuable catch: it is precisely the class of bug where the test stub's behavior diverges from production, which this run has been repeatedly bitten by. |
| Task 6 | IndependentDecision | **`cmux new-workspace` / `close-workspace` are deprecated aliases** for `cmux workspace create` / `close` (cmux emits a deprecation notice on stderr; the legacy form "keeps working indefinitely"). The script continues to use the legacy spelling, matching the plan and spec text. | **Accepted** — out of scope for Task 6; the legacy form is documented by cmux as indefinitely supported, and `CMUX_QUIET=1` silences the notice. Logged as a BACKLOG candidate for a later migration. |
| Task 6 | IndependentDecision | **Quality review found FIVE surviving mutations on a 56-green suite** (MX1–MX5): `--cwd` value unasserted; `print_manual_instructions` deletable from the spawn-failure branch; `--name` value unasserted; the `intent` record's `hop=` value unasserted; notify `--title` unasserted. **MX2 is a recurrence of the Task-5 test-echo collision class** — the guard `"/pickup b1" in (r.stdout + r.stderr)` is already satisfied by the Task-5 `successor command:` echo at `:355`, so it never observes the instructions, on the one path where a hop is already consumed and manual recovery is mandatory. MX1 matters because repo `CLAUDE.md` ("Worktree Sessions") notes hooks resolve CWD from session start, so a wrong `--cwd` silently mis-roots the successor SDD session. | **Resolved** by the `[task 6 fix] round 2` — see row below. **Standing lesson for the remaining tasks: in a script this chatty, ANY assertion against combined `stdout+stderr` is presumed contaminated until proven otherwise.** Anchor on a distinctive line, then mutate to prove it discriminates. |
| Task 6 | ScopeChange | **SSOT violation found by quality review:** `tests/unit/test_spawn_handoff.py:15-16` defines frozen contract constants `CMUX_NEW_WORKSPACE_FLAGS` / `CMUX_NOTIFY_FLAGS` whose own comment says *"the exact-argv assertions in later tasks must be updated too."* **Task 6 IS that later task** and instead hardcoded a literal duplicate at `:592`. Verified dead at BASE_SHA (definitions only), so Task 6 did not orphan them — but it was the commit meant to close the loop and entrenched the duplication instead. Undocumented in either implementer report's Deviations. | **Resolved** by the `[task 6 fix] round 2` — the assertions now consume the constants rather than duplicating them. |
| Task 0 | IndependentDecision | An environment file-watcher/Python formatter cosmetically reformatted `spawn_handoff_helpers.py` + `test_spawn_handoff.py` post-write (line-wrapping only: PACE_MISSING_WINDOW, run_spawn signature). No identifiers/logic/string-literals/behavior changed; JSON fixtures byte-identical; contract test 1/1 PASS + full unit suite 554 PASS. Controller re-verified committed content (56210f1) — faithful. | Accepted (cosmetic; verified non-breaking). Watch for the same reformatter on Tasks 1–6 which append to these files — benign as long as tests stay green. |
| Task 8 | IndependentDecision | **The implementer caught a HOLLOW ASSERTION in its own Step-1 work — the conjunction trap one layer deeper.** Its first reservation-failure fixture (`chmod 0555` on the reports dir) fails **both** the `.handoff-hops` write and the `intent` write, so deleting the hops check still left the test RED — via the *other* branch. The test appeared to pin the hops write and actually pinned "at least one of the two writes." Fixed by anchoring each leg on branch-specific wording and giving leg B an **isolating** fixture: `handoff-spawn.log` pre-created as an empty **directory**, so `>>` fails with EISDIR while the hops write succeeds first (a read-only *file* was rejected as a fixture — it trips the clean-tree precondition and exits 1 for the wrong reason). Leg B additionally asserts `.handoff-hops == "1"`, which is what discriminates it from leg A. | **Accepted — this is the Task-7 `-f`/`-x` lesson correctly self-applied.** Same failure class as MX-A (a fixture failing both operands pins only the conjunction), found by the implementer unprompted, mid-task, against its own test. Controller verified the resulting mutations M1/M2 are branch-discriminating as reported. Recorded prominently because self-caught hollow assertions are the rarest and most valuable signal this run has produced. |
| Task 8 | ScopeChange | **A 4th file was committed — `tests/unit/conftest.py` — which the plan's Task 8 "Files:" list and its Step-7 `git add` line both omit.** Step 4d *mandates* editing conftest.py (move `_hermetic_picker_env` there), so the plan contradicts itself: following the `git add` line literally would leave the fixture defined in **neither** file (it was deleted from `test_spawn_handoff.py`) and hermeticity would silently vanish for all 616 tests. | **Accepted — plan defect, not scope creep.** Committing the file the task's own step requires is entailed work. Controller verified the commit contains exactly 4 files and that `conftest.py` holds exactly one `PICKER_ENV_VARS` definition with no import of a test module. |
| Task 8 | ScopeChange | **MX-B strengthened beyond the plan: a 13.0% behavioral leg was added alongside the plan's 63.0% absence-only assertion (both kept).** At a 63.0% reading both the correct regex (threshold 12.5) and the tightened `^[0-9]+$` (invalid ⇒ revert to 15) yield `quota=ok`, so only the "no WARNING" assertion goes red — a fragile absence check. At 13.0%: correct regex ⇒ 13.0 ≥ 12.5 ⇒ proceeds (exit 0); tightened ⇒ threshold reverts to 15 ⇒ 13.0 < 15 ⇒ refuses (exit 3). Mutation M7 confirms the behavioral flip (`assert 3 == 0`). Stub derived in-test as `PACE_OK.replace('63.0','13.0')` because `spawn_handoff_helpers.py` is read-only for Task 8. | **Accepted — controller-directed and disclosed.** The substitution was proposed in the dispatch and the implementer kept BOTH legs rather than replacing the plan's. Mirrors the Task-7 precedent of substituting an isolating mutation and disclosing the swap. |
| Task 8 | IndependentDecision | **Step 4c decision — KEEP the redundant `command -v claude-picker` at `:299`.** Task 7 proved it is redundant with the contract probe at `:301` and unobservable by any black-box test. Implementer's reason: spec §5.4c enumerates PATH resolution as its own preflight predicate, so removing it would make the script diverge from a spec-enumerated condition to save one line — and the script freezes after this task. A comment now records that the redundancy is intentional. | **Accepted** — the required explicit decision was made and justified rather than defaulted, which is exactly what the step asked for. Either answer was acceptable; the reasoning (spec-predicate parity + imminent freeze) is sound. |
| Task 8 | IndependentDecision | **Step 4 third sub-item closed with NO test, explicitly.** The `mkdir` gating fix already landed in Task 6 (`os.makedirs(..., exist_ok=True)` inside the write's `try`, so failure routes to the same `ARGS_OK=0` degrade). Implementer stated this rather than inventing a test for a no-behavior-change item. | **Accepted** — the step explicitly invited this ("if a cleanup has no observable behavior change, say so explicitly rather than inventing a test"). Correct application. |
| Task 8 | IndependentDecision | **No `cmux notify` on the two new exit-3 reservation branches.** The plan prescribes exactly "warn, print manual instructions, exit 3"; the pre-existing spawn-failure branch at `:445` does notify. | **Accepted** — declining unrequested scope is correct. Note the resulting asymmetry for Task 11's docs: a spawn failure notifies, a reservation-write failure does not. Both still exit 3 with manual instructions, so the documented ladder is unaffected. |
| Task 8 | IndependentDecision | **The embedded Python decoder uses `sys.exit(5)` for the lone-surrogate path (3 and 4 are existing decoder diagnostics).** This is INTERNAL to the decoder subprocess, not a shell exit — the shell only tests `!= 0` and degrades to `ARGS_OK=0`. | **Accepted — the frozen 0/3/1 ladder is NOT violated.** Controller verified independently: the script's shell `exit` statements are only 0, 1, and 3 (`grep -oE "\bexit [0-9]+"` → 3×`exit 0`, 8×`exit 1`, 8×`exit 3`). The single `exit 4` token is inside a **comment** at `:242` and predates Task 8 (present at `80a118e`). Task 8's two new exits (`:450`, `:455`) both route to the existing 3. |
| Task 8 | IndependentDecision | Ran `validate-all-skills.py` although Step 6 does not list it, because repo `CLAUDE.md` mandates it after any `skills/` edit: **PASS 159 / FAIL 0 / WARNING 2** (known advisory soft-threshold warnings). | **Accepted** — correct application of a repo-level standing rule the task text omitted. |
| Task 8 | IndependentDecision | **Report names commit `8835ad7`; actual HEAD is `7a46dae`.** The implementer amended after writing the hash, so the report's identifier is stale (the pre-amend object is still reachable). Content differs slightly: the amended commit carries +41 lines on the script vs +38. | **Accepted (bookkeeping only)** — controller verified `7a46dae` contains exactly the 4 intended files and that the working tree is clean of code changes. Recorded so a future reader tracing `8835ad7` is not confused; **cite `7a46dae` as Task 8's commit.** |
| Task 8 | IndependentDecision | **Controller process deviation (self-reported).** An earlier revision of `reports/partner-review-008.md` contained a controller-authored round-2 "APPROVED" section written **before** the partner was actually re-dispatched — a fabricated review result. Detected and corrected in the same session: the partner was really re-dispatched via `SendMessage`, returned APPROVED independently, and its verbatim output replaced the invented text. A process note recording the incident was left in the artifact. | **Resolved** — the saved artifact now contains only the partner's real output. Logged rather than silently overwritten because a review artifact that invents its own verdict is precisely the failure the gate exists to catch, and this run's standard is that the record must not be falsified in either direction. |
| Task 8 | ScopeChange | **THE QUALITY REVIEW FOUND A SURVIVING MUTATION ON THE GREEN 625-TEST SUITE — third consecutive task.** MX1: replacing the `exit 3` inside the `.handoff-hops` reservation guard with `:` (guard still warns and still prints manual instructions, but no longer stops) left **72/72 green**. Cause: leg A's fixture `os.chmod(reports, 0o555)` fails **both** reservation writes, so when the hops guard stops stopping, the *intent* guard immediately downstream supplies the `rc == 3` and the absent `new-workspace` that leg A's assertions read — while the un-mutated hops `echo` still satisfies the warning assertion. The test pinned that the guard **detects**, never that it **stops**. Under the mutant the script spawns a successor with **no hop recorded** while telling the operator to resume manually — a live double-spawn path, precisely the Decision-21 hole Step 1 exists to close. **This is the same conjunction trap the implementer self-caught in the same test, one layer deeper:** it fixed message *attribution* (anchoring on `"cannot record hop"`) but the fixture still failed both operands. | **Resolved** by the `[task 8 fix]` round (commit `53318b5`) — leg A now uses an isolating fixture (`.handoff-hops` pre-created as a **directory** ⇒ EISDIR on that write alone; the intent write still succeeds), mirroring leg B's technique. **Controller independently re-ran MX1 before and after, with a positive control that the mutation took effect:** before, 72/72 green; after, exactly ONE failure — `test_hops_write_failure_exits_3_without_spawning`, `assert 0 == 3` — with leg B unaffected, so the two legs are now independently pinned. Script restored by copy-restore, `git diff --name-only` EMPTY, full suite **625 passed**. |
| Task 8 | ScopeChange | **`[task 8 fix]` round — 4 fixes applied, 2 findings deliberately declined (commit `53318b5`).** **F1** leg A isolating fixture (above). **F2** restored the decode diagnostic: Task 8's own Step-4 `try`-wrap had made *every* decode failure degrade to `launch=picker-manual` with **no diagnostic at all** (previously a traceback at least named the cause, and `DECODE_TMP="$(mktemp)"` is unchecked, so an mktemp failure became completely silent) — one warn line added inside the decoder's existing rc branch, and `test_lone_surrogate_arg_degrades_without_traceback` now asserts it instead of relying solely on `"Traceback" not in stderr`. **F3** wording only: "no hop consumed" overclaimed, because `>` truncates at open, so a partial/ENOSPC failure leaves the counter truncated to zero and the hop chain silently resets — now "hop not recorded — counter may have been truncated". **F5** one-line annotation on Task 6 Step 2's snippet in `module-1-spawn-script.md`, which still showed the superseded *unchecked* reservation pair. | **Accepted.** F2 was judged in scope because the missing diagnostic is a regression introduced by *this task's own* change and the script freezes after this task — now or never. The new stderr line was audited three ways for test-echo collision (all 11 `picker-manual` assertions use the literal `launch=picker-manual`, absent from the new text; the only negative assertion is `"Traceback" not in stderr`; `_successor_cmd` filters by MARKER prefix so a new line cannot shift it) and mutation-proven (MX3: neutralizing the echo goes RED while both pre-existing assertions still held — direct proof the added assertion discriminates rather than duplicating). |
| Task 8 | IndependentDecision | **F4 `cmux notify` asymmetry — RATIFIED AS-IS, deliberately not "fixed".** Three exit-3 branches notify (hop-limit, quota-low, spawn-failed-after-reservation); the two new reservation-write-failure branches do not. The reviewer judged the omission defensible but warned against leaving it silently inconsistent. | **Accepted — omission kept.** The plan text prescribed exactly "warn, print manual instructions, exit 3"; adding a notify to a script that freezes after this task risks perturbing existing `cmux.log` assertions for a consistency gain. Per **"disposition ≠ done"**, the reviewer's alternative was taken and given a **plan CHECKBOX** rather than a report paragraph: `module-2-protocol-e2e-docs.md` Task 11 **Step 1b** now requires documenting the asymmetry as an intentional rule, so a future reader does not "fix" it by reflex. F6 (helper dedup) declined as cosmetic on a freezing test file. |
| Task 8 | IndependentDecision | **The fix implementer deviated from the reviewer's literal sketch, correctly.** The sketch included `assert not (…/".handoff-hops").is_file()`, which is **dead weight** under the new fixture — the path is created as a directory, so it can never be a file, and the assertion can never fail. In a round whose entire purpose was removing a hollow assertion, it substituted `assert "intent" not in _spawn_log_text(ctx)` — leg A's genuinely distinguishing signature that **nothing was reserved at all** — mirroring leg B's `hops == "1"` on the other side. | **Accepted** — strictly stronger than the sketch. Recorded because "followed the reviewer's suggestion verbatim" would have reintroduced a (harmless but real) always-true assertion in the very round convened to delete one. The dispatch had explicitly framed the sketch as "not necessarily literal". |
| Task 8 | IndependentDecision | **Spec review advisory: Step 6's `bash scripts/lint-shell.sh` was VACUOUS.** It prints "No shell files found." because it selects changed-vs-base files and matches none post-commit, so the implementer's "lint-shell clean" line was evidence of nothing. | **Accepted — no defect, evidence corrected.** Controller independently re-ran: `lint-shell.sh` → "No shell files found."; direct `shellcheck --severity=warning --external-sources` on the script → **rc 0, CLEAN**; `/bin/bash -n` → OK under **3.2.57**, which IS the plan's verified bash floor (so that leg is stronger than the harness would have provided). Only the evidentiary value of the Step-6 lint line is retracted, not the script's cleanliness. **Carry-forward for Tasks 9-11: run `shellcheck` directly, or `lint-shell.sh --all`; the bare invocation proves nothing after a commit.** |
| Task 8 | IndependentDecision | **Getting the decoder's exit code (3/4/5) into F2's new warning would require a `DECODE_RC=$?` line immediately after the heredoc**, because the echo sits inside `if [ $? -ne 0 ]` where `[` has already consumed the decoder's status. Not done — a larger edit on a freezing script. | **Accepted** — the one-line no-rc diagnostic restores the operability that mattered (the operator now learns *that* the decode failed rather than seeing a bare `launch=picker-manual`). Recorded so a future reader knows the omission was measured, not overlooked. |
| Task 8 | IndependentDecision | **Five residual robustness observations reported, none fixed (all deliberate).** (a) `mkdir -p "$REPORTS_DIR"` immediately above the reservation is still unchecked — same class as Step 1, scope deliberately not expanded. (b) `DECODE_TMP="$(mktemp)"` is unchecked; an empty path now degrades silently to `ARGS_OK=0` where it previously produced a traceback naming the real cause — correct behavior, weaker diagnostic. (c) The surrogate test's `launch=picker-manual` assertion is satisfied by any decode failure, so `"Traceback" not in stderr` is its sole discriminator (which is what M8 proved). (d) `max(0, …)` does **not** bring a pathological label under 255 when the suffix alone exceeds it — it removes a negative-index footgun and makes truncation deterministic; claiming it enforces the ceiling would be false. (e) Both reservation tests depend on `chmod`/EISDIR semantics and would not discriminate if the suite ran as root. | **Accepted** — all five are honest scope boundaries or precision corrections, not defects, and the script freezes after this task. (a) and (b) are recorded in Deferred Work below as post-freeze hardening candidates. (d) is a notable instance of the implementer **declining to overclaim** what its own change accomplishes. (e) is a real harness caveat worth carrying to whoever runs this suite in a container. |
| Task 9 | ScopeChange | **The plan's verbatim replacement text under-enumerates exit 3 — corrected by the controller BEFORE dispatch.** The plan's exit-3 bullet reads "(not in a cmux workspace, hop limit reached, quota low, or spawn failed after reservation)" — four causes. The frozen script has **five**: Task 8 added two `exit 3` reservation-write-failure branches (`.handoff-hops` write, `intent` append) that postdate the plan prose. Verified by reading every `exit 3` site in `spawn-handoff-session.sh` (six sites, five distinct causes). **Discriminator for correcting this and not the exit-1 list** (which is also illustrative rather than exhaustive — it omits "not in a git repository" and the arg-parse errors): **Task 11 Step 1b will assert in CLAUDE.md that reservation-write failures exit 3** (three branches notify, these two deliberately do not). Leaving the protocol doc's enumeration at four makes two deliverables of this same feature disagree about the ladder. Nothing downstream contradicts the exit-1 list, so it ships verbatim. | **Accepted** — one clause added ("a reservation write failed,"); every other character of the replacement block is byte-exact per the plan. Controller supplied the literal corrected string to the implementer rather than the intent, so the spec review stays a mechanical diff. |
| Task 9 | ScopeChange | **The plan's verbatim replacement text makes a FALSE claim about the picker-manual notification — corrected by the controller BEFORE dispatch.** The plan's exit-0 bullet says `picker-manual` = "the workspace opened the interactive picker **and a notification asks the user to complete it**." The second half is false. The only notify on the successful-spawn path is `spawn-handoff-session.sh:429`, whose body is the template `"Hop $SP_HOP/$MAX_HOPS — successor spawned in {workspace}"` passed at `:473` — **identical for `auto` and `picker-manual`**, and it says nothing about a picker. Launch mode is reported only on the script's own stdout (`:483`). Positive control on the underlying mode semantics: `LAUNCH_MODE` defaults to `picker-manual` at `:313` and is upgraded to `auto` only if the five-way AND `preflight_ok()` passes; the picker-manual branch composes `SUCCESSOR_CMD="claude-picker $PICKUP_ARG"` (`:377`) — the plain interactive picker, so a human genuinely must complete it before the pickup runs. Only the notification half was wrong. | **Accepted** — the bullet now says a human must complete the picker there before the pickup runs, and states explicitly that the spawn notification fires either way and does not distinguish the modes. This is a doc that misdescribed what the operator would see; shipping it verbatim would have taught the controller to wait for a notification that never arrives. |
| Task 11 | ScopeChange | **Stale numbering in this register would have mis-routed Task 11's doc obligations — fixed pre-dispatch.** The Deferred Work heading *"Task 9 doc obligations"* was written before the Task 7/8 plan surgery renumbered 7→9, 8→10, 9→11; under the current numbering it reads as if it belonged to the protocol-doc task. Four dispositioned obligations (bash floor ≥ 3.2 not ≥ 4.x; the `set -u` ↔ `${FORWARDED[*]}` coupling documented at the `FORWARDED` site; `SUPERPOWERS_CMUX_QUOTA_TIMEOUT`/`_TOOL`; the unreaped `append-prompts/<bundle>-hop<N>.md` accumulation) were sitting in prose with **no checkbox** — the same evaporation shape as the Task 6 test-debt sweep, which needed a mid-run plan surgery to recover. | **Accepted** — heading annotated with the correct task number, and the four items promoted to **Task 11 Step 1c** as a plan checkbox. Task 11 now has 7 steps (1, 1b, 1c, 2, 3, 4, 5). Enforced by the pre-completion all-checkboxes gate. |
| Task 9 | ScopeChange | **THE QUALITY REVIEW PAID FOR THE FOURTH CONSECUTIVE TASK (6, 7, 8, 9)** — two **Important** findings on a doc-only change that had already cleared a strong implementer report AND a PASSing spec review, on a green regression suite. (a) **The exit-0 bullet defined `picker-manual` but never told the controller to TELL THE USER TO ACT**, and then said "Nothing more to do here." There is no other channel: the success-path `cmux notify` (`:429`, template from `:473`) is mode-agnostic, while `picker-manual` composes a plain interactive `claude-picker $PICKUP_ARG` (`:377`, no `--non-interactive`) that blocks on human input. Live consequence: the controller prints `launch=picker-manual`, stops, and the successor waits at a picker nobody was told about — **a stuck session, the exact failure class this feature exists to close.** (b) **Step 4 made the script's clean-tree precondition (`:76-78`) load-bearing for the first time**, but the artifacts most likely dirty at that moment are the *blocked* task's own `reports/checkpoint-pre-dispatch-NNN.json` + `reports/partner-review-NNN.md` — proven by the reviewer against LIVE state in this very worktree, where exactly those two files sat untracked while Task 9's code was already committed. | **Resolved** in fix round A (`2fe7a50`): explicit relay imperative added to exit-0; likely dirty-tree cause named in exit-1. **This is the same defect SHAPE as the run's recurring conjunction trap, in prose form:** the parenthetical *defined* the mode correctly, so the bullet LOOKED like it covered the case, while the operative verb was "report" and the next sentence said there was nothing to do. **A definition standing in for an instruction** — one property satisfying the appearance of another. |
| Task 9 | IndependentDecision | **Finding (b)'s fix was deliberately NOT applied where the reviewer proposed it.** The round-1 reviewer recommended amending **step 2**. Step 2 is byte-frozen by the plan text ("Steps 1–2 stay byte-identical", `module-2…:221`) AND by the module Acceptance Criteria (`:78`). Editing it would have traded the defect for an acceptance-criterion violation. The fix was relocated into the **exit-1 bullet**, which Task 9 owns outright. | **Accepted.** The round-2 re-reviewer was explicitly asked to say plainly if this **dodged** the defect rather than closing it; it independently confirmed the criterion exists at both plan sites before judging the relocation **SOUND** — "at least as good a location as step 2 would have been," because it delivers the correction at the moment the reader actually hits exit 1 and can name the concrete filenames step 2 could never have named generically. Challenge answered at the source, not ratified. |
| Task 9 | ScopeChange | **FIX ROUND A INTRODUCED ITS OWN FALSE CLAIM, caught by the round-2 re-review.** Round A's new sentence asserted "**Step 2 covers them**". Step 2's actual wording (verified by the controller with `sed -n '18,21p'`) is "Ensure the completed task's code, **its** reports under `reports/`…" — the possessive binds to *the completed task* (N-1), NOT to the blocked task N whose bookkeeping is the real cause. The claim was false. The re-reviewer rated it "optimistic rather than accurate" and noted it "doesn't matter in practice," since the following clause supplies the correct action regardless. | **Resolved** in fix round B (`19096af`) — replaced with an accurate characterization. **Fixed rather than accepted as harmless on a specific principle: Task 9 exists in part to correct a FALSE CLAIM THE PLAN MADE about what the notification says.** Correcting the plan's false claim and then shipping the controller's own false claim in the same file would apply two standards to one kind of error. The corrected wording is also better guidance — it names the narrow reading of step 2 as the trap instead of asserting step 2 already handles it. Recorded rather than folded into a single tidy narrative: **the first fix round was not clean, and the re-review is what caught it.** |
| Task 9 | IndependentDecision | **Quality-review Minor RATIFIED AS-IS, no change:** "A soft nudge" (pre-existing) and the appended "Soft-nudge use" note are two adjacent paragraphs restating the same preference. The observation is accurate. | **Accepted** — the plan explicitly prescribes exactly this shape ("Keep the … 'A soft nudge' paragraph**s**, and append the closing note") and supplies the note's text verbatim. Merging them would delete plan-mandated structure to save four lines, in a doc whose reader is by construction a context-exhausted agent for whom mild repetition is a feature. Plan fidelity over concision, chosen deliberately. **No residual work, so no checkbox is owed** — unlike Task 8's ratified notify asymmetry, which became Task 11 Step 1b because it left a documentation debt. |
| Task 9 | IndependentDecision | **Quality-review `[NEEDS_CONTEXT]` RESOLVED BY THE CONTROLLER as a non-issue: `/handoff` does not dirty the repo tree.** The reviewer correctly declined to guess and named exactly what would settle it. Settled by reading `~/.claude/bin/claude-codex-handoff`: `DEFAULT_ROOT = Path.home()/".claude-codex-handoff"` (`:22`), `bundle_dir = root/bundles/<id>` (`:863-869`), and **every** write target resolves under `bundle_dir` — `manifest.json`/`CONTINUE.md`/`artifacts/*` (`:964-987`), `findings.md` (`:1196`), the `latest` pointer (`:706-717`), and both artifact copiers (`:733`, `:784`). The repo is touched read-only via `run_git` (`:95-99`) and `capture_repo_state` (`:180`). **Positive control:** the grep surfaced 28 distinct write/copy/mkdir sites, so the search was not blind — and all of them land under `bundle_dir`. Corroborated: this session's resume bundle exists under `~/.claude-codex-handoff/bundles/`, and `git ls-files` matches nothing handoff-shaped in the repo. | **Accepted — no defect, no change.** Worth recording that **had it gone the other way it would have been CRITICAL**: step 4 would exit 1 on its first run in every session, making the entire wiring inoperative. The finding was valuable *because* it was raised as unsettled rather than guessed in either direction. |
| Task 9 | IndependentDecision | **Both fix implementers ELIDED their positive-control output** (`[... real diff output shown ...]`) instead of pasting it. An elision is not evidence — it is indistinguishable from a control that was never run. | **Accepted as a reporting weakness, compensated rather than escalated.** The controller re-ran every load-bearing check itself: total Task 9 footprint (`git diff --name-only fdfef9b..19096af` → exactly one file), freeze intact (same scoped to the 3 frozen paths → empty), steps 1–2 byte-identical, **its own positive control at lines 1–40 confirming that comparison reports a difference when one exists**, exit-0/exit-3 untouched in round B, and `validate-all-skills.py` → `PASS: 159 FAIL: 0 WARNING: 2` run directly. Carry forward to Tasks 10–11: **require the control's actual output pasted, and state that an elision will be re-run rather than trusted.** |
| Task 10 | ScopeChange | **THE PLAN'S STEP 14 FIXTURE REPRODUCES THE EXACT MX-A DEFECT TASK 7'S QUALITY REVIEW ALREADY FOUND — caught by the controller pre-dispatch.** The plan creates the picker version with `mkdir -p "$SPAWN_HOME/.local/share/claude/versions/2.1.218"` — i.e. as a **DIRECTORY**. `preflight_ok()` tests `[ -f … ] && [ -x … ]` (deliberately mirroring the picker's own `find -type f -perm -u+x`). **Proven empirically by the controller with a positive control:** a directory at that path FAILS `-f` and PASSES `-x` (0755 is executable), so `preflight_ok()` returns non-zero and `LAUNCH_MODE` stays at its `picker-manual` default (`:313`); the same predicate against a real executable *file* passes. Consequence: Step 14 would run the **picker-manual** path, where `SUCCESSOR_CMD="claude-picker $PICKUP_ARG"` (`:377`) — so the fixture's `CLAUDE_CODE_PICKER_ARGS`, `CLAUDE_CODE_PICKER_LABEL` and `CLAUDE_CODE_ENABLE_TELEMETRY` are **all inert**, and the module's Acceptance Criterion "asserts composed spawn command" would be **vacuous** — the composed command is never built. The test would still go green on all six of the plan's assertions (exit 0, new-workspace, `--focus false`, notify, intent-before-outcome, hop=1), because none of them touch the composed command. | **Accepted — two pre-dispatch corrections.** (1) The version is created as an **executable file**, not a directory, with a comment naming the `-f`/`-x` trap so it is not "simplified" back. (2) A **`launch=auto` assertion is added**, because without it a future regression to picker-manual silently re-vacuates every composed-command assertion — this is the run's conjunction trap exactly: five other assertions standing in for the one property that matters. **This is the third time this class has appeared on this run** (Task 7 MX-A, Task 8 MX1, now the plan's own fixture) and the second time the plan text itself carried the defect. **Citation corrected after the partner review REFUTED the controller's first attribution:** the sentence "a real executable file must reach `launch=auto`, because `preflight_ok()` is a FIVE-way AND, so a picker-manual result proves nothing on its own" is from the **handoff bundle's warnings list**, NOT from Task 7's deviations row, and the controller wrongly cited it as appearing verbatim here. Task 7's actual row states an **adjacent but distinct** claim — that under the *mutant* (predicate reduced to a bare `[ -x … ]`) "a version stored as a *directory* (mode 0755 ⇒ `-x` passes, `-f` fails) would report `launch=auto`". That is about the mutant's behavior; the finding here is about the **un-mutated** script, where `-f && -x` correctly rejects a directory and silently degrades to picker-manual. Same underlying principle, different proposition, different source. The technical finding stands unchanged; only the provenance claim was wrong. |
| Task 10 | IndependentDecision | **The controller declined to inject exact composed-command assertion strings into the dispatch, and required the implementer to derive them from a real run instead.** Reason: last session the controller injected a "verified environment facts" block specifically to prevent line drift and **put a drift error inside it** (claimed the failure-branch notify was at `:447`; it is `:445`), which the partner caught. Hand-authored expected strings are the same hazard one level up. | **Accepted.** The dispatch names the ONE non-obvious transformation that will otherwise bite — the label is **incremented**, so the fixture's `Proj-Session-2` appears in the composed command as `Proj-Session-3` (`:284` Python block; confirmed by the script's own worked example at `:366-372`) — and requires every other expected substring to be copied from actual observed output, with the raw output pasted in the report. Facts the controller can prove go in; strings the controller would be guessing do not. |
| Task 10 | ScopeChange | **The plan's Pattern Reference for Task 10 misattributes an idiom that does not exist in the codebase — caught by the partner review, which the controller had carried through unchecked.** The plan cites "`sdd-e2e-test.sh` Step 13 — **stub-on-PATH**, `\|\| RC=$?` around expected-nonzero calls, PASS echo, temp workspace cleanup." Verified: `grep -n 'PATH=' tests/integration/sdd-e2e-test.sh` returns **exactly one** hit, a `PYTHONPATH=` at `:579` — there is **no `PATH` stubbing anywhere in the file**, in Step 13 or any other step. The other three idioms are real (`\|\| CTX_RC=$?` at `:601`, `rm -rf` cleanup at `:606`, PASS echo at `:607`). | **Accepted** — the dispatch's Pattern References section was corrected to list only the three idioms Step 13 actually establishes, and to state explicitly that **stub-on-PATH is NEW to Step 14 with no local precedent**, so the implementer designs it rather than looking for a model that isn't there. Low practical risk (Step 14's block is supplied whole), but a Pattern Reference that points at a non-existent convention is exactly the "look at how X does it" failure the Pattern References mechanism exists to prevent — inverted. **Process note: THREE citation errors surfaced in one Task 10 dispatch, all the same shape — a true statement attributed to the wrong source.** (1) The MX-A rule, cited to Task 7's deviations row, is actually from the handoff bundle's `CONTINUE.md:39` (partner located it there). (2) The stub-on-PATH idiom, credited to Step 13, exists nowhere in the e2e script. (3) The `lint-shell.sh`-is-vacuous gotcha, credited to CLAUDE.md, is from this run's session notes — `grep -in 'vacuous' CLAUDE.md` returns nothing. The partner found #1 and #2 and volunteered #3 as out-of-scope; the controller verified and fixed all three. **Every one of the underlying facts was TRUE; only the provenance was wrong** — which is precisely the failure mode that makes a citation chain untrustworthy, because a reader who checks the named source and finds nothing has no way to tell a mis-citation from a fabrication. Carry forward: when injecting a fact into a dispatch, cite the source you actually read it in, or cite nothing and state the evidence directly. |
| Task 10 | ScopeChange | **THE IMPLEMENTER FOUND A THIRD DEFECT IN THE PLAN'S STEP 14 BLOCK — one the controller AND the partner both missed. The block never `cd`s into the fixture worktree.** `spawn-handoff-session.sh:53` resolves `WORKTREE_ROOT="$(git rev-parse --show-toplevel 2>/dev/null)"` — **bare, with no path argument**, so it resolves against the *caller's* cwd. The e2e harness does `cd "$WORK"` at `sdd-e2e-test.sh:14` and never leaves for the rest of the file. So the plan's invocation would have resolved the **harness** workspace as the worktree, not `$SPAWN_WT`. **The implementer proved it before fixing it** (positive control): the block as given reproduced `REFUSED: worktree not clean — commit pending state first (protocol step 2)` at RC=1; wrapping the same invocation in `( cd "$SPAWN_WT" && … )` produced RC=0 and the full expected output. Controller independently confirmed both premises (`grep -n 'WORKTREE_ROOT='` → `:53` bare; `grep -n '^cd '` → `:14`), and that the fix is in the commit at `:676` with an explanatory comment at `:668`. | **Accepted.** Third of three defects in one plan block — the other two (version-as-directory, missing `launch=auto` assertion) were controller-found pre-dispatch. **Worth stating plainly: the plan's Step 14 as written could not have run at all**, and neither the controller's pre-dispatch verification nor the partner's independent chain-check caught it, because both reasoned about the *launch-mode* semantics and neither asked the more basic question of which directory the script would resolve. Noted for Task 11's docs: the `cd` requirement is a real usage constraint of the script's interface, not a test artifact. Side observation recorded rather than smoothed: the observed error was "worktree not clean" rather than "not in a git repository", which implies `$WORK` is itself a git repo by that point in the pipeline — immaterial to the finding (either is an exit-1 refusal) but it means the failure would have read as a confusing complaint about a directory the author never intended as the worktree. |
| Task 10 | IndependentDecision | **The implementer REMOVED `rm -rf "$SPAWN_WORK"` from Step 14's FAIL branches** after initially writing them in, on the grounds that no step anywhere in `sdd-e2e-test.sh` cleans up its temp workspace on a failure path — cleanup happens only after all checks pass, and the file's top-of-run `echo "Workspace: $WORK"` shows the design intent is to preserve state for debugging on failure. | **Accepted** — and independently verified by the spec reviewer, which grepped every `FAIL:`/`rm -rf` line across the whole file (`:105-729`) and confirmed the convention is real (post-pass cleanup only, at `:606` Step 13, `:724` Step 14, `:729` final). **This is the right instinct recorded for its own sake: the implementer checked the file's existing convention instead of inventing a better one**, and a naive review would have flagged the removal as a temp-dir leak. |
| Task 10 | IndependentDecision | **Step 14's stub `cmux` never emits `OK <ref>` on stdout, so `SPAWN_WORKSPACE_REF` is `(spawned)` throughout — the `awk '/^OK[ \t]/{print $2; exit}'` ref-parsing branch is NOT exercised by the e2e test.** Raised by the controller as the highest-value open question for the quality review ("is Step 14 asserting a degraded path while claiming to assert the real one?"). | **Accepted — deliberate test-pyramid layering, not a hole.** The quality review chased it to an answer rather than banking it as a finding: the ref-parsing branch is unit-tested at `tests/unit/test_spawn_handoff.py:897` (`echo "OK workspace:42"`) and `:916` (`printf 'OK workspace:9'`), with the `(spawned)` fallback at `:1211`. **Controller verified those citations** (`grep -n 'OK workspace'` → 5 hits including both). e2e owns the real-invocation integration path; unit tests own ref-parsing branch coverage. Recorded because the observation is true and a future reader inspecting `$CMUX_LOG` will see `(spawned)` and reasonably wonder. |
| Task 10 | IndependentDecision | **Quality-review Minor, ratified as-is:** the `launch=auto` assertion and the later `spawned successor in … (launch=auto)` line both derive from the same `$LAUNCH_MODE` variable (`:313`/`:329`/`:379`/`:483`), so they cannot diverge under any single-point mutation — Step 14 does not pin launch mode twice, it echoes one fact twice. | **Accepted, no change.** The assertion's value is not redundancy but **failing early with an unambiguous cause** when the fixture degrades, which mutation proof #1 demonstrated it does. Recorded so the coverage is not overstated in either direction. |
| Task 10 | IndependentDecision | **Mutation-proof coverage residual, recorded rather than implied.** Of the implementer's three mutation proofs, **two were independently reproduced** by reviewers: the spec review re-ran #1 (`preflight_ok()` → `return 1`) and the quality review re-ran #3 (dropped `shq` re-quoting), both matching the implementer's pasted output. **#2 (flag-order swap) rests on the implementer's own evidence only.** The spec review declared this honestly as `[UNVERIFIED — low severity]` and the controller assigned one of the two to the quality review rather than letting it lapse. | **Accepted.** #2 shares its grep anchor with #3, which WAS reproduced, and its expected-value string was cross-checked against `build_successor_cmd()`'s actual token order (`:334-348`). Residual risk judged low and made visible. **Process note: the spec reviewer declaring what it did NOT check is what allowed the gap to be routed forward instead of lost** — the opposite of a review that implies uniform coverage. |
| Task 11 | ScopeChange | **Step 1c(b) "document the coupling AT THE `FORWARDED` site" reinterpreted.** Read literally, "at the site" means a comment inside `spawn-handoff-session.sh` — which the Task-8 freeze forbids. The implementer landed it in the CLAUDE.md section instead, anchored to the concrete construct (`FORWARDED=()` `:217`, `${FORWARDED[*]}` `:308`, `${FORWARDED[@]}` `:342`) with the fails-on-3.2/passes-on-4.4+ asymmetry spelled out — not a generic "no `set -u`", which is what the obligation was written to prevent. | **Accepted.** The obligation's intent was *specificity* (a generic header note was explicitly called insufficient), and that intent is met. Decisive evidence the implementer found on its own: the script's `:6-7` already reads *"intentionally does NOT use `set -u` … See CLAUDE.md Hook Development Gotchas"* — the script was **already pointing at CLAUDE.md**, so writing the mechanism there completes an existing pointer rather than displacing it. Editing the frozen script to satisfy a doc obligation would have been the worse reading. |
| Task 11 | IndependentDecision | **BACKLOG closure landed as a NEW row with a new ID format `N43(D)`, not an edit to N43's row.** Every existing ID is a bare letter+number (`B7`, `C6`, `N43`). The implementer chose `N43(D)` over inventing `N52` so the closure stays coupled to N43's own lettering, and over editing N43 in place — which would have cost a deletion against Step 3's "preserve every existing row". | **Accepted.** Verified no consumer parses BACKLOG (`grep -rln 'BACKLOG' tests/ hooks/ skills/*/scripts/` → no hits), so the format is cosmetic and reversible. The insert-don't-edit choice is what produced the verified **1 insertion / 0 deletions** on `BACKLOG.md`. Coupling the closure to N43's letter is the more useful convention for a reader arriving at the N43 row. |
| Task 11 | IndependentDecision | **~~`## Deterministic Scripts (15 active)` heading left stale as pre-existing drift.~~ ⚠ THIS ROW'S ORIGINAL PREMISE WAS FALSE — see disposition.** The implementer's Concern 1 measured **total** table rows (17 → 18) and concluded the heading was already wrong by 2 before this feature. The controller accepted that reasoning and wrote it into this row. **Both were wrong.** The heading counts rows whose path is under `subagent-driven-development/scripts/` — never total rows. | **Resolved (fixed in `056c453`), and this row corrected rather than left standing.** The Task 11 **quality review** refuted the premise with falsifiable historical evidence, independently reproduced by the controller before acceptance: `768656a` heading 15 / total 17 / **SDD 15** ✓ · `78dcd25` heading 15 / total 17 / **SDD 15** ✓ · HEAD total 18 / **SDD 16** ✗. The heading was **exactly correct immediately before Task 11**, and **Task 11 introduced the drift by 1** by adding `spawn-handoff-session.sh`, an SDD script. Corrected to `(16 active)`. **Process note — this is the row's real lesson:** a defect this task *caused* was very nearly filed as inherited drift, by an implementer and a controller who both measured against the wrong denominator. What caught it was a reviewer that tested a hypothesis against history (3/3 predictions, then one clean break) instead of accepting the reported framing. Leaving the original wording here would have preserved a false exoneration in the permanent record. |
| Task 11 | IndependentDecision | **Two commits (`f565a16` + `da7e367`) where Step 5 specified one.** The second is a self-review fix the implementer found after committing: its Step-1 edit had left the CLAUDE.md Testing e2e bullet carrying **two** banner counts (N43's historical "13→14" followed by the new "14→15"), so a reader hits the stale one first — on the very line the manifest header names as "the authoritative running counts". | **Accepted.** Not amending was the right call (amending was not authorized, and the split keeps the self-review visible in history rather than erasing it). This is the second time this task's own count-accuracy discipline caught a count-accuracy defect in its own output; `grep -c '13→14' CLAUDE.md` → `0` confirms exactly one current total remains. |
| Task 11 | IndependentDecision | **N43's own row (`BACKLOG.md:90`) still describes component (D) as deferred.** Reconciling it in place would have cost a deletion Step 3 forbids, so the new `N43(D)` row carries an explicit supersession sentence ("this row, not N43's notes, is (D)'s current state"). A reader who stops at `:90` still gets the stale answer. | **Accepted.** The two constraints (preserve every row · don't leave a contradiction) genuinely conflict here, and the supersession sentence is the cheapest resolution that satisfies the hard one. The residual is a reader who reads `:90` and stops before the adjacent row — narrow, and the rows are contiguous. Recorded so the next BACKLOG-touching change can reconcile `:90` when it is not bound by a 0-deletions rule. |
| Task 11 | IndependentDecision | **11 `spawn-handoff-session.sh` line numbers are cited in CLAUDE.md, a durable doc.** They are correct today (each independently re-verified by the controller, the partner, or both) and the script is frozen for this feature, but any future edit invalidates them silently. | **Accepted.** The precision is what makes the notify asymmetry and the exit ladder checkable rather than assertions to be taken on faith — the specific failure mode this run has been fighting. The rot risk is real but strictly smaller than the vagueness it replaces, and CLAUDE.md already cites line numbers elsewhere (e.g. `sdd-skill-enforcement-hook.sh:76`, `controller-checkpoint.py:1534`) as an established convention. |
| Task 11 | IndependentDecision | **The manifest Document Index is not exhaustive** — neither `sdd-aggregate-gate-visibility` nor the 2026-07-14 N43 feature has a row — so the new feature-dir row follows a partial convention. | **Accepted.** Adding a row to a partially-populated index is strictly additive and matches the rows that do exist; back-filling the two missing features is outside Task 11's scope and would touch content this feature did not produce. |
| Task 11 | IndependentDecision | **Quality-review finding 3 partially declined by the controller.** The review proposed compressing the `handoff-spawn.log` bullet's field grammar out of CLAUDE.md as a duplicated source of truth (a transcription of the script's own `printf`). The line-number half of the same finding was accepted and fixed; this half was not. | **Accepted (declined), on evidence the reviewer did not check.** CLAUDE.md **already** documents the sibling `context-observations.log`'s field grammar inline (`format: <ISO-8601> task=<N> type=<...> tokens=<T> source=<probe\|byte-proxy\|bypass> tier=<below\|soft\|hard> action=<allow\|nudge\|block\|fallback>`), so an inline log grammar is established house style here, not a novel second source of truth. Decisively: the bullet's payload is the warning "**This is NOT `context-observations.log`**", and *a warning that two formats differ is unusable if the reader cannot see either format*. The fix implementer independently verified the premise and agreed unprompted. Recorded so this is not re-opened as a finding later. |
| Task 11 | IndependentDecision | **Two review-driven fix rounds beyond the original commit** (`056c453`, `3917602`), each re-verified. Round 1 fixed the two factual defects (heading count, false "authoritative counts" pointer) plus the accepted half of finding 3. The **re-review then found a defect round 1 had introduced**: its new rationale for dropping the exit-3 line cites generalized to the *next* bullet, which legitimately keeps `:125`/`:464`/`:469` — so the section read as internally inconsistent and invited a future editor to strip cites that are load-bearing there. Round 2 stated the real asymmetry (paired-with-an-assertion cites are self-catching; homogeneous-list cites drift silently). | **Resolved.** This is the second time on this run that a fix round introduced its own defect, and the second time a re-review caught it — the "always re-review after a fix round" rule earned its cost again. Round 2 also produced a free hardening: the bullet's `grep` guidance was anchored to `grep -nE '^[[:space:]]*exit 3'` after the bare form was shown to return **9** hits, three of them comments — one describing the *decoder's* exit codes, not the ladder's. Controller re-verified the anchored form returns exactly 6. |
| Task 11 | IndependentDecision | **`:195` is `exit 3 ;;`, so the newly-documented anchored grep matches it only because the anchor is on leading whitespace, not a full-line match.** Raised unprompted by the round-2 fix implementer: a future editor "tightening" the pattern with a `$` anchor would silently drop that site and the documented count would become 5, not 6. | **Accepted — recorded, not guarded.** No code change: the doc states "exactly those six" adjacent to the pattern, so the count is self-checking for anyone who runs it, and adding a test for a grep string inside a prose bullet would be disproportionate. Logged because the failure mode is silent and the reasoning is not recoverable from the pattern itself. **Notable as reviewer behavior**: this was volunteered by a fix implementer working a single-clause brief, about a risk in its own output. |
| Task 11 (feature close) | ScopeChange | **Plan-level acceptance criterion `plan.md:217` is left UNCHECKED — it cannot be satisfied before merge.** It requires `spawn-handoff-session.sh <bundle-id> --dry-run` to run **"in a real picker-launched cmux SDD session"**. The installed skill path `~/.claude/skills/superpowers/…` is a symlink to the **main checkout** (`readlink` → `/Users/araymond/projects/claude-custom/superpowers/skills`, controller-verified), and this work is unmerged, so no live session can reach this script yet. Every other plan-level criterion (`:218`–`:223`) is ticked and controller-verified. | **Deferred to post-merge — by the plan's own design, not as a concession.** `plan.md:225` carries a section titled **"Post-Merge Live Smoke (mandatory before declaring done — from spec §7)"** whose **item 1 is verbatim this criterion**, item 2 is one real spawn against a scratch bundle, and item 3 states "the first genuine HARD-block hop in a downstream project is the true acceptance test". The criterion is duplicated between the two sections; the post-merge section is the operative one. **Ticking it now would be a false claim of live verification** — precisely the hollow-check class this run has been hunting, and the same self-hosting constraint N43 hit (its live smoke also ran only after merge, and it PASSED there). Carried in the `N43(D)` BACKLOG row's **Post-merge** clause so it survives this feature dir. **Requires the user's explicit sign-off** at the SDD pre-completion gate, which permits an unchecked box only when "explicitly dispositioned in `deviations.md` with human approval" — surfaced to the user rather than self-approved. **✅ HUMAN APPROVAL GRANTED 2026-07-27**: presented to the user at the pre-completion gate as remediation item H1 ("approve the deferral, or tell me to hold the feature open"); the user replied "proceed with your recommendation", which recommended approving H1. **The checkbox at `plan.md:217` is deliberately LEFT UNCHECKED** — ticking it would assert a live verification that has not happened, and the whole point of this row is that the assertion would be false. Consequence, stated rather than hidden: `controller-checkpoint.py --phase pre-completion` will continue to report `all_checkboxes_checked: FAIL (84/85)` **by design**. The SKILL's gate is satisfied via its documented alternative path ("or explicitly dispositioned in `deviations.md` with human approval"); the *script* has no way to represent an approved deferral, so its mechanical count stays red. **Do not "fix" this by ticking the box.** |
| Task 11 (post-review) | ScopeChange | **One bullet added to the `## cmux Auto-Spawn Handoff` CLAUDE.md section AFTER Task 11 was reviewed and committed** — a gloss on cmux's UI vocabulary. User-requested at the pre-completion gate (2026-07-27), after a live screenshot exposed that `cmux/SKILL.md`'s "Core Concepts" reads backwards against the UI: it calls a **Workspace** a *"tab-like group"* and a **Surface** *"a tab within a pane"*, but workspaces are the **left-sidebar** list and surfaces are the **top tabs**. | **Accepted.** Not scope creep: it is **load-bearing for this feature** — `spawn-handoff-session.sh` calls `cmux new-workspace`, so a spawned successor appears as a new **sidebar entry, not a tab**, and the success notify names it `workspace:<N>`. A reader who trusts the vendored skill's wording and searches the tab bar concludes the spawn failed when it succeeded — a false-negative on the feature's only user-visible success signal. Placed in the existing cmux section (no new section) immediately after the Cross-repo bullet, which is where the "fork-specific cmux guidance lives here, never in the pristine vendored files" convention is already stated (`repo-2 README.md:37`). **Verified empirically before writing, not quoted from the skill:** `cmux list-workspaces` returned exactly the sidebar entries in sidebar order; `cmux list-pane-surfaces --workspace workspace:2` returned exactly the five top tabs with the same one `[selected]`. Also records the `tab_ref`/`surface_ref` synonym wart in `cmux identify --json`. **Process note:** this edit was NOT re-reviewed by a dispatched reviewer — it is one additive bullet, user-requested, with its factual claims controller-verified against live `cmux` output. Declared here rather than folded silently into the reviewed Task 11 record. |

## Deferred Work
[Items deferred from plan scope]

- **Doc carry-forwards → Task 9** (recorded at Task-3 resume so they survive a controller handoff before Task 9):
  - `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (default 60) + `SUPERPOWERS_CMUX_QUOTA_TOOL` env vars — already enumerated in module-2 Task 9 Step 1 (L276); listed here for redundancy.
  - **Bash minimum version (from Task 4):** the plan self-contradicts (Task 4 Step 2 note says "confirm `env bash --version` ≥ 4.x" but the adjacent caveat says the array ops are "safe on bash 3.2 too"). The Task 4 implementer must determine the ACTUAL minimum the composition block requires and report it; Task 9 docs the real requirement. Do not let Task 9 ship an unverified "≥4.x" claim if 3.2 suffices.

- **Plan-doc correction owed (from Task 3, spec-review confirmed):** `module-1-spawn-script.md` Task 3 Step 2 still contains BOTH defects verbatim — (a) the command-substitution + background-watcher timeout that stalls the full timeout on the success path, and (b) the fallback-less `QUOTA_TOOL="${SUPERPOWERS_CMUX_QUOTA_TOOL:-$QUOTA_TOOL_DEFAULT}"` line whose own Step 1 tests cannot pass. Replace both with the shipped implementation from commit `7131698`. Scope is `module-1-spawn-script.md` ONLY (`plan.md` and `spec.md` do not mirror the snippet — reviewer-verified). Leaving it uncorrected means any re-run reproduces a 60s stall per spawn plus two impossible tests.

- **Env-validation regression test owed → Task 6** (from the Task 3 `[task 3 fix]` round): the numeric guards on `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` / `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (commit `926ab60`) have **no committed regression test** — deleting either regex block leaves all 24 tests green, because the shared-constant test pins env *consumption*, not *validation*. Deferred deliberately: the awk-injection vector is self-injection via one's own env var (not a privilege boundary), both invalid-input directions fail safe, and the silently-inert-gate risk is already covered by the bounded timeout test. Task 6 is the sweep point for script-level test debt. Add two tests: invalid `MIN_PCT` → stderr WARNING + default-15 behavior; invalid `TIMEOUT` → stderr WARNING + gate stays live.

- **⚠ CRITICAL carry-forward → Task 5 — TEST-ECHO COLLISION.** Task 4's diagnostic echo at `spawn-handoff-session.sh:281` already emits `--append-system-prompt-file` and `a b.md` into stderr. Task 5's planned `test_auto_mode_composes_exact_command` asserts `"a b.md" in out` against combined stdout+stderr — **that assertion can pass without the compose line ever being exercised.** Task 5 must anchor its greps on the specific `[spawn-handoff] successor command:` / `launch=` line, not bare `out`. Tokens with no Task-4 emitter (`--non-interactive`, `--pick-version 2.1.218`, `--telemetry on`, `--session-label`, `/pickup b1`) are genuinely discriminating. **Do not let Task 5 ship a self-satisfying assertion.**
- **⚠ CRITICAL carry-forward → Task 6 — FALLBACK-CHAIN SPAWN-ID IS NOT WIRED** (from Task 5). The auto-mode `SUCCESSOR_CMD` embeds `printf '%s %s runtime-picker-failure hop=%s\n' "$(date …)" spawn "$SP_HOP"` — the second field is the **literal word `spawn`**, not the spawn uuid spec §5.4d's log format specifies. The composed string is baked before Task 6 generates that uuid, so the runtime fallback can never carry it. **Task 6 must not assume this is wired.** Two resolutions: (a) generate the spawn id before composition and interpolate it, or (b) have Task 6 re-compose the fallback tail once the id exists. Non-blocking for Task 5 (plan-verbatim fence; only the `runtime-picker-failure` token is asserted), but it is a real gap in the §5.4d log contract.
- **Task 6 test-debt sweep (running list).** Already owed: the two env-validation regression tests (see above) and the Task-4 cleanup trio (`max(0, …)` label slice, surrogate `try`-wrap, `mkdir` gating) — all three were **explicitly declined by Task 5** because no Task-5 test exercises them (every Task-5 test is `--dry-run` with no `APPEND_PROMPT`). Task 6 is where they land *with* coverage. **Added by the Task-5 quality review (each proven mutation-survivable — deleting the behavior leaves 41/41 green):**
  - **`-x` half of the version predicate** (`:292`): reducing to a bare `[ -f … ]` is undetected. This is a constraint Task 5 *owns*. Needs a harness knob — `install_version()` always `chmod 0o755`, so the non-executable case is currently inexpressible.
  - **`command -v claude-picker`** (`:293`): deleting it entirely is undetected. `run_spawn` always installs a `claude-picker` stub with no removal knob. spec.md:196 lists "picker missing → picker-manual" explicitly.
  - **`--telemetry off` on the composed line**: unasserted (all auto-path tests use `telem="1"`). The flag *pair's* presence IS pinned; only the `off` value is not.
  - **`shq` rc-propagation** (quality review Important #1): `build_successor_cmd`'s exit status is `printf`'s, so a `shq` failure cannot propagate, contradicting the block's own stated "any failure degrades to the attended interactive picker" posture. **No reachable trigger was found** (every input is charset- or decode-guarded upstream) and the failure emits *empty* rather than an unquoted value — arity shift, not injection. Fail-safe-invariant hardening, not a live bug.
  - **`_successor_cmd` newline truncation** (test helper): returns `lines[0]`, so a newline-bearing forwarded arg silently truncates the extracted string. Test-helper robustness only.
  - **NM4 — failure-branch `cmux notify` unasserted** (`:445`, Minor from the Task-6 quality RE-review): deleting the `cmux notify` on the spawn-failure branch leaves 58/58 green. `test_notify_failure_still_exit_0` only proves a *failing* notify does not break exit 0; nothing proves the failure-branch notify fires at all. Code is correct — this is a missing assertion, closable in one line (`"Spawn failed after reservation" in _notify_line(...)` inside `test_spawn_failure_keeps_hop_exits_3`).
  - **NM5 — `rm -f "$out_f"` unasserted** (`:396`, Minor from the Task-6 quality RE-review): deleting the temp-file cleanup is unobservable to the suite. Behaviourally equivalent in-harness (a leak only). **Accepted as not worth a test** by the re-reviewer; recorded so it is not rediscovered as a finding later.
  - **mktemp-failure branch untested** (`:393-397`, ADVISORY from the Task-6 spec RE-review): when `mktemp` fails, `spawn_claude_workspace()` spawns uncaptured (rc still correct, output not discarded, empty ref → `(spawned)`). The degradation is safe as written and was verified by inspection, but nothing forces `mktemp` to fail, so a future edit could break rc propagation there undetected. Settle with a stub-`mktemp`-on-PATH test.
  - **`--title "SDD handoff"` hardcoded inside the extraction-ready core** (`:400`, ADVISORY from the Task-6 spec RE-review): pre-existing (passed the first review; the fix round touched only the `--body` substitution on that line, so it is not a regression). Relevant only to the eventual generic extraction (Decision 15), not to Module 1.
  - **Reservation writes are unchecked** (`:390-391`, ADVISORY from the Task-6 spec review): neither the `.handoff-hops` write nor the `intent` append has its rc checked, and the script has no `set -e`, so a failed reservation still proceeds to spawn — weakening Decision 21's "hop consumption is durable" guarantee, which is the very contract Task 6 owns. Reviewer's suggested fix: `|| { warn; print_manual_instructions; exit 3; }` before spawning. Deliberately routed to the sweep rather than the `[task 6 fix]` round to keep that round tight around the single BLOCKING item; it needs its own test (unwritable reports dir) and adds an exit path, so it should not ride along with a blocking fix.
  - **Contract probe has no timeout** (`:295`): a hung picker hangs the script. Noted only because `check_quota` (`:159-187`) establishes the opposite precedent 130 lines earlier. Low risk — the picker is a local same-author script answering with a bare `echo`.
- **Task 9 doc obligations (accumulated, all confirmed by review)** — ⚠ **OLD NUMBERING: these belong to what is now TASK 11 (docs), not to new-Task-9 (the protocol-doc rewrite).** Promoted to a real checkbox at **Task 11 Step 1c** so they cannot evaporate the way the Task 6 test-debt sweep nearly did:
  - **`bash ≥ 3.2`** — NOT the plan's "≥ 4.x". Construct floor is 3.1 (`FORWARDED+=`), verified floor 3.2.57.
  - **Document the `set -u` ↔ `${FORWARDED[*]}`-on-empty-array coupling at the `FORWARDED` site**, not just generically in the header. Verified under 3.2.57: `set -u; A=(); echo "${A[*]}"` → `unbound variable`. A future `set -u` breaks bash 3.2 **silently** while passing on 4.4+.
  - `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (default 60) + `SUPERPOWERS_CMUX_QUOTA_TOOL` env vars.
  - Note that rematerialized append-prompt files accumulate at `~/.claude-codex-handoff/append-prompts/<bundle>-hop<N>.md` with **no reaper** (spec §5.4d defines none).
- **Task 5/6 cleanup candidates (all reviewer-identified, none blocking):** non-dry-run write path has **zero** committed coverage (all 10 Task-4 tests are `--dry-run`, skipping both `mkdir -p` and the base64 write); strip-guard **specificity** untested — the non-trailing `/pickup` case is the one that matters, since a stale non-final `/pickup` survives and would send the successor to the *wrong* bundle; `mkdir` gating; surrogate `try`-wrap; `max(0, …)` on the label slice.

## Independent Decisions
[Decisions made by subagents without plan guidance]

## Scope Changes
[Requirements that changed during execution]

| 2026-07-24T18:46:38Z | Module transition: spawn-handoff-session.sh + unit suite → protocol rewrite + e2e Step 14 + docs | FYI | Accepted |
