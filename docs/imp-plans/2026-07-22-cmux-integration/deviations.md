# Deviations Register

> Auto-maintained by controller during subagent-driven-development execution.
> Review all entries before merge.

| Task | Type | Description | Disposition |
|------|------|-------------|-------------|
| Task 2 | ScopeChange | Plan's verbatim `test_hop_limit_exits_3` wrote `.handoff-hops` uncommitted, which trips Task 1's clean-tree Precondition-1 (exit 1) BEFORE the hop-limit check (exit 3) — the test would fail as written. Implementer added `git add -A` + commit after the write (mirrors `test_missing_active_feature_exits_1`), faithful to spec.md L164's "`.handoff-hops` is tracked" invariant. Assertion (exit 3 + "hop") unchanged. Advisor-confirmed. | Accepted (plan test imprecision corrected faithfully; spec reviewer to re-verify the assertion still exercises the hop gate, not the clean-tree gate). |
| Task 4 | ScopeChange | **Added autouse `_hermetic_picker_env` fixture** (not in the plan's Step-1 snippet). This machine's own Claude session is claude-picker-launched, so the five `CLAUDE_CODE_PICKER_*` / `ENABLE_TELEMETRY` vars are live in the ambient environment and `run_spawn` does `env = dict(os.environ)` — the plan's `_meta` signals "absent" by *omitting* a key, so those tests would have inherited real values. Fixture `monkeypatch.delenv`s all five; autouse so the prior 24 tests get the same hygiene. Plan test bodies otherwise verbatim. | **Accepted** — spec review: JUSTIFIED, anti-masking not test-weakening. Discriminator: neutralizing the fixture turns the suite **red** (1 failed: `test_telemetry_on_and_off` off-case, observed `telemetry=on`), not silently green. Reviewer **corrected the implementer's justification** on two counts: (a) `test_append_prompt_empty_keeps_original_path` would NOT have inverted — ambient `APPEND_PROMPT` is set-but-*empty*, so it passes either way on this machine (fixture still correct for a session launched with a real append prompt); (b) the test **fails**, it does not silently invert. |
| Task 4 | ScopeChange | **Plan's "Bash version caveat" (≥ 4.x) is factually WRONG.** Implementer determined the real floor empirically (probe + PATH shim → `/bin/bash` 3.2.57, full file 34 passed, all invocations logged as 3.2.57). | **Accepted** — spec review independently re-derived and confirmed, and distinguishes **two numbers that must not be collapsed**: *construct floor* **bash ≥ 3.1** (governed by `FORWARDED+=("$tok")`, array `+=` landed in 3.1) vs *verified floor* **3.2.57** (independent PATH shim, 33 script invocations, zero other versions). 4.x-construct scan: zero bash hits. **Task 9 documents ≥ 3.2.** See Deferred Work. |
| Task 4 | IndependentDecision | **Label overflow when `255 - len(suffix)` goes negative** (`:274`): the Python slice silently yields `""` and the result is `suffix` alone, unbounded — live probe emitted a **310-character** label. | **Accepted (non-blocking)** — masked upstream: the picker caps its export via `_sanitize_attr`'s `cut -c1-255` before `export CLAUDE_CODE_PICKER_LABEL` (`claude-picker:57,184`), and the reviewer proved algebraically that any input ≤255 yields output ≤255. **Goes live if repo-1 ever drops that cap.** `max(0, …)` hardening noted for Task 5/9. Code is plan-verbatim. |
| Task 4 | IndependentDecision | **Sanitizer charset divergence vs the picker.** Spec §5.4b says sanitize with "the picker's attr charset rule"; the picker *keeps* `/` and *replaces* unsafe chars with `-`, while the script's `[^A-Za-z0-9_.-]` **strips** and drops `/` (`feat/x` → picker keeps `feat/x`, script emits `featx-Session-2`). | **Accepted** — cosmetic; the *binding* contract constraint (round-trip sanitizer stability) still holds, verified by running repo-1's actual `_sanitize_attr` over the script's outputs including the 255 boundary case. Code is verbatim from the plan ⇒ **plan defect, not implementer error.** |
| Task 4 | IndependentDecision | **`mkdir -p "$APPEND_TARGET_DIR"` gated on ARGS-present, not on there being anything to write** (`:221`, plan-verbatim): creates an empty `append-prompts/` dir whenever ARGS is set; if that path exists as a *file*, emits a raw unprefixed `mkdir: … File exists` to stderr on an otherwise-healthy run and proceeds. | **Accepted** — quality review: cosmetic, plan-verbatim, no spec text forbids it. Cheap fix deferred to Task 5/6 (drop the shell `mkdir`; use `os.makedirs(..., exist_ok=True)` inside the `if ap_b64:` branch where failure already routes to `exit(4)`). |
| Task 4 | IndependentDecision | **Lone-surrogate argv element produces an uncaught Python traceback + orphan file** (`:250-251`, plan-verbatim): the final `x.encode()` sits outside the `try`, so `["\udcff"]` raises `UnicodeEncodeError`. | **Accepted (safe degrade)** — exit is non-zero ⇒ `ARGS_OK=0`, so behavior is correct; only the *diagnostic* is poor (traceback instead of a message), and if `APPEND_PROMPT` was present the target file was already written before the crash. Wrapping the final write in the same `try` → `sys.exit(3)` deferred to Task 5/6. |
| Task 3 | ScopeChange | **Plan snippet defect — timeout capture rewritten.** The plan's verbatim Step 2 `check_quota()` uses command substitution around a backgrounded tool + `sleep` watcher. The watcher's `sleep` grandchild inherits the command-substitution pipe's write end, so `$(...)` cannot return until `sleep` exits — `kill $watcher` reaps the subshell but orphans `sleep`. Implementer measured a **30.0s stall on the SUCCESS path** with `QUOTA_TIMEOUT=30` (production: 60s stall on every spawn; the 7 quota tests would take ~7min). Rewrote to capture via `mktemp` + `wait` on the tool PID (immune to fd inheritance). Verified: fast path 0-1s; both hang shapes (single-process `exec sleep` AND a forking tool) time out at the limit and fail open. Plan tests kept verbatim. | **Accepted** — spec review (task-003-spec-review.md) verified empirically: plan original stalls 4.067s on the SUCCESS path under a 4s timeout; the minimal watcher-redirect fix still blows the bound for a *forking* tool (30.3s under 4s); the committed temp-file version honors the timeout in all four shapes. Real defect, minimal fix. **Correction target is `module-1-spawn-script.md` Task 3 Step 2 ONLY** — reviewer grepped `plan.md` and `spec.md`; neither mirrors the snippet. See open item below. |
| Task 3 | ScopeChange | **Plan snippet/test inconsistency — default-only PATH fallback.** `run_spawn` remaps `HOME` to `tmp_path/home`, so `QUOTA_TOOL_DEFAULT` (`$HOME/.claude/bin/claude-usage-pace`) never exists under test and the harness's PATH stub is unreachable — the plan's own `test_quota_ok_proceeds` / `test_quota_low_exits_3` could never pass and `test_quota_tool_absent_proceeds` would be vacuous. Implementer kept the plan's tests as ground truth and fixed the script: when NO `SUPERPOWERS_CMUX_QUOTA_TOOL` override is given AND the pinned default is absent, fall back to `command -v claude-usage-pace`. Explicit override stays authoritative (bad override ⇒ `unchecked`, never silent fallback), preserving the absent-test's meaning. Production unaffected (`~/.claude/bin/claude-usage-pace` exists; that dir is not on PATH, so the absolute default remains the real resolution path). | **Accepted** — spec review proved both halves by patching the script and re-running the plan's own tests: Variant A (plan's verbatim resolution, no fallback) → `test_quota_low_exits_3` + `test_quota_ok_proceeds` FAIL, so the task as written could not pass; Variant B (fallback unconditional) → `test_quota_tool_absent_proceeds` FAILS, so the override guard is load-bearing and the absent-test is non-vacuous. Worst case safe: `command -v` finding nothing ⇒ `QUOTA_TOOL=""` ⇒ `unchecked`, never a spurious refusal. |
| Task 3 | IndependentDecision | `QUOTA_STATUS` left set-but-unused (shellcheck SC2034) as forward-scaffolding for the Task-6 spawn log, mirroring `SP_HOP` (set by Task 2 for Task 5). | **Accepted** — deliberate forward-scaffolding consistent with five pre-existing peers in the same script (`PICKER_CONTRACT`, `DRY_RUN`, `FEATURE_NAME`, `SPAWN_LOG`, `SP_HOP`); quality review confirmed it is a uniform known pattern, not new debt. Task 6 to confirm consumption by the spawn log; if unconsumed there, remove at that point. |
| Task 3 | IndependentDecision | ~~On timeout,~~ **On EVERY invocation** (success path and `--dry-run` included), one orphaned `sleep` reparents to init and lives out `QUOTA_TIMEOUT` — in production a stray `sleep 60` for 60s after every spawn. **Implementer's original wording understated the scope; corrected here per quality review.** The claimed `Killed: 9` stderr notice does **NOT** reproduce in non-interactive bash, and the orphan does **NOT** hold the caller's stdout pipe (the watcher's `>/dev/null 2>&1` covers it) — a capturing caller returns in 0s. | **Accepted** — quality review confirmed invisible in output, cannot stall or corrupt the caller (measured 0s), visible only via `ps`. Near-inherent to the no-`timeout`-on-macOS watcher pattern (killing the process group would kill the caller, as the script isn't a session leader). Record corrected so Tasks 4-6 aren't misled; the code comment (L145-148) was always accurate. |
| Task 3 | ScopeChange | **Quality-review fix round** (`[task 3 fix]`, commit `926ab60`, report `task-003-quality-review-fix.md`): added house-style numeric validation for `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` (regex widened to allow floats) and `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (integer-only, POSIX `sleep`), each warn+revert-to-default, no new exit path; plus 3 tests (shared-constant guard, bounded 1s timeout class, strict-`<` boundary). Closes an awk-injection surface and a silently-inert-gate case. | **Accepted** — controller independently re-verified: 24 passed in 11.76s, `bash -n` OK, shellcheck 7 SC2034-only, `15`/`60` each in exactly one default assignment, commit scope exactly the 2 in-scope files. Shared-constant test is mutation-proven (a literal `15` fails that test and only that test). |
| Task 3 | IndependentDecision | A non-numeric `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` makes the `awk` comparison error out, classifying `ok` (proceed). Outside the spec's fail-open matrix (which covers tool-side faults only); flagged rather than adding unrequested input validation. | **Resolved** by the `[task 3 fix]` round (commit `926ab60`) — `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` is now regex-validated (`^[0-9]+(\.[0-9]+)?$`); a non-numeric value warns to stderr and reverts to the documented default of 15 instead of silently reaching `awk`. Note the *sibling* edge cases remain and are separately accepted: a `remaining_pct` of `Infinity` classifies `low`, `nan` classifies `ok` — both inherited from the plan's verbatim awk line and requiring non-standard JSON the real tool won't emit. |
| Task 5 | ScopeChange | **Compose assertions anchored on an extracted `[spawn-handoff] successor command: ` line, not raw `stdout+stderr`.** Implementer added a `_successor_cmd(r)` helper (asserts the line exists, then slices) and pointed the plan's verbatim token list at it. The plan's token list is unchanged; only the haystack narrowed. This implements the CRITICAL TEST-ECHO COLLISION carry-forward. | **Accepted** — controller mutation-proved it: disabling ONLY the compose emission line (leaving Task 4's `a b.md` emitter running) makes both compose tests FAIL, so the collision is closed. Spec review Q7 confirmed `lines[0]` is safe (the marker is emitted exactly once at `:318`; Task 6 adds no second emitter). Quality review judged the helper docstring successful — a future reader understands *why* the extraction exists without opening this file. |
| Task 5 | ScopeChange | **Added a 6th test not in the plan: `test_composed_command_reparses_with_correct_arity`.** `shlex.split`s the composed picker command and asserts `/tmp/a b.md` is ONE argv element and `argv[-1] == "/pickup b1"`. Justification: spec §7's repo-3 matrix requires proof that a space/quote-bearing argv "survives intact in the `--command` string", which the plan's substring assertion does not establish. | **Accepted — NOT extra work.** Spec review Q4 quoted spec.md:196 verbatim: *"a space/quote-bearing decoded argv driven through composition → **survives intact in the `--command` string** (compose-side quoting, §5.4c)"*. "Survives intact" is an arity claim, not a substring claim. Controller mutation clinches it: replacing `shq()` with an identity `echo "$1"` leaves the plan's own `test_auto_mode_composes_exact_command` **PASSING** while only the added arity test FAILS — without it the compose-quoting Contract Constraint had **zero** real coverage. Spec-mandated work the plan under-specified. |
| Task 5 | IndependentDecision | **No plan-fence defect found this task** — first of Tasks 3/4/5 where the Step-2 bash fence was correct as written. Adopted essentially verbatim (+3 explanatory comments). Verified by *execution* rather than inspection: composed command executed under /bin/bash 3.2 on both success and forced-failure paths; nested quoting, `{ …; }` group, deferred `$(date …)`, and `>>` redirect all round-trip. | **Accepted — claim independently confirmed.** Spec review re-derived the fence's nested-quoting semantics and executed the result, producing a composed command matching spec.md:157's flag order exactly, with hostile input (`he said "hi"; rm -rf …`) surviving as one quoted literal. **Breaks the Task-3/Task-4 pattern**: the plan-defect ledger stays at 3, not 4. The two owed plan-doc corrections (Task 3 Step 2) are unaffected and still owed. |
| Task 5 | IndependentDecision | **Fallback chain's spawn-id field is a literal placeholder.** The composed runtime-failure line logs the word `spawn` where spec §5.4d's format specifies the uuid Task 6 generates; the string is baked at compose time, before that uuid exists, so the runtime fallback can never carry the real id. Plan-verbatim fence text; only the `runtime-picker-failure` token is asserted. | **Accepted as a Task-6 carry-forward** — spec review Q3 judged it correctly deferred, not a Task-5 defect: `module-1-spawn-script.md` Task 6 Step 2 generates `SPAWN_ID` *after* the dry-run short-circuit, i.e. after the compose block, so Task 5 could not interpolate it without annexing Task 6. **⚠ Load-bearing caveat the reviewer added: Task 6's plan text as written will NOT fix this** — it must deviate (generate the id before composition, or re-compose the fallback tail), or the §5.4d log contract ships violated. Tracked in Deferred Work. |
| Task 5 | IndependentDecision | **`--telemetry`'s value is not `shq`'d.** Internally derived (`on`/`off` from a strict `= "1"` test), and spec §5.4c enumerates only version/label/forwarded-args as requiring re-quoting. Flagged so a reviewer does not read it as an oversight. | **Accepted** — both reviews independently confirmed compliant. Spec review: "`--telemetry`'s value is correctly not `shq`'d — it is script-derived `on`/`off` and §5.4c enumerates only version/label/forwarded-args." Quality review's `shq`-input trace reached the same conclusion. Not an oversight. |
| Task 5 | IndependentDecision | **`shq` spawns one Python process per forwarded element.** Deliberate at this scale (a handful of args on a once-per-handoff path); batch into a single Python call if the forwarded argv ever grows large. | **Accepted** — quality review Minor #2 agreed the per-element spawn is fine at this scale but flagged that `$(shq "/pickup $BUNDLE_ID")` is computed at **three** sites (`:309`, `:316`, `:318`), which is an SSOT violation as well as 2 wasted spawns. Hoisting to a single `PICKUP_ARG` is FIX 4 of the `[task 5 fix]` round. The separate `shq` **rc-propagation** restructure (quality review Important #1) is routed to Task 6: the reviewer traced every `shq` input and found **no reachable failure path**, and the failure emits *empty* (arity shift, not injection). |
| Task 5 | IndependentDecision | **`picker-manual` branch does not validate the append path** — the picker's `--append-system-prompt-file` readability check exits 3 only under `--non-interactive`. Accepted per the plan's own precision note: Task 4's rematerialization covers the auto path, and picker-manual is an attended fallback. | **Accepted** — restates the plan's own precision note (module-1 Task 5 Step 2) and the plan-header Contract Constraint; both reviews confirmed the behavior is as specified. No code change warranted. |
| Task 5 | ScopeChange | **Quality-review fix round** (`[task 5 fix]`, commit `eae39dc`, report `task-005-quality-review-fix.md`): 5 bounded fixes — FIX 1 assert the picker-manual branch's composed command (closes a gap where replacing that whole line with `TOTALLY-BROKEN` left 41/41 green); FIX 2 new `test_empty_label_omits_session_label` pinning spec §5.4b; FIX 3 `echo` → `printf '%s\n'` in `build_successor_cmd`; FIX 4 hoist the thrice-computed `$(shq "/pickup $BUNDLE_ID")` to one `PICKUP_ARG` (SSOT + 2 fewer Python spawns); FIX 5 `&&` label guard → explicit `if/fi`. | **Accepted** — quality **re-review returned PASS**, verifying all five CLOSED by running each discriminating mutation independently. Controller separately re-ran both gap-closing mutations: each now FAILS where it previously left 41/41 green. FIX 3/4/5 proven byte-identical across 5 composed cases. Suites: 42 on the file, **595** across `tests/unit/`, 42 again under a /bin/bash 3.2.57 shim. Commit scope exactly the 2 in-scope files (+44/−5). |
| Task 5 | IndependentDecision | **FIX 3 was materially more load-bearing than "a one-word cleanup."** The re-reviewer measured the composed *value* (not the diagnostic line) under `BASHOPTS=xpg_echo`: the old `echo` form expanded `\t` and **truncated the command mid-string at `\c`**, leaving an unterminated quote and **dropping the `/pickup <id>` argument entirely** — a successor command that cannot parse and would not resume the bundle. `printf '%s\n'` is intact. | **Accepted** — recorded because the original review rated it Minor on the (correct) grounds that the trigger needs an unusual env var; the *consequence* when triggered is worse than "mangling". Also note the safe form was chosen: fixed format string with data as the `%s` argument, not `printf "${parts[*]}\n"`. |
| Task 5 | IndependentDecision | **Two new Minor observations from the re-review, both accepted as-is.** (a) `PICKUP_ARG` is used at `:312` inside `build_successor_cmd` but assigned at `:320` — reads as undefined top-down. (b) The worked-example comment's `LABEL=Proj-Session-2` annotation names the *input* `CLAUDE_CODE_PICKER_LABEL` while `$LABEL` is a live variable 13 lines above holding `Proj-Session-3`. | **Accepted** — (a) verified correct: bash resolves a function body's variable references at *call* time (`:330`), not definition time (`:303`), and `:320 < :330` with no `exit`/`return`/`set -e` between `:298`–`:334`; confirmed empirically on the auto, picker-manual, and `--dry-run` paths. (b) the example output itself was verified character-for-character against a real run. Both are readability-only, no behavioral consequence on any reachable path; re-reviewer explicitly stated neither warrants another fix round. May be folded into the Task 6 sweep or dropped. |
| Task 0 | IndependentDecision | An environment file-watcher/Python formatter cosmetically reformatted `spawn_handoff_helpers.py` + `test_spawn_handoff.py` post-write (line-wrapping only: PACE_MISSING_WINDOW, run_spawn signature). No identifiers/logic/string-literals/behavior changed; JSON fixtures byte-identical; contract test 1/1 PASS + full unit suite 554 PASS. Controller re-verified committed content (56210f1) — faithful. | Accepted (cosmetic; verified non-breaking). Watch for the same reformatter on Tasks 1–6 which append to these files — benign as long as tests stay green. |

## Deferred Work
[Items deferred from plan scope]

- **Doc carry-forwards → Task 9** (recorded at Task-3 resume so they survive a controller handoff before Task 9):
  - `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (default 60) + `SUPERPOWERS_CMUX_QUOTA_TOOL` env vars — already enumerated in module-2 Task 9 Step 1 (L276); listed here for redundancy.
  - **Bash minimum version (from Task 4):** the plan self-contradicts (Task 4 Step 2 note says "confirm `env bash --version` ≥ 4.x" but the adjacent caveat says the array ops are "safe on bash 3.2 too"). The Task 4 implementer must determine the ACTUAL minimum the composition block requires and report it; Task 9 docs the real requirement. Do not let Task 9 ship an unverified "≥4.x" claim if 3.2 suffices.

- **Plan-doc correction owed (from Task 3, spec-review confirmed):** `module-1-spawn-script.md` Task 3 Step 2 still contains BOTH defects verbatim — (a) the command-substitution + background-watcher timeout that stalls the full timeout on the success path, and (b) the fallback-less `QUOTA_TOOL="${SUPERPOWERS_CMUX_QUOTA_TOOL:-$QUOTA_TOOL_DEFAULT}"` line whose own Step 1 tests cannot pass. Replace both with the shipped implementation from commit `7131698`. Scope is `module-1-spawn-script.md` ONLY (`plan.md` and `spec.md` do not mirror the snippet — reviewer-verified). Leaving it uncorrected means any re-run reproduces a 60s stall per spawn plus two impossible tests.

- **Env-validation regression test owed → Task 6** (from the Task 3 `[task 3 fix]` round): the numeric guards on `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` / `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (commit `926ab60`) have **no committed regression test** — deleting either regex block leaves all 24 tests green, because the shared-constant test pins env *consumption*, not *validation*. Deferred deliberately: the awk-injection vector is self-injection via one's own env var (not a privilege boundary), both invalid-input directions fail safe, and the silently-inert-gate risk is already covered by the bounded timeout test. Task 6 is the sweep point for script-level test debt. Add two tests: invalid `MIN_PCT` → stderr WARNING + default-15 behavior; invalid `TIMEOUT` → stderr WARNING + gate stays live.

- **⚠ CRITICAL carry-forward → Task 5 — TEST-ECHO COLLISION.** Task 4's diagnostic echo at `spawn-handoff-session.sh:281` already emits `--append-system-prompt-file` and `a b.md` into stderr. Task 5's planned `test_auto_mode_composes_exact_command` asserts `"a b.md" in out` against combined stdout+stderr — **that assertion can pass without the compose line ever being exercised.** Task 5 must anchor its greps on the specific `[spawn-handoff] successor command:` / `launch=` line, not bare `out`. Tokens with no Task-4 emitter (`--non-interactive`, `--pick-version 2.1.218`, `--telemetry on`, `--session-label`, `/pickup b1`) are genuinely discriminating. **Do not let Task 5 ship a self-satisfying assertion.**
- **⚠ CRITICAL carry-forward → Task 6 — FALLBACK-CHAIN SPAWN-ID IS NOT WIRED** (from Task 5). The auto-mode `SUCCESSOR_CMD` embeds `printf '%s %s runtime-picker-failure hop=%s\n' "$(date …)" spawn "$SP_HOP"` — the second field is the **literal word `spawn`**, not the spawn uuid spec §5.4d's log format specifies. The composed string is baked before Task 6 generates that uuid, so the runtime fallback can never carry it. **Task 6 must not assume this is wired.** Two resolutions: (a) generate the spawn id before composition and interpolate it, or (b) have Task 6 re-compose the fallback tail once the id exists. Non-blocking for Task 5 (plan-verbatim fence; only the `runtime-picker-failure` token is asserted), but it is a real gap in the §5.4d log contract.
- **Task 6 test-debt sweep (running list).** Already owed: the two env-validation regression tests (see above) and the Task-4 cleanup trio (`max(0, …)` label slice, surrogate `try`-wrap, `mkdir` gating) — all three were **explicitly declined by Task 5** because no Task-5 test exercises them (every Task-5 test is `--dry-run` with no `APPEND_PROMPT`). Task 6 is where they land *with* coverage. **Added by the Task-5 quality review (each proven mutation-survivable — deleting the behavior leaves 41/41 green):**
  - **`-x` half of the version predicate** (`:292`): reducing to a bare `[ -f … ]` is undetected. This is a constraint Task 5 *owns*. Needs a harness knob — `install_version()` always `chmod 0o755`, so the non-executable case is currently inexpressible.
  - **`command -v claude-picker`** (`:293`): deleting it entirely is undetected. `run_spawn` always installs a `claude-picker` stub with no removal knob. spec.md:196 lists "picker missing → picker-manual" explicitly.
  - **`--telemetry off` on the composed line**: unasserted (all auto-path tests use `telem="1"`). The flag *pair's* presence IS pinned; only the `off` value is not.
  - **`shq` rc-propagation** (quality review Important #1): `build_successor_cmd`'s exit status is `printf`'s, so a `shq` failure cannot propagate, contradicting the block's own stated "any failure degrades to the attended interactive picker" posture. **No reachable trigger was found** (every input is charset- or decode-guarded upstream) and the failure emits *empty* rather than an unquoted value — arity shift, not injection. Fail-safe-invariant hardening, not a live bug.
  - **`_successor_cmd` newline truncation** (test helper): returns `lines[0]`, so a newline-bearing forwarded arg silently truncates the extracted string. Test-helper robustness only.
  - **Contract probe has no timeout** (`:295`): a hung picker hangs the script. Noted only because `check_quota` (`:159-187`) establishes the opposite precedent 130 lines earlier. Low risk — the picker is a local same-author script answering with a bare `echo`.
- **Task 9 doc obligations (accumulated, all confirmed by review):**
  - **`bash ≥ 3.2`** — NOT the plan's "≥ 4.x". Construct floor is 3.1 (`FORWARDED+=`), verified floor 3.2.57.
  - **Document the `set -u` ↔ `${FORWARDED[*]}`-on-empty-array coupling at the `FORWARDED` site**, not just generically in the header. Verified under 3.2.57: `set -u; A=(); echo "${A[*]}"` → `unbound variable`. A future `set -u` breaks bash 3.2 **silently** while passing on 4.4+.
  - `SUPERPOWERS_CMUX_QUOTA_TIMEOUT` (default 60) + `SUPERPOWERS_CMUX_QUOTA_TOOL` env vars.
  - Note that rematerialized append-prompt files accumulate at `~/.claude-codex-handoff/append-prompts/<bundle>-hop<N>.md` with **no reaper** (spec §5.4d defines none).
- **Task 5/6 cleanup candidates (all reviewer-identified, none blocking):** non-dry-run write path has **zero** committed coverage (all 10 Task-4 tests are `--dry-run`, skipping both `mkdir -p` and the base64 write); strip-guard **specificity** untested — the non-trailing `/pickup` case is the one that matters, since a stale non-final `/pickup` survives and would send the successor to the *wrong* bundle; `mkdir` gating; surrogate `try`-wrap; `max(0, …)` on the label slice.

## Independent Decisions
[Decisions made by subagents without plan guidance]

## Scope Changes
[Requirements that changed during execution]
