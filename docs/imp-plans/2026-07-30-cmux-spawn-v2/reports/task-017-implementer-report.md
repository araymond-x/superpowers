---
schema_version: 1
task_id: 17
task_type: implementation
status: DONE
files_changed:
  - path: "tests/integration/sdd-e2e-test.sh"
    description: "Rewrote Step 14 from the workspace-era stub/assertions to a v2 surface-topology pipeline. Replaced the catch-all cmux stub with a per-verb stub emitting real stdout (new-surface OK surface:7…, rename-tab full action=rename shape, send/send-key OK, wait-for exit 0, read-screen with /rename+/rc anchors, list-pane-surfaces with the `* ` selected-row marker, workspace create OK workspace:9, ping PONG). Added a committed .sdd-session.json generated by materialize-manifest.py from a 5-task plan fixture. Split into three sub-runs: success (launch=auto, handshake=ok, self-commit), policy=ask refusal (rc 3), over-expected advisory."
tests:
  written: 19
  passing: 19
  command: "bash tests/integration/sdd-e2e-test.sh"
  result: PASS
contract_compliance:
  - constraint: "v2 verb shapes mirror cmux-verb-shapes.json + _CMUX_V2_STUB"
    status: compliant
    detail: "Stub emits the frozen shapes: new-surface `OK surface:7 pane:2 workspace:5`, rename-tab full `OK action=rename tab=tab:77 workspace=workspace:29` (untruncated), send/send-key `OK surface:7 workspace:5`, wait-for exit 0, workspace create `OK workspace:9`, list-pane-surfaces carries the `* ` marker, ping PONG. read-screen carries the rc_anchor `/remote-control is active` + rename anchor."
  - constraint: "Success path reaches handshake=ok via captured surface ref"
    status: compliant
    detail: "capture_cmux_ref() parses surface:7 from new-surface stdout; rename/send/wait-for/post-spawn all target it; outcome record asserted handshake=ok surface=surface:7 workspace=TEST-WS tasks_done=0 with no post_spawn=partial."
---

## Implementation Summary

Rewrote Step 14 to drive the real reworked `spawn-handoff-session.sh` through its v2 surface-topology success path plus two policy sub-runs, deriving every stub shape/anchor/verb-order from the shipped script and the frozen fixtures.

Key derivations from the script:
- Success verb order: `ping` → `new-surface` (captures `surface:7`) → `rename-tab --surface surface:7` → `send` (inline env + composed picker cmd) → **one** `wait-for` → post-spawn `/rename` + `/rc` (each send/send-key/read-screen) → outcome `handshake=ok` → notify → mechanics card → `chore(sdd): record handoff hop 1` self-commit → clean tree.
- The read-screen stub emits `Session renamed to: hop1 SDD feat`, `Session renamed to: hop2 SDD feat`, and `/remote-control is active`, so post-spawn `/rename` (`grep -qiF "Session renamed to: $TAB_TITLE"`) and `/rc` (`grep -qiF "/remote-control is active"`) both verify — proven load-bearing by asserting the outcome carries no `post_spawn=partial`.
- `.sdd-session.json` is generated by the real `materialize-manifest.py` from a 5-task standard-tier plan (`handoff_spawn: auto`), yielding exactly `total_tasks:5, task_range:[0,4], handoff:{expected_hops:2, spawn_policy:auto}`. It and the plan are committed inside the throwaway `$SPAWN_WT` only.
- Policy-ask sub-run: manifest rewritten to `spawn_policy:ask` (committed), run without `--user-approved` → rc 3, stderr `reason=policy-ask`, hop file still `1`, intent count still 1 (refuses at Precondition 2b before reserving).
- Over-expected sub-run: `expected_hops:1` (policy back to auto) + a prior `tasks_done=0` outcome + one committed DONE report (`count_tasks_done()`→1, progress made → no stall) → rc 0, and the advisory notify mentioning `expected` appears in that run's cmux log.

Whole suite runs green: `E2E PIPELINE PASS - 15 steps composed correctly` (banner unchanged — still one top-level Step 14 block; 15 headers confirmed).

House-style: no `set -u/-e/pipefail` added to the stub; used the existing `( cd "$SPAWN_WT" … ) || SPAWN_RC=$?` idiom and `|| { FAIL }` guards so expected-nonzero runs don't trip the file's `set -e`/ERR trap; `grep -c … || true` guards the count captures; ambient `SUPERPOWERS_CMUX_MAX_HOPS`/`_MAX_STALL_HOPS` neutralized to empty (mirrors the unit harness).

## Source Files Read

- `skills/subagent-driven-development/scripts/spawn-handoff-session.sh` (full, both pages) — authoritative for verb order, flags (`new-surface --workspace … --type terminal --working-directory … --focus false`; `rename-tab --workspace … --surface … TITLE`), single `wait-for` on the happy path, `SENT_CMD` = `export SUPERPOWERS_SPAWN_ID=…; <composed>`, the `successor command:` diagnostic line, reservation intent-before-outcome ordering, exit ladder (2b policy=ask → exit 3 pre-reservation), over-expected budget notify, and the `chore(sdd): record handoff hop N` N64 self-commit + clean tree.
- `tests/unit/spawn_handoff_helpers.py` `_CMUX_V2_STUB` — the authoritative v2 stub vocabulary; my bash stub is its shell analog (Task 17 spec pins surface:7 for list-pane-surfaces vs the helper's surface:11).
- `tests/unit/fixtures/spawn-handoff/cmux-verb-shapes.json` — exact stdout strings; `selected_row_marker` (`* ` prefix), `rename_tab.stdout` (`action=rename`, not a ref), `rc_confirmation_screen.rc_anchor` (`/remote-control is active`).
- `skills/subagent-driven-development/scripts/_handoff_support.py` — `spawn-policy`/`expected-hops`/`tasks-done`/`stall-streak` semantics; `count_tasks_done` (task_id int + DONE status), `stall_streak` (trailing outcomes vs current count).
- `skills/subagent-driven-development/scripts/materialize-manifest.py` + `write-mechanics-card.py` — manifest emission shape and the committed `handoff-mechanics.md` (why the tree stays clean after the self-commit).
- Current Step 14 block — kept the fixture scaffolding (mktemp dirs, executable picker-version FILE with the MX-A trap note, matching-repo_id bundle, `SPAWN_ARGS`, the load-bearing `cd "$SPAWN_WT"`).

## CLAUDE.md Files Read

- Repo-root `CLAUDE.md` (Testing, Behavioral Test Gotchas, cmux Auto-Spawn Handoff sections) — via the system context. No `tests/` or `tests/integration/` CLAUDE.md exists (checked).

## Deviations from Plan

None. All shapes/anchors were confirmed against the script and fixtures; no discrepancy between prompt and script surfaced.

## Self-Review Findings

- Stub emits correct per-verb stdout (not a catch-all); verified against `_CMUX_V2_STUB` and `cmux-verb-shapes.json`.
- `launch=auto` asserted first; composed-command greps anchored on the `successor command:`/`export SUPERPOWERS_SPAWN_ID=` lines, not bare `$out`.
- Success reaches `handshake=ok` (read-screen anchors present) with exactly one `wait-for`; the extra no-`post_spawn=partial` assertion makes the read-screen content load-bearing.
- Policy-ask and over-expected sub-runs assert the exact rc + tokens read from the script (`reason=policy-ask`, `expected`).
- Full suite green to completion; banner count (15) read from the run.
- Write scope held: only `tests/integration/sdd-e2e-test.sh` committed (explicit path; never `git add -A`). The other dirty/untracked files are the controller's SDD orchestration artifacts, left untouched.

## Concerns

None.

Verbatim tail of the run:

```
=== Step 14: spawn-handoff-session.sh end-to-end (v2 surface topology + handshake + policy dial) ===
PASS: Step 14a — success: surface topology, launch=auto, handshake=ok, self-commit
PASS: Step 14b — policy=ask refuses (rc 3, reason=policy-ask, no hop consumed)
PASS: Step 14c — over-expected advisory notify fires without a stall refusal
PASS: Step 14 — spawn end-to-end: surface topology, handshake, policy dial, bookkeeping commit

E2E PIPELINE PASS - 15 steps composed correctly
```
