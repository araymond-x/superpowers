**PASS** — Task 17 is spec compliant and contract compliant.

## Verification performed

1. **Read the shipped script end-to-end** (`skills/subagent-driven-development/scripts/spawn-handoff-session.sh`, both pages, 970 lines) and the rewritten Step 14 block (`tests/integration/sdd-e2e-test.sh:610-871`).

2. **Verb shapes checked against the frozen contract:**
   - `new-surface` → `OK surface:7 pane:2 workspace:5` (test line 694) — matches both `_CMUX_V2_STUB` (spawn_handoff_helpers.py:186) and `cmux-verb-shapes.json`'s pattern.
   - `rename-tab` → `OK action=rename tab=tab:77 workspace=workspace:29` (test line 695) — full, untruncated shape, byte-identical to `_CMUX_V2_STUB` line 188 and the frozen fixture's `rename_tab.stdout`.
   - `send`/`send-key` → `OK surface:7 workspace:5` (test lines 696-697) — matches `_CMUX_V2_STUB` lines 195-196 exactly (the plan text's shorthand "`OK`" is an abbreviation; the actual reference shape both stub and fixture use is the longer string, and the e2e stub matches it).
   - `list-pane-surfaces` → `* surface:7  SDD resume: demo  [selected]\n` (test line 710) — carries the `* ` selected-row marker per `cmux-verb-shapes.json`'s `selected_row_marker` fact.
   - `workspace create` → `OK workspace:9` (test line 705), `ping` → `PONG` before logging (test line 691), `wait-for` → exit 0 (test line 698). All match.
   - Fixture `.sdd-session.json` (test lines 653-681) is generated by the real `materialize-manifest.py` from a 5-task standard-tier plan; `expected_hops(5, "standard") = ceil(5/2.5) = 2` (`_handoff_support.py:32`), so the committed manifest correctly yields `total_tasks:5, task_range:[0,4], handoff:{expected_hops:2, spawn_policy:auto}` as required.

3. **Control-flow / argv assertions traced against the actual script:**
   - `new-surface --workspace "$CMUX_WORKSPACE_ID" --type terminal --working-directory "$WORKTREE_ROOT" --focus false` (script line 647-648) ↔ test's grep for `--workspace TEST-WS --type terminal` + `--focus false` (lines 768-770).
   - `rename-tab --workspace "$SPAWN_WORKSPACE_REF" --surface "$SPAWN_SURFACE_REF" "$TAB_TITLE"` (script line 689) ↔ test's `rename-tab .*--surface surface:7` (line 772).
   - Exactly one `wait_for_token()` call on the happy path (script line 845, only re-invoked inside the `if ! wait_for_token` failure branch at line 851) ↔ test's `WAITFOR_N -eq 1` (line 792-793).
   - `handshake=ok` comes purely from `wait_for_token` succeeding (script line 845), independent of read-screen content — but the test's companion assertion that `$OUTCOME` does **not** contain `post_spawn=partial` (line 801) is genuinely contingent on the read-screen anchors (`post_spawn_send_verified`, script lines 885-899, greps `"Session renamed to: $TAB_TITLE"` and `"/remote-control is active"`). The stub's read-screen output (`Session renamed to: hop1 SDD feat` / `hop2 SDD feat` / `/remote-control is active`) correctly matches the actual composed `TAB_TITLE` (`"hop{hop} SDD {feature}"` → `"hop1 SDD feat"` / `"hop2 SDD feat"` given `FEATURE_NAME=feat`), so this is a real, non-coincidental check, not a hardcoded match.
   - Policy-ask precondition (2b, script lines 195-217) fires and exits 3 with `reason=policy-ask` **before** Precondition 3 (cmux reachability) and well before the hop reservation at line 776+ — confirming the test's assertion that the hop file stays `1` and intent count stays `1` (lines 830-833) is architecturally guaranteed, not accidental.
   - Over-expected sub-run: `SP_HOP=2 > expected_hops=1` triggers the advisory notify at script line 337-341 (`"Hop $SP_HOP exceeds expected_hops=$EXPECTED_HOPS"`) — matches test's `grep ... -qi "expected"` (line 866).

4. **Positive control (mutation test):** Changed `new-surface`'s stubbed surface number from `surface:7` to `surface:99` in the test file, re-ran the suite — it correctly failed at `FAIL: rename-tab not scoped to --surface surface:7`, proving the assertions are not vacuous. Restored the file via direct copy-back (diff-verified against a pre-mutation backup in scratchpad) and confirmed `git diff` shows zero delta against `fd672ed` — no git checkout/stash used.

5. **Ran the full suite** (`bash tests/integration/sdd-e2e-test.sh`): all steps (1 through 14c) PASS, closing banner reads `E2E PIPELINE PASS - 15 steps composed correctly`, matching the implementer's claim and the actual `echo` count of top-level `=== STEP` headers (14 numbered blocks + the closing banner line itself, unchanged per Step 3/4 of the task).

6. **Changed-files scope:** `git show --stat fd672ed` shows only `tests/integration/sdd-e2e-test.sh` (242 lines changed) — matches the "commit only the e2e test" requirement.

7. **Report completeness:** `task-017-implementer-report.md` has all required sections and a verbatim run tail. "No deviations / no concerns" is plausible and confirmed by independent code inspection — no undisclosed gap found.

No findings to report.

_(Spec reviewer: sonnet model. Saved verbatim by controller.)_
