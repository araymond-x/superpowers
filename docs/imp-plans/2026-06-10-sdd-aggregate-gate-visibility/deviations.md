# Deviations Register

> Auto-maintained by controller during subagent-driven-development execution.
> Review all entries before merge.

| Task | Type | Description | Disposition |
|------|------|-------------|-------------|
| Setup | SelfHostingHazard | H1: This run's own pre-completion Check 7/9 execute main's PRE-FIX code (via the `~/.claude/skills/superpowers` symlinks), so they stay blind to THIS feature's own Module-1 archives. Non-blocking: blindness only narrows the min-tier ratio denominator to Module-2 evidence (same posture as sprint 3). The fix first protects the NEXT multi-module run; the new e2e Step 12 (Task 14) — which runs THIS checkout's `$PROJECT` scripts — is the in-sprint proof of archive-awareness. | Accepted |
| Setup | SelfHostingHazard | H2: The plan/module files quote risk-surface keywords (N22's pattern list, N25 prose). Main's pre-N22 `check_integration_test_risk` is fence-blind and reads per-file frontmatter, so the live plan-validation gate would self-WARN. Mitigation: all three plan files declare `integration_test: {path: tests/integration/sdd-e2e-test.sh}` in frontmatter (advisory WARNING suppressed; the e2e step genuinely IS the integration test — honest declaration). Advisory only; the gate blocks on FAIL, not WARNING. | Accepted |
| Setup | SelfHostingHazard | H3: Check 9 visibility for the verification task (Task 13) depends on its implementer dispatch entry being in the LIVE dispatch log at pre-completion. Guaranteed by placing Task 13 in Module 2 — its dispatch is logged AFTER the Module-1→2 transition truncates the live log, so the live log still holds it at completion. | Accepted |
| Setup | SelfHostingHazard | H4: N7/N3a/N18/N16 are all on main, so the sprint-3 hazards (Source-Contracts-`None` pre-execution FAIL, no-Task-0 first-task block, verification-report rejection for empty `files_changed`) do NOT recur this run. Recorded for traceability; no action needed. | Accepted |
| Setup | PlanDefect (audit Order 1) | Task 5 (N20) snippet declared `def _fence_marker(line: str) -> str \| None:` — PEP-604 union that the regression Category-8 text-scanner hard-FAILs (masked from pytest by the 3.14 venv). Fixed IN-PLAN to `def _fence_marker(line):` + `# type: (str) -> Optional[str]` comment (3.9-safe; auditor confirmed it is the only Category-8 hit). validate-plan PASS, Task 5 span 194. | Resolved |
| Setup | PlanDefect (audit Order 2) | Task 13 (verification) Step 4 wrote its report to `task-012-implementer-report.md`, but Task 13 is `id: 13` — would collide with Task 12's report and leave Task 13 report-less at pre-completion. Fixed IN-PLAN to `task-013-implementer-report.md`. | Resolved |
| Task 2 | CrossTaskSequencing | The new docstrings in `controller-checkpoint.py` (`_review_tiers_per_task` [Task 1] + `_merged_dispatch_times` [Task 2]) state "One of the 5 documented archive-aware lookups (see CLAUDE.md)", but CLAUDE.md currently documents 3 (N4's two + N10's Check 5). The CODE is correct (5 archive-aware lookups exist as of Task 2); the DOC catches up at Task 12 (updates CLAUDE.md + manifest to 5 sites, naming both new functions) and Task 13 verifies. In-plan forward reference, not a defect. Both spec + quality reviewers confirmed planned, benign. | Accepted |
| Task 3 | PlanDefect (partner review) | Task 3's plan test stubs (module-1, `test_marked_fix_logs_type_fix_not_implementer` + `test_check3b_allows_gate_artifact_names`) call `setup_full_sdd_workspace(tmpdir, task_count=5)`, but the real signature is `setup_full_sdd_workspace(tmpdir, total_tasks, completed_tasks)` (sdd_test_helpers.py:335; `task_count` is not a param and `completed_tasks` is required) — a blind copy-paste TypeErrors at the TDD verify-fail step. Surfaced by the Task-3 controller-partner (haiku) BLOCK. Corrected in the implementer dispatch: use `setup_full_sdd_workspace(tmpdir, total_tasks=5, completed_tasks=2)` (mirrors the existing tests at test_sdd_classification.py:177/241). The plan's own note already says "mirror existing usage"; this records the literal-stub correction so the per-task reviewers know the implementer diverged from the literal stub on purpose. | Resolved |
| Task 3 | Observation (DONE_WITH_CONCERNS) | Stage-3 `\bfix\b\|remediat` heuristic logs `type=fix-unattributed` for ANY markerless dispatch whose description contains "fix" (e.g. an Explore "investigate the fix for X"). Intentional tamper-evidence per the plan design — changes no enforcement, adds no task attribution, never matches Check 9's `type=implementer` regex. Only effect is a noisier live `.dispatch-log`. Plan-faithful; flagged for awareness. | Accepted |
| Task 3 | Observation (DONE_WITH_CONCERNS) | `check-hooks.sh` hard-codes `SUPERPOWERS_PATH=/Users/araymond/projects/claude-custom/superpowers` (main checkout) for the settings.json registration scan, while hashing `REPO_ROOT` (the worktree). They agreed here (registration block identical + unchanged), so the baseline re-capture is valid. Pre-existing behavior, out of scope for Task 3; a divergent settings.json on another machine would surface in the registration block, not the hash block. Noted as a tool-robustness candidate; not added to BACKLOG unless it recurs. | Accepted |
| Task 3 | Observation (DONE_WITH_CONCERNS) | Stage-0 re-review (`exit 0`) and Stage-3 markerless-fix paths do not write the dispatch-log sentinel (sentinel is written only in the Stage-1 reviewer block). If such a path were the FIRST write to a fresh log, a later implementer dispatch would emit the WARN-only "no hook-written sentinel" line (hook ~:323) — never blocks, zero enforcement impact. In normal flow the first dispatch is always a task-1 partner/reviewer dispatch (writes the sentinel), so this is an edge case. Implementer followed the plan's Stage-0 spec exactly (correct — no freelancing). Recorded as a plan-level follow-up candidate (Module 2 / BACKLOG), not a Task 3 implementation defect. | Accepted |
| Task 4 | TestHarnessChoice (sanctioned) | Task 4's plan Step 1 was a STUB (`...` placeholder) sketching an in-process `_transition.validate_module_completion(...)` call with a `tasks: [{id:1}]` comment. The implementer instead used the file's established subprocess `run_transition` harness (the script filename is hyphenated; no `_transition` import exists) and task 3 as the verification task (matching `create_manifest`'s Core module `[0,1,2,3]`). This was explicitly directed by the dispatch's "Test fixture guidance" override (model on the N17 test at :236) and the plan's own NOTE to "model the manifest/workspace construction on the existing tests." Not a divergence from a concrete instruction — the stub was illustrative. RED confirmed for the right reason (task 3 missing spec/quality review). | Accepted |
| Task 5 | DeadImportCleanup | After the D15 `_load_script` hoist made `test_fence_aware_parsing.py`'s local loader obsolete, the implementer removed the now-dead `import importlib.util` and `import os` (plan-named) PLUS `import re` (NOT plan-named) — `re` had a single occurrence (its own import line) and would otherwise trip the quality reviewer's blocking dead-import check. Left `argparse` (live) and `pytest` (conventional) untouched. Defensible scope-adjacent cleanup, not a logic change; verified by the regression suite (145/0/3 unchanged) + full unit suite (483 passed). Per-task reviewers backstop. | Accepted |
| Task 6 | PlanOversight (D15 import) | Plan Step 1 prescribed `from sdd_test_helpers import _load_script` (only `_load_script`), but `test_c2_integration_gate.py` also consumes `ROOT` at line ~168 (`CHECKPOINT_SCRIPT` path, used by the pre-existing `TestC2Check10`). The implementer correctly imported BOTH — `from sdd_test_helpers import ROOT, _load_script` (the hoisted `ROOT` Task 5 added is the identical expression) — and removed the now-dead local `ROOT`/`_load_script`/`import importlib.util`. D15/SSOT-faithful fix for a plan oversight (the plan missed test_c2's line-155/168 ROOT dependency); zero behavior change. Verified: regression 145/0/3 unchanged, full unit suite 486 passed. | Accepted |
| Task 8 | RobustnessRefinement | Plan Step 4 placed the `manifest_feature_dir` capture AFTER `_load_all_plan_contents(_md, _gr)` inside the try. The implementer moved it to immediately AFTER `_md = json.loads(...)` and BEFORE `_load_all_plan_contents` (controller-checkpoint.py:1392, with an explanatory comment), so a failure in `_load_all_plan_contents` (which falls to the `except` → `all_plan_contents=[plan_content]`) cannot silently leave `manifest_feature_dir=""` and disable the N25a feature-window fix. Verified in code; behavior-equivalent on the happy path, strictly more robust on the failure path. | Accepted |
| Task 8 | ScopeRefinement | Plan Step 5's `if _merge_base_is_head(...)` is gated additionally on `manifest_feature_dir` being non-empty (`if manifest_feature_dir and _merge_base_is_head(...)`, :1734), so the feature-window lookup + the `on_base_no_window` note only fire in manifest mode (where `feature_dir` is known) — non-manifest mode keeps the exact pre-N25a behavior and the on-base note never misattributes a non-manifest cause. Also added a 4th test (real-parent / non-root feature-window path) beyond the plan's 3. Verified: 31 Check-10 tests pass (fail-closed counter-fixture green), full unit suite 492 passed. | Accepted |
| Task 8 | Concern (DONE_WITH_CONCERNS, forward note) | `_feature_window_base` assumes the feature dir is a real repository subdirectory with its own commit history (it `git log`s `-- <feature_dir>`). Two edges (CORRECTED per Task-8 quality review — my earlier text wrongly said both "return None"): (a) feature_dir with NO commit touching it → `git log` empty → returns **None** → caller keeps the untracked-only changeset + on-base FAIL note (fail-SAFE). (b) feature_dir == "." (the literal repo root) → `git log -- .` matches every commit, first = root commit, `rev-parse root^` fails → returns **`_EMPTY_TREE_SHA`** → Step-3b direct-diffs the empty tree vs the working tree → ANY existing path is "in changeset" → effectively fail-OPEN **for that degenerate edge**. NOTE: returning `_EMPTY_TREE_SHA` is the INTENDED PASS path for a *normal* feature whose first commit is the repo root (the on-main-fresh-repo case the fix targets — exercised by `test_on_main_committed_integration_test_passes`); the fail-open concern is ONLY the pathological feature_dir="." case. Both edges are UNREACHABLE under the live SDD invariant (feature dirs are always real subdirs like `docs/imp-plans/YYYY-MM-DD-<feature>`, never `.`). Out of scope for N25a; recorded as a forward note, not a defect. | Accepted |
| Task 9 | IndependentDecision (N25f design) | Chose option (b) caller-side attribution to name the source plan file in malformed messages: `_integration_test_paths` keeps its single `plan_contents` param; the Check-10 caller derives `_plan_label = os.path.basename(args.plan_file)` and prefixes all three malformed-detail branches (malformed-only, infra-error, mixed). Minimal blast radius — option (a) (thread a filename list into the helper) would have rippled to the helper-message-pinning tests. The plan explicitly delegated this choice to the implementer ("choose the minimal change… document it"). | Accepted |
| Task 9 | Concern (multi-module attribution) | The option-(b) label uses `args.plan_file`, which in a multi-module plan names the ACTIVE module being driven, not necessarily the module file containing the malformed declaration — it narrows the author to the plan set, not the precise file. Precise per-declaration attribution would need option (a)'s parallel-list threading. Flagged for spec review. | Accepted |
| Task 9 | Observation (test coverage) | Added 2 tests beyond the plan's 3 skeletons: a line-anchoring proof for the second consumer (`_integration_test_paths`), and a Check-10-caller test asserting the FAIL detail names the plan file (the plan's NOTE required the Check-10 detail assertion, which the helper-only skeleton did not provide). Coverage improvement; no scope change. | Accepted |
| Task 9 | ToolObservation (controller-side) | The report validator's frontmatter extractor (validate-report.py / `_report_utils`) splits on the literal `---` substring, so a `^...$`-style fence marker written inside a report-frontmatter description value truncated the YAML mid-string ("unexpected end of stream") — the SAME naive-split class N25b fixes for PLAN scanning, but in the REPORT frontmatter extractor. Worked around by rewording the description to avoid the literal triple-dash. The Task-9 quality reviewer independently confirmed the broader scope: **7 other scripts still use the same naive `content.find("---", 3)`** — `validators.py:56`, `materialize-manifest.py:48`, `validate-plan.py:540`, `transition-module.py:68`, `validate-report.py:95`, `context-summary.py:165,187`. Candidate BACKLOG follow-up: promote `_frontmatter_block` into `_report_utils.py` and migrate all 8 sites to a true codebase-wide SSOT. **Added as BACKLOG N30 on 2026-06-23 (the trace audit caught that Task 14 dropped this promised row and flagged it MUST-FIX; remediated).** Not in Task 9 scope. | Accepted |
| Task 10 | SanctionedTrim (word ceiling) | The plan's verbatim Step-2/Step-3 replacement blocks netted +26 words over the W0=4911 hard ceiling (post=4937 first pass). Per Step 4's explicit "trim until <= 4911" instruction, the implementer trimmed the two parenthetical Check explanations, the trailing reframe clauses, and the pre-dispatch "Verify:" list — touching ONLY the two target sites — landing at exactly 4911. All Check references (5c/6b/4b), the hook-enforced framing, and the "optional early check" reframe are preserved; only the verbatim wording differs (the path Step 4 authorizes). Net `wc -w` unchanged (4911); regression 145/0/3; commit cbff47e touches only SKILL.md (+3/-4). | Accepted |
| Task 11 | IntegrationChoice (SSOT) | The plan's Step-2 snippet showed an inline `import re as _re` + function-local `_direct_entry_re`. The implementer instead defined a module-level `DIRECT_ENTRY_RE = re.compile(...)` (:141) reusing the file's existing module-level `import re` (:25), matching the file's `*_RE` UPPER_SNAKE convention — the cleaner integration the plan's own prose ("module-level regex near the other patterns") calls for, and the path the controller partner explicitly recommended. Avoids a redundant import the quality reviewer would flag (SSOT/dead-code). Behavior-identical (F6 keys on the structural Direct-entry signal). Both per-task reviewers confirmed: spec PASS (regex byte-exact, intent-robustness proven empirically), quality APPROVED (single compile, no dup import). Regression 145/0/3; commit 86ddb95 touches only validate-all-skills.py (+9/-2). | Accepted |
| Task 12 | ToolObservation (controller-side) | The Task-12 implementer saved its report as `task-012-implementer.md` (non-canonical — hook Check 4b / find_report_file expect `task-NNN-implementer-report.md`) with a non-schema frontmatter shape (`files_changed` as bare strings, `contract_compliance` using `invariant:` not `constraint:`/`detail:`, missing `schema_version`). The controller re-saved under the canonical name with schema-correct frontmatter (prose preserved verbatim; validates COMPLETE) and removed the orphan. **The Task-14 implementer report had the SAME issue (saved as `task-14-implementer-report.md` with bare-string `files_changed`/`contract_compliance`); the controller likewise re-saved it canonically as `task-014-implementer-report.md` (validates COMPLETE) and removed the orphan.** No impact on either doc edit (commits f3cfd72 + 6705c7c correct). Recurring-pattern candidate for an implementer-prompt report-filename/schema reminder (now a 2-occurrence pattern). | Accepted |
| Task 14 | LiveFinding (checkpoint verification-blindness) | The Task-14 pre-dispatch checkpoint FAILed `previous_spec_review` + `previous_quality_review` because PREV (Task 13) is a `task_type: verification` task that legitimately has NO spec/quality reviews. Root cause: `controller-checkpoint.py` pre-dispatch (lines ~1244-1299) is NOT verification-aware for the PREVIOUS task, while the live HOOK IS (sdd-pre-dispatch-hook.sh:504-505 skips Checks 4b/4c when `PREV_TASK_TYPE=verification`) — so the authoritative gate (the hook) ALLOWS the Task 14 dispatch; only the checkpoint script's self-assessment false-FAILs. Direct sibling to the live-discovered N18 (checkpoint pre-dispatch lacked the hook's module-boundary skip-guard). Decision: PROCEED — the gate's structural requirement (Check 5c: checkpoint file exists >50 bytes) is satisfied, and the real gate (hook) passes; per the "tool's rule doesn't account for a legitimate pattern" guidance, logged a tool-improvement note (new BACKLOG row, sibling to N18) instead of silently editing the gate script on the last task. Surfaced to the user at the honesty-check stop for a fix-now-vs-defer decision. | Accepted |
| Task 14 | FixtureAdjustment (sanctioned) | The plan's Step-12 e2e snippet declared `modules[].file` = `module-1.md`/`module-2.md` in the fixture manifest but created NEITHER on disk. `materialize-manifest.py` sets `active_module_file` to the first module's file, and pre-completion HARD-ERRORS (`{"error":"Plan file not found"}`, before producing the `checks` dict) when the active/declared module plan file is missing. Caught by the CRITICAL-instruction run-against-live-JSON sanity check BEFORE editing the e2e. Fix: the implementer wrote minimal `module-1.md`/`module-2.md` (valid frontmatter + task header) and hand-built the archived state (matching the plan NOTE's framing) rather than running a real `transition-module.py` transition — the N27 assertion is purely about the checkpoint READING archived reports/logs, so a real transition would add heavy module-1 completion-fixture overhead with zero extra coverage. Both checks proven non-vacuous (a flat glob would PASS both). Documented inline in the e2e + the report. Controller re-ran the e2e independently: 13 steps PASS. | Accepted |
| Honesty (Q5/Q6) | RelianceUncertainty | The controller did NOT personally re-run the full unit suite (497) or the install suite (104) this session — relied on the implementer + spec/quality reviewers, who each reported identical totals. The e2e (13 steps) + regression (145/0/3) WERE independently re-run by the controller. To remove the reliance, the controller will re-run ALL FOUR suites itself in the pre-completion gate (Check 6). RESOLVED: controller independently re-ran all four 2026-06-23 — unit 497 passed (1 harmless pre-existing warning), regression 145 PASS / 0 FAIL / 3 WARNING, install PASSED (0 warnings), e2e 13 steps PASS. | Resolved |

## Deferred Work
[Items deferred from plan scope]

## Independent Decisions
- **Setup — proceeded past the pre-execution `stale_artifacts` WARNING (false positive).** controller-checkpoint.py pre-execution flagged `deviations.md (has content)` + 2 pre-execution audit files as "from a prior session." These are THIS session's own freshly-created setup artifacts (verified clean fresh worktree at orientation: no `reports/`, `deviations.md`, or `.sdd-session.json` pre-existed). Status was PASS, blockers empty — the WARNING is advisory. Archiving valid gate-required setup files would be self-defeating, so I proceeded. Tool-improvement note: `detect_stale_artifacts` flags content-presence, not staleness (no session-start boundary to compare mtimes against) — a candidate refinement (e.g. compare file mtime to manifest `created`/session start, or look for an explicit prior-feature archive marker). Non-blocking; not added to BACKLOG unless it recurs as friction.

## Scope Changes
[Requirements that changed during execution]

### Resolved at setup (pre-execution audit orders — fixed in-plan)
- **Task 5 (N20) `_fence_marker`** and **Task 13 verification-report filename** were both corrected directly in `module-2-calibration.md` during the pre-execution audit (see the two `Resolved` rows above and `reports/pre-execution-audit.md`). The plan is now the correct source of truth; implementers will read the fixed snippets. The Task-5 implementer dispatch additionally restates the union-syntax correction as belt-and-suspenders.

### Watch (carry into the relevant dispatch; not yet a deviation)
- **Task 8 (N25a)** Step 4's `manifest_feature_dir` capture reconstructs the manifest branch as if `_load_all_plan_contents` weren't already called at ~:1295 — it is. The implementer must reconcile against the real `run_pre_completion` code rather than blindly pasting the snippet (flag in the Task-8 dispatch; per-task reviewers backstop).

| 2026-06-23T01:01:47Z | Module transition: aggregate-visibility → calibration | FYI | Accepted |
