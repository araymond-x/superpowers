# Deviations Log — Adaptive Enforcement Tiers

| Task | Category | Description | Disposition |
|------|----------|-------------|-------------|
| 4 | Bug fix | **Midpoint formula `range_size` definition.** Plan reference code uses `range_size = end - start + 1`, but this produces midpoints outside the task range for single-task inputs (e.g., range (1,1) yields midpoint 2, failing `midpoint_in_range` validator). Changed to `range_size = end - start`, which is consistent with the test fixtures in Task 5 and passes all edge cases. The existing `sdd-pre-dispatch-hook.sh` uses `(TOTAL_TASKS + 1) / 2` (1-indexed); the manifest formula operates on 0-indexed task IDs and will replace the hook calculation. | Accepted — plan code is buggy; formula matches spec intent and test fixtures |
| 4 | Simplification | **`active_module_file` stores bare filename, not joined path.** Plan reference code uses `os.path.join(feature_dir, first.file)`, but the committed test fixture `test_valid_multi_module_session` uses `"active_module_file": "m2.md"` (bare filename). Used bare filename to match the established test contract. | Accepted — matches committed test fixture |
| 6 | Bug fix | **Plan's hook reference code reconstructs `active_module_file` wrong.** Plan shows `MANIFEST_MODULE_FILE="$GIT_ROOT/$MANIFEST_MODULE_FILE"`, which would produce `$GIT_ROOT/m2.md` given Module 1's bare-filename convention (deviation row 2). Corrected to `"$GIT_ROOT/$FEAT/$MANIFEST_MODULE_FILE"` so the reconstructed path includes the feature directory. Surfaced by controller partner review before dispatch; implementer applied the fix. | Accepted — plan reference code is buggy; correction matches Module 1's committed manifest format |
| 6 | IndependentDecision | **Extra variable initializations at outer scope.** Plan reference code initializes the manifest variables only inside the `if [MANIFEST_FILE exists]` block. Implementer added empty-string initializations for `FEAT`, `DEVIATIONS_FILE`, `REPORTS_DIR`, `DISPATCH_LOG`, `MANIFEST_TIER`, `MANIFEST_TASK_START`, `MANIFEST_TASK_END`, `MANIFEST_PLAN_FILE`, `MANIFEST_MODULE_FILE` BEFORE either conditional branch to satisfy `set -uo pipefail` semantics. Strictly additive — no plan logic changed. | Accepted — required for set -u safety; no behavior change |
| 6 | Observation | **`feat_path()` helper is defined inside the legacy `if MANIFEST_MODE=false` block.** Bash function definitions inside `if` blocks are still global to the shell, BUT if `MANIFEST_MODE=true`, the function is never defined. Implementer verified `feat_path()` is only called from within the legacy block itself. Future authors (Tasks 7-10) must NOT call `feat_path()` from manifest-mode code paths. | Accepted — current behavior safe; flagged as constraint for Tasks 7-10 |
| 7 | IndependentDecision | **Extra variable initializations at top of manifest-mode dispatch block.** Plan reference code only initializes `SUBAGENT_TYPE` inline (the line that assigns it from jq). Implementer added explicit initialization of `SUBAGENT_TYPE=""`, `IS_REVIEWER=false`, `IS_IMPLEMENTER=false`, `REVIEW_TASK=""`, `REVIEW_TYPE="unknown"`, `TASK_NUMBER=""` at the TOP of the new manifest block per controller instruction (set -u safety mandate). Matches Task 6's pattern. | Accepted — required for set -u safety; no behavior change |
| 7 | Observation | **Duplicate section-header comment in legacy block.** Both `# ─── Determine dispatch type ───` (original header) and `# ─── Legacy regex-based dispatch detection ───` (new wrapper label) now appear inside the legacy `if [ "$MANIFEST_MODE" = false ]` block. Harmless redundancy; no code removed. | Accepted — cosmetic; not worth a fix-up edit |
| 7 | Observation | **Empty `MANIFEST_TASK_START`/`MANIFEST_TASK_END` strings cause silent pass-through of task-range validation.** If jq returns empty strings (manifest missing `task_range`), the integer comparison `2>/dev/null` suppresses errors and the conditional evaluates false — task-range check is a no-op rather than a hard block. Safe (no false positives), but documentation gap: Module 1's Pydantic `SddSession` model SHOULD enforce that `task_range` is always present, which would make this case unreachable in practice. | Accepted — current behavior is safe failure mode; Pydantic model already enforces task_range as required |
| 8 | IndependentDecision | **Outer-scope variable initializations for 5 new gate variables.** Initialized `NEED_AUDIT=""`, `NEED_PROV=""`, `NEED_CHECKPOINT=""`, `NEED_PARTNER=""`, `CONTEXT_SUMMARY_AT=""` at the manifest-block setup section (near line 78). Required for `set -uo pipefail` safety since each variable is read inside its check's gate. Pattern matches Tasks 6-7. | Accepted — required for set -u safety; no behavior change |
| 8 | Observation | **Check 6 in manifest mode degrades missing-header to TOKEN_WARNING instead of BLOCK.** In legacy mode, if the task header (`### Task N:`) is missing from any plan file, the hook BLOCKS. In manifest mode, the check just uses `MANIFEST_MODULE_FILE` or `MANIFEST_PLAN_FILE` directly without verifying the header is present in it — a missing header degrades to a soft TOKEN_WARNING. Deliberate softening: Task 7 already validates `TASK_NUMBER` is in `MANIFEST_TASK_START..MANIFEST_TASK_END` range, so an in-range task missing its plan header indicates a stale plan file (not a malicious dispatch). | Accepted — task range validation in Task 7 prevents the failure mode the legacy header-existence check guarded against |
| 8 | Observation | **Null-handling behavior for missing `enforcement` key in malformed manifest.** If the `enforcement` object is missing entirely from `.sdd-session.json`, jq returns the string `"null"` for any field lookup. The gate condition `[ "$VAR" = "false" ]` is then false (since `"null" != "false"`), so the check RUNS rather than skips. This is the safe default — a malformed manifest doesn't accidentally disable enforcement. Verified with spot test. | Accepted — safe failure mode (enforcement preserved on null/missing) |
| 9 | IndependentDecision | **12 outer-scope variable initializations.** Initialized `PR_DISPATCH`, `PR_SPEC`, `PR_QUALITY`, `PR_PARTNER`, `PR_DEVLOG`, `PR_CHECKPOINT`, `PROCESS_CONTRACT`, `SENTINEL_LINE`, `SESSION_ID`, `SENTINEL_HASH`, `SENTINEL`, `TEMP_LOG` to empty strings at outer scope in the manifest-block setup section. Pattern matches Tasks 6-8. Required for `set -uo pipefail` safety. | Accepted — required for set -u safety; no behavior change |
| 11 | Bug fix | **Plan's `setup_manifest_workspace` reference code uses wrong midpoint formula.** Plan reference (module-2 Step 1 lines 40-42) uses `range_size = end - start + 1` and `midpoint = start + (range_size + 1) // 2`. This produces midpoints outside `task_range` for small ranges (e.g., `(0,1)` → midpoint=2 → Pydantic validator fails). Implementer applied Module 1's correct formula (`range_size = end - start`) matching `materialize-manifest.py:compute_midpoint`. | Accepted — plan reference was buggy; correction matches Module 1's authoritative compute_midpoint |
| 11 | IndependentDecision | **Extended `make_hook_input` with optional `subagent_type` keyword arg.** Needed for the Explore passthrough test. Backward-compatible — all existing callers unaffected (default value preserved current behavior). | Accepted — minimal extension required for new test coverage |
| 11 | IndependentDecision | **Added `_write_manifest_prereqs_for_task` private helper to test file.** Writes pre-execution audit, N-1 task reports, dispatch log entries, checkpoint, and optional partner review for manifest-mode test setup. The plan referenced helpers (`create_reports_for_task`, `create_full_task_prerequisites`) that didn't exist; rather than create them as public helpers, the implementer scoped this to a private helper in the test file. | Accepted — minimal new helper, kept scope contained |
| 12 | Bug fix | **Plan's Step 4 midpoint formula in `transition-module.py` reference code is buggy.** Plan reference uses `range_size = next_mod.task_ids[-1] - next_mod.task_ids[0] + 1` and `midpoint = next_mod.task_ids[0] + (range_size + 1) // 2`. This produces midpoints outside `task_range` for single-task and two-task ranges (e.g., next module with `task_ids=[3,4]` would yield midpoint=5, failing Pydantic's `midpoint_in_range` validator). Implementer applied Module 1's authoritative formula (`range_size = end - start`, matching `materialize-manifest.py:compute_midpoint`) — same correction made for Tasks 4 and 11. Extracted the formula into a local `compute_midpoint(start, end)` helper for clarity and added a docstring noting the precedent. | Accepted — plan reference is buggy; correction matches Module 1's authoritative compute_midpoint and Tasks 4/11 precedent |
| 12 | ForwardConcern | **`compute_midpoint` duplicated across `materialize-manifest.py` and `transition-module.py`.** Quality reviewer flagged single-source-of-truth violation: the same formula now exists in two scripts, and the plan-code bug has been corrected three times (Tasks 4, 11, 12). Recommend extracting to a shared module (e.g., `skills/subagent-driven-development/scripts/_midpoint.py` or `skills/scripts/models/midpoint.py`) in a follow-up refactor. Not addressed in Task 12 to keep scope contained — the duplication is functionally correct, and consolidation would require touching `materialize-manifest.py` (outside Task 12's owned files per Module 3 write-scope partitioning). | Deferred — log only; scope expansion beyond Task 12. Track for post-Module-4 refactor or future plan author. |
| 12 | Observation | **Quality review flagged minor robustness gaps** (not addressed in Task 12): (a) `--completed-module == --next-module` would create self-contradictory manifest state (recommend 3-line guard); (b) `shutil.move` collides on re-runs if archive contains same-named files (acceptable for current single-pass usage); (c) docstring exit-code list omits some exit-1 sub-cases. None block functionality or downstream tasks. | Accepted — Minor-tier findings; defer to future hardening pass. |
| 13 | Bug fix | **`create_manifest` test fixture needs `git init` in `tmp_path`.** `transition-module.py` calls `git -C <manifest_parent_dir> rev-parse --show-toplevel` (line 115-123) to resolve git-root-relative paths. Without a git repo in `tmp_path`, the call exits non-zero and the script returns exit 2, causing 6 of 7 tests to fail with `"Cannot determine git root"` in stderr. Plan anticipated this in the CRITICAL note and directed the implementer to diagnose empirically and add `subprocess.run(["git", "init", "-q"], cwd=str(tmp_path), check=True)` to `create_manifest` BEFORE writing the manifest. Verified: with `git init` added, all 7 tests pass. | Accepted — required for script's git-root resolution; pattern matches `setup_manifest_workspace` in `sdd_test_helpers.py`. |
| 13 | ForwardConcern | **Test assertion tightening + helper duplication.** Quality reviewer noted 4 minor robustness gaps in `tests/unit/test_transition_module.py`: (a) `test_rejects_single_module_plan` could also assert stderr substring; (b) `test_deviations_log_updated` could match the full pipe-row format, not just `"Module transition"` substring; (c) `test_manifest_updated_after_transition` could assert `module_reports_archived == True`; (d) `test_dispatch_log_archived_and_truncated` could verify archived content survived truncation. Also: `create_manifest` helper duplicates ~80% of `setup_manifest_workspace` in `sdd_test_helpers.py` — track alongside the existing `compute_midpoint` duplication (row 15) for a post-Module-4 refactor. None affect correctness; all 7 tests already exercise the right contracts. | Deferred — log only; tightening can happen alongside the consolidation refactor. |
| 14 | Bug fix | **Plan's Step 2 reference uses brittle `parent.parent.parent` for git-root resolution.** Plan reference (module-3 Step 2 line 467) hardcodes manifest depth = 3 from git root, breaking for tests/non-standard layouts. Implementer used `git -C <manifest_parent> rev-parse --show-toplevel` (matches `transition-module.py:115-123` precedent, same correction made in Task 12 for that file). `parent.parent.parent` retained as a fallback when `git rev-parse` exits non-zero, with a stderr warning. | Accepted — plan reference is buggy; correction matches Task 12 precedent and avoids re-introducing fragility. |
| 14 | IndependentDecision | **Extracted `_load_manifest_config` private helper rather than triplicating manifest-load logic across three phase functions.** Plan reference text shows a 5-line block at the top of `run_pre_dispatch` (with "and similar for other phases" note). Triplicating would violate the Single Source of Truth principle (`~/.claude/rules/architectural-principles.md`) and was explicitly required by the task contract. Helper signature: `_load_manifest_config(args) -> Tuple[Optional[str], Optional[dict]]` returns `(tier, enforcement_dict)` or `(None, None)`; mutates `args.plan_file` in place. Each phase calls it at the top. Backward-compat preserved — returns early when `args.manifest is None`. | Accepted — required by task contract and SSOT architectural principle. |
| 14 | IndependentDecision | **`--plan-file` argparse `required` flag relaxed from True to False.** With `--manifest`, `--plan-file` is redundant. Added explicit guard in `main()` that prints `{"error": "Either --plan-file or --manifest is required."}` to stderr and returns exit code 3 when both are absent. Avoids letting argparse error out — preserves the script's JSON-on-stderr + exit-3 contract for script errors. | Accepted — required so manifest-only invocations work; argparse-native enforcement would violate the script's error contract. |
| 14 | IndependentDecision | **Used legacy `typing.Optional`/`typing.Tuple` annotations instead of PEP 604 `\|` syntax for the new helper.** The Python 3.9 compatibility category of `tests/ARaymond-skill-regression/validate-all-skills.py` enforces legacy syntax for SDD scripts. Other Module 1/Module 3 scripts (`materialize-manifest.py`, `transition-module.py`) already violate this rule (committed previously without remediation) and produce 9 pre-existing FAILs in the regression. Adding fresh violations would worsen the regression delta; conforming to the established rule keeps `controller-checkpoint.py` at zero Python 3.9 issues. | Accepted — conforms to in-tree regression policy; pre-existing violations in sibling files are out of scope for Task 14. |
| 14 | ForwardConcern | **Task 15 reference test code uses wrong check keys.** Plan reference for Task 15 (module-3 Step 1, line 613) reads `checks.get("honesty_check", {})`, but the existing code (and Task 14 instructions) use `honesty_check_missing` and `trace_audit_missing`. Task 15 implementer must update the test to look up `"honesty_check_missing"` / `"trace_audit_missing"` (or change the assertion accordingly) — otherwise the assertion will dereference an empty dict and fail spuriously. | Resolved in Task 15 — `test_micro_tier_skips_honesty_check` reads `checks.get("honesty_check_missing", {})`. |
| 15 | Bug fix | **Plan's Step 1 reference test code uses wrong check key.** Plan reference (module-3 Step 1) reads `checks.get("honesty_check", {})`, but Task 14's production implementation uses `honesty_check_missing` (and `trace_audit_missing`). Updated `test_micro_tier_skips_honesty_check` to look up `"honesty_check_missing"`. Reconciles Task 14's ForwardConcern (row 29). | Accepted — plan reference is buggy; correction matches Task 14's actual check keys. |
| 15 | Bug fix | **`setup_checkpoint_workspace` fixture requires `git init` in `tmp_path`.** `_load_manifest_config` in `controller-checkpoint.py` resolves git root via `git -C <manifest_parent> rev-parse --show-toplevel`. Without a git repo in `tmp_path`, the call exits non-zero and the script falls back to `parent.parent.parent` — which resolves to `tmp_path/docs/`, one directory short of the test tmp root. Joining that fallback with the git-root-relative `plan_file` ("docs/imp-plans/...") then double-nests `docs/`, breaking plan-file resolution. Added `subprocess.run(["git", "init", "-q"], cwd=str(tmp_path), check=True)` to the fixture BEFORE writing the manifest. Pattern matches Task 13's `create_manifest` precedent (deviations row 23). | Accepted — required for script's git-root resolution; matches Task 13 precedent. |
| 15 | IndependentDecision | **Aliased `CHECKPOINT_SCRIPT = SCRIPT_PATH` rather than defining a separate path constant.** Plan reference code defines a fresh `CHECKPOINT_SCRIPT = os.path.join(...)` constant, but the file already has `SCRIPT_PATH` pointing to the same file. Aliasing avoids divergence (SSOT principle, `~/.claude/rules/architectural-principles.md`). | Accepted — both constants would resolve to the same path; alias prevents drift if path changes. |
| 15 | IndependentDecision | **Renamed plan's `run_checkpoint` helper to `run_checkpoint_cli`.** The test file already has a top-level `run_checkpoint(plan_content, deviations_content, report_files)` helper used by `TestStaleArtifactDetection` with a different signature (constructs its own tmpdir, takes plan content rather than paths). Renaming avoids name collision with no semantic change to plan reference behavior. | Accepted — preserves backward compatibility with existing helper; minimal scope. |
| 15 | IndependentDecision | **Omitted `import pytest` from plan reference imports.** Plan reference code includes `import pytest` but the new tests use only the auto-injected `tmp_path` fixture (no decorators, marks, `raises`, or other `pytest.*` API). Avoids unused-import lint warning. | Accepted — `tmp_path` is auto-injected; no `pytest.*` symbols referenced. |
| 15 | ForwardConcern | **Helper duplication tracked alongside compute_midpoint (row 12) and create_manifest (row 24).** `setup_checkpoint_workspace` (this task) shares the git-init-plus-manifest-construction pattern with `create_manifest` (`test_transition_module.py`, Task 13) and `setup_manifest_workspace` (`sdd_test_helpers.py`). Three slight variations exist because each test exercises a different script (`controller-checkpoint.py`, `transition-module.py`, hook script) with different default `task_range`/`midpoint`/`modules` shapes. Track for the post-Module-4 helper consolidation refactor. | Deferred — log only; consolidation refactor already tracked in rows 12 and 24. |
