# Superpowers Fork — Improvements Backlog

Living ledger of open, in-flight, and completed process/tooling improvements for this fork. Items are derived from production SDD sessions and cross-repo evaluations (see **Sources** below). This is the durable, version-controlled tracker — individual planning memos are ephemeral.

## How to use this

- **IDs are stable.** `B*` = items from the BTD consolidation assessment; `I*` = items inferred from that report's data; `C*` = net-new from cross-repo findings / deferred infra; `N*` = other net-new (N3–N9 = findings from the 2026-05-31 SSOT audit + pipeline-flexibility execution; N10–N17 = the 2026-06-01 sdd-enforcement-hardening feature; N18–N28 = the 2026-06-10 sdd-cleanup-and-integration-gate feature — N18 found live at the first module transition and fixed in-feature, N19–N28 are its accepted follow-ups; see that feature's deviations.md for full provenance); N29–N31 = the 2026-06-22 upstream v6 (obra/superpowers) sync assessment (see `docs/process-improvement-findings/2026-06-22-upstream-v6-sync-assessment.md`), N32 = shellcheck findings surfaced by the A2-ported `lint-shell.sh`; `P*` = process entry-point items. Reuse the same ID when an item moves to in-flight/done so history is traceable.
- When an item ships, set **Status** to `done` and record the worktree/commit in **Where / Notes**. Don't delete done rows — they're the audit trail.
- New items: append with the next ID in the relevant series and fill every column.
- **Freshness / "last updated":** none is recorded inline, by design — git is the source of truth. For the last-edit date run `git log -1 --format=%cd -- docs/process-improvement-findings/BACKLOG.md` (or `git log --oneline -- docs/process-improvement-findings/BACKLOG.md` for history). Please don't re-add a hand-maintained date stamp — it only drifts, which is the SSOT anti-pattern this fork exists to fight.

**Sizing rubric:** **S** = ~1 file, localized, low risk · **M** = a few files across model→script→skill, 2–4 tasks · **L** = cross-cutting / new infrastructure / many consumers / needs new test fixtures + design.

**Status values:** `open` · `in-flight` · `done` · `deferred` · `won't-do`

**Improves axes:** friction · speed · quality (`enabler` = unblocks another item; `—` = no change intended).

---

## Ledger

| ID | Title | Source | Improves | Size | Status | Where / Notes |
|---|---|---|---|---|---|---|
| B1 | Fix `general-purpose` subagent_type bug (hook exits before reviewer detection) | BTD High-1 | friction, speed | M | done (merged 7030b25) | `sdd-hook-improvements` worktree — 3-stage classification in `sdd-pre-dispatch-hook.sh` |
| B2 | Inline validation errors in hook output | BTD High-2 | friction, speed | S | done (merged 7030b25) | `sdd-hook-improvements` — `head -n 12` excerpt in Check 4b |
| B3 | Auto-create dispatch log on first reviewer dispatch (cold start) | BTD High-3 | friction | S | done (merged 7030b25) | `sdd-hook-improvements` — idempotent `mkdir -p`+`touch` |
| B4 | Per-task `review_tier` (task-aware minimum-tier threshold) | BTD High-4 | friction, speed | M | done (merged 7030b25) | `sdd-hook-improvements` Module 1 — `review_tier` field + declared-minimum ratio exclusion (task-aware variant, not raise-to-70%) |
| B5 | Remove legacy (non-manifest) dispatch detection path | BTD High-5 | quality | M | done (merged 7030b25) | `sdd-hook-improvements` — manifest-required guard clause |
| B6 | `verification`/no-code task type (skips full dispatch/review cycle) | BTD Med-5 | friction, speed | M | done (merged e53cbaf) | **Implemented in `pipeline-flexibility`, merged to main 2026-05-31 (`e53cbaf`).** First LIVE exercise was Task 7 of `sdd-enforcement-hardening` (2026-06-03) — surfaced **N16** (validate-report rejects empty-files_changed verification reports). `Task.task_type` model field; `validate-plan.py` write-suggesting-keyword WARNING; hook `get_task_type` reader + review-skip on Checks 4b/4c/5d (`sdd-pre-dispatch-hook.sh`); `controller-checkpoint.py` verification-ratio cap (≤30%, Check 8) + git-reality check (Check 9); SKILL.md "Verification Tasks" section + writing-plans "Declaring `task_type`"; e2e steps. Paired anti-smuggling guard (keyword WARNING + ratio cap + git-reality) instead of C4. |
| B7 | Batch reviewer dispatches for minimum-tier tasks | BTD Med-6 | speed | M | open | One dispatched reviewer covering several declared-minimum tasks. Must stay *dispatched* (provenance-logged). Lower leverage now that B4 removes most blocking. |
| B8 | Async-compatible honesty check (`mode: autonomous`) | BTD Med-7 | friction | M | open | User-confirmed needed for unattended standard-tier runs. Add field to `Enforcement` (`skills/scripts/models/sdd_session.py`) + gate in `controller-checkpoint.py` (~L999–1028). **Guard:** prominent log + queued human re-review; must not become a silent bypass (see C3). |
| B9 | `reports/` subdirectory organization (`reports/tasks/` + `reports/process/`) | BTD Low-8 | navigation | L | deferred | Broad blast radius — touches every path consumer (hook, checkpoint, validators, transition-module). Revisit only if navigation becomes a real cost. |
| B10 | Context summary conditional on actual context pressure | BTD Low-9 | friction | M | open | In-flight work narrowed the metric but kept the hard task-number trigger (`compute_midpoint` → `enforcement.context_summary_at` → hook Check 6b). Depends on C6. **UNBLOCKED by N43 (2026-07-14):** N43 adds a direct `ctxcheck` context-window-fill reading to `sdd-pre-dispatch-hook.sh` — the real cumulative-pressure signal B10 needed, superseding the C6(b) improved-proxy dependency (direct measurement > better proxy). B10 becomes a **fast-follow on N43's proven sensor**: convert Check 6b from the `context_summary_at` task-number trigger to consume N43's shared pressure primitive (fire report-compression at a lower absolute rung, below N43's 300k nudge / 400k block — same sensor, escalating ladder). Sequenced AFTER N43 ships so the sensor is proven live on one gate first; folding it into N43 was explicitly declined this session (would ~2× the surface — touches `compute_midpoint`, `materialize-manifest.py`, `transition-module.py` N11 recompute, manifest schema, N21 helper + tests — against a not-yet-proven sensor). |
| B11 | Audit `executing-plans` inline-execution path for parity with SDD enforcement | 2026-06-12 session (closeflow pattern extraction) | quality | M | open | Inline execution is a legitimate first-class mode with structural requirements that make SDD impossible: (1) Workflow-tool tasks can only run in the main session — a dispatched subagent can't invoke the Workflow tool; (2) human gates require the controller to pause mid-plan — SDD's continuous-execution model forbids that; (3) no per-task controller-direct mode in SDD for the hybrid workflow/gate tasks. Plans declare this explicitly (`"REQUIRES inline execution … Do NOT execute via SDD"`). The `executing-plans` skill currently has no equivalent to: pre-execution audit gate, deviations register convention, dispatch-log tamper evidence, honesty check, or trace audit. The hook's no-manifest guard false-positive (fixed `b27c332` 2026-06-12) revealed the gap — the hook assumed any `docs/imp-plans/` artifact meant SDD. Audit what guardrails an inline plan *can* have (some SDD checks are SDD-specific by nature), add them to the `executing-plans` SKILL.md, and verify `plan-validation-gate-hook.sh` handles inline plans correctly. |
| N1 | Hook preflight — report all missing prerequisites at once | inferred I5 | friction, speed | M | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Test-only resolution: `test_n1_multi_error_accumulation.py` proves the hook ALREADY accumulates via `ERRORS+=()` and emits all errors before exit 2 (3 simultaneous violations → 3 distinct BLOCKED lines, rc=2). Regression guard; no hook edit, baseline untouched. **Original analysis:** Audit early-exit points in `sdd-pre-dispatch-hook.sh`; accumulate errors instead of fail-one-retry-fail. The B1 bug cascaded into 6 separate blocks. |
| N2 | SSOT audit: reconcile SKILL.md manual prescriptions vs hook-enforced checks | derived from C6 (confirmed drift) | friction, quality | M | done | **Done 2026-05-31 — see `2026-05-31-ssot-audit.md`.** Audited 15 SKILL.md vs 4 of the 6 registered hooks (the other 2 are advisory exit-0, out of reconciliation scope). Result: the token-estimation redundancy (C6) was the *only* pure retire case and was already retired by C6(a); no second instance found. 0 retire / 1 strengthen / 7 keep; 0 live threshold drift (verification ≤30% and minimum-tier ≤50% both verified to match code). Net-new actionable findings spun out as N3–N9 below. Headline: SSOT is healthier than C6 implied; the real gaps are the multi-module hook path (Check 4c ↔ `transition-module.py`) and the advisory-only Stop-hook pre-completion gate (`sdd-stop-hook.sh` is registered on `Stop` but exits 0 — informs, never blocks). |
| N3 | Check 4c (dispatch provenance) rejects valid plans whose first in-scope task isn't Task 0 | live SDD runs — practerus marketing-cta-guard 2026-05-31 (no-Task-0) + pipeline-flexibility module transitions | friction, quality | M | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Tasks 2+3).** N3a: Check 4c now skips when `PREV < MANIFEST_TASK_START` (`sdd-pre-dispatch-hook.sh`). N3b: `transition-module.py:validate_module_completion` re-verifies boundary provenance before truncation — the sibling enforcement. **Original analysis:** **Two manifestations, one root cause — hit first-hand in both runs.** `sdd-pre-dispatch-hook.sh` Check 4c (dispatch provenance for task N-1) sits *outside* the "first task in module" skip (which wraps only the N-1 report-file checks). Its exemption guard `TASK_NUMBER -gt 0` assumes the first in-scope task is always Task 0 — but it legitimately isn't in two cases. **(1) No-Task-0 plans:** Task 0 is *conditional* (`writing-plans`/SDD SKILL.md mandate it only for plans with external Source Contracts), so a contract-free plan validly starts at Task 1; Check 4c then sets `PREV=0` and greps the log for `task=0 type=spec-review`, which can never exist → `BLOCKED: … dispatch the spec reviewer for Task 0.` The only way past is forging a `task=0` entry — violating the exact provenance model the check protects. **(2) Module boundaries:** `transition-module.py` Step 5 truncates the live dispatch log (`open(dispatch_log,"w").close()`), so the next module's first task has no prior provenance in the live log and Check 4c BLOCKs on the (now-archived) N-1 reviews. This execution abandoned the prescribed `transition-module.py` flow for a manual manifest advance both times to dodge it. **Fix (covers both):** give Check 4c a parallel exemption — skip the PREV provenance check when `PREV < MANIFEST_TASK_START` (no prior in-scope task), preserving full enforcement for plans that *do* declare Task 0 and for non-first tasks within a module. Regression fixtures: a Task-1-start manifest PASSes; a module-2-first-task manifest PASSes; a Task-0 manifest still gets PREV provenance-checked. Same-file sibling to **N1**; pairs with **N4** (together make `transition-module.py`-driven multi-module SDD runnable without manual workarounds). The 3-stage hook's module-boundary Check 4c is a new, untested path (both prior multi-module features ran older hooks). |
| P1 | Direct-to-`writing-plans` entry mode (re-installs setup guardrails) | user-raised | friction, speed | M | done (merged e53cbaf) | **Implemented in `pipeline-flexibility`, merged to main 2026-05-31 (`e53cbaf`).** `Plan.entry_mode` field (`direct`\|`brainstorming`); `writing-plans/SKILL.md` Step 0.5 ("Resolve feature directory") with 4-branch stale-`.active-feature` conflict detection (ported from brainstorming), worktree/branch guard, optional `check-distillation.sh` when a distilled spec is supplied, and entry-mode recording in plan YAML frontmatter. Hook enforces the plan-quality half (plan-validation-gate). Entry-mode dial for the planning pipeline, sibling to `adaptive-enforcement-tiers`. |
| P2 | Reliable project closeout (lifecycle close) | user-raised | friction, quality | M–L | open | Closeout is purely manual + under-cleans, and there's no persisted "done" signal, so completed features pile up unclosed (`.active-feature` left set, feature dirs never archived). **Decisions:** archive feature dir → existing `docs/imp-plans/archive/`; trigger via SessionStart nudge + entry-time GC (no Stop-hook auto-prompt — would fire mid-smoke-test). **(a) Foundation [S]:** add `completed_at`/`status` to `SddSession`, written by `controller-checkpoint.py` at pre-completion gate PASS; gitignore `.sdd-session.json` ✓ **shipped 2026-05-30** (was un-ignored; written to the *feature dir* so the entry is no-leading-slash. One instance — the merged sdd-hook-improvements manifest — is already committed; `git rm --cached` pending user decision on whether to keep it as feature history). **(b) Closeout [M]:** extend `finishing-a-development-branch` to archive the feature dir + remove the manifest (today it only clears `.active-feature`/`.allow-main`/branch/worktree). **(c) Discovery [S–M]:** SessionStart nudge **gated on `.active-feature` present in repo root AND manifest `status: complete` AND nothing in-flight** — stays silent in BiG/outside/non-superpowers repos (no `.active-feature` → instant no-op). **(d) Backstop [M]:** entry-time GC in brainstorming/writing-plans using the signal; in-flight detection (uncommitted changes, branch≠main, unchecked tasks, pending deviations) protects active work. Pairs with P1 as "lifecycle hygiene" (clean entry + clean exit). |
| N4 | Make pre-completion gate archive-aware | N2 SSOT audit + pipeline-flexibility deviations (Module 1→2) | quality | M | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Task 1).** `controller-checkpoint.py` `find_report_file`/`find_all_report_files` now recurse into `archive-*/` — the pre-completion gate passes with archived completed-module reports. (Chosen the recurse-into-`archive-*` option, not the manifest-trust option.) **Original analysis:** `controller-checkpoint.py` `find_all_report_files`/`find_report_file` (L186–189, L121–127) glob only `reports_dir`, not `archive-<module>/`. After a `transition-module.py` archive, completed-module reports read as "missing" at the final gate. Fix: recurse into `archive-*` subdirs, or read `module_reports_archived` from the manifest and trust prior gates. **Pairs with N3** — together they make multi-module SDD runnable through the prescribed scripts (today both quirks force manual workarounds). |
| N5 | Make the fence-blind task-header regex fence-aware (BOTH sites) | N2 SSOT audit | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Fence-aware parsing at ALL sites (7+1 spec'd, plus 2 extra found: `task_zero_is_first`, pre-completion `all_task_ids`) via a single `_unfenced_content` helper, consolidated into `_report_utils.py` (SSOT, lazy pydantic import). 7 tests in `test_fence_aware_parsing.py`. Known minors → N20. **Original analysis:** `^###\s+Task\s+(\d+)` exists as TWO identically-valued regexes — `TASK_HEADER_RE` (`validate-plan.py`:48) AND `TASK_HEADER_PATTERN` (`controller-checkpoint.py`:58) — both must be fixed. Matches `### Task 91/93/94/95` **inside fenced code fixtures** in a plan, inflating the verification-ratio denominator (Check 8), the pre-completion checkbox count, AND `all_tasks_have_reports` (demanded reports for fixture task ids — confirmed false-positive that blocked the pipeline-flexibility pre-completion checkpoint; real tasks 0-9 all had reports). Fix: skip headers inside ``` fenced blocks (sibling of N9 dedup). Low risk, removes a silent miscount. |
| N6 | "Hook enforces this" framing pass on SDD SKILL.md | N2 SSOT audit (extends C6(a)) | friction | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `cbff47e`.** Doc-only, no behavior change. Apply the C6(a) treatment (§258–265, already done) to §282–286 (pre-dispatch checkpoint, enforced by Check 5c), §286/Check-6b (context summary, enforced by Check 6b), and §426–428 (report validation, enforced by Check 4b): state the hook enforces the gate automatically so the manual run is an optional early check, not required ceremony. Removes residual skip-guilt + false "controller-only honor system" impression. Pairs with any future SDD SKILL edit (file is at its word-count ceiling). |
| N7 | Pre-execution `source_contracts: FAIL` false positive | N2 SSOT audit + pipeline-flexibility deviations (Ingestion) + CLAUDE.md Hook Dev Gotchas | friction | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Pre-execution now reports OK (valid-absent) for `Source Contracts: None`/empty; PASS for non-empty; OK for absent. Kills the per-run accepted deviation; CLAUDE.md gotcha rewritten. **Original analysis:** `controller-checkpoint.py --phase pre-execution` reports `source_contracts: FAIL` on a legitimately-empty prose `Source Contracts: None` (treats "None" as present-but-malformed). Documented + accepted as a deviation **every run**. Fix: read frontmatter `source_contracts` field, or treat prose "None" as valid-absent. Recurring friction with a known fix. |
| N8 | F6 regression check is literal-substring brittle | N2 SSOT audit + pipeline-flexibility deviations (Task 6) | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `86ddb95`.** F6 now keys on intent (the presence of a Direct-entry signal) rather than an exact phrase. **Original analysis:** `validate-all-skills.py:569` greps for the literal `"invoked directly"` / `"skipping brainstorming"` to assert standalone-invocation guidance exists; a semantically-equivalent rewording silently FAILs (this execution hit it — required a minimal phrasing fix to satisfy F6). Fix: key on intent (e.g., presence of a "Direct entry" section/heading) rather than an exact phrase. |
| N9 | Extract `_task_ids_where(plan_contents, field, value)` SSOT helper | N2 SSOT audit + pipeline-flexibility deviations (Task 4) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** `_task_ids_where(plan_contents, field, value)` + `_load_all_plan_contents(manifest, git_root)` landed; all callers updated; also fixed a latent manifest-mode aggregation double-count/parent-omission. 7 tests. **Original analysis:** `controller-checkpoint.py` `_declared_minimum_task_ids` (L228) and `_verification_task_ids` (L263) are near-verbatim frontmatter parsers. Collapse into one `_task_ids_where(...) -> (set, parsed_any)`. ~20 lines, behavior-preserving (both ratio paths tested). Flagged by Task 4 quality review; mirrors architectural-principles "single source of truth for logic." |
| N10 | Make the hook's Check 5 Task-0 lookup archive-aware | pipeline-flexibility deviations (multi-module) — coined in `2026-06-01-sdd-enforcement-hardening` spec/plan | quality | S | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Task 2).** `sdd-pre-dispatch-hook.sh` Check 5's Task-0 lookup now globs `archive-*/` as well, so a Source-Contracts plan still finds an archived Task 0 at module 2. Local glob only — the shared `task_report_glob` helper is unchanged. Sibling of N4 (the checkpoint-side archive-awareness); together with N3 they make `transition-module.py`-driven multi-module SDD runnable without manual workarounds. (Coined as a feature-internal ID — went spec→plan without a prior open BACKLOG row; logged here for the audit trail.) |
| N11 | `transition-module.py` did not recompute `enforcement.context_summary_at` on module transition | discovered during `2026-06-01-sdd-enforcement-hardening` plan review | quality | S | done | **Discovered + resolved by `2026-06-01-sdd-enforcement-hardening` (Task 3).** `transition-module.py` did not recompute `enforcement.context_summary_at` on module transition — it stayed pinned to the completed module's midpoint, firing Check 6b early for later-module tasks. Fixed: `transition()` recomputes it for the next module's range. |
| N12 | Transition gate vs hook gate divergence (micro+modules) | `2026-06-01-sdd-enforcement-hardening` deviations (Task 3 spec review) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Transition now gates provenance ONLY on `enforcement.dispatch_provenance` (file-existence stays on review modes ≠ skip) — hook↔transition SSOT; micro+modules transitions PASS with self-review files and no dispatch log. SSOT-agreement truth table still green. **Original analysis:** `transition-module.py:validate_module_completion` gates provenance on `process_requirements.{spec,quality}_review_mode != "skip"`, while the hook gates on `enforcement.dispatch_provenance`. For a micro+modules plan (dispatch_provenance=False but modes=self_review≠skip), the transition would require provenance the hook never wrote (over-enforcement). Only reachable in micro+modules (which `validate-plan.py` already WARNs against). Fix: align the transition gate with `enforcement.dispatch_provenance`, or document the intent. Task 3 was spec-compliant (spec MANDATED `!= "skip"`). |
| N13 | plan.md Task-4 SSOT-test snippet un-runnable as written | `2026-06-01-sdd-enforcement-hardening` deviations (Task 4) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Backport corrected TWICE: the sprint plan's originally-prescribed backport was itself a no-op (plan defect caught by the remediated Task 3 quality review — the mkdir lines belonged at the top of `test_minimum_signal_agreement`, matching the shipped test, not in `_hook_requires_quality_prov`). Hardening plan.md now matches the shipped test. **Original analysis:** The canonical Task 4 test snippet in `docs/imp-plans/2026-06-01-sdd-enforcement-hardening/plan.md` passes nonexistent subdirs to two subprocess drivers (git-init-before-mkdir → `FileNotFoundError`); the SHIPPED test added two `mkdir()` lines. Fix: backport the 2 `mkdir` lines into the plan's verbatim block so a future re-run from plan.md doesn't re-hit the error. Plan-hygiene; not ship-blocking. Instance of C1 (plan-reference code unrun). |
| N14 | C1 pre-existing pipe bug also present in main's advisory skill-enforce hook | `2026-06-01-sdd-enforcement-hardening` deviations (Task 0) | quality | S | done | **RESOLVED by the merge to main (`e708d9b`, 2026-06-03).** The `grep`-into-`grep -q` (piped) `pipefail` SIGPIPE bug was latent in main's then-advisory `sdd-skill-enforcement-hook.sh`; the sprint's de-piped, now-blocking hook replaced that copy at merge (verified: advisory heredoc gone, `exit 2` live). |
| N15 | Hook-integrity baseline (`baseline.txt`) was stale | `2026-06-01-sdd-enforcement-hardening` deviations (Task 7 verification) | quality | S | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Task 7 fix-now, commit 52f130f).** `tests/ARaymond-hook-baseline/baseline.txt` was last captured 2026-04-14 and never re-captured across the SDD-hook-improvements + pipeline-flexibility merges, so `check-hooks.sh` FAILed on 5 hooks even on main (registration unchanged; sha256 hash drift only). This feature's Tasks 0+2 legitimately edited 2 more hooks. Fix: `check-hooks.sh --capture` regenerated all 7 hashes (the 5 pre-existing verified byte-identical to main = deployed reality); committed alone at 52f130f. Process gap: editing a baselined hook should re-capture the baseline in the same change (like migrations+code). |
| N16 | `validate-report.py` rejects `task_type:verification` reports (empty files_changed) | `2026-06-01-sdd-enforcement-hardening` deviations (Task 7 verification) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** `ImplementerReport.task_type` field added; `files_changed_non_empty_for_done` exempts verification; CLI fixture tests prove the full validate-report.py pipeline accepts a verification report with empty files_changed and still rejects implementation+DONE+empty. Non-last verification tasks unblocked. **Original analysis:** First live verification run surfaced it: `ImplementerReport.files_changed_non_empty_for_done` (implementer_report.py:48-53) raises when status is DONE/DONE_WITH_CONCERNS AND `files_changed` is empty — but a `task_type:verification` task legitimately changes ZERO files. `validate-report.py` FAILs on a correct verification report (not gamed with a fake file). Not a mechanical blocker for the last task (no hook Check 4b on it; pre-completion runs no per-report validation), but blocks any future verification task that ISN'T last. Pipeline-flexibility introduced verification tasks but never updated the report model. Fix: add `task_type` to ImplementerReport and exempt verification, or relax the rule when files_changed empty + tests.result PASS. |
| N17 | `transition-module.py` verification-id source has no main-plan fallback | `2026-06-01-sdd-enforcement-hardening` final code review (Task 3) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Main-plan fallback added (mirrors the hook). Known residual: hook checks `-f` existence, transition checks truthiness — SET-but-missing module.file diverges fail-closed → N19. **Original analysis:** `validate_module_completion` reads verification task_type only from the completing module's `module.file` (`if module.file:` → empty set otherwise, transition-module.py:110), whereas the hook falls back to `MANIFEST_PLAN_FILE` when `active_module_file` is empty (sdd-pre-dispatch-hook.sh:297-298). Harmless today (multi-module manifests always set `module.file`; single-module plans never transition), but a latent asymmetry/foot-gun if a future manifest shape omits `module.file`. Fix: add the same main-plan fallback, or document why the guard is intentional. |
| C1 | Plan-reference code execution validation | skill-eval §2.1 | quality | L | open | **Highest correctness leverage.** Reference snippets in plans are approved unrun; this fork's own `compute_midpoint` bug shipped 3×. plan-document-reviewer is codebase-isolated by design and can't catch it. Needs new fixtures + design. |
| C2 | Integration-test coverage requirement | skill-eval §2.2 | quality | M | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Full C2 shipped: `IntegrationTest` model + `Plan.integration_test`; validate-plan advisory risk-surface WARNING (+sections entry); pre-completion **Check 10** (`integration_test_present`) — declared path must `is_file()` AND be in the feature changeset (untracked ∪ diff vs NEWEST merge-base among origin/HEAD/main/master — the stale-unpushed-origin fail-open was found live and fixed; present-but-malformed declarations FAIL with shape guidance — final-review fix 911f025); writing-plans docs; e2e Step 11. 22 tests in `test_c2_integration_gate.py` incl. the BLOCKING merge-base fixture. Follow-ups → N22/N25. **Original analysis:** Unit coverage enforced, e2e isn't (a cache-poisoning vuln + unreachable endpoint passed all unit tests). Plan-time declaration for contract/route/security-touching features + hook check. |
| C3 | Migrate gameable gates from controller-invoked → PreToolUse hooks | skill-eval §10 | quality | L | open | The meta-pattern: out-of-band hooks hold; controller-invoked steps get skipped under pressure (minimum-tier abuse, forged `.dispatch-log`, honesty run only when prompted). B8 must be designed within this principle. |
| C4 | Computed tier recommendation heuristics | adaptive-tiers v1.1 (deferred) | quality | M | open | Flag high-risk tasks (auth/credentials/migrations) wrongly downgraded by task-count alone. Natural guard for B6. |
| C5 | Cross-artifact validation (Pydantic Phase 3) | pydantic Phase 3 (deferred) | quality | L | open | Detect manifest↔plan↔reports↔deviations drift (e.g., "manifest says spec-review dispatched but report is controller-written" — catches the forgery class behind C3). |
| C6 | Resolve the duplicated/manual context-budget step | adaptive-tiers v1.1 + live honesty-check evidence | friction, quality | M | (a) done 2026-05-30 · (b) open | **Reframed — not a "usability" fix.** **(a) shipped:** SKILL.md §257–269 manual prescription retired → now states the hook enforces it automatically (SDD skill 5029→4753 words; regression 145 PASS). Root cause: SKILL.md §257–269 prescribes a *manual* run of `estimate-task-tokens.py` before each dispatch, but the pre-dispatch hook Check 6 (L660–708) **already runs it automatically** and blocks `TOO_LARGE` — so the manual step is redundant ceremony, correctly skipped on judgment (live evidence: honesty-check "skipped required step"). The two have drifted (skill passes `--task-file --constraints-file`; hook passes `--plan-file --task` only), and *both under-measure*: they count task text (+constraints for the skill variant) but NOT real injected context (source files the task lists, CLAUDE.md refs, prior reports, scene-setting) — so a small ≤200-line task can still dispatch a huge prompt. Note the 200-line cap is only a `validate-plan.py` WARNING, not a blocker, so the "validated ≤200-line plan ⇒ within budget" proxy is unsound. **(a) [S, quick win]** retire the manual prescription in SKILL.md → state the hook enforces it automatically (single source of truth; removes skip-guilt). **(b) [M]** strengthen the hook auto-estimate to pass `--constraints-file` (script already accepts it) + estimate injected context. Instance of C3 (controller-invoked → hook-enforced). Strengthened estimate gives B10 a real pressure signal. **UPDATE (N43, 2026-07-14):** the "strengthened estimate gives B10 a pressure signal" linkage is SUPERSEDED — N43 measures cumulative context-window fill DIRECTLY via `ctxcheck` (better than any byte/token proxy), so B10 no longer depends on C6(b). C6(b)'s remaining value is narrower and stands on its own: improving the PER-DISPATCH `TOO_LARGE` estimate (Check 6 — is *this one task's* injected prompt too big?), a distinct concern from cumulative controller pressure (Check 6b / N43). Don't conflate the two: per-dispatch prompt size ≠ accumulated window fill. |
| I1 | Per-feature pipeline too heavy for refactors | inferred (time table) | friction, speed | — | open | Structural; substantially covered by P1 + B6. Track here so it isn't re-discovered. |
| I2 | Subagent dispatch overhead dominates test/doc tasks | inferred | speed | — | open | Covered by B6 / B7. |
| I3 | Honesty check structurally defeated in autonomous runs | inferred | quality | — | open | = B8. |
| I4 | Report validators (`result ∈ {PASS,FAIL}`, `passing ≤ written`) | inferred | — | — | won't-do | Validators are correct — keep them. The pain was diagnosis, fixed by B2. Logged so they aren't loosened by mistake. |
| I5 | Dispatch-log cold-start cascade | inferred | friction | — | open | = N1. |
| N18 | Checkpoint pre-dispatch lacked the hook's N3a module-boundary skip-guard | LIVE — first `transition-module.py` transition, sdd-cleanup sprint-3 | quality | S | done | **Found + fixed in-feature 2026-06-10 (`sdd-cleanup-and-integration-gate`, commits c45f5f7 + 9d0e9c8).** First live transition archived module-1 reports; `controller-checkpoint.py --phase pre-dispatch` for module-2's first task FAILed on the previous task's archived reports (the hook had the N3a guard since hardening; the checkpoint never got it). Fix: previous-task checks SKIP when `previous_task < manifest task_range[0]`; detail distinguishes prior-module vs no-Task-0 cells via `manifest_has_prior_modules`; boundary completion re-verified by transition + terminal pre-completion backstops. 5 tests. |
| N19 | Hook↔transition `module.file` fallback divergence (set-but-missing) + transition cosmetics | sdd-cleanup Task 6 reviews | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `ae05d8a`.** `transition-module.py` now adopts the stricter AND-exists fallback (set-but-missing module.file no longer yields an empty exemption set); dead `verif_ids` initializer and line-number comment rot cleaned up in one pass over `validate_module_completion`. **Original analysis:** Hook checks `-n` AND `-f` (existence) before using module.file; transition (N17 fix) checks truthiness only — a SET-but-MISSING module.file falls back in the hook but yields an empty exemption set (fail-closed) at transition. Spec-faithful; arguably the hook should adopt the stricter semantic. Plus: dead `verif_ids = set()` initializer (both branches reassign) and line-number comment rot ("hook lines ~294-299" → name the construct). One pass over `validate_module_completion`. |
| N20 | Fence-helper edge cases: tilde fences + unclosed-fence-at-EOF | sdd-cleanup Task 3 quality review | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `a8a76cf`.** `_unfenced_content` now honors `~~~` fences and an UNCLOSED fence at EOF emits a WARNING instead of silently blanking the file; `_load_script` importlib loader hoisted (D15). **Original analysis:** `_unfenced_content` ignores `~~~` fences (CommonMark-legal; repo uses backticks only) and an UNCLOSED ``` at EOF blanks the rest of the file — fails closed in validate-plan (header/frontmatter mismatch) but fails OPEN in `all_tasks_have_reports` (missing tasks silently skipped). One characterization test + optional tilde support; single SSOT site now (`_report_utils.py`). |
| N21 | `create_manifest` test helper not tier-faithful (`context_summary_at`) | sdd-cleanup Task 5 reviews | quality | S | open | Carried into `sdd-aggregate-gate-visibility` (not scoped this sprint); still open. Helper hardcodes `context_summary_at: 2` for ALL tiers; real micro materializes `None` — micro-tier fixtures exercise the N11 recompute branch real micro skips (fixture-fidelity gap vs "fixtures must match actual system output"). Make the override tier-faithful (profile value; override only standard). |
| N22 | C2 risk-surface scan calibration (stemming + fence-awareness) | sdd-cleanup Tasks 9/11 | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `efc9204`.** `_C2_RISK_PATTERNS` now uses stem-style patterns (`auth\w*`, `migrat\w*`, …) and the scan runs over `_unfenced_content(content)` so plans quoting keywords in code fences no longer self-warn. **Original analysis:** `_C2_RISK_PATTERNS` is singular-only (`migrations`/`caches`/`routers`/`authentication` all miss) and `routes/` needs a following word-char (misses end-of-token); the scan is raw-content so plans QUOTING keywords in code fences self-warn (module-2's own plan does — quote-vs-use limitation, advisory-only). Fix: stem-style pattern (`auth\w*`, `migrat\w*`, …) + optionally scan `_unfenced_content(content)`. |
| N23 | `extract-execution-trace.py` dead `STATUS_VALUE_PATTERN` ref + fix-dispatch misattribution | sdd-cleanup Task 9 quality review + trace audit | quality | S | open | Carried into `sdd-aggregate-gate-visibility` (not scoped this sprint); still open. (a) extract-execution-trace.py:39-40 reads nonexistent `_mod.STATUS_VALUE_PATTERN` BEFORE `VALID_STATUSES`, so the AttributeError aborts the try and the hardcoded fallback set is ALWAYS used — the lazily-preserved SSOT export has zero live consumers; fix the dead ref or delete the export. (b) The extractor misattributes ad-hoc fix dispatches (task_number 1 / -1000) and lacks cross-session records — weakens the "0 anomalies" signal. |
| N24 | Python 3.9 PATH-`python3` FAILs all frontmattered plans (pydantic subprocess) | sdd-cleanup Task 9 quality review | quality | S | open | Carried into `sdd-aggregate-gate-visibility` (not scoped this sprint); still open. `sdd_session.py:33` uses PEP 604 `int \| None`; validate-plan's pydantic subprocess uses `sys.executable` — under a 3.9 PATH python3 it exits 1 → BLOCKER on every frontmattered plan (fails CLOSED). Not live here (PATH python3 = 3.14) but a portability trap. Fix: Optional[] in models, or pin the subprocess to the venv python. |
| N25 | Check 10 follow-up batch | sdd-cleanup Task 10 + fix reviews | quality | S–M | open | **Partly done 2026-06-22 (`sdd-aggregate-gate-visibility`):** (a) `c79531b` (Check 10 feature-window fallback — on-main false-block fix); (b,d,f) `077cd92` (line-anchored frontmatter scan in `_integration_test_paths` + directory/malformed diagnostics that name the source plan file); (c) `c06b230` (module-level `_git_run` SSOT across 3 git sites). **STILL OPEN, pointed at this feature:** (e) checkpoint's own git calls not config-nulled (only test fixtures hardened); (g) newest-merge-base heuristic unprobed on exotic topologies (long-lived release branches). **Original analysis:** (a) on-base-branch false-block: remoteless repo + SDD on main (`.allow-main`) → merge-base==HEAD makes COMMITTED integration tests invisible → hard FAIL despite legit work (Aaron prefers improving on-main flows); (b) `content.find("---", 3)` not line-anchored in `_integration_test_paths` (inherited from `_task_ids_where` — fix both together); (c) third near-identical git subprocess wrapper → module-level `_git_run` SSOT; (d) directory-as-declared-path says "missing on disk" (fail-closed but misleading detail); (e) checkpoint's own git calls not config-nulled (only test fixtures hardened); (f) malformed-declaration messages don't name the source plan FILE; (g) newest-merge-base heuristic unprobed on exotic topologies (long-lived release branches). |
| N26 | Dispatch-log classification gaps: fix/re-review dispatches + gate-artifact filenames | sdd-cleanup trace audit + live Check 3b block | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `7dc7812`.** Hook now logs review-driven FIX dispatches + partner RE-review rounds (fix/re-review markers, `type=fix`/`type=fix-unattributed` lines) and Check 3b's allowed-prefix list admits the gate-required report names. Baseline recaptured in the same change. **Original analysis:** (a) Review-driven FIX dispatches and partner RE-review rounds log as `type=unknown` or not at all (reviewer-stage passthrough without task number) — the tamper-evidence backbone can't see the fix cycle (N18's fix + 2 reviews left zero entries; corroboration required session-jsonl forensics). (b) Hook Check 3b's allowed-prefix list excludes the GATE-REQUIRED report names (`honesty-check-*`, `execution-trace-audit.md`, `final-code-review.md`, `checkpoint-pre-completion.json` is exempt as .json) — benign today (those artifacts appear after the last implementer dispatch) but a latent block for any post-gate implementer dispatch in the same workspace (bit this session via `fix-n18-*` names). |
| N27 | Pre-completion AGGREGATE gates only police the final module | sdd-cleanup final review (Important #2) | quality | M | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — Check 7 `c27fd79` + Check 9 `9039c97` + docs `f3cfd72` + Task-14 archive-aware e2e proof (this feature's commit).** Both aggregate inputs are now archive-aware: Check 7's `_review_tiers_per_task` globs `archive-*/` (archived module reviews stay in numerator AND denominator) and Check 9's `_merged_dispatch_times` merges archived `archive-*/.dispatch-log` files so verification windows survive live-log truncation. e2e Step 12 is the in-sprint end-to-end proof (H1 self-hosting hazard: live hooks run main's pre-fix scripts, so the e2e — which exercises THIS checkout — is the only archive-aware run this sprint). **Original analysis:** Now that transitions work: Check 7 (min-tier ratio) reads observed tiers via the intentionally-flat glob (archived module reviews leave numerator AND denominator) and Check 9 (git-reality) skips tasks absent from the truncated live dispatch log. Per-task existence/provenance IS boundary-verified by `validate_module_completion`; only cross-module POLICY aggregates lose visibility. Fix options: archive-aware ratio/git-reality inputs (archived dispatch logs exist under `archive-*/`), or transition-time aggregate snapshots. Deserves its own planned task. |
| N28 | Test-hygiene batch from sprint-3 reviews | sdd-cleanup Tasks 4/9 minors | quality | S | open | **(c) done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `a8a76cf` (`_load_script` hoist, D15).** (a,b,d) carried into this feature, still open. (a) N7 test asserts `!= FAIL` — upgrade to `== OK` + blocker-absence (the actual N7 payload); (b) pre-execution three-way arms (non-empty→PASS, absent→OK) have no direct unit coverage; (c) `_load_script` importlib loader now duplicated in `test_fence_aware_parsing.py` + `test_c2_integration_gate.py` — hoist to `sdd_test_helpers.py` on next touch; (d) C2 WARNING `frontmatter=None`-with-risk + explicit-null cells got tests in Task 10 Step 0 — verify they survived (they did; listed for completeness). |
| N29 | Port upstream v6 `writing-skills` authoring additions (Match-the-Form-to-the-Failure + Micro-Test Wording) | upstream v6 sync 2026-06-22 (assessment §A3) | quality | S | open | The remaining Category-A item from the v6 sync (A1/A2/A4 already taken). Content-port two new sections into `skills/writing-skills/SKILL.md`: **"Match the Form to the Failure"** (a table for picking prohibition-vs-worked-example guidance — a flat "don't do X" works for discipline slips but backfires when the problem is output *shape*) and **"Micro-Test Wording"** (sample a phrasing a handful of times against a no-guidance control and read every result by hand before committing). Upstream commits `cbc8273` (+ `fdb0f42`, `1aa45d2`). **Do NOT `git cherry-pick`** — those commits are entangled with the vendor-neutral prose rewrite (CSO→SDO, "your agent") we deliberately decline (assessment §D3); hand-port the section content only. `writing-skills/SKILL.md` is near its word-count ceiling — offset with a `references/` extraction if needed; re-run `validate-all-skills.py` after. Aligns with `docs/prompting-best-practices.md`. |
| N30 | Adopt architecture-neutral SDD cost wins from the upstream v6 review rewrite | upstream v6 sync 2026-06-22 (assessment §B-neutral) | speed, quality | M | open | Three pieces of upstream's v6 SDD rewrite that are **independent of the one-vs-two-reviewer architecture** and so takeable regardless of N31's outcome: **(1) file-based handoff** — upstream's `task-brief` + `review-package` scripts write task text + review diff to files for the subagent to read instead of pasting into context (upstream calls the pasted diff "the single biggest reviewer cost"); extends our existing `reports/` file pattern. **(2) enforced model-naming per dispatch** — our `~/.claude/rules/workflows.md` already *wants* this; upstream made it a template requirement after an unnamed model silently inherited the session's top tier (one run put all 26 reviewers on the most expensive model). **(3) progress ledger for resume** — compare to our `context-summary.py` + checkpoint files; may be redundant or may improve. Evaluate each against our hook-enforced model; (2) likely the cheapest high-value adopt. Pairs with N31. |
| N32 | Clean up shellcheck findings surfaced by `lint-shell.sh --all` | A2 linter first `--all` run, 2026-06-23 | quality | S | open | The first full run of the A2-ported linter flagged ~5 minor warnings — **NONE of the severe SIGPIPE/`set -u` fail-open class.** In 2 baselined enforcement hooks: `handoff-acceptance/scripts/handoff-gate-hook.sh:38` (SC2044 — `for` over `find` output, fragile on whitespace paths) and `subagent-driven-development/scripts/sdd-pre-dispatch-hook.sh:236` (SC2034 — unused `label`, dead-code smell per architectural-principles). Plus `brainstorming/scripts/start-server.sh:100` (SC2164 unguarded `cd`) + `:125` & `stop-server.sh:26` (SC2034 unused `i`); and several `tests/` scripts (SC2064 trap-quoting, SC2320 `$?`-after-echo, SC2010 `ls\|grep`). Fix the 4 skill/hook scripts first — **editing the 2 baselined hooks requires a `check-hooks.sh --capture` baseline re-capture in the same change** (ship together). Test scripts lower priority. Deferred out of the v6-sync commit to keep that scope clean. |
| N31 | STRATEGIC DECISION: one-reviewer-two-verdicts (v6) vs our two-reviewer + partner + provenance model | upstream v6 sync 2026-06-22 (assessment §B-fork) | speed, quality | L | open (needs decision session) | The centerpiece of the v6 sync. Upstream collapsed two per-task reviewers → ONE `task-reviewer-prompt.md` (spec + quality verdicts in one pass, plus a "can't-verify-from-diff" verdict) + one whole-branch review on the top model at the end. Eval-claimed **~2× faster / ~50% fewer tokens**. **Confirmed collision with our enforcement:** `sdd-pre-dispatch-hook.sh` classifies dispatches into spec-review/quality-review/trace-audit/partner-review (lines 163–166) and **line 478 hard-blocks** any task dispatch missing a per-task `task-NNN-spec-review.md`; we still ship `spec-reviewer-prompt.md` + `code-quality-reviewer-prompt.md`. Adopting v6's combined prompt is **not a prompt swap — it's rearchitecting the enforcement layer** (Checks 4b/4c, dispatch provenance, partner gate). The real question: upstream brings outside eval evidence that our two-reviewer+partner+provenance model may be **over-engineered on cost**. Options: **(a)** keep our model, take only N30 cost wins; **(b)** pilot v6 one-reviewer in a throwaway branch, measure against our enforcement suite, decide on evidence; **(c)** full migration (largest; unwinds significant fork investment). Default **(a)/(b)**, not (c). Note: v6's anti-gaming rules (controller can't tell a reviewer what to ignore; reviewers read-only + skeptical of "left it unabstracted on purpose" rationales) align with our philosophy and could be ported into our *existing* two-reviewer prompts independently of this decision. |
| N33 | controller-checkpoint.py pre-dispatch not verification-aware for the PREVIOUS task | sdd-aggregate-gate-visibility Task 14 (live-discovered) | quality | S | open | Pre-dispatch FAILs `previous_spec_review`/`previous_quality_review` when the PREVIOUS task is a `task_type: verification` task (which legitimately has no reviews). The live HOOK is verification-aware (`sdd-pre-dispatch-hook.sh:504-505` skips Checks 4b/4c when `PREV_TASK_TYPE=verification`); `controller-checkpoint.py` pre-dispatch (~lines 1244-1299) is NOT — a checkpoint-script-only false-FAIL (the real gate, the hook, allows the dispatch). Direct sibling to N18 (checkpoint lacked the hook's module-boundary skip-guard). Fix: mirror the hook's PREV-verification skip in the checkpoint pre-dispatch previous-review checks. **(Renumbered N29→N33 on merge: the upstream-v6 sync independently claimed N29–N32.)** |
| N34 | Promote `_frontmatter_block` to `_report_utils.py` — codebase-wide line-anchored frontmatter SSOT | sdd-aggregate-gate-visibility Task 9 quality review + trace audit | quality | S | open | Task 9 (N25b) added a line-anchored `_frontmatter_block` helper to `controller-checkpoint.py` but it is LOCAL to that file. **7 other scripts still use the naive `content.find("---", 3)`** that the helper fixes: `validators.py:56`, `materialize-manifest.py:48`, `validate-plan.py:540`, `transition-module.py:68`, `validate-report.py:95`, `context-summary.py:165,187`. The report validator's naive split bit this very session (a `^---$` inside a Task-9 report-frontmatter value truncated the YAML). No correctness impact today (those sites haven't hit a triple-dash-in-value), but it is a real SSOT gap. Fix: promote `_frontmatter_block` into `_report_utils.py` (stdlib-only — keep validate-plan.py bare-python3 safe) and migrate all 8 sites. (Promised in the Task-9 deviation; created 2026-06-23 by the trace audit's MUST-FIX.) **(Renumbered N30→N34 on merge: the upstream-v6 sync independently claimed N29–N32.)** |
| N35 | pre-dispatch hook Check 5c remediation command + the 3 SKILL.md "Controller Health Checkpoints" commands omit `--manifest` → module-boundary checkpoints fail | live `inbox-agent-int` v1-core-pipeline (first downstream module boundary following the hook's emitted checkpoint cmd, post-N18; Module 1→2) | friction | S | done 2026-06-26 (merged 761efed) | **Surfaced at the first module boundary in a real downstream SDD session that followed the hook's emitted Check-5c command verbatim.** (Sprint 3's 2026-06-10 transition — the first-ever live `transition-module.py` run — built the N18 machinery in THIS repo and drove the checkpoint with `--manifest` directly, so it never exercised the hook's emitted command. N18 also only shipped in Sprint 3, so pre-N18 no boundary-skip existed to arm.) `sdd-pre-dispatch-hook.sh:644` emitted a checkpoint command with `--plan-file <plan.md>` and **no `--manifest`**, so controller-checkpoint.py's N18 boundary-skip (needs `manifest_task_range`, set only with `--manifest`, lines 1113–1118 / 848–851) never armed → the checkpoint hunted the prior (archived) module's reports in the flat `reports/` dir and FAILed; the controller rediscovered `--manifest` by trial-and-error (exactly the transcript). Fix: hook now emits `--manifest $MANIFEST` (drops the unfilled `<plan.md>` placeholder — `--plan-file` is "Required *unless* --manifest", and the manifest resolves plan_file). Companion: the 3 SKILL.md commands (§272–292) used a **non-existent `--feature-dir` flag** (that flag is on `materialize-manifest.py`, not `controller-checkpoint.py`), omitted the *required* `--deviations-file`/`--reports-dir`, and lacked `--manifest` — reconciled to the e2e-verified `--manifest` form. Only bit at module boundaries (why it surfaced now). Baseline re-captured; unit 497 / regression 143-0-2 / e2e 13 green. Sibling to N18/N33 (hook↔checkpoint drift). |
| N36 | Post-implementation `/simplify` cleanup pass at the SDD pre-completion gate | user-raised 2026-06-26 (this session) | quality | M | open | **Design captured; revisit later.** Idea: after code is written following a plan, run the built-in `/simplify` (reuse / altitude / efficiency cleanup that *applies* fixes — distinct from the existing SDD review passes that only *report*). **Decisions reached this session:** **(1) Runner = the controller (orchestrator)** — NOT the implementer subagent (`/simplify` itself fans out to the `code-simplifier` agent, so a subagent can't reliably spawn it / invoke the Skill machinery; the implementer is also scoped to one task's write-scope, and running it there breaks fresh-agent isolation) and NOT the partner (verify/read-only role — must not mutate code). The controller is the only seat in the dispatch tree allowed to have children + invoke skills. **(2) Timing = ONCE at the Pre-Completion Gate** (`SKILL.md:120`, immediately before "Dispatch final code reviewer") — NOT per-module-boundary. The highest-value simplification is *cross-module* reuse, which only becomes visible once all modules exist; a per-boundary pass is structurally blind to it AND risks "simplifying away" a shared seam (e.g. an M1 foundation helper) that later modules import; it also costs 6× the multi-agent fan-out for zero reduction in final-review diff size (simplify edits land as commits either way). A module is an execution-ordering unit, not a shipping unit — if the end-of-feature diff feels too big, the *feature* was too big (split into 2 SDD runs). Required sequence: **simplify → re-run tests (incl. the declared `integration_test`) → final code reviewer** (the review must see the post-simplify code, else it's stale). Per-module only as an explicit opt-in for genuinely independent vertical-slice modules; off by default. **(3) Scope = source files ONLY**, computed from the union of implementers' `files_changed` (already recorded per task in `ImplementerReport`) MINUS tests, fixtures, generated/vendored files, and SDD process artifacts — NOT a raw `git diff base...HEAD`. This is the answer to "would it ever run against non-code?": YES if diff-scoped, and it is actively destructive — it would try to "simplify" golden / byte-identity fixtures, SQL lifted byte-for-byte from a spec, the `reports/` + `deviations.md` audit trail, CLAUDE.md / README prose, and tests (weakening the very guardrail validating the refactor). Only the controller at pre-completion holds the complete file inventory to compute this scope safely — a second reason for end-of-feature timing. **(4) Plan vs SKILL.md = SKILL.md.** "Always simplify after writing" is a uniform *process* rule (identical in every plan) → belongs in the skill = single source of truth; baking it into each plan re-creates the "forget it under pressure" failure mode + duplication. A frontmatter field whose value would always be identical is a smell. The ONLY per-feature signal that legitimately belongs in plan frontmatter is an **exclusion list of simplify-hostile source paths** (byte-identity SQL, golden generators) — that is per-feature *data*, not the simplify decision. **(5) Enforcement (fork philosophy: advisory SKILL.md text gets skipped under pressure):** Phase 1 = SKILL.md Pre-Completion Gate step (cheap, ships now, advisory); Phase 2 = `controller-checkpoint.py` pre-completion check requiring a `reports/simplify-pass.md` artifact (>50 bytes), tier-gated to SKIP on micro — same pattern as the honesty / trace-audit gates; opt-out via `simplify_pass: false` frontmatter that defaults to ON (safe default = on, skipping is the deliberate act). **Open decisions for the revisit:** tests in/out of simplify scope (lean out — risk of weakening assertions); non-redundancy with the existing per-task quality reviewer + final code reviewer (additive because it *applies* vs they *report*, and it's holistic/cross-file vs per-task); commit convention (its own `refactor: simplify <feature>` commit, not amending task commits). **Verify before building:** `/simplify` is a built-in Claude Code skill (not in this fork) — confirm empirically whether it dispatches subagents (the runner conclusion holds either way). Touches SDD SKILL.md + `controller-checkpoint.py` + a new report artifact + `Plan` frontmatter (exclusion list / opt-out) → M. Next step = `superpowers:brainstorming` to pin timing / exclusion-field shape / enforcement phase, then a small plan. |
| N37 | Make per-role model/effort picks executable (encode allocation defaults into dispatch templates) | user-raised 2026-07-06 (this session) — model/effort allocation analysis | friction, speed | S–M | open | **Make the per-role model/effort picks executable, not advisory.** Today `subagent-driven-development/references/model-selection.md` (rewritten 2026-07-06 with the two-dial capability/effort framing + per-role table + current-model footnote) is guidance the *controller reads and applies by judgment* at each dispatch — the kind of controller-invoked step that gets skipped under pressure (cf. C3). N37 bakes a `Recommended model / effort` line into each SDD dispatch prompt template (`implementer-prompt.md` / `spec-reviewer-prompt.md` / `code-quality-reviewer-prompt.md` / `trace-auditor-prompt.md` / `pre-execution-audit-prompt.md`) so the pick travels *with* the template. The pattern already half-exists: `controller-partner-prompt.md` header already says "**Model:** Use haiku". **Two honest constraints.** (1) *Rot:* hard-coding model IDs into N templates reverses the model-neutral-body design that keeps a model launch touching only ONE footnote (validated 3× on 2026-07-06 across the Sonnet 5 / Opus 4.8 / Fable 5 launches — only the footnote moved) — so encode *tier references* ("standard tier", pointing at `model-selection.md`), NOT literal IDs. (2) *Effort not wire-able via the dispatch surface:* SDD dispatches through the Agent/Task tool, which exposes a per-call `model` override but **no `effort` param** (effort is a Workflow-tool `opts.effort` concept / session-level setting) — so N37's *model* half is executable per-dispatch, its *effort* half stays advisory prose unless dispatch moves to the Workflow tool. **Recommended minimal version [S]:** add a one-line tier reference to the 2–3 highest-confidence/most-stable templates (implementer→cheap for well-specified tasks; reviewers→standard/capable; trace-auditor→standard+high), each pointing at `model-selection.md` for resolution — captures most of the consistency benefit, model IDs stay in the single footnote. **Full version [M]:** all templates + a controller-side tier-resolution step. Behavior-touching (changes what the controller actually dispatches) → own brainstorming→plan→SDD cycle, not a doc tweak. **Overlaps N30(2)** (upstream v6's enforced model-naming-per-dispatch): N30(2) is "name *a* model so it isn't silently the top tier" (a cost-safety guard); N37 is "name the *right* tier per role" (the allocation defaults). Do them together — N30(2) supplies the enforcement (must name a model), N37 supplies the defaults (which tier). Analysis + full rationale: `docs/process-improvement-findings/2026-07-06-model-effort-allocation-fable5.md`. Deferred pending one real SDD run that exercises the allocation. |
| N38 | Scope fences must survive spec distillation (negative contract material) | telemetry-exp stable-fact-store spec review 2026-07-07 — finding M1: spec.md §1.2 out-of-scope fence had no counterpart in spec-distilled.md | quality | S | done 2026-07-07 | **The distillation pipeline had no category for negative scope, and every layer leaned toward deleting it:** Rule 2 ("'we considered but rejected' text is stripped") pattern-matches an out-of-scope/deferred list; the distilled template had no fence section; the reviewer prompt's artifact-removal check flags alternative-language (could flag a PRESERVED fence as an artifact); `check-distillation.sh` had only negative checks. Since the distilled spec is the ONLY doc the plan writer + implementers read, a dropped fence has no downstream recovery path — scope fences are what stop an implementer from "helpfully" building deferred work mid-task. **RED evidence:** production M1 (fence fully dropped) + synthetic subagent baseline (fence demoted to one line inside Contract Facts, heading + per-item deferral targets lost — the current rules were followed *correctly*, so this was a rules defect, not distiller error). **Fix (R1–R5, TDD per writing-skills):** brainstorming SKILL.md Distillation Rule 3 "Scope fences preserved" (negative contract material; never merged/summarized/demoted; old rules 3–6 renumbered 4–7); `## Out of scope — do not build` template section preceding Contract Facts (matches the telemetry-exp remediation shape); distillation-review checklist item 6; `distillation-reviewer-prompt.md` fence-preservation check + explicit not-an-artifact carve-out + "near the top" tolerance for Contract Facts; design-presentation coverage now leads with scope boundaries (R5, so specs reliably HAVE a fence to carry); `check-distillation.sh` optional 2nd arg (full-spec path) FAILs (exit 1, `fence: MISSING` + `fence_detail`) when a heading-level out-of-scope/non-goals/do-not-build fence has no distilled counterpart — heading-level matching only (body prose doesn't count); one-arg mode unchanged (writing-plans direct entry calls it with one arg); script is NOT in the hook baseline (no re-capture). **GREEN:** same subagent scenario with updated rules preserved all 5 fence items + deferral targets; checker PASS/`fence: PRESENT`; JSON output valid in every branch (no pipes into `grep -q` — the C1 SIGPIPE class avoided by here-file greps in `if` conditions). 12 pytest cases (`tests/unit/test_check_distillation.py`) pin the one-arg contract + two-arg fence semantics. Suites: unit 509, regression 158/0/2 (pre-existing advisories), install PASS. |
| N39 | Execution Handoff menu: standard fresh-session SDD option via /handoff bundle | user-raised 2026-07-07 (live writing-plans completion gate — user hand-typed a 4th menu option because the standard list lacked it) | friction | S | done 2026-07-07 | The writing-plans Execution Handoff menu offered only same-session SDD or inline execution — but executing SDD in the session that just did brainstorming+planning starts the controller (a sustained-context role, cf. `model-selection.md`) on an already-depleted context window. New standard option 2 **"Subagent-Driven, fresh session"**: invoke the `handoff` skill to build a claude-codex-handoff bundle with entry skill `superpowers:subagent-driven-development` (the toolkit README's own example; `/pickup` invokes the entry skill via the Skill tool → SDD enforcement hooks armed in the fresh session), remind the user to start the fresh session FROM the worktree (hook session-start CWD binding), then STOP without executing. Graceful fallback to options 1/3 when the toolkit is absent (fork↔toolkit cross-dependency kept soft). RED = the live gate incident itself; menu confirmed single-sited (grep: "Inline Execution" appears only in writing-plans). writing-plans SKILL.md 4595→4726 words (`wc -w`; advisory WARN unchanged in kind, under the 5000 hard limit). Regression 158/0/2. |
| N40 | validate-plan.py: two formatting sensitivities surfaced by a live plan run | telemetry-exp launch-hierarchy-normalization plan session 2026-07-14 (charter Module 3) | friction, quality | S | open | **Two findings from a real 5-file modular plan run, both worked around by reformatting rather than tool fixes.** (1) **Task-size counter counts fenced code lines**: the 200-line task limit counts every line between task headers including fenced code blocks — but writing-plans mandates "complete code in every step", so the limit pressures exactly the content that must NOT be compressed. Live effect: a 204-line task was trimmed to 200 by merging SQL lines and shortening a docstring — pure counter-satisfaction, zero content improvement (and mildly worse readability). Consider: exclude fenced-block interiors from the count (keep counting prose), or add an in-fence allowance. (2) **`Source Contracts:` non-empty parse is layout-sensitive**: `**Source Contracts:** None *(consumes Task 0's fixtures…)*` on ONE line parses as non-empty → false "Task 0 missing" BLOCKER on module files whose contracts are genuinely None. The working form (M2-precedent) is `None` standalone with the parenthetical as a separate paragraph — a formatting convention the validator enforces implicitly but no doc states. Consider: treat a value beginning `None` (before any parenthetical) as None, or document the required layout in writing-plans' module template. Both fixes are localized to `validate-plan.py` (+ possibly one template line) → S. |
| N41 | `find <path>` silently skips the `~/.claude/skills/superpowers` symlink's contents (BSD find, no `-L`) | telemetry-exp launch-hierarchy-normalization session 2026-07-14 — agent ran `find ~/.claude/skills/superpowers -name check-distillation.sh`, got 0 results, concluded the script was missing, and fell back to searching the fork by name | friction | S | done 2026-07-14 | **A third instance of the "foreign-CWD path resolution" failure class already tracked in `docs/ARaymond-skills-best-practices.md`'s Script Path Resolution section** (joins the bare `scripts/` and bare `*-prompt.md` incidents). Root cause is BSD `find` semantics, not a missing file or a stale symlink: on macOS, `find <symlink-to-dir> -name X` treats the symlink argument as a leaf node and does not descend into it — reproduced live, `find ~/.claude/skills/superpowers -type f` → 0 results, `find -L ~/.claude/skills/superpowers -type f` → 139 (the real count). Every other way of touching the path (`ls`, `cat`, `bash <path>`, the `Read` tool, a plain absolute reference in skill text) dereferences the symlink transparently; `find` alone doesn't. Agent self-corrected (found the script via the fork's real path), so no functional harm — but it's a landmine for any future bare `find` against a `~/.claude/skills/<name>` path. **No code fix shipped**: confirmed (grep) no bare `find ~/.claude/skills/superpowers` currently exists anywhere in this fork's own scripts/docs, so there was nothing to correct — this entry is the documentation-only fix: `docs/ARaymond-skills-best-practices.md` "Script Path Resolution" gains the incident + the `find -L` rule so it's remembered rather than rediscovered per-agent. |
| N42 | `finishing-a-development-branch`: advisory reminder to update an external charter/parent doc's status | telemetry-exp charter Module 2→3 handoff 2026-07-13/14 — Module 2 merged but the program charter (`alternative-spec.md`) and its README status table were never updated to reflect it, and nothing in the pipeline prompted the fix | quality | S | done 2026-07-14 | Module 2's `spec.md` referenced the charter by relative path (`> **Charter:** ../<dir>/alternative-spec.md`) — the established, working pattern for charter-carved modules — but no step anywhere asks "does completing this feature leave an external doc stale?" so the charter's status banner and its README's module-status table both went stale after merge (confirmed not actually harmful this time: Module 3's own `spec.md` independently re-derived the correct facts during its own brainstorming, but the next module might not). **Fix shipped — "Light" tier** (a "Full" tier — a `Plan.charter_reference` Pydantic field + a `validate-plan.py` risk-surface-style heuristic WARNING + regression test, mirroring how `integration_test` was formalized — was scoped and explicitly declined: no existing fork concept of an external-reference pointer exists yet, and formalizing a schema commitment off one occurrence was judged premature). Added Step 1.5 ("Check for External References") to `finishing-a-development-branch/SKILL.md`, between "Verify Tests" and "Detect Environment" — advisory and non-blocking; looks for a `> **Charter:** ...` header line or a relative path pointing outside the feature's own directory in `spec.md`/`plan.md`, and if found, asks the user whether to update the referenced doc's status before finishing. Files: `skills/finishing-a-development-branch/SKILL.md`, `docs/ARaymond-customization-manifest.md`. Regression suite: 158/0/2 (unchanged pre-existing warnings). |
| N43 | Context-aware SDD auto-handoff (two-tier `ctxcheck` pressure gate → N39 fresh-session handoff) | user-raised 2026-07-14 (this session) — recurring long-SDD context-exhaustion quality degradation | quality, friction | M | done-pending-merge | **DONE (pending merge) — implemented via SDD 2026-07-14; feature dir `docs/imp-plans/2026-07-14-sdd-context-aware-handoff/`. The merge-commit annotation is added during `finishing-a-development-branch`.** Recurring pain: long SDD *controller* sessions exhaust the context window and quality degrades (shortcuts, unflagged judgment calls). The controller is the sustained-context role (cf. N39, `model-selection.md`); subagents get fresh context per dispatch and are NOT the problem. **Design (spine, approved this session):** add a two-tier context-pressure gate to `sdd-pre-dispatch-hook.sh` that reads REAL context-window fill via `ctxcheck` (`~/.claude/bin/claude-ctx-check --json --window <N>`), upgrading today's Check 7 byte-proxy (sums plan+deviations+reports bytes ÷4 → systematically undercounts; blind to reviews, tool output, reasoning, skill loads). **Sensor confirmed viable this session:** a Bash-tool subprocess (same spawn path a hook uses) inherits `CLAUDE_CODE_SESSION_ID` and `ctxcheck --json` returns clean `used_percentage`; window size is NOT auto-detectable from a hook (bare `model` in the transcript, nothing in env; the `[1m]` marker lives ONLY in OTEL — confirmed present in the user's telemetry-exp capture, absent in generic downstream sessions) → which is exactly why the trigger uses **absolute `total_tokens`** and needs no window. **Metric decision (resolved this session):** trigger on **absolute token count**, NOT percentage-of-window — the only metric that behaves identically across the user's **mixed 200k/1M (predominantly 1M)** sessions. **Thresholds (user-set, absolute):** soft NUDGE **300k** tokens, hard BLOCK (`exit 2`) **400k** tokens (~30%/40% of 1M — optimized for the common 1M window; a 200k session auto-compacts before 400k, so protection there relies on a lower per-session env override). Env-overridable (`SUPERPOWERS_CTX_SOFT_TOKENS` / `SUPERPOWERS_CTX_HARD_TOKENS`); tuned from the observation log after run 1. Byte-proxy retained as automatic FALLBACK when ctxcheck fails; bypass `SUPERPOWERS_CTX_HANDOFF_BYPASS`. **At the hard block:** controller commits, builds an **N39** fresh-session `/handoff` bundle (entry skill `subagent-driven-development`), STOPs; the fresh session's `/pickup` resumes SDD mid-plan via the documented recovery path (`references/session-recovery.md` — `reports/` flight recorder + `.sdd-session.json` resume validation + first-unchecked-task). The spine REUSES existing machinery (N39 + session-recovery) — net-new code is the sensor + the mid-execution trigger wiring only. **Posture:** two-tier nudge+block, chosen over advisory-only (the degraded agent ignores nudges — the self-monitoring paradox) and hard-block-only (no grace window); the hard block is the seam future cmux autonomy plugs into. **Scope = spine only (component A).** Deferred to their own specs: **(B)** cross-session honesty aggregation (stop-hook already captures honesty logs globally); **(C)** pace-aware pause/resume around the 5h/weekly windows via `cupace` (different sensor, different remedy = sleep-until-reset, not handoff); **(D)** cmux auto-spawn of the next session (real binary `/Applications/cmux.app/Contents/Resources/bin/cmux`, but unattended self-spawn carries runaway + quota-burn risk → needs safeguards). Targets the SDD controller specifically (task boundaries = durable pause points); writing-plans/brainstorming have fewer clean seams (later). **Impact on B10/C6:** see those rows — N43's direct `ctxcheck` reading IS the real cumulative-pressure signal B10 was waiting on (supersedes the C6(b) improved-proxy dependency); B10 becomes a fast-follow (convert Check 6b from its `context_summary_at` task-number trigger to consume N43's shared pressure primitive) once the sensor is proven live on this one gate. **H1 self-hosting hazard:** the live hooks resolve to THIS main checkout via settings.json absolute paths — editing `sdd-pre-dispatch-hook.sh` affects the running session; execute in a worktree + re-capture the hook baseline (`check-hooks.sh --capture`, hook is baselined). **Shipped (component A, spine):** `context-probe.py` sensor + the two-tier context-pressure gate in `sdd-pre-dispatch-hook.sh` (nudge/block/byte-proxy-fallback + streak escalation) + `references/context-handoff-protocol.md` + operational/troubleshooting docs + e2e Step 13. **Fast-follow now UNBLOCKED — B10** (pressure-conditional context-summary): convert Check 6b from its `context_summary_at` task-number trigger to consume N43's now-proven direct `ctxcheck`/probe pressure sensor (see the B10 row). **Post-merge:** confirm the INSTALLED hook (main checkout, resolved by settings.json) fires live — the e2e proves the checkout code path only. |
| N43(D) | cmux auto-spawn of the successor SDD session (N43 component **D**) | N43 deferred component (D); `cmux-integration` design spec approved 2026-07-22 (`spec.md:3-6`) | quality, friction | M | done-pending-merge | **DONE (pending merge) — implemented via SDD 2026-07-22 → 2026-07-25; feature dir `docs/imp-plans/2026-07-22-cmux-integration/`. The merge-commit annotation is added during `finishing-a-development-branch`.** Closes the **(D)** component the N43 row above still lists as deferred ("cmux auto-spawn of the next session ... needs safeguards") — **this row, not N43's notes, is (D)'s current state.** **Shipped:** `skills/subagent-driven-development/scripts/spawn-handoff-session.sh` — spawns the successor in a new cmux workspace via repo-1's extended `claude-picker`, exit ladder **0** spawned / **3** manual fallback / **1** refused; plus the safeguards N43 made a precondition for (D): hop limit `SUPERPOWERS_CMUX_MAX_HOPS` (3) against runaway chains, session-quota refusal `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` (15) against quota burn, reserve-before-spawn bookkeeping (`reports/.handoff-hops` + `reports/handoff-spawn.log`), and clean-tree + bundle-identity preconditions. Also: `references/context-handoff-protocol.md` steps 3–5 rewritten so the protocol drives the script, `test_spawn_handoff.py` (72), e2e Step 14 (banner 15). **Shipped alongside (NOT an N43 component):** the vendored cmux skills repo `~/projects/claude-custom/cmux-custom-skills` (repo-2 — separate git repo, own `verify-install.sh`). ⚠ This feature's own spec calls that repo its "component A" (`spec.md:181`); that is **not** N43's component A (the context-gate spine) above — the letter is overloaded, so name the thing. **Scope is Claude-only:** codex parity stays **N51**, still `blocked` on codex-side SDD — closing (D) does not move it. **Post-merge: ✅ DONE 2026-07-28 — live smoke RAN AND PASSED against the installed skill path on merged main (`46365d7`).** Dry run: exit 0, `launch=auto`, `quota=ok:93.0`, correct composed line, nothing created. Real spawn: exit 0, `workspace:13`, `.handoff-hops`=1, `intent`→`outcome` records 1s apart (reserve-before-spawn confirmed live), `--focus false` held. Successor verified by eye AND from its own transcript: `/pickup` ingested the scratch bundle (0 refs to the instruction bundle → no recursion), entry skill loaded through the real `Skill` tool with the plan-validation gate hook firing, and its status bar confirmed `--telemetry`/`--session-label`/`--pick-version` were APPLIED, not merely composed. `plan.md:217` now legitimately ticked. **Two sub-clauses were vacuous in the live env** (empty `CLAUDE_CODE_PICKER_ARGS` → nothing to decode; label had no `-Session-N` suffix → seed branch, not the increment branch) and were covered by labelled synthetic `--dry-run` probes, not counted as live evidence — full detail in the feature's `deviations.md` `Task 11 (feature close)` row. **STILL OPEN — the true acceptance test** (`plan.md:225` item 3): the first genuine HARD-block hop in a downstream project. This smoke exercised the launch mechanism on demand; it did NOT exercise the context-gate trigger that fires it. |
| N44 | SDD self-inflicted-blocker doc fixes: honesty-check filename (A), controller-facing report-schema shape (B), checkpoint "automatic" wording (E), TodoWrite de-hardcode (G) | 2026-07-19 `recoverable-enrichment` SDD run (field bug report, Issues A/B/E/G) + this-session source verification | friction, quality | S | done (this session, unmerged) | **Doc-only — no hook/code edits, no hook-baseline recapture.** **(A)** three authoring surfaces (`honesty-check-prompt.md`, `commands/honesty-sdd.md`, `references/honesty-check-block.md`) instructed writing `reports/honesty-check.md`, which three consumers reject — Check 3b naming allowlist in `sdd-pre-dispatch-hook.sh:511` (**not** tier-gated → blocks mid-feature dispatch), the pre-completion glob `controller-checkpoint.py:1534`, and the vault-copy glob `sdd-stop-hook.sh:102` — all require `honesty-check-*.md`. Aligned all four surfaces incl. `SKILL.md:444` to `honesty-check-<YYYY-MM-DD>[-module-N/-session-N].md` (date+scope; date-only collides at same-day module boundaries). **(B)** `SKILL.md:419` controller-facing prose named the fields but not the strict nested shapes (`files_changed:[{path,description}]`, `tests:{written,passing,command,result}`, `contract_compliance:[{constraint,status,detail}]`) → a controller-persisted report failed `validate-report.py` and blocked the NEXT dispatch; now cross-refs `implementer-prompt.md` "Report Format" + states the shapes. **(E)** `SKILL.md:274` "enforces … automatically at dispatch time" overstated (hook only verifies the saved checkpoint exists); reworded. **(G)** SKILL.md hardcoded "TodoWrite" (~15×); a TaskCreate/TaskUpdate harness read it as a skipped step — defined it as the session's generic tracker + named plan-file checkboxes the durable SoT. Regression suite green (PASS w/ the two documented word-count warnings; SDD body 49xx/5000). |
| N45 | `transition-module.py` builds archive dirs from unsanitized module TITLES (+ heterogeneous `completed_modules`) | 2026-07-19 `recoverable-enrichment` SDD run (field bug C; telemetry-exp commits `d842fcf`/`a8f0160`/`9039b7a`) + source verification | quality, friction | M | open | **Confirmed at source.** `transition-module.py:220` `os.path.join(reports_dir, f"archive-{completed_module}")` uses the raw `--completed-module` value; a title containing `/` (e.g. "Operational CLI (enrich / repair / describe-enrichment)") makes `os.makedirs` create a NESTED tree of space-padded, paren/space-hostile dirs. `:243` appends the raw value to `completed_modules`, yielding a heterogeneous list (IDs "1".."5" mixed with titles). **Fix candidates:** resolve to module ID via the manifest (`archive-module-07/`) or slugify (strip `/`, collapse spaces); store `completed_modules` as `{id,title}`; add tolerance/migration so existing nested archive dirs stay discoverable. **Structural** (design + migration) → brainstorm before coding; touches transition unit + e2e tests. |
| N46 | `context-summary.py` is not archive-aware → misleading counts at the midpoint it exists to serve | 2026-07-19 `recoverable-enrichment` SDD run (field bug D) + source verification | quality | M | open | **Confirmed at source.** `context-summary.py:418` `find_report_files` globs only `reports_dir/task-*-implementer-report*`, never `archive-*/`; after transitions archive prior reports, the midpoint summary sees only live reports → `{"reports_processed":1,"files_tracked":0}`, "Tasks completed: 1 of 22". Same archive-awareness class already fixed for `controller-checkpoint.py` (N4/N27) but MISSED here. ("of 22 vs the feature's 27" is a secondary `--total-tasks`/`count_plan_tasks` caller detail — diagnose separately.) **Fix:** traverse `archive-*/` (depends on N45 so nested slash-dirs don't break traversal) or derive completion from manifest+plan checkboxes; at minimum label the summary's scope. Needs an archived-modules fixture. |
| N47 | `validate-report.py` DONE-vs-Deviations/Concerns warning fires on already-dispositioned deviations | 2026-07-19 `recoverable-enrichment` SDD run (field bug F) | quality | S | open (by-design — likely won't-do) | `validate-report.py:90-104` warns "status is DONE but report has non-empty Deviations or Concerns" whenever those sections are non-empty. **NOT a blocker** (non-blocking stderr WARNING, exit unchanged) and **consistent with the fork's own rule** (`implementer-prompt.md:236`: "Use DONE_WITH_CONCERNS if you have any entries in Deviations or Concerns"). Field session found it noisy when every deviation was already dispositioned (Accepted) in `deviations.md`. Changing it = a deliberate rule change (allow DONE with dispositioned deviations, e.g. cross-check `deviations.md` status), not a bug fix. Lowest priority; document the intended rule either way. |
| N48 | Document SDD fix-cycle intent (partner review for `[task N fix]`; re-review depth/model) | 2026-07-19 `recoverable-enrichment` SDD run (field observation H) | quality | S | open | Doc-only clarity, not a bug — behavior exists in the hook, SKILL text is silent. (1) Fix dispatches (`MARKED_FIX=true`; `sdd-pre-dispatch-hook.sh:248/295/829`) skip provenance/partner gates that initial implementer dispatches get; if intended, state it in the Controller Partner Verification section (else close the gap). (2) Nothing guides re-review depth/model per round; field recommendation: proportional to fix surface — controller-pinned test-only edits may use the cheap tier w/ a tight checklist, any PRODUCTION-code fix gets full-capability re-review. Add to `references/model-selection.md` + the Review section. |
| N49 | `validate-report.py`: emit a minimal valid example (and/or `--print-template`) on frontmatter failure | 2026-07-19 field bug B (suggestions 2/3) — deferred from N44's doc fix | friction | S | open | N44 fixed the DOC (SKILL.md:419); this makes the VALIDATOR self-serviceable at the point of failure. The per-field Pydantic errors are good but leave the exact valid shape to guesswork — print a minimal valid frontmatter example on failure, and/or add `--print-template` emitting a skeleton. Code + unit test (not a baselined hook → no baseline recapture). Complements N44/N50. |
| N50 | Regression coverage for the self-inflicted-blocker class | advisor (durable safeguard) + 2026-07-19 field report "General cautions" | quality (enabler) | M | open | Prevent silent re-drift of N44/N45/N46: (a) **naming-allowlist test** enumerating every skill-instructed artifact filename, asserting each matches Check 3b's allowlist AND the relevant consumer glob (guards A/honesty-check); (b) **validate-report round-trip** from the shipped `implementer-prompt.md` template/example (guards B); (c) **transition slash-title test** (guards N45); (d) **context-summary archived-modules fixture** (guards N46). Fits `tests/unit/` + `validate-all-skills.py` cross-ref checks. Also polish Check 3b's BLOCKED message (`sdd-pre-dispatch-hook.sh:522`) — it says "All reports must use task-NNN-{type}.md format" even though `honesty-check-`/`pre-execution-audit`/`context-summary`/`partner-review` are separately allowed, which misled the field agent about the honesty-check file it had legitimately created. |
| N52 | `extract-execution-trace.py` anomaly detectors are structurally INERT — the trace-audit gate cannot fail | cmux-integration pre-completion gate 2026-07-25 (this feature's own trace audit) | quality | M | open | **The SDD pre-completion trace audit has never been able to fail.** All six rules in `detect_anomalies()` (`extract-execution-trace.py:787-887`) gate on `subagent_return["found"]` / `["status"]`, and the extractor does not populate either — verified across a 15-session merged trace: `found` is `False` and `status` is `None` for **13/13** task records. So `"total_anomalies": 0` is **vacuous, not clean**, and *every prior SDD run's* trace audit passed a check that structurally could not fail. Corroborating internal contradictions in the same artifact: `plan_checkbox_updated:false` on 13/13 while the plan files were fully ticked, and `code_quality.dispatched:false` for tasks 1/2/5/6/11 while `.dispatch-log` shows quality-review rows for all five. **Second defect, same instrument (found while verifying the fix wrapper): task attribution is a TEXT MATCH, so a session that merely DISCUSSES task N is recorded as having executed it.** Re-running the extraction *after* the audit manufactured a phantom Task-4 record whose "dispatch" was the trace-auditor prompt itself — the act of auditing the gap closed it in the evidence. A second audit therefore always looks cleaner than the first. **Fix:** populate `subagent_return`, or re-gate the rules on report-file status (available and reliable); tighten attribution to actual `Agent` tool-call records rather than prose matches; and add a self-test that **seeds an anomaly and asserts it fires** — an assertion the current suite cannot make. **Until fixed, treat no historical "0 anomalies" trace as evidence** and audit against `.dispatch-log` + report files + `deviations.md` instead. Evidence + a working multi-session merge wrapper: `docs/imp-plans/2026-07-22-cmux-integration/reports/execution-trace-audit.md` and `reports/merge-execution-traces.py`. |
| N53 | Dispatch provenance does not record `SendMessage`-resumed reviews | cmux-integration trace audit 2026-07-25 | quality | S | open | A re-review conducted by **resuming an existing reviewer agent** via `SendMessage` does not pass through the `PreToolUse -> Agent` hook, so **no `.dispatch-log` row is written**. The log then shows a `type=fix` row with no closing re-review even when a full re-review happened. Live instance: the trace auditor flagged Task 8's fix as closed without re-review; in fact `task-008-quality-review.md` carries `# Round 2 — Quality Re-Review after the fix round (verdict: PASS)` with MX1 independently reproduced. The auditor's *observation* was right and its *inference* wrong — which is the problem: **the log cannot distinguish "re-reviewed via resume" from "re-review skipped".** Matters because `SendMessage` resume is the documented recovery when a reviewer dies mid-response (used repeatedly on this run), and because Check 4c / `transition-module.py:validate_module_completion` both reason from these rows. **Fix candidates:** have the controller append a provenance row when resuming a reviewer, or hook the resume path, or record re-review rounds in the report file's frontmatter where the checks can read them. |
| N54 | `SKILL.md`'s trace-extract command cites a non-existent `--feature-dir` flag | cmux-integration pre-completion gate 2026-07-25 | friction | S | open | SDD `SKILL.md` Pre-Completion Gate step 8 documents `extract-execution-trace.py --session-file <f> --feature-dir <dir> --output <out>`. **There is no `--feature-dir` flag** — the script takes `--deviations-file` and `--reports-dir`. Copy-pasting the documented command fails with `unrecognized arguments`, mid-gate. **Same defect class as N35**, which fixed the *checkpoint* commands for exactly this reason and missed the trace command. Also worth fixing alongside: the step's discovery idiom `ls -t … | head -1` silently audits ONE session of a many-session feature (this run spanned 15) — see N52's wrapper for the multi-session shape. One-line doc fix plus a discovery-idiom note. |
| N55 | No opt-in/opt-out control for cmux auto-spawn — and `MAX_HOPS`, the nearest thing to a kill switch, fails OPEN | user-raised 2026-07-27 (cmux-integration pre-completion gate): "is there a step in the process that allows me to opt in or out of the CMUX integration?" | quality, friction | S | open | **Answer to the question that raised this: no, there is no such step anywhere.** Verified, not recalled: `grep` for `cmux` in `subagent-driven-development/SKILL.md`, `skills/scripts/models/sdd_session.py`, and `skills/scripts/models/plan.py` returns **zero hits** — so unlike `enforcement_tier` / `review_tier` / `task_type` / `entry_mode` there is **no declarative dial** in plan frontmatter or the session manifest. Nothing prompts at `/pickup`, at plan ingestion, or at the gate. `references/context-handoff-protocol.md` step 4 says "Run:" unconditionally. **Root cause is a spec gap, not a bug:** every precondition in `spawn-handoff-session.sh` asks *"can we spawn?"* (cmux reachable `:125`, hop limit `:137`, quota `:195`) and none asks *"does the user want us to?"* — consent was never modeled as a precondition. **Mitigating:** the spawn is **not hook-wired** — `grep -rln 'spawn-handoff-session' skills/ hooks/ ~/.claude/settings.json` matches only the protocol doc and the script itself, so the hook merely BLOCKS (`exit 2`) and a controller *agent* chooses to run the script. Agent-driven, not machine-driven. **Today's de-facto opt-outs, all side effects:** (a) not being in cmux → exit 3 manual fallback (the real default, degrades gracefully); (b) `SUPERPOWERS_CMUX_MAX_HOPS=0` → `0 >= 0` true → exit 3 on the first attempt — works, but repurposes a runaway-chain guard as a consent flag and **logs the wrong reason** ("hop limit reached"); (c) `SUPERPOWERS_CTX_HANDOFF_BYPASS` → too blunt, also disables the N43(A) context protection that is the entire point. **Second defect, found while testing (b): `MAX_HOPS` has NO validation guard**, unlike `SUPERPOWERS_CMUX_QUOTA_MIN_PCT` (`:27`, regex + warn-and-revert) and `_QUOTA_TIMEOUT` (`:154`, same). Measured: `MAX_HOPS='0'` → BLOCKS (exit 3) ✓; `MAX_HOPS='abc'` → `[ "$HOPS" -ge "$MAX_HOPS" ]` raises "integer expression expected", the branch is not taken, execution **falls through and SPAWNS**; `MAX_HOPS=''` → also proceeds. **So the one lever closest to a kill switch fails OPEN on a typo, while its sibling quota vars fail SAFE** — an inconsistency in the script's own defensive posture, and the worst possible direction for a runaway guard. **Proposed fix (S):** (1) an explicit `SUPERPOWERS_CMUX_AUTOSPAWN=0` checked as **precondition 0**, exiting 3 with an honest "auto-spawn disabled by config" message rather than a borrowed one; (2) a `MAX_HOPS` numeric guard mirroring the quota vars' warn-and-revert-to-default, so a typo refuses instead of spawning; (3) optionally a per-plan `auto_spawn: false` frontmatter field for repos that should never self-spawn. **Note the script is FROZEN for the cmux-integration feature** — this is deliberately deferred rather than slipped in at the pre-completion gate. |
| N56 | cmux auto-spawn is OPEN-LOOP — post-spawn liveness is unverified (originally filed as "structurally unverifiable"; **that premise was disproven 2026-07-28** — see the UPDATE in Notes) | live cmux/codex spawn exercise 2026-07-27 (user-driven, this session) | quality | ~~M~~ → **S** | open | **The spawn automates the launch, not the outcome.** `spawn_claude_workspace` (`spawn-handoff-session.sh:415`) makes exactly one call — `cmux new-workspace --name … --cwd … --command … --focus false` — parses `OK <ref>` from stdout, and returns cmux's exit code. **cmux exits 0 as soon as the WORKSPACE EXISTS.** Nothing ever confirms that `claude` launched, that `/pickup <id>` ingested the bundle, that the SDD skill loaded, or that execution resumed. After the spawn the script's model of the world is "assume it worked". **It cannot be fixed by trying harder, because cmux has no surface-read:** `cmux --help` exposes `send`, `send-key`, `send-panel`, `send-key-panel` — write-only. There is **no** capture/read/screen command, so the parent has no API by which to observe the child's terminal. **Concrete failure mode, hit live this session:** launching a codex session into a fresh directory raised Codex's *directory-trust prompt* ("Do you trust the contents of this directory?") and the session sat on a blocking modal. Claude Code has the equivalent folder-trust prompt. A successor spawned into a **newly created worktree** could hit it, and the parent would report `exit 0`, append an `outcome` record, fire a "successor spawned" notification, and STOP — every visible signal saying success while nothing runs. **The exit ladder has no rung for this**: all six exit-3 causes describe things that fail BEFORE the spawn. (Common-case mitigation: the successor's cwd is `$WORKTREE_ROOT`, the parent's already-trusted worktree — so a same-worktree hop is likely safe. A fresh worktree is the exposure.) **UPDATE 2026-07-28 (post-merge live smoke) — the gap is NARROWER than filed.** The smoke operator verified the spawned successor **objectively, from the successor's OWN session transcript**, not just by eye. That matters: a spawned Claude session writes its own `.jsonl` to `~/.claude/projects/<encoded-cwd>/`, so the parent never needs to *read the screen* — it can read the child's transcript FILE. This is an already-persistent liveness channel that requires **no cmux change at all**, and it is cheaper than fix candidate (a). It does NOT fully close the gap: there is a startup race (the transcript may not exist yet when the parent exits) and a **correlation problem** (which of the N transcripts in that directory belongs to the child?). But it reframes N56 from *structurally impossible* to *needs a correlation trick* — e.g. have the successor's composed command echo its own spawn id, or match on the `/pickup <bundle-id>` first prompt, which is unique per spawn. **Prefer this over (a) unless correlation proves harder than a heartbeat.** **Fix candidates:** (a) a child-side heartbeat — have the composed successor command append a `started` record to `handoff-spawn.log` once the pickup runs, giving the parent (or a later reader) a positive liveness signal in the file that already exists; (b) upstream request for a cmux surface-read/capture command; (c) at minimum, document that `exit 0` means "workspace created", NOT "successor running". (a) is cheap and needs no cmux change. Relates to the never-run-live gap (`plan.md:217`). **UPDATE 2026-07-28 (cmux CLI capability audit) — THE PREMISE OF THIS ROW IS FALSE; SIZE DROPS M→S.** Full analysis: `docs/process-improvement-findings/2026-07-28-cmux-cli-capability-gap-analysis.md`. Two independent corrections, both verified against the **installed** binary (`cmux 0.64.20 (100) [14e3400b9]`), which this fork's own rule says outranks the vendored skills and the doc URLs. **(1) `cmux read-screen` EXISTS** — it is in `cmux --help`, listed directly above `send` (`read-screen [--workspace] [--surface] [--window] [--scrollback] [--lines <n>]`, "Read terminal text from a surface as plain text"), plus the tmux-compat `capture-pane` and a streaming `pipe-pane --command <sh>`. So "cmux has no surface-read … write-only" is simply wrong, and the sentence "It cannot be fixed by trying harder" told every subsequent session to stop looking. **(2) Screen-reading is not even the right tool — `cmux wait-for` makes it unnecessary, and it is now EMPIRICALLY PROVEN cross-workspace.** Probe run 2026-07-28, three tests: positive control (signalled token → waiter exit 0); negative control (`--timeout 3` unsignalled → `Error: wait-for timed out…`, **exit 1**, real elapsed 3.031s, so the timeout is genuinely enforced); and the one that matters — a parent blocked in this workspace was released by a child in a **separately spawned `workspace:16`** (`PARENT-WAIT-EXIT=0`). The token namespace is server-scoped, consistent with `wait-for --help` exposing no `--workspace` flag. **Remedy (supersedes fix candidates (a)/(b)):** compose the successor command with a trailing `cmux wait-for -S sdd-hop-$SPAWN_ID` **after** the `/pickup` arg so it fires only once pickup has run; parent then blocks on `cmux wait-for sdd-hop-$SPAWN_ID --timeout <N>`. Exit 0 = successor confirmed live (write `outcome` as today); exit 1 = the new ladder rung this row asks for (spawned-but-never-started → notify + manual instructions). This beats the 2026-07-28 smoke-derived transcript-file idea: **no startup race and no correlation problem**, because `$SPAWN_ID` (already generated at `spawn-handoff-session.sh:359`) *is* the token. Sibling row **N61** carries the implementation. **UPDATE 2026-07-29 (trust-dialog mechanism TESTED — the proposed explanation is DISPROVEN for our worktree convention).** `2026-07-29-handoff-hardening-recommendations.md` §1 proposed that Claude's trust is exact-path keyed with **no** inheritance, and therefore that spawning a successor into a freshly created worktree "should be expected to raise Claude's trust dialog" while the script reports `exit 0` — i.e. N56's failure with a named mechanism. That document labelled its final step `inferred` and said to test it. **Tested, two arms, both a-run:** the version binary was launched directly (`~/.local/share/claude/versions/2.1.220`, matching `claude-picker`'s `exec` path, deliberately NOT the `claude` shim whose wrapper sets `CLAUDE_CONFIG_DIR` and would skew the reading). Arm 1 — an **unseen subdirectory of an accepted root** (`superpowers/docs`, absent from `~/.claude.json`): **NO dialog**, straight to the prompt. Arm 2 — positive control, a path with **no accepted ancestor** (`/tmp/trust-probe-…`): **dialog FIRES** (*"Quick safety check: Is this a project you created or one you trust?"*, options `1. Yes, I trust this folder` / `2. No, exit`). Without arm 2 the result would not distinguish "inherited" from "dialogs never fire here" — it does. **Conclusion: trust IS effectively inherited from an accepted ancestor.** Load-bearing consequence: this fork's worktrees live at `<project-root>/.worktrees/<name>`, **under** the accepted project root, so a fresh worktree does **not** raise the dialog and the proposed mechanism does not apply to the normal path. **The residual exposure is real but much narrower than filed:** only a cwd with NO accepted ancestor — a worktree placed outside the project root, a scratch/temp dir, or a freshly cloned repo. So the trust dialog is a *possible* trigger for the open-loop failure, not the *expected* one; N56 still needs **N61**'s handshake, which detects a stall whatever caused it. Probe left no trace: `~/.claude.json` had 36 tracked paths before and after, no flags flipped, and the dialog was never answered. |
| N57 | Successor topology: workspace-per-hop vs. the user's repo-workspace / session-surface model | user-raised 2026-07-27 — "I put projects/repos in a dedicated workspace then independent work sessions in separate surfaces" | friction, quality | M | open | **Today every hop creates a sibling WORKSPACE** (`cmux new-workspace`), so a 3-hop chain leaves three top-level sidebar entries with no repo grouping. The user's model — repo = workspace, work session = surface within it — is organizationally better and would keep the sidebar clean. **The blocker is one missing flag, and it shaped the current design by accident:** `cmux new-surface` has `--working-directory`, `--workspace`, `--focus` … but **NO `--command`**. Only `new-workspace` can create-and-submit atomically. Verified against the installed binary's `--help` (0.64.20). **Three consequences of switching to surfaces:** (1) **the atomic launch becomes a race** — it splits into `new-surface` then `cmux send --surface <ref> "<line>\n"` (`send` does support `\n` for Enter), with no way to know the shell is ready because there is no surface-read (see **N56**); (2) **the exit ladder gains an unmapped state** — "surface created but the send raced a not-ready shell" returns success from both calls, so the script reports `exit 0` with the hop already reserved and nothing running, failing SILENTLY where today's `:491` fails loudly; (3) **schema/test/doc churn** — `SPAWN_WORKSPACE_REF` and the log field literally named `workspace=` are pinned by spec §5.4d, asserted by e2e Step 14, and documented in CLAUDE.md. **RECOMMENDED MIDDLE PATH:** `new-workspace` already accepts **`--group <id|ref>`**, `--group-placement afterCurrent|top|end`, `--group-reference <workspace>`. Grouping successors under the repo gets the organizational win while keeping `--command`, the atomic launch, the exit ladder and the log schema **completely unchanged** — a one-flag addition instead of an architecture change. **Unverified and must be checked first:** how workspace groups are created/referenced (`list-workspace-groups` is not a command; only the `new-workspace` creation flags were confirmed). **Do NOT reach for `new-surface --type agent-session --provider claude`** as a shortcut: it bypasses `claude-picker` entirely and discards version pinning, telemetry routing, session-label increment and append-prompt forwarding — i.e. the whole of repo-1's contribution under Decision 19. **UPDATE 2026-07-28 (cmux CLI capability audit) — consequences (1) and (2) are LARGELY DISSOLVED; the row's real blocker is narrower than filed.** Analysis: `docs/process-improvement-findings/2026-07-28-cmux-cli-capability-gap-analysis.md`. This row inherited **N56**'s false "there is no surface-read" premise; both corrections apply here. **(1) "the atomic launch becomes a race … no way to know the shell is ready":** `cmux wait-for` removes the race outright (proven cross-workspace 2026-07-28 — see N56's UPDATE), and `respawn-pane [--surface <ref>] --command <cmd>` ("Send a command … to a surface", verified in `--help`) sends a command to an EXISTING surface, so `new-surface`'s missing `--command` is no longer the hard wall it looked like. `read-screen`/`capture-pane` are a further fallback. **(2) "the exit ladder gains an unmapped state … fails SILENTLY":** the same handshake closes it — a surface that never starts times out with exit 1 rather than reporting success. **(3) schema/test/doc churn stands unchanged** — `SPAWN_WORKSPACE_REF`, the `workspace=` log field (spec §5.4d), e2e Step 14 and CLAUDE.md all still pin the workspace vocabulary, so that cost is real regardless. **Still genuinely unverified, as originally filed:** how workspace **groups** are created/referenced — `list-workspace-groups` is confirmed ABSENT from `cmux --help` on 0.64.20, so only the three `new-workspace` creation flags (`--group`, `--group-placement`, `--group-reference`) are known to exist. The `--group` middle path therefore remains the recommendation, and it remains the cheap option — but "surfaces are blocked" is no longer an accurate reason to avoid the alternative. |
| N58 | Re-vendor decision for repo-2's cmux skills (64 lines behind upstream) + capture the doc-source hierarchy | cmux docs/version validation 2026-07-27 (user-raised: "do we validate this against the latest documentation for the version I am running?") | quality | S | open | **Measured drift (2026-07-27):** vendored `cmux-custom-skills/skills/cmux/SKILL.md` is **84 lines**; upstream `main` is **52** — **64 changed lines**. `VENDOR.md` pins `fc093e03…` vendored 2026-07-23. **Characterized before recommending:** the delta is a **condensing prose rewrite**, with **no command or flag differences** (`comm` over the command lists returned empty), and the Window/Workspace/Pane/Surface definitions **byte-identical** — so the backwards-reading vocabulary documented in CLAUDE.md is CURRENT upstream, not a vendoring artifact. **Therefore re-vendoring is NOT urgent and NOT routine hygiene:** upstream's condensed 52-line version has not been assessed as *better for our purposes*, and re-vendoring would discard 32 lines of usage detail our agents currently read. Decide deliberately: re-vendor via `sync-cmux-skills.sh` (updating `VENDOR.md`'s SHA + date), or record an explicit decision to hold at `fc093e03…` with a re-check cadence. **Version-tracking hazard the check exposed (already captured in CLAUDE.md's cmux section):** THREE revisions are in play and none track each other — the installed app (`cmux --version` → `0.64.20 (100) [14e3400b9]`, a build hash pinning the real revision), the vendored skills' SHA, and the doc URLs from `cmux docs settings`, which resolve to **`main` = latest, NOT the installed version**. **Rule recorded: for flag-level facts, `cmux <cmd> --help` on the installed binary outranks all three** — the only source guaranteed to describe the code that will execute. Docs for intent, CLI for contract. |
| N59 | Pace-aware pause/resume around the 5h/weekly quota windows (promoted from N43 component **(C)**) | N43 design 2026-07-14 (deferred at carve-out); promoted 2026-07-28 after the live smoke proved the sensor works | quality, friction | M | open | **Promoted from N43's deferred component (C)** — "pace-aware pause/resume around the 5h/weekly windows via `cupace`" — now that the sensor is proven live rather than theoretical. **What EXISTS today (do not re-implement it):** `spawn-handoff-session.sh:159-187` `check_quota()` already calls the tool (`SUPERPOWERS_CMUX_QUOTA_TOOL`, default `$HOME/.claude/bin/claude-usage-pace`, aliased `cupace`) with `--json --no-log`, bounded by the macOS background-watcher pattern, and **live-verified in the post-merge smoke: `quota=ok:93.0`**. **But it uses the output as a BINARY VETO at a single instant, nothing more:** it extracts exactly ONE scalar — `remaining_pct` of the window whose `key=="session"` — and collapses it to `low:<pct>` (below `SUPERPOWERS_CMUX_QUOTA_MIN_PCT`, default 15 → notify + manual instructions + exit 3), `ok:<pct>` (proceed), or `unchecked` (proceed — **fail-open by design**, since a broken reading must not strand a legitimate handoff). Burn rate, the 5h-vs-weekly window split, time-to-reset, and ahead/behind-pace are all DISCARDED. **Why pacing is a different feature, not a bigger threshold (N43's original rationale, still correct):** context pressure means *this session* is degraded → remedy is hand off to a fresh one; quota exhaustion means *every* session is throttled → **handing off changes nothing, because the successor hits the same wall.** Spawning to escape a quota limit is actively counterproductive: it burns quota to create something that cannot use quota. Hence today's correct behavior is REFUSE, not pace. The remedy pacing needs is **sleep-until-reset**, which does not exist anywhere in this fork. **Design notes for whoever picks this up:** (1) the trigger point is probably NOT spawn time — the quota check fires only when a handoff is already being attempted, whereas pace-awareness is most useful **between tasks**, before the controller starts work it cannot finish; (2) it needs the window metadata `check_quota` currently throws away, so the extraction is the first change; (3) keep the fail-open contract — a pacing feature that can strand a session on a bad reading is worse than no pacing; (4) it is a **different sensor and a different remedy** from N43(A)'s context gate, so resist folding them into one ladder. |
| N60 | Enumerate the full cmux CLI surface against our integration — we scoped the feature to what the VENDORED skill documents, not what the binary exposes | user-raised 2026-07-28 ("have we limited ourselves to the CLI features exposed in the official cmux skills we vendored?") cross-referenced against <https://github.com/yigitkonur/awesome-cmux> | quality, friction | S–M | open | **The user's hypothesis was confirmed measurably, and the miss is a CATEGORY, not one command.** Analysis: `docs/process-improvement-findings/2026-07-28-cmux-cli-capability-gap-analysis.md`. The vendored `cmux/SKILL.md` is 84 lines; probed for 13 commands the installed binary exposes, it returned **0 hits for all 13**. Across all four vendored skills exactly one appears — `read-screen`, once, at `cmux-custom-skills/skills/cmux-workspace/references/commands.md:66`, in a *different* skill's reference file the spawn design never consulted. **Unused and verified present on 0.64.20:** `read-screen` / `capture-pane` (read a surface), `pipe-pane --command` (stream pane text into a shell command), `wait-for` (named sync token → **N61**), `events` (structured NDJSON stream with `--name`/`--category`/`--after`/`--cursor-file`/`--reconnect` — a durable-cursor lifecycle channel), `set-status`/`set-progress`/`log`/`todo` (sidebar pills, progress bar, activity feed), `hooks setup` / `hooks <agent> install` (native agent lifecycle — and note `cmux hooks --help` says "Claude Code hooks are injected automatically by the cmux Claude wrapper", i.e. a cmux-side channel may ALREADY be live in our sessions that our integration neither reads nor coordinates with), `surface resume`/`restore-session`/`agent-hibernation` (persistence), `respawn-pane --command`, `rpc`/`capabilities` (full JSON-RPC control plane), `find-window --content`. **Root cause worth naming:** CLAUDE.md's rule *"for flag-level facts, `cmux <cmd> --help` on the installed binary OUTRANKS all three [doc sources]"* was written 2026-07-27 — the same day N56 was filed reasoning from the vendored skill instead. The rule existed; the **enumeration** never happened. A vendored skill is a curated teaching document, not an API manifest. **Deliverable:** a capability-vs-usage matrix (every top-level command × used / considered-and-rejected-with-reason / unexamined), so the unexamined column is explicit rather than invisible. **Two quick wins likely to fall out of it, both near-zero cost:** (a) sidebar telemetry — the pre-dispatch hook already computes the context token count, so `cmux set-progress <ctx/HARD> --label ctx` + `cmux set-status task "N/M"` / `hop "$SP_HOP/$MAX_HOPS"` turns the invisible 300k/400k thresholds into a live gauge (the awesome-cmux README lists "context-window fill percentage" as an established community progress-bar driver); (b) evaluate `cmux events` as a replacement for inference-from-screen-state. **Community prior art to read before extending ours (secondhand — README summaries, repos unread):** `owizdom/context-brdige-for-cmux` ("cold-start context injection via briefing handoff" — N43(D) by another name), `hummer98/using-cmux` (billed as the complete orchestrator reference), `cmux-agent-toolkit` ("barrier-style sync primitives" — almost certainly `wait-for`), `niaeee/cmux_skill` (802-line IDLE/STALL/ERROR state machine). Pairs with **N58** (re-vendor decision): a thicker vendored skill is not the fix — the fix is not treating the vendored skill as the capability ceiling. |
| N61 | Close the auto-spawn loop with a `cmux wait-for` handshake (implements the corrected N56) | N56 premise correction + cross-workspace probe, 2026-07-28 | quality | S | open | **Implementation row for the corrected N56; the mechanism is already proven, only the wiring is left.** Probe evidence and full design: `docs/process-improvement-findings/2026-07-28-cmux-cli-capability-gap-analysis.md` (Addendum). **Design:** (1) in `build_successor_cmd()` (`spawn-handoff-session.sh:334`), append `cmux wait-for -S sdd-hop-$SPAWN_ID` to the composed successor command, positioned **after** the `/pickup` arg so the token fires only once pickup has actually run — not merely when the shell starts; (2) after `spawn_claude_workspace` returns 0, block on `cmux wait-for sdd-hop-$SPAWN_ID --timeout <N>`; (3) exit 0 from the wait → successor confirmed live, write the `outcome` record and exit 0 as today; exit 1 (timeout) → **new exit-ladder rung**: spawned-but-never-started, notify + `print_manual_instructions` + exit 3, with an `outcome` record carrying a distinguishing field. **Why this shape:** `$SPAWN_ID` already exists (`:359`) and is already interpolated into the composed fallback tail, so the token IS the correlation key — no startup race, no transcript-directory correlation problem, no polling. **Decisions to pin before coding:** the timeout default (must exceed cold `claude` start + skill load + `/pickup` ingestion — measure, don't guess) and its env override name (`SUPERPOWERS_CMUX_SPAWN_WAIT_TIMEOUT`, mirroring the existing knobs); whether a timeout should keep or refund the reserved hop (Decision 21 says reservations are durable, so **keep** — but state it); and whether the wait is opt-out, since it makes a previously fire-and-forget script block for up to N seconds. **Note the fail-direction inversion:** every other precondition in this script fails OPEN or refuses BEFORE spawning; this one runs AFTER the spawn, so a timeout must never imply "nothing was spawned" — the successor may be alive and merely slow. Word the message accordingly. **Ship with:** a unit test using a stub that signals the token, a test that asserts the timeout rung fires and exits 3, and an e2e step. Not a baselined hook → no `check-hooks.sh --capture`. Pairs with **N55** (the same edit is the natural moment to add the numeric `MAX_HOPS` guard and the `SUPERPOWERS_CMUX_AUTOSPAWN` consent flag). **VERIFY AT THE RIGHT LAYER — added 2026-07-30 after upstream [manaflow-ai/cmux#5639](https://github.com/manaflow-ai/cmux/issues/5639) showed cmux's own team making exactly this mistake.** They shipped a fix for lost hooks on Claude resume, tested it, passed review — and it did not work, because the test asserted the *argv string* while the defect was in *shell resolution*. Their own post-mortem: *"only asserted the **argv string** … Nothing exercised the **actual shell resolution** … The fix was verified at the wrong layer."* **The identical trap is available here:** it is easy and satisfying to assert that the composed successor command CONTAINS `cmux wait-for -S sdd-hop-$SPAWN_ID` — and that assertion passes whether or not the token ever fires. A composed string is not a behavior. **Required:** at least one test in which the token is actually signalled and the parent's wait returns 0, and one in which it is not and the wait exits 1 — the same RED/GREEN and positive-control discipline that caught the N62 test which passed pre-fix for the wrong reason, and that made the 2026-07-29 trust probe conclusive. String-shape assertions are fine as *additional* checks; they may not be the only ones. Ship the timeout rung with a test that observes the timeout, not one that reads the flag. |
| N62 | `.active-feature` was an unchecked path authority with WRITE reach outside the worktree | **outside (Codex) review** of `spawn-handoff-session.sh`, 2026-07-28 — finding **M2**; review bundle `2026-07-29T01-40-25Z-superpowers` | quality | S | done 2026-07-28 (unmerged) | **Found by an independent non-Claude reviewer; missed by this fork's own reviews AND by a same-session Claude review of the same file.** The script read the entire contents of `.active-feature` (`:58`) and interpolated it straight into `REPORTS_DIR` (`:59`) with no validation, so a stale, hostile, or conflict-marked value such as `../../elsewhere` redirected `mkdir -p`, the hop counter and the spawn log **outside the worktree** — and the run still reported a normal spawn, because Precondition 1's clean-tree check cannot see destinations outside the tree. The sharp observation was the **internal inconsistency**: the bundle path immediately below (`:84-94`) is charset-checked, canonicalized with `pwd -P` and containment-tested, while the path carrying WRITE authority got none of that. **Fixed:** `head -n 1` (single-line by spec; not a pipe, so no SIGPIPE hazard) + reject empty + reject absolute + segment-precise `..` rejection (`*/../*`, NOT `*..*` — the latter would refuse a legitimate `v1..2`) + a `pwd -P` containment check guarded on directory existence (the feature dir legitimately may not exist yet; `mkdir -p` creates `reports/` later). Lexical rules deliberately mirror the fork's existing `IntegrationTest` model (non-empty, non-absolute, no `..`). 5 tests in `tests/unit/test_spawn_handoff_hardening.py`, RED-verified against the pre-fix script. Not a baselined hook → no `check-hooks.sh --capture`. **ID-collision note:** the concurrently-running N60 audit session was instructed to propose rows starting at N62; renumber its proposals on merge (precedent: the N29→N33 renumber). |
| N63 | Post-spawn `outcome` log writes are unchecked → a false-complete audit trail on disk errors | outside (Codex) review 2026-07-28 — finding **m1** | quality | S | open | **The mirror image of something a prior review counted as a strength.** The two *reservation* writes are explicitly checked (`if ! printf` at `:457` and `:466`) — and a same-session Claude review cited exactly that as evidence of care, then did not check whether the other writes matched. They don't: both `outcome` appends (`:480`, `:486`) ignore their status. If storage fills or `reports/` becomes unwritable **after** `cmux new-workspace` succeeds, the script still prints `spawned successor`, exits 0, and leaves only an `intent` record — indistinguishable from a process that died between reservation and spawn. `references/context-handoff-protocol.md:53-57` directs operators to consult this log FIRST when diagnosing, so the one artifact the protocol trusts is the one that can silently go missing. The failed-spawn outcome record can be lost the same way. **Fix (note the direction):** check each append, but do NOT convert a successful spawn into a retryable failure — the successor exists and re-spawning it is the runaway risk the whole design guards against. Emit an explicit durable-audit warning (and ideally a notify) saying the successor was created but its outcome could not be recorded, and still exit 0. Needs fault-injection coverage: writable `intent`, unwritable log. Pairs with **N61** (same function, same edit window). |
| N64 | Protocol promises the successor resumes from committed state, but a successful spawn deliberately dirties the tree | outside (Codex) review 2026-07-28 — finding **m2** | quality, friction | S | open | `references/context-handoff-protocol.md:18-20` step 2 states the fresh session "resumes from committed state only", yet the script writes `.handoff-hops` and `handoff-spawn.log` (`:457-470`) **after** the clean-tree precondition and before the successor starts — so in the normal success case the successor begins in a dirty worktree. Not unsafe in itself, but the stated invariant is misleading and nothing instructs the controller to acknowledge or commit those artifacts. **It also compounds:** the next hop's Precondition 1 refuses a dirty tree with exit 1, which is a **second instance of the class the protocol already documents** at `:78-84` (where the dirty-tree cause is the blocked task's own checkpoint/partner-review files). Two different mechanisms produce the same confusing refusal; only one is documented. **Fix:** state plainly that a successful spawn creates two local, committable bookkeeping artifacts, and tell the successor to fold them into its next commit — or introduce a deliberate post-spawn commit if the clean-state invariant is meant to hold literally. Doc-first; consider whether the exit-1 guidance at `:78-84` should enumerate both causes. |
| N65 | Ship the cmux transport for `/handoff-review` into the handoff toolkit (repo: `claude-codex-handoff`) | this session's live cross-tool review trial, 2026-07-28 | friction, quality | M | open (bundle prepared) | **Proven by a live end-to-end trial before being proposed.** `/handoff-review auto-cdx` already dispatches Codex **headlessly** and attaches findings. What headless cannot do: survive an interactive trust/sandbox prompt, carry telemetry attribution + a session label, or be watched while it runs. This session drove the cmux variant by hand and it worked end-to-end in **1m 59s** — bundle (`--target-tool codex`, `--review-subject plan` so the target is named by path rather than framed as a `git diff` the files aren't in) → `cmux new-workspace` running `codex-picker --non-interactive --telemetry on --session-label …` → `$pickup <bundle-id>` (**the `$` skill syntax resolved correctly**) → codex read-only review → `claude-codex-handoff findings` attach → our side reports `Findings: attached`. `git status` was byte-identical before and after, confirming read-only. It found 2 Major + 2 Minor, one of which (**N62**) no Claude review had found. **Where the work belongs:** the toolkit repo `~/projects/claude-custom/claude-codex-handoff` (`src/claude-commands/handoff-review.md` + `src/bin/claude-codex-handoff`, with `tests/test_command_skill_parity.py` enforcing command↔skill parity), NOT this fork — so a `/handoff` work bundle was prepared to carry it there: **`2026-07-29T02-01-24Z-claude-codex-handoff`** (entry skill `superpowers:brainstorming`; artifacts = the verbatim hand-driven recipe, this analysis doc, and the Codex findings). Run it from the toolkit repo with `/pickup 2026-07-29T02-01-24Z-claude-codex-handoff`. **Note for N51:** its blocker text reads as though codex integration is stalled; what is blocked is *SDD auto-spawn* (codex has no SDD skills). The **review** path needs nothing from SDD and demonstrably composes today. **Untested rung:** no permission prompt occurred in the trial, so the interactive-approval path — the main reason to prefer this over headless — remains unexercised. **UPDATE 2026-07-29 — that rung is now DETERMINISTICALLY REPRODUCIBLE, which makes it testable rather than incidental.** Verified a-run (see N56's 2026-07-29 update for the two-arm method): Claude's folder-trust dialog fires exactly when the launch cwd has **no accepted ancestor** in `~/.claude.json` — proven by a positive control in `/tmp` — and does **not** fire for an unseen subdirectory of an accepted root. Codex's is the opposite shape: `~/.codex/config.toml` `[projects]` is ancestor-inherited, and `/Users/araymond/projects` is trusted wholesale, so it essentially never prompts for repo work. **So the test this row asks for is now a recipe, not a wait:** dispatch the same review into a cwd with no accepted ancestor, both ways, and assert the interactive transport lets an operator answer while headless `auto-cdx` hangs to timeout. That contrast **is** the mode's justification and is the blocker the prepared bundle names. The hardening doc's H1 (preflight the trust files and refuse rather than hang, `unknown` ⇒ prefer interactive) is the shipped form of the same insight. |
| N66 | Sidebar telemetry: surface SDD context pressure, task and hop in the cmux sidebar | N60 capability audit 2026-07-28 (quick win (a) named in N60's notes) | quality, friction | S | open | **The 300k/400k context thresholds are invisible folklore today; this makes them a live gauge.** Design + measurements: `docs/process-improvement-findings/2026-07-28-cmux-capability-usage-matrix.md` §3 Q2 (diff sketches for both sites). **Split by data ownership, not preference:** the **hook** (`sdd-pre-dispatch-hook.sh`) owns `set-progress <ctx/HARD> --label` + `set-status sdd_ctx <tier>` + `sdd_task <N>` — it is the only place the token count exists; the **spawn script** owns `set-status sdd_hop "$SP_HOP/$MAX_HOPS"` — the only place those exist. **The load-bearing guard is NOT `\|\| true`:** measured 2026-07-28, with the cmux env stripped `cmux set-status x y` returns **`OK`, exit 0** and writes to the **selected** workspace (landed on `workspace:2` "Superpowers", not the caller's `workspace:17`). It never errors — it silently mislabels ANOTHER session's sidebar, and two concurrent controllers would overwrite each other. So: gate on `[ -n "$CMUX_WORKSPACE_ID" ]` **and** pass `--workspace "$CMUX_WORKSPACE_ID"` explicitly. Only a missing `cmux` binary actually fails (exit 127). **Cost:** 54–55 ms per call measured → ~110 ms per dispatch; background the block. Use unique `sdd_*` keys (`set-status --help` documents keys as the anti-collision mechanism). Add `clear-status sdd_*` to the SDD completion path or pills go stale. **Ship with:** `check-hooks.sh --capture` in the SAME commit — `sdd-pre-dispatch-hook.sh` is one of the 7 baselined hooks (the spawn script is not, and needs no re-capture). Cosmetic only: nothing may gate on a status write. |
| N67 | Use `cmux new-workspace --env` to carry successor state instead of the composed command string | N60 capability audit 2026-07-28 | quality, friction | S–M | open (probe DISCHARGED 2026-07-30 — dispositioned by **N76**; watch item, not a subtraction — see the UPDATE in Notes) | **The one finding that SUBTRACTS shipped code rather than adding surface.** `cmux new-workspace --help` on 0.64.20 exposes **`--env KEY=VALUE` (repeatable) and `--env-file <path>`** — **both ABSENT from the top-level `cmux --help` summary line for the same command** (which reads `new-workspace [--name] [--description] [--cwd] [--command] [--layout] [--window] [--focus] [--group] [--group-placement] [--group-reference]` — the two `--env*` flags and only those are missing), which is why the design never saw them (confidence: a-help, read from the installed binary; **not exercised — probe before building**). Today `spawn-handoff-session.sh` forces ALL successor state through one composed shell string: the v1 base64 argv codec (`:222-264`), `shlex.quote` re-quoting of every element (`:333`), the `printf`-not-`echo` xpg_echo workaround (`:347`), and base64 append-prompt rematerialization. A shell re-parses that string in the spawned workspace — hence the quoting machinery. `--env` is a second channel needing none of it: hop number, `$SPAWN_ID`, and `SUPERPOWERS_CMUX_*` overrides could ride as env vars. **Scope honestly:** the append-prompt is CONTENT, not a scalar, so `--env` does not obviously replace rematerialization (`--env-file` may — probe it); reserved `CMUX_*` names cannot be overridden. Also unexamined on the same command, though this one WAS in the top-level summary and so is a plain miss rather than a discovery gap: **`--layout <json>`, whose "Layout surfaces define their own commands"** partially dissolves N57's "`new-surface` has no `--command`". Full detail: `2026-07-28-cmux-capability-usage-matrix.md` §2.7. **UPDATE 2026-07-30 — DISPOSITIONED BY N76; the "not exercised — probe before building" instruction above is DISCHARGED, do not re-run it.** The probe was run (`cmux-spawn-v2` Module 1 Task 1; full transcript `2026-07-30-sp2-workspace-env-probe.md`) and this row's premise is **confirmed on the flags half and blocked on the benefit half**. *Confirmed, upgraded a-help → a-run on both spellings:* `--env`/`--env-file` are accepted on the canonical `cmux workspace create` as well as the deprecated `new-workspace`, and they genuinely **export** — verified on the configured view AND an in-surface read-back — with all four documented `--env-file` semantics holding. *Blocked:* the **subtraction** this row exists for does not materialize, because `cmux-spawn-v2`'s primary topology is `new-surface` into an already-existing workspace and **no documented CLI verb carries env to it**, so the quoting machinery this row wanted to retire stays mandatory. **Do not schedule this as a subtraction, and do not close it as not-viable** — N76 carries it as a watch item whose flip condition is written against the shipped script's spawn verb. Two sharpenings for whoever picks it up: the `--layout` `surfaces[].env` channel named above is **real and exercised** (a-run) but **workspace-creation-time only**; and `cmux workspace env` is **not** ground truth — reserved `CMUX_*`/`TERM` values are silently accepted and stored, then overridden at shell spawn. |
| N68 | Decide the cmux Claude-wrapper posture: picker launches bypass it, and the channel is thinner than it looks | N60 capability audit 2026-07-28 (answers N60's "is a cmux-side lifecycle channel already live?") | quality | S (decide) / M (adopt) | open | **Answer to the question N60 posed: the channel is real and live — but our sessions are not on it, and its Claude ceiling is two events per session.** Evidence: `2026-07-28-cmux-capability-usage-matrix.md` §3 Q1. **(1) The wrapper exists:** `which -a claude` resolves FIRST to a cmux shim (`/var/folders/…/cmux-cli-shims/<uuid>/claude`) that `exec`s `/Applications/cmux.app/Contents/Resources/bin/cmux-claude-wrapper`. **(2) It injects hooks by MERGING SETTINGS** — the wrapper's own strings show `CMUX_BASE_SETTINGS="$HOOKS_JSON"`, `CMUX_USER_SETTINGS+=("${arg#--settings=}")`, `export CLAUDE_CONFIG_DIR=…`, and the failure string `cmux: warning: --settings merge failed; your --settings was ignored`. **(3) We bypass it, two independent proofs:** *code-verified* — `claude-picker` never resolves `claude` via PATH; all three launch paths (`:324`, `:435`, `:442`) `exec caffeinate -i "$binary"` where `$binary` is an absolute `$VERSIONS_DIR/<version>`, so the shim is unreachable BY CONSTRUCTION; *live-verified* — this direct-version-binary session emitted ZERO agent events (session id absent from the retained log; a 15s live `cmux events --category agent` stream during active tool use captured 0 lines; `latest_seq` advanced only 9894→9896). **(4) The prize is small:** of 56 claude-sourced events across 7 sessions, the ONLY hook events are `SessionStart`/`SessionEnd` — no `PreToolUse`, no `Stop`, `tool_name: null`. Codex emits all six because `cmux hooks codex install` exists; Claude has no equivalent. So the channel CANNOT serve `sdd-pre-dispatch-hook.sh`, which needs per-dispatch granularity it already gets from `context-probe.py`. **⚠ Before anyone proposes "just route through the shim":** this fork's ENTIRE enforcement layer lives in `~/.claude/settings.json` (7 scripts pinned by sha256 in `tests/ARaymond-hook-baseline/baseline.txt`). Adopting the wrapper puts a third-party settings-merge in front of it, with a documented mode where your `--settings` is silently dropped. That is a hook-composition change needing its own test plan, NOT a free upgrade. **Recommended disposition: accept the bypass, record why, and coordinate by PUSHING to cmux (N66) rather than pulling from it.** Also verified: `~/.claude/settings.json` has zero cmux references; no cmux-generated Claude config exists; `cmux set-hook --list` → "No hooks configured" (a second, entirely unused lifecycle channel). **AMENDED 2026-07-29 — `cmux claude-hook` is a first-party PUSH entrypoint this row did not know about, and it upgrades the recommendation from "push cosmetics" to "push real lifecycle".** Verified a-run on 0.64.20: `cmux claude-hook --help` → *"Hook for Claude Code integration. Reads JSON from stdin"*, with **seven** subcommands — `session-start`, `active`, `stop`, `idle`, `notification`, `notify`, `prompt-submit` — plus `--workspace`/`--surface`. It is **hidden from `cmux --help`** (not among the 127 this audit enumerated), **absent from all four vendored skills**, and **present in `docs/cli-contract.md`** (lines 160, 371 — where `hooks claude <event>` is named the canonical form and `claude-hook` "remains as the main-compatibility alias"). **What changes:** this row's "the prize is small" holds for what the wrapper *emits* (SessionStart/SessionEnd, `tool_name: null`) but NOT for what we can *send*. Our own `settings.json` hooks — which already fire on exactly these moments — can `echo '<json>' \| cmux claude-hook prompt-submit` etc., giving cmux real SDD lifecycle signal **with no wrapper adoption, and therefore none of the settings-merge risk this row flags as its blocker.** The wrapper decision and the push decision are now independent; only the wrapper one carries the enforcement-layer hazard. **Do NOT treat this as free:** it is unexercised beyond `--help` (a-help for behavior, a-run for existence), the stdin JSON schema is undocumented outside the wrapper, and per this fork's rule a hidden command may change without notice — so probe it, and pin what you observe. Discovery provenance worth recording: it surfaced from reading an unmaintained 0-star community repo (`umitaltintas/cmux-agent-toolkit`) that this document's §5 recommended NOT reading — see the amendment to **N72**. **AMENDED 2026-07-30 (a) — "the prize is small" is WRONG; the wrapper wires NINE hook events, not two.** Read from the installed `/Applications/cmux.app/Contents/Resources/bin/cmux-claude-wrapper` (`HOOKS_JSON=`, a-file — the authoritative statement of what the wrapper injects): `SessionStart`→`hooks claude session-start`; `Stop`→ **three** hooks (`stop`, `feed --source claude` async, **`auto-name`** async/timeout 120); `SubagentStop`→`feed`; `SessionEnd`→`session-end`; `Notification`→`notification`; `UserPromptSubmit`→`prompt-submit`; `PreToolUse`→ **two** (matcher `CronCreate`→`cron-create-guard`, plus a catch-all `pre-tool-use` async); `PostToolUse`→ matcher `PushNotification`→`push-notification`; and **`PermissionRequest`→`feed --source claude` with `timeout: 125`** — i.e. cmux's Feed **is** an approval surface for Claude permission prompts, directly relevant to **N65**'s untested approval rung and to the hardening doc's H3. Also sets `"preferredNotifChannel":"notifications_disabled"` (cmux takes over notification delivery). **Reconciling with this row's original finding** (56 claude-sourced events across 7 sessions showing only SessionStart/SessionEnd): both can hold — 9 hooks are *wired*, but only 2 appear to produce retained `agent.hook.*` events, the rest driving UI state (pills, Feed, auto-name) without a retained event. That reconciliation is **inference from two observations, not verified**; confirm by watching `cmux events` from a genuinely wrapper-launched session before relying on it. **AMENDED 2026-07-30 (b) — the interception mechanism, per upstream issue [manaflow-ai/cmux#5639](https://github.com/manaflow-ai/cmux/issues/5639)** (closed-completed 2026-06-10; whether the fix is in our build `0.64.20 (100)` is UNKNOWN — build→date is not resolvable locally). `claude` reaches the wrapper only because the **cmux shell integration**, sourced per-shell, does two things (`cmux-bash-integration.bash:206-275`): installs the per-surface PATH shim `$TMPDIR/cmux-cli-shims/<uuid>/claude` **and defines a `claude()` shell function** that execs the wrapper. Neither is in the user's `.zshrc`/`.zprofile`; both exist only while the integration is active in that shell. So an absent shim is **not** "cmux stopped shimming" — it is "not active in this shell context", and `which -a claude` alone MISSES the function half (use `type claude`). Verified here: `type claude` and `zsh -lic 'command -v claude'` both return the real binary while `CMUX_SHELL_INTEGRATION_DIR` is set and `ZDOTDIR` unset — i.e. our automation's shell context has no interception, which is the context that matters for the spawn script. **How to reach the wrapper deliberately:** `find_real_claude()` honors **`CMUX_CUSTOM_CLAUDE_PATH`** FIRST ("Honor custom path from Settings > Automation > Claude Code > Claude Binary Path"), with a self/shim guard — so a picker can exec the wrapper and still pin its own version. Upstream's own fix direction #1 is the same shape ("emit the wrapper's real path … instead of bare `claude`"). **Prefer the bundle path `/Applications/cmux.app/Contents/Resources/bin/cmux-claude-wrapper` over `$CMUX_CLAUDE_WRAPPER_SHIM`** — upstream lists them as equivalent, but ours is STALE (points at a deleted temp file) while the bundle path is present. **Codex is symmetric:** `cmux-codex-wrapper` + `find_real_codex()` + `CMUX_CUSTOM_CODEX_PATH`. **Unchanged risk:** the wrapper injects `--settings` (merge path `HOOKS_JSON="$CMUX_MERGED_SETTINGS"`), and how Claude Code precedences a `--settings` flag against `~/.claude/settings.json` — where this fork's ENTIRE enforcement layer lives — is **untested**. Test that before routing anything through the wrapper. Narrower than originally filed, though: `normalize_claude_config_dir()` only rewrites `CLAUDE_CONFIG_DIR` when it is already set AND points into `~/.subrouter/codex/claude` or `~/.codex-accounts/claude`; it does not set it when unset (a separate `--resume` self-heal path does export it). |
| N69 | N57's workspace-group premise is settled — `cmux workspace-group` exists with full CRUD, and anchors have a lifecycle catch | N60 capability audit 2026-07-28 | quality | XS (amend N57) | open | **Amends N57, which currently says "how workspace **groups** are created/referenced [is] genuinely unverified — `list-workspace-groups` is confirmed ABSENT".** `list-workspace-groups` is indeed absent; **the functionality is not** — it lives under a different name, reachable as `cmux workspace group <sub>` or `cmux workspace-group <sub>`, with 17 operations: `list [--json]`, `create [--name] [--cwd] [--from <id>,…]`, `ungroup`, `delete`, `rename`, `collapse`, `expand`, `pin`, `unpin`, `add`, `remove`, `set-anchor`, `new-workspace <group> [--placement]`, `set-color`, `set-icon`, `move`, `focus`. Verified a-run: `cmux workspace group list` → `No groups`, exit 0; all 17 mirrored in `cmux capabilities` as `workspace.group.*`. **This is the SECOND instance of the same error class as `read-screen`** — a capability declared absent on the strength of one guessed command name when the real name was one `--help` away. Two instances is a pattern (see N72). **New design constraint N57 does not mention: anchor semantics are load-bearing.** `--help`: *"Each group is owned by an 'anchor' workspace; the group header IS the anchor's sidebar representation. Closing the anchor dissolves the group while preserving its other members."* So grouping a successor chain under its parent creates a lifecycle dependency — closing hop 1 dissolves the grouping for hops 2–3. Detail: `2026-07-28-cmux-capability-usage-matrix.md` §2.11. |
| N70 | Migrate the spawn script off cmux's deprecated legacy workspace verbs | N60 capability audit 2026-07-28 | quality | XS | open | **The fork already hit this deprecation and suppressed the symptom without recording the fix.** `cmux workspace --help`: *"Canonical noun for workspace operations. Legacy verbs (new-workspace, list-workspaces, close-workspace, rename-workspace, select-workspace) keep working and **print a one-time deprecation hint** pointing here."* `spawn-handoff-session.sh:414` calls `cmux new-workspace`, and `spawn_claude_workspace()` sets `CMUX_QUIET=1` (`:417`) specifically to silence that notice — the code comment even names the `new-workspace` → `workspace create` alias. Canonical form is `cmux workspace create` (same flags). **Not urgent** — `--help` says legacy verbs keep working — but it is one line, it is known now, and it becomes urgent on a cmux upgrade rather than at a time of our choosing. **Ship with:** the e2e Step 14 stub and `tests/unit/test_spawn_handoff.py` both assert on the literal `new-workspace`, so they change in the same commit. Not a baselined hook → no `check-hooks.sh --capture`. Also available on the canonical noun and unexamined: `workspace env`, `workspace loading <on\|off>`, `workspace status`, `workspace reconnect/disconnect`. |
| N71 | Mirror SDD plan checkboxes into the cmux per-workspace todo list | N60 capability audit 2026-07-28 | friction | S | open | **`cmux todo` is a per-workspace checklist explicitly designed to be agent-written, and SDD already has exactly the state it wants.** Verified a-run on 0.64.20: `add\|list\|check\|uncheck\|start\|edit\|rm\|clear\|set\|open`, `--state pending\|in-progress\|completed`, `--origin user\|agent`, 50-item cap, `--json`. The key subcommand is **`cmux todo set`**, which atomically replaces the whole list from a JSON array of `{text, state?, id?, origin?}` (inline or piped on stdin) and **preserves identity for items whose `id` matches** — so a controller can mirror plan state IDEMPOTENTLY rather than append-and-drift. `controller-checkpoint.py` already knows the task range and per-task completion. **Cost:** needs a JSON projection of plan state that does not exist yet; 50-item cap is a non-issue (plans run ~10–15 tasks). **Strictly cosmetic — nothing may gate on it**, same contract as N66. Pairs naturally with N66 (same "make SDD state visible in the sidebar" motivation, same never-fail discipline). Detail: `2026-07-28-cmux-capability-usage-matrix.md` §2.8. |
| N72 | Add a capability-drift guard so the cmux surface is re-enumerated, not re-guessed | N60 capability audit 2026-07-28 — the durable fix for the error class N60 identified | quality | S | open | **N60 named the root cause ("the rule existed; the enumeration never happened"); this is the mechanism that stops it recurring.** The audit found the error class has now occurred **twice**: `read-screen` declared nonexistent in N56, and workspace groups declared unverified in N57 — both refuted by one `--help` call. A third instance is a matter of time, because nothing re-runs the enumeration. **Proposal:** a checked-in snapshot of `cmux --help`'s command list plus a test that re-enumerates against the installed binary and FAILS on drift, reporting added/removed commands. Added commands are the signal that matters — they are exactly the "we didn't know it existed" case. **Design notes:** key on the top-level command list; record `cmux --version` in the snapshot (`0.64.20 (100) [14e3400b9]` at capture); **skip cleanly when `cmux` is absent** so the suite stays green off-cmux and in CI. **AMENDED 2026-07-29 — the original "top-level command list ONLY" design cannot detect the failure class this row exists to prevent, and the row half-spotted it.** Its own baseline note already concedes `workspace-group` is *"reachable but not listed at top level"* — so the motivating example is invisible to the proposed mechanism. **`cmux --help` is not a complete enumeration.** Three confirmed instances on 0.64.20: (1) `workspace-group` — a command absent from the list, reachable as `cmux workspace group` / `cmux workspace-group`; (2) `new-workspace --env` / `--env-file` — flags absent from the top-level summary line for a command that IS listed (see N67); (3) `cmux claude-hook` — a fully-functional 7-subcommand entrypoint absent from the list entirely (see N68). A snapshot diff of the 127 would have caught **none** of them, in either direction. **Required second axis: source divergence.** `docs/cli-contract.md` documents all three (`claude-hook` at :160/:371; `--env`/`--env-file` at :106/:236-239), so the guard must diff the **contract doc's** command/flag tables against `cmux --help` and **treat any name the contract mentions but the help omits as a hidden-surface finding** — that is the signal, and it is mechanically checkable. Add a small probe list of known-hidden names (`workspace-group`, `claude-hook`) asserted to still resolve via `cmux <name> --help`, so a silent removal is caught too. **Consequence for this whole document:** the headline "127 top-level commands" is a precise, reproducible count of what `--help` PRINTS, not of what cmux ACCEPTS — the denominator is a floor, and every percentage derived from it is optimistic. Provenance worth keeping: `claude-hook` was found by reading `umitaltintas/cmux-agent-toolkit`, the 0-star unmaintained repo §5 of this document assessed as "do not adopt" from its README alone. Three of that repo's behavioral claims ARE contradicted by our own a-run observations (it says `new-workspace` returns a UUID not a ref; that unfocused-workspace surfaces reject `send`; and it treats `pipe-pane` as a stream) — so "do not depend on it" was right, while **"do not read it" was wrong**, and §5's "read one repo, depend on none" should be retired in favor of "verify everything, dismiss nothing." **Baseline established by this audit: 127 top-level commands — 3 used, 4 considered-and-rejected (all 4 reasons now invalid), 120 unexamined** (plus `workspace-group`, reachable but not listed at top level — exactly the discovery gap that produced N69) (`2026-07-28-cmux-capability-usage-matrix.md` §1). **Related correction the audit produced, worth noting as evidence the guard is needed:** the gap-analysis doc describes `pipe-pane` as *"**Stream** pane text into a shell command"* (read from the top-level summary); exercised, it is a **one-shot capture** — 1751 bytes captured, zero growth over 5 further seconds of pane output. A category-(a-help) claim presented as behavior, made in the same document that corrected a category-(b) one. **THIRD AXIS ADDED 2026-07-29 (see N75) — the two axes above cover only CLI-REACHABLE surface, and a fourth discovery gap proved that is not the whole failure space.** cmux's "Fork Conversation" (0.64.19+) creates a new agent session preserving conversation history and working directory — and it is **UI-only**: absent from `cmux --help`, from all 255 `capabilities` RPC methods, from `tab-action`'s validated action list, from `docs/cli-contract.md`, and from `cmux docs shortcuts`. So the contract-vs-help divergence axis **would also miss it**, because the contract does not document it either. A capability with no programmatic surface leaves no trace in any machine-readable source. **Required third axis: watch the release channel** — cmux's blog/release notes (`https://cmux.com/blog/`) and the `cmux --version` build number, which is the only signal a UI-only capability ever emits. The version gate is the tell: Fork shipped at a specific release boundary (0.64.19) that nothing in our tooling observes. Practical form: record `cmux --version` in the snapshot (already specified above) and **treat a version bump as a prompt to re-read the release notes**, not merely to re-diff the command list. |
| N73 | Adopt surface-per-session topology (repo = workspace, each SDD session = a top tab) — EXERCISED, and it supersedes N57's group recommendation | user-raised 2026-07-28 during the N60 audit ("I put projects/repos in a dedicated workspace then independent work sessions in separate surfaces"); probe run same day | friction, quality | M | open | **N57 asks whether this topology is possible and recommends a workaround because it looked blocked. It is not blocked — the whole chain was EXERCISED against `cmux 0.64.20` on 2026-07-28.** Probe transcript: `docs/process-improvement-findings/2026-07-28-cmux-capability-usage-matrix.md` §4. **Proven, in order:** `cmux new-surface --workspace <ws> --type terminal --working-directory <path> --focus false` → `OK surface:43 pane:19 workspace:19` (a **richer** ref line than `new-workspace`'s, carrying pane + workspace); `cmux list-panes` → `pane:19 [2 surfaces]`, i.e. the surface joined the SAME pane and is therefore a **top tab, not a split** — the user's model exactly; `cmux respawn-pane --surface <ref> --command <cmd>` runs a command in the existing surface (`OK`, exit 0); a `cmux wait-for -S $TOK` inside that command released a parent blocked on `cmux wait-for $TOK --timeout 25` (**`PARENT-WAIT-EXIT=0`, reproduced twice**) — the closed-loop handshake works INTO A SURFACE; `cmux read-screen --workspace <ws> --surface <ref>` read the marker back from an **unfocused background** workspace; `cmux send --surface <ref> "…\n"` is an equivalent driver (`OK surface:45 workspace:19`). **Two behaviors invisible from `--help`, found only by running it — both must shape the implementation: (1) `respawn-pane --command` DESTROYS the surface when the command exits** (short-lived probe → `Error: not_found: Surface not found`; a trailing `sleep 300` kept it alive). Harmless for a real `claude-picker` launch (Claude runs indefinitely), but a fast-failing launch **leaves no tab to inspect** — the evidence deletes itself, so the exit ladder must rely on the handshake, not post-mortem inspection. **(2) A COLD surface has no readable terminal** — `read-screen` on a never-driven surface returns `internal_error: Failed to read terminal text` until something is sent to it; `surface-health` reports `in_window=false` for readable and unreadable surfaces alike, so it does NOT predict this. This **vindicates `wait-for` over screen-scraping**: a `read-screen` liveness poll would error on exactly the case it exists to detect and would plausibly be retried as transient. **Effect on N57: its blockers are empirically dead and its RECOMMENDATION is now the weaker option** — N57 proposes `new-workspace --group` as the cheap middle path *because surfaces looked blocked*, and groups additionally carry the anchor lifecycle coupling (closing the anchor dissolves the group, see N69) that the surface model does not have. **The one real cost N57 names still stands, unchanged and mechanical:** `SPAWN_WORKSPACE_REF`, the `workspace=` log field pinned by spec §5.4d, e2e Step 14 and CLAUDE.md all speak "workspace" — schema/test/doc churn, not architecture. **LIVE REHEARSAL 2026-07-29 (§4.1) — the SHIPPED launch command was run in a tab, not a toy command:** `cmux new-surface` into the caller's own workspace → `cmux rename-tab "DEMO haiku"` → `cmux send --surface <ref> "claude-picker --non-interactive --pick-version 2.1.220 --telemetry off --session-label demo-haiku\n"` → **Claude Code v2.1.220 live in ~3 s**, its TUI then driven (`send` + `send-key Enter`) and its output read back from an **unfocused background tab**, then `close-surface`. `--focus false` held; the tab never stole focus. **`rename-tab` is the mechanism for the incremented session numbering this topology implies** ("Session 2", "Session 3") — nothing else in the surface API names a tab. **DESIGN DECISION CHANGED BY THE REHEARSAL: use `cmux send`, NOT `respawn-pane`, to start the successor.** `respawn-pane` replaces the surface's process, so when the command exits **the surface is destroyed** and the failure evidence goes with it (caveat (1) above); `send` types into the surface's existing shell, so a picker that dies on a bad `--pick-version` **leaves the tab open with the error readable via `read-screen`**. For a launcher whose entire failure story is "we could not see what happened," that is decisive — `respawn-pane` is the right tool for replacing a process, the wrong one for starting a session you may need to debug. **Also required — the `OK <ref>` parser does NOT generalize (§4.2):** `spawn_claude_workspace()`'s `awk '/^OK[ \t]/{print $2}'` (`:419`) is correct for `new-workspace` (`OK workspace:19`) but `new-surface` returns `OK surface:43 pane:19 workspace:19` (three refs), `rename-tab` returns `OK action=rename tab=tab:46 …` (field 2 is a key=value pair, not a ref), and **`close-surface` returned `OK surface:47` when closing `surface:46` — a well-formed ref that is simply the wrong one**, so a naive parser gets a plausible value and no error. Re-derive the field per command. **Sequence after N61** (the `wait-for` handshake is a precondition for doing this safely, and both touch `spawn_claude_workspace()`). **Do NOT reach for `new-surface --type agent-session --provider claude`** — N57's warning stands: it bypasses `claude-picker` and discards version pinning, telemetry routing, session-label increment and append-prompt forwarding. Not a baselined hook → no `check-hooks.sh --capture`; but `tests/unit/test_spawn_handoff.py` and e2e Step 14 both assert the workspace vocabulary and change with it. |
| N74 | Session-label continuity across spawns is trapped in one SDD script — move label composition into the pickers so every consumer inherits it | user-raised 2026-07-29 ("carry forward the telemetry settings… continue the telemetry label… update the label indicating the type of invocation and the model provider, with a session increment") | quality, friction | M | open | **The forwarding mechanism ALREADY EXISTS and was built for exactly this; what is missing is that the COMPOSITION RULE lives in one file that only SDD can reach.** **What exists (verified against the installed scripts 2026-07-29):** `claude-picker:178-186` `_set_picker_env()` exports four vars — `CLAUDE_CODE_PICKER_{VERSION,LABEL,ARGS,APPEND_PROMPT}`; `codex-picker:213-216` mirrors them as `CODEX_PICKER_*`. **The `CLAUDE_CODE_*` prefix is load-bearing, not cosmetic** — `claude-picker:429` carries the comment *"OTEL_* does NOT survive Claude Code's subprocess env filter; CLAUDE_CODE_* (below) does"*, i.e. the prefix was chosen precisely so launch metadata reaches a spawned successor. Telemetry resource attrs are built by `_build_resource_attrs` (`claude-picker:63-71`): `project`, `branch`, **`launcher=claude-picker`**, `launch_id`, optional `label`, optional `sys_prompt_append{,_sha}`; `codex-picker:63,71` emits `launcher=codex-picker`. **CORRECTION TO THE ASK — provider is ALREADY a clean telemetry dimension** via `launcher=`; duplicating it into the label is redundant *for querying*. It is still defensible for HUMAN legibility (the label surfaces in the status line and in cmux tab names) — just do it knowing it duplicates an existing attribute. **The genuinely missing dimension is INVOCATION TYPE** (`review` \| `work` \| `sdd`), which is captured nowhere today and arguably belongs as a resource attr (`invocation=<type>`) *as well as* in the label. **THE GAP, precisely:** the increment rule exists in exactly ONE place — `spawn-handoff-session.sh:341-360` (at `b5e56e5`), an inline Python block; `grep` for the regex across `skills/` and `~/.local/bin/` returns that file alone. **Neither picker increments anything** — both take `--session-label` verbatim and only sanitize it (`claude-picker:254`, `codex-picker:342`). So SDD gets label continuity and every other consumer (the `claude-codex-handoff` toolkit, ad-hoc launches) gets none. Second, subtler gap: outside a picker-launched session the `*_PICKER_*` vars are simply ABSENT, so there is no label to continue — today that degrades correctly (empty ⇒ omit `--session-label`, `spawn-handoff-session.sh:395-398`) but the successor loses attribution entirely. **⚠ HARD CONSTRAINT — THE INCREMENT REGEX IS END-ANCHORED.** `re.search(r"-Session-(\\d+)$", raw)` (`:344`). Append ANY segment after `-Session-N` and the match fails, execution takes the `else` branch (`n=2`, `base=` the whole string), and `Foo-Session-3` becomes **`Foo-Session-3-codex-Session-2`** — the chain silently restarts at 2 and the label grows without bound on every hop. **Every new segment MUST be inserted BEFORE `-Session-N`**; target shape `<base>-<type>-<provider>-Session-<N>`. Two siblings: the charset filter strips anything outside `[A-Za-z0-9_.-]` (`:350`), and the 255-char ceiling (`:361`, `max(0, 255 - len(suffix))`) means each added segment eats the base's budget — that `max(0, …)` exists because a negative slice bound silently truncated the base from the RIGHT and leaked a middle fragment of the old label. **RECOMMENDED BOUNDARY — put composition in the PICKERS, not in either consumer.** `_set_picker_env` is already what WRITES `*_PICKER_LABEL`, so label semantics are already picker-owned, and repo-1 `telemetry-exp` owns `claude-picker` and its v1 forwarding contract under Decision 19. Moving it there: superpowers **deletes** its Python block (code subtracted, one less thing to drift), the handoff toolkit gets it FREE and never implements it, `codex-picker` gains parity in the same change, and the end-anchor rule is enforced in exactly one place. Callers supply only what the picker cannot know — the invocation type: e.g. `claude-picker --session-label-role review` composes `<base>-review-Session-<N+1>`. **⚠ DESIGN THIS DELIBERATELY — CHAINS CROSS PROVIDERS.** A Claude session dispatching a Codex reviewer READS `CLAUDE_CODE_PICKER_LABEL` and WRITES via `codex-picker --session-label`: **the picker that reads is not the picker that writes.** If increment lives in the pickers, each must also read the OTHER's label var (preferring its own) or the chain resets to `-Session-2` at every provider hop — the same silent failure as the end-anchor bug, arriving from a different direction. It cannot be fixed by collapsing to one shared variable name, because `CLAUDE_CODE_*` is mandatory on the Claude side for the subprocess-env-filter reason above. **SCOPE — three repos, three owners:** pickers in `telemetry-exp` (repo-1), existing logic in this fork, consumer in `claude-codex-handoff`. **Keep this OUT of the handoff toolkit's round-trip transport feature** (scoped 2026-07-29 as transport-only) — folding it in blurs a clean scope decision and spans an ownership boundary that feature does not cross. **Ship with:** a test that a label already ending `-Session-N` increments rather than restarting; a test that an added role/provider segment lands BEFORE `-Session-N`; a cross-provider chain test (claude→codex→claude preserves the counter); and the existing empty-label ⇒ omit-flag behavior pinned so attribution-less launches still work. Related: **N51** (codex-picker parity — already notes codex has no `--pick-version` and a picker-OWNED `--developer-instructions-file` rather than a passthrough, both of which the composition must respect). |
| N75 | WATCH ITEM: cmux "Fork Conversation" is UI-only — no CLI/RPC surface, so it cannot be composed into a skill today | user-raised 2026-07-29 (cmux blog post <https://cmux.com/blog/cmux-fork>), verified against installed `cmux 0.64.20 (100) [14e3400b9]` | quality (enabler) | XS (watch) / M (if it gains a CLI verb) | open | **What it is (b-doc, from the post):** Fork Conversation *"creates a new agent session from the current thread, preserving its conversation history and working directory"* — supports Codex, Claude Code, OpenCode, Pi, Hermes; requires **0.64.19+**; invoked by right-click on a terminal/tab or `Cmd+Shift+P`; forks share an initial snapshot then diverge independently. **Exhaustively verified ABSENT from every programmatic surface (a-run, 2026-07-29):** not among the 127 top-level commands (`fork` appears in `cmux --help` exactly once, as `vm <base\|new\|…\|fork\|…>` — **cloud-VM forking, an unrelated concept**); no fork/clone/branch/conversation method among the **255** `cmux capabilities` RPC methods; **rejected by `tab-action`**, whose action list is validated — `fork`, `fork-conversation`, `forkConversation` and `duplicate-conversation` all return `Error: invalid_params: Unknown tab action`, byte-identical to a nonsense probe (tested in a throwaway workspace, cleaned up); absent from `docs/cli-contract.md` except the two `vm`/`cloud` usage lines; absent from `cmux docs shortcuts`; absent from all four vendored skills. **Why this is NOT for the context-handoff path — the most important line in this row.** Fork preserves conversation history; **N43's entire purpose is to SHED it.** A controller at 400k tokens that forks carries 400k forward, duplicating the problem it is trying to escape. Our handoff is **lossy by design** (the `reports/` + bundle artifacts are a deliberate compression); fork is **lossless by design**. Opposite tools for opposite problems — conflating them would be a genuine design error, so this row exists partly to stop a future session reaching for fork when the gate fires. **Where it WOULD fit, if it ever becomes agent-invocable:** `superpowers:dispatching-parallel-agents` (N agents on one problem with identical priming, no re-establishing context N times — the post's own headline use case); `brainstorming` (design alternatives from a shared understanding); `systematic-debugging` (fork at a hypothesis branch point, test both, keep the investigation state); plan review (second opinion, no re-explanation). **Three blockers, in severity order:** (1) **an agent cannot invoke it** — everything in this fork is agent-orchestrated, and a UI-only capability composes into no skill, hook or gate; it is an affordance for the human, not the system; (2) **same working directory, no conflict resolution** — the post: *"Give them separate tasks or files when concurrent edits could overlap"*; this session hit that hazard first-hand when two agents shared one worktree and one moved the other's branch, and forks reintroduce it *without* the git-worktree isolation SDD would otherwise use; (3) **no lifecycle or cleanup guidance** — unbounded fork chains with no counter, the runaway concern `SUPERPOWERS_CMUX_MAX_HOPS` handles for spawns. **Watch trigger:** on each cmux upgrade, re-probe `cmux capabilities \| grep -i fork` and `tab-action --help`; if a CLI verb appears, evaluate for parallel-agent dispatch — explicitly NOT for the handoff path. **This is the FOURTH discovery gap and a NEW class:** the prior three (`workspace-group`, `claude-hook`, `new-workspace --env`) were CLI-reachable-but-unlisted and are caught by N72's contract-vs-help axis; this one has **no programmatic surface at all**, so it leaves no trace in any machine-readable source and is caught only by watching the release channel — hence the third axis added to **N72**. **Caveat on provenance:** the post was read via a summarizer, not the raw page; the CLI-absence findings above are independent of it and verified against the binary, but a flag the summary dropped would not have been seen. |
| N76 | SP2 disposition — cmux `--env`/`--env-file` work exactly as documented, but cannot subtract the spawn script's quoting machinery; N67 is confirmed-but-blocked-by-topology | SP2 probe, `cmux-spawn-v2` Module 1 Task 1, 2026-07-30; exercised live against installed `cmux 0.64.20 (100) [14e3400b9]` | quality | S (proposal only — no change ships this sprint) | open (watch — conditioned on the shipped script's spawn verb; see Flip condition) | **Disposition: (a) viable — FALLBACK path, SCALARS only — with a recommendation NOT to adopt it in this sprint.** Full transcript, negative controls and evidence chain: `2026-07-30-sp2-workspace-env-probe.md`. **N67 is upgraded a-help → a-run and PARTIALLY closed.** *The flags half is confirmed:* `--env`/`--env-file` are accepted on the canonical `cmux workspace create` (not only the deprecated `new-workspace`), and they do not merely record values — they **export** them into every shell spawned in the workspace, verified on BOTH halves (`cmux workspace env` configured view AND an in-surface read-back: `SP2_WS_MARK=alpha\|beta\|file_plain\|file_export`). All four documented `--env-file` semantics hold a-run (`#` comments ignored, blank lines ignored, leading `export ` stripped, `--env` beats a same-key file value). *The BENEFIT half does not materialize.* N67's value was **subtraction** (retire the base64 argv codec, `shlex.quote` re-quoting, the `printf`-not-`echo` workaround). That subtraction is blocked by topology, not by the flag: this sprint's **primary** topology is `new-surface` into the caller's EXISTING workspace, and **no env channel any documented CLI verb reaches gets to it** (read that qualifier literally — see the Residual below) — (1) `new-surface` has no `--env` — and note the two documentary signals (absent from `--help`; the upstream contract's `new-surface` row is bare) are **absence of evidence, which this repo's own standard rejects**, so the load-bearing datum is a **read-back**: a surface created with `--env SP2_SURF=gamma` reported `SP2_SURF_MARK=\|alpha` — `SP2_SURF` **empty** while the workspace-level `SP2_PROBE=alpha` arrived as an in-band positive control, proving the payload ran and the right surface was read. A third leg: `SP2_SURF` never appears in `cmux workspace env` either, so the flag is not merely unexported — it is never recorded (re-captured a-run 2026-07-30 with full transcript: `workspace:41` / `surface:108`); (2) an existing workspace's env **cannot be mutated** — `workspace env` is read-only (`workspace env set` → exit 1 "Invalid workspace handle: set"; `--env` → exit 1 unknown flag; `set-environment` → exit 2 unknown command), and `cmux capabilities` exposes exactly one env method across the whole RPC set (`workspace.env`, the read) with none among the 26 `surface.*`; (3) the one real per-surface channel, `--layout` `surfaces[].env`, **works** (a-run: `SP2_LAYOUT=delta`) but is **workspace-creation-time only**. So the composed command string stays mandatory for the primary path, and adopting `--env` on the fallback alone would **fork the shared wrapper's env channel** — precisely what Decision 2's "ONE shared wrapper, same inline env for both topologies" exists to prevent: a second mechanism added, none removed. **Flip condition — written against SHIPPED code, because the surface topology has not shipped.** (Corrected 2026-07-30; the original read "if the surface topology is ever *abandoned*", which presupposes it was adopted and left no branch for "never adopted".) **Actionable whenever the shipped `spawn-handoff-session.sh` reaches successor state ONLY through workspace creation.** That is **true today** — its sole spawn verb is the `nw=(cmux new-workspace …)` array in `spawn_claude_workspace` (cite the construct, not a line number) — and it stays true if `cmux-spawn-v2`'s surface topology is never adopted or is later reverted; it stops being true once the surface path ships as primary. So: **sprint landed** ⇒ not actionable; **sprint stalled or reverted** ⇒ actionable, and `--env` becomes strictly better than the inline prefix, making N67's subtraction available in full. The check is one `grep` over the shipped script, not a judgment about intent. **Residual, stated because the doc's original bound was wrong:** the "no env channel on any axis" claim covers **documented CLI verbs** (verified 2026-07-30 by sweeping all 127 top-level commands' helps — exactly one, `new-workspace`, exposes `--env`). It does **not** exclude an undocumented env param reached via `cmux rpc <method> [json-params]`, a first-class CLI verb calling any v2 method with arbitrary params, which was not probed; `cmux capabilities` cannot settle it (names only, no schemas) and an exit code cannot either (`cmux rpc` silently ignores unknown params — a-run). Closing it needs a create plus a read-back. The disposition survives by **argument**, not by that inference: the capability matrix §2.9 already recommends *"do not build on `rpc`"*, `CLAUDE.md` makes the CLI the contract surface, and depending on an unversioned internal param is the drift exposure **N72** exists to prevent. **Methodology note worth keeping — the probe nearly filed a third false-premise row.** `cmux new-surface … --env SP2_PROBE=alpha` returns **exit 0 / `OK surface:N`**, which reads as an undocumented flag. A negative control refutes it: `--sp2-not-a-real-flag zzz` is *also* accepted (exit 0) — **`new-surface` silently ignores unknown flags**, while `workspace create` strictly validates (exit 1, and its error text *enumerates* `--env KEY=VALUE, --env-file <path>`). Two commands on one binary with opposite unknown-flag policies: **never infer capability from an exit code without a negative control.** **Feeds N72 — a FOURTH enumeration hole, and a new kind:** `cmux workspace env --json` **works**, yet `--json` is omitted from BOTH the noun help AND the binary's own *"Known flags: --workspace, --window, --mask"* **error message**. N72's three recorded instances are all *help/doc* omissions; this is an **error message that purports to enumerate a command's known flags and omits a working one**, so any drift guard keying on help or error text inherits the hole. Two further doc↔binary divergences recorded: `cmux workspace <sub> --help` prints the **noun** help for every subcommand (so the canonical spelling's help *never* mentions `--env` — an irony for N70, which wants migration to exactly that spelling), and this is absent from the contract's own "Current Help Caveats" list; and `--help`'s *"Reserved CMUX_* variables cannot be overridden"* is misleading — they are **silently accepted and stored** (`cmux workspace env` cheerfully reports `CMUX_WORKSPACE_ID=BOGUS_WS`, `TERM=bogusterm`) and only overridden **at shell spawn**, as the contract correctly states. **Consequence: `cmux workspace env` misreports the effective environment for protected keys and shows no layout per-surface env — never treat the configured view as ground truth.** |
| N51 | codex-picker parity → codex-side SDD auto-spawn | user-raised 2026-07-23 (cmux-integration session); spec had deferred "codex-picker parity" as out-of-scope | quality, friction | M | **blocked** (on codex-SDD triggering) | Extend the cmux-integration auto-spawn (N43(D)) to Codex controllers. **(a) codex-picker — DONE 2026-07-23** (telemetry-exp `355f95f` on main): v1 forwarding contract mirroring claude-picker — `--handoff-contract`→`1`, `--non-interactive/--session-label/--telemetry`, and 4 exports `CODEX_PICKER_{VERSION,LABEL,ARGS,APPEND_PROMPT}`. Divergences from claude-picker (matter for part b): **no `--pick-version`** (single `CODEX_BIN`); append-prompt is a picker-OWNED `--developer-instructions-file <path>` translated to `-c developer_instructions=<TOML>` (NOT a passthrough `--append-system-prompt-file`), content carried in `CODEX_PICKER_APPEND_PROMPT`. **(b) `spawn-handoff-session.sh` harness-awareness — DEFERRED, and BLOCKED:** detect claude-vs-codex (via which `*_PICKER_*` env is present), read the matching env, compose the codex launch profile, spawn via `codex-picker`. The clean seam is narrow — only launch-composition forks; bundle validation / preconditions / cmux spawn+notify+reservation / exit ladder are harness-agnostic and reused. The spawn core is **extraction-ready** (D15) for this second consumer (consider lifting `spawn_claude_workspace()` to `skills/scripts/` then). **BLOCKER (confirmed 2026-07-23):** the superpowers **skills are NOT installed on codex** — codex's integration surface today is only the harness-agnostic handoff toolkit (`/handoff`, `/handoff-plan`, `/pickup`), NOT the SDD skill/hook machinery. So a codex session never runs this fork's SDD and never reaches the context-handoff protocol → part (b) would be a **never-invoked, non-live-exercisable branch** (fails the "one real spawn" acceptance; violates git-reality). Do part (b) only AFTER codex actually runs this fork's SDD and hits the handoff seam (needs codex-side SDD skills + a codex trigger equivalent to Claude's pre-dispatch HARD-block). Also then: `~/.agents/skills` symlinks for the 4 vendored cmux skills (repo-2 currently symlinks only `~/.claude/skills`; codex has `~/.agents/skills/pickup` + the big-* skills but not the cmux skills). Kept OUT of repo-3 (Decision 19; user chose "ship Claude-scoped now" 2026-07-23). |

---

## Recommended sequencing

Ordered by leverage; sequenced so nothing collides with the in-flight `sdd-hook-improvements` worktree.

1. **B1–B5 — ✓ DONE (merged 7030b25).** 3-stage hook classifier + `review_tier`.
2. **Quick wins — ✓ DONE 2026-05-30:** C6(a) manual token-estimate step retired from SKILL.md; `.sdd-session.json` gitignored (one already-committed instance pending untrack decision).
3. **B6, P1, N2 — ✓ DONE & merged (`pipeline-flexibility`, `e53cbaf`, 2026-05-31):** `task_type: verification` (B6, with keyword+ratio+git-reality anti-smuggling guard in place of C4), direct-`writing-plans` `entry_mode` (P1), and the SSOT audit (N2, see `2026-05-31-ssot-audit.md`).
4. **N3 + N4 + N10 + N11 — ✓ DONE & merged (`2026-06-01-sdd-enforcement-hardening`, `e708d9b`, 2026-06-03):** the multi-module hook fixes + N15 (baseline) + skill-enforce blocking (C5). `transition-module.py`-driven multi-module SDD now runs without manual workarounds. N14 resolved by the merge. **STILL OPEN:** **N1** (same file, `sdd-pre-dispatch-hook.sh` — fold into the next edit), and the sprint's own follow-ups **N12/N13/N16/N17**. **CAVEAT:** the multi-module + live `task_type:verification` paths are test-validated (unit+e2e) but NOT yet run in a real multi-module session — the next real multi-module SDD run is the first live exercise, and **N16 must land before any non-last verification task**.
5. **P2** — project closeout / lifecycle close; user-raised; shares `.active-feature` / completion-signal plumbing with the now-done P1.
6. **B8** — user-confirmed for unattended runs; design within C3's principle.
7. **C6(b)** strengthen the hook estimate; **C1** highest correctness leverage.
8. **C2 + C3** — trust-at-scale.
9. **Cheap SSOT cleanups — N5, N6, N7, N8, N9 — ✓ ALL DONE** (N5/N7/N9 in `sdd-cleanup-and-integration-gate` 2026-06-10; N6/N8 in `sdd-aggregate-gate-visibility` 2026-06-22, `cbff47e`/`86ddb95`).
10. **B10, C5** — B10 is now a fast-follow on **N43**'s direct `ctxcheck` sensor (supersedes the C6(b)-proxy dependency), not opportunistic; sequence it right after N43 ships. C5 opportunistic.
11. **B7, B9, C4 (standalone)** — revisit; B7/B9 may prove unnecessary now that B6 has shipped.

Each open item becomes its own brainstorming → writing-plans → SDD cycle, validated by `validate-all-skills.py`, `tests/unit/`, and `tests/integration/sdd-e2e-test.sh`. C1/C2 additionally need a new fixture proving a known-bad reference snippet / missing integration path is now caught.

---

## Sprint 3 (planned 2026-06-03 — EXECUTED & merged 2026-06-10 as `sdd-cleanup-and-integration-gate`)

**Outcome:** Track 1 (N16/N5/N7/N1/N12/N13/N17 + N9) and Track 2 (C2) shipped in one 2-module SDD run; Track 3 partially proven — the multi-module path ran LIVE for the first time (clean transition; surfaced + fixed N18), but the plan declared no `task_type:verification` task, so the non-last-verification live exercise remains open. N6/N8 were NOT folded in this sprint (budget) — subsequently done 2026-06-22 in `sdd-aggregate-gate-visibility` (`cbff47e`/`86ddb95`). Follow-ups N19–N28 logged above (N19/N20/N22/N26/N27 + N25(a,b,c,d,f) + N28(c) closed by `sdd-aggregate-gate-visibility`). Original plan below for the audit trail.

Theme: **close the loop on sprint-1/2 features + raise the integration-test floor.** Anchor (Track 2) chosen by Aaron on 2026-06-03 — C2 over P2/C1/C3 because it's the only Critical-severity item that's M-sized and execution-ready, and its plan-time-declaration + hook-check fix shape is a concrete down-payment on C3's out-of-band-enforcement principle. Track-1 rows flip to `in-flight` when execution starts; this block is a planning artifact, not a status change.

### Track 1 — Cleanup bundle (one standard-tier feature; **N16 first**)

Deliberately bundled **now**, not folded in opportunistically (sequencing item #9) — opportunistic fold-in is the "I noted it but kept going" anti-pattern (`2026-05-21-skill-evaluation.md` §10.8). ~7 files (hook, checkpoint, `validate-plan.py`, `validate-all-skills.py`, `implementer_report.py`, `transition-module.py`, SDD SKILL.md) with real behavior changes ⇒ needs integration tests; the three bug-fixes carry a failing-test-first obligation.

| Item | Role in bundle |
|---|---|
| **N16** | Bug fix (test-first). Add `task_type` to `ImplementerReport`; exempt `verification` from `files_changed`-non-empty. **Unblocks Track 3.** |
| **N5** | Bug fix (test-first). Fence-aware `^### Task` regex in BOTH `validate-plan.py` + `controller-checkpoint.py`. |
| **N7** | Bug fix (test-first). Treat prose `Source Contracts: None` as valid-absent; kills a per-run accepted deviation. |
| **N1** | Hook preflight — accumulate all missing prereqs (same file as the just-merged hardening work). |
| **N12, N13, N17** | `sdd-enforcement-hardening` follow-ups: align transition gate w/ `enforcement.dispatch_provenance`; backport `mkdir` into plan snippet; add transition main-plan fallback. |
| N6, N8, N9 | Fold in if budget allows: hook-enforces-this framing pass; intent-based F6 check; `_task_ids_where` SSOT helper. |

### Track 2 — Anchor: **C2** integration-test gate

Lead with a short fixture spike (sequencing note already requires C2 to ship a fixture proving a missing integration path is now caught): design the plan-time declaration field for contract/route/security-touching features + the pre-completion hook check, validated against a fixture modeling the practerus M15 cache-poisoning miss. Then ship as a standard-tier feature.

### Track 3 — Prove it live (Phase-4 gap)

After N16 lands, run one real SDD session whose plan **deliberately includes a non-last `task_type:verification` task and crosses a module boundary** — the only exercise of the never-run-live multi-module + verification paths. (The Track-1+Track-2 work could itself be authored as a 2-module plan to serve double duty as this live exercise.)

### Cross-track sequencing

Track 1 and C2 **both** modify `sdd-pre-dispatch-hook.sh` and `controller-checkpoint.py`. Run **Track 1 before C2** so C2's new checkpoint check builds on a base where N5 (fence-blind miscount) and N7 (source-contracts false positive) are already gone — otherwise C2 inherits two known-buggy code paths.

### Deferred (not sprint 3)

- **Sprint-4 design spikes (need brainstorming first):** **C1** (plan-ref execution validation, L), **C3 + B8** (gameable-gates→hooks + async honesty, L).
- **Lower-leverage / revisit:** B7, B9 (may be unnecessary post-B6); B10 (now rides **N43**'s direct sensor — de-paired from C6(b); fast-follow once N43 ships); C6(b) (narrowed to the per-dispatch estimate; see its row); C4; C5.

---

## Sources

- `docs/2026-05-28-sdd-session-assessment-btd-consolidation.md` — BTD consolidation SDD session post-mortem (B*/I* items).
- `docs/process-improvement-findings/2026-05-21-skill-evaluation.md` — cross-repo Critical findings (C1–C3).
- `docs/imp-plans/2026-05-17-adaptive-enforcement-tiers/` — deferred v1.1 candidates (C4, C6).
- `docs/plans/2026-04-24-pydantic-meta-design.md` — Pydantic Phase 3 candidates (C5).
- `docs/imp-plans/2026-05-28-sdd-hook-improvements/` — work addressing B1–B5 (merged 7030b25).
- Live SDD run in the `practerus-platform` repo — `docs/imp-plans/2026-05-27-marketing-cta-guard/` Task 1 pre-dispatch (2026-05-31); surfaced **N3** (no-Task-0 manifestation).
- `docs/process-improvement-findings/2026-05-31-ssot-audit.md` — SKILL.md-vs-hook SSOT audit (N2; sources N3–N9).
- `docs/imp-plans/2026-05-31-pipeline-flexibility/` — B6 + P1 implementation; `deviations.md` sources the execution-discovered hook/checkpoint gaps (N3 module-transition manifestation, N4, N5, N7, N8, N9).
- `docs/imp-plans/2026-06-01-sdd-enforcement-hardening/` — multi-module enforcement gap closure (N3/N4/N10/N11 fixed; N15 baseline fixed) + skill-bypass hook blocking (C5, incl. the C1 SIGPIPE de-pipe caught in review); **merged to main `e708d9b` 2026-06-03** (N14 resolved by the merge). `deviations.md` + `reports/final-code-review.md` + `reports/execution-trace-audit.md` source the follow-ups N12 (transition↔hook gate divergence), N13 (un-runnable plan snippet), N16 (validate-report rejects verification reports), N17 (transition verif-id no main-plan fallback).
- `docs/process-improvement-findings/2026-06-22-upstream-v6-sync-assessment.md` — upstream v6 (obra/superpowers, `896224c`) sync assessment vs our fork at merge-base `f2cbfbe`. Sources **N29** (A3 writing-skills authoring port), **N30** (architecture-neutral v6 SDD cost wins), **N31** (the one-vs-two-reviewer strategic decision). Records what was taken now (A1 ultrathink-keyword fix, A2 `lint-shell.sh`, A4 worktree global-dir removal) and what was deliberately declined (v6.0.3 `.git/` fix, new-harness support, vendor-neutral prose rewrite, evals submodule split).
- Live SDD run in the `telemetry-exp` repo — `docs/imp-plans/2026-07-15-recoverable-enrichment/` (worktree `.worktrees/recoverable-enrichment`, `enforcement_tier: standard`, Module 8 / Tasks 21–22 of 27); 2026-07-19 field bug report (Issues A–H; evidence in that worktree's `reports/` + commits `d842fcf`/`a8f0160`/`9039b7a`). Sources **N44** (A/B/E/G doc fixes, shipped this session) and **N45–N50** (C/D/F/H + the deferred validator-self-example and regression-coverage follow-ups). Diagnosis + source verification: `docs/process-improvement-findings/` (this session's triage of all eight).
- `docs/process-improvement-findings/2026-07-28-cmux-cli-capability-gap-analysis.md` — cmux CLI capability audit, cross-referencing <https://github.com/yigitkonur/awesome-cmux> against the installed `cmux 0.64.20 (100) [14e3400b9]`. Disproved **N56**'s "no surface-read / structurally unverifiable" premise (`read-screen`, `capture-pane`, `pipe-pane` all exist) and proved `cmux wait-for` works **cross-workspace** by probe. Narrowed **N57**'s blocker; sources **N60** (capability-vs-usage audit) and **N61** (`wait-for` closed-loop handshake).
- `docs/process-improvement-findings/2026-07-28-cmux-capability-usage-matrix.md` — the **N60** deliverable, produced by a dedicated audit session: every top-level command from `cmux --help` on 0.64.20 classified used / considered-and-rejected / unexamined (**3 / 4 / 120**, and all 4 rejection reasons refuted), drill-downs on the high-value unexamined block, the two commissioned questions (cmux-side Claude lifecycle channel; sidebar telemetry), and a fully-exercised surface-per-session topology probe. Sources **N66–N74**. Two rows were **amended 2026-07-29** after `cmux claude-hook` was discovered: **N68** (the Claude push channel is larger than the wrapper's emit ceiling) and **N72** (a `--help`-only drift guard cannot see hidden commands — needs a contract-vs-help divergence axis). §5's "read one repo, depend on none" is **retracted** in the same pass. Caveat for readers: its `:NNN` citations into `spawn-handoff-session.sh` are pinned to `df46c76` and were shifted by the N62 hardening commit — search the quoted anchor text, not the line number.
- `docs/process-improvement-findings/2026-07-29-handoff-hardening-recommendations.md` — same audit session, same day; captured verbatim from the `2026-07-29T02-01-24Z-claude-codex-handoff` bundle (see the provenance header). Addressed to the **`claude-codex-handoff` toolkit repo**, not this fork. Its §1 reads both tools' trust state from live config: Codex `~/.codex/config.toml` `[projects]` is ancestor-inherited; Claude `~/.claude.json` `projects[<abs path>].hasTrustDialogAccepted` is exact-path keyed (36 paths tracked, 30 accepted, 6 not) — with the non-inheritance step itself labelled `inferred` and flagged as needing a two-minute test. Bears on **N56** and **N65**. ⚠ **Its §1 non-inheritance step — the one it labels `inferred` — was TESTED 2026-07-29 and DISPROVEN: Claude's trust IS effectively inherited from an accepted ancestor, so a worktree under an accepted project root does NOT raise the dialog.** The document is preserved verbatim, so read §1 with that correction in hand; the corrected conclusion and the two-arm method live in the **N56** row. Its H1–H6 recommendations, the scenario table and the test list are unaffected — and H1's "preflight the trust files, refuse rather than hang" is *strengthened* by the result, since the predicate is now known-good. Also carries H1–H6 hardening recommendations, a 9-row scenario table (rows 1–3 and 9 marked preflightable today from files), and a 6-item test list.
- `docs/process-improvement-findings/2026-07-29-cmux-mode-option-surface.md` — same audit session, same day, same bundle, captured verbatim. Option-surface design input for the toolkit's cmux transport (**N65**): recommends splitting config across command token / backend flag / env var rather than growing the `auto-cdx`-style compound tokens; states that the review **round trip already exists** (`dispatch` + `findings` + `/pickup`) while a **one-way spawn does not exist in that repo** (`grep -rniE 'cmux|new-workspace|spawn' src/` → nothing); warns that `--ready-timeout` and `--timeout` are different clocks with different failure meanings; and its §6 raises the cross-repo SSOT question — two repos about to implement the same cmux spawn — recommending the toolkit own the generic mechanics *after* both work independently, never as part of the first feature.
