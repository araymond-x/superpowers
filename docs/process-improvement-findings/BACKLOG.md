# Superpowers Fork — Improvements Backlog

Living ledger of open, in-flight, and completed process/tooling improvements for this fork. Items are derived from production SDD sessions and cross-repo evaluations (see **Sources** below). This is the durable, version-controlled tracker — individual planning memos are ephemeral.

## How to use this

- **IDs are stable.** `B*` = items from the BTD consolidation assessment; `I*` = items inferred from that report's data; `C*` = net-new from cross-repo findings / deferred infra; `N*` = other net-new (N3–N9 = findings from the 2026-05-31 SSOT audit + pipeline-flexibility execution; N10–N17 = the 2026-06-01 sdd-enforcement-hardening feature; N18–N28 = the 2026-06-10 sdd-cleanup-and-integration-gate feature — N18 found live at the first module transition and fixed in-feature, N19–N28 are its accepted follow-ups; see that feature's deviations.md for full provenance); N29–N31 = the 2026-06-22 upstream v6 (obra/superpowers) sync assessment (see `docs/process-improvement-findings/2026-06-22-upstream-v6-sync-assessment.md`), N32 = shellcheck findings surfaced by the A2-ported `lint-shell.sh`; `P*` = process entry-point items. Reuse the same ID when an item moves to in-flight/done so history is traceable.
- When an item ships, set **Status** to `done` and record the worktree/commit in **Where / Notes**. Don't delete done rows — they're the audit trail.
- New items: append with the next ID in the relevant series and fill every column.
- **Freshness / "last updated":** none is recorded inline, by design — git is the source of truth. For the last-edit date run `git log -1 --format=%cd -- docs/process-improvement-findings/BACKLOG.md` (or `git log --oneline -- docs/process-improvement-findings/BACKLOG.md` for history). Please don't re-add a hand-maintained date stamp — it only drifts, which is the SSOT anti-pattern this fork exists to fight.

**Sizing rubric:** **S** = ~1 file, localized, low risk · **M** = a few files across model→script→skill, 2–4 tasks · **L** = cross-cutting / new infrastructure / many consumers / needs new test fixtures + design.

**Status values:** `open` · `in-flight` · `done` · `deferred` · `won't-do`

**Improves axes:** friction · speed · quality (`enabler` = unblocks another item; `—` = no change intended).

---

## Ledger

| ID | Title | Source | Improves | Size | Status | Where / Notes |
|---|---|---|---|---|---|---|
| B1 | Fix `general-purpose` subagent_type bug (hook exits before reviewer detection) | BTD High-1 | friction, speed | M | done (merged 7030b25) | `sdd-hook-improvements` worktree — 3-stage classification in `sdd-pre-dispatch-hook.sh` |
| B2 | Inline validation errors in hook output | BTD High-2 | friction, speed | S | done (merged 7030b25) | `sdd-hook-improvements` — `head -n 12` excerpt in Check 4b |
| B3 | Auto-create dispatch log on first reviewer dispatch (cold start) | BTD High-3 | friction | S | done (merged 7030b25) | `sdd-hook-improvements` — idempotent `mkdir -p`+`touch` |
| B4 | Per-task `review_tier` (task-aware minimum-tier threshold) | BTD High-4 | friction, speed | M | done (merged 7030b25) | `sdd-hook-improvements` Module 1 — `review_tier` field + declared-minimum ratio exclusion (task-aware variant, not raise-to-70%) |
| B5 | Remove legacy (non-manifest) dispatch detection path | BTD High-5 | quality | M | done (merged 7030b25) | `sdd-hook-improvements` — manifest-required guard clause |
| B6 | `verification`/no-code task type (skips full dispatch/review cycle) | BTD Med-5 | friction, speed | M | done (merged e53cbaf) | **Implemented in `pipeline-flexibility`, merged to main 2026-05-31 (`e53cbaf`).** First LIVE exercise was Task 7 of `sdd-enforcement-hardening` (2026-06-03) — surfaced **N16** (validate-report rejects empty-files_changed verification reports). `Task.task_type` model field; `validate-plan.py` write-suggesting-keyword WARNING; hook `get_task_type` reader + review-skip on Checks 4b/4c/5d (`sdd-pre-dispatch-hook.sh`); `controller-checkpoint.py` verification-ratio cap (≤30%, Check 8) + git-reality check (Check 9); SKILL.md "Verification Tasks" section + writing-plans "Declaring `task_type`"; e2e steps. Paired anti-smuggling guard (keyword WARNING + ratio cap + git-reality) instead of C4. |
| B7 | Batch reviewer dispatches for minimum-tier tasks | BTD Med-6 | speed | M | open | One dispatched reviewer covering several declared-minimum tasks. Must stay *dispatched* (provenance-logged). Lower leverage now that B4 removes most blocking. |
| B8 | Async-compatible honesty check (`mode: autonomous`) | BTD Med-7 | friction | M | open | User-confirmed needed for unattended standard-tier runs. Add field to `Enforcement` (`skills/scripts/models/sdd_session.py`) + gate in `controller-checkpoint.py` (~L999–1028). **Guard:** prominent log + queued human re-review; must not become a silent bypass (see C3). |
| B9 | `reports/` subdirectory organization (`reports/tasks/` + `reports/process/`) | BTD Low-8 | navigation | L | deferred | Broad blast radius — touches every path consumer (hook, checkpoint, validators, transition-module). Revisit only if navigation becomes a real cost. |
| B10 | Context summary conditional on actual context pressure | BTD Low-9 | friction | M | open | In-flight work narrowed the metric but kept the hard task-number trigger (`compute_midpoint` → `enforcement.context_summary_at` → hook Check 6b). Depends on C6. |
| B11 | Audit `executing-plans` inline-execution path for parity with SDD enforcement | 2026-06-12 session (closeflow pattern extraction) | quality | M | open | Inline execution is a legitimate first-class mode with structural requirements that make SDD impossible: (1) Workflow-tool tasks can only run in the main session — a dispatched subagent can't invoke the Workflow tool; (2) human gates require the controller to pause mid-plan — SDD's continuous-execution model forbids that; (3) no per-task controller-direct mode in SDD for the hybrid workflow/gate tasks. Plans declare this explicitly (`"REQUIRES inline execution … Do NOT execute via SDD"`). The `executing-plans` skill currently has no equivalent to: pre-execution audit gate, deviations register convention, dispatch-log tamper evidence, honesty check, or trace audit. The hook's no-manifest guard false-positive (fixed `b27c332` 2026-06-12) revealed the gap — the hook assumed any `docs/imp-plans/` artifact meant SDD. Audit what guardrails an inline plan *can* have (some SDD checks are SDD-specific by nature), add them to the `executing-plans` SKILL.md, and verify `plan-validation-gate-hook.sh` handles inline plans correctly. |
| N1 | Hook preflight — report all missing prerequisites at once | inferred I5 | friction, speed | M | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Test-only resolution: `test_n1_multi_error_accumulation.py` proves the hook ALREADY accumulates via `ERRORS+=()` and emits all errors before exit 2 (3 simultaneous violations → 3 distinct BLOCKED lines, rc=2). Regression guard; no hook edit, baseline untouched. **Original analysis:** Audit early-exit points in `sdd-pre-dispatch-hook.sh`; accumulate errors instead of fail-one-retry-fail. The B1 bug cascaded into 6 separate blocks. |
| N2 | SSOT audit: reconcile SKILL.md manual prescriptions vs hook-enforced checks | derived from C6 (confirmed drift) | friction, quality | M | done | **Done 2026-05-31 — see `2026-05-31-ssot-audit.md`.** Audited 15 SKILL.md vs 4 of the 6 registered hooks (the other 2 are advisory exit-0, out of reconciliation scope). Result: the token-estimation redundancy (C6) was the *only* pure retire case and was already retired by C6(a); no second instance found. 0 retire / 1 strengthen / 7 keep; 0 live threshold drift (verification ≤30% and minimum-tier ≤50% both verified to match code). Net-new actionable findings spun out as N3–N9 below. Headline: SSOT is healthier than C6 implied; the real gaps are the multi-module hook path (Check 4c ↔ `transition-module.py`) and the advisory-only Stop-hook pre-completion gate (`sdd-stop-hook.sh` is registered on `Stop` but exits 0 — informs, never blocks). |
| N3 | Check 4c (dispatch provenance) rejects valid plans whose first in-scope task isn't Task 0 | live SDD runs — practerus marketing-cta-guard 2026-05-31 (no-Task-0) + pipeline-flexibility module transitions | friction, quality | M | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Tasks 2+3).** N3a: Check 4c now skips when `PREV < MANIFEST_TASK_START` (`sdd-pre-dispatch-hook.sh`). N3b: `transition-module.py:validate_module_completion` re-verifies boundary provenance before truncation — the sibling enforcement. **Original analysis:** **Two manifestations, one root cause — hit first-hand in both runs.** `sdd-pre-dispatch-hook.sh` Check 4c (dispatch provenance for task N-1) sits *outside* the "first task in module" skip (which wraps only the N-1 report-file checks). Its exemption guard `TASK_NUMBER -gt 0` assumes the first in-scope task is always Task 0 — but it legitimately isn't in two cases. **(1) No-Task-0 plans:** Task 0 is *conditional* (`writing-plans`/SDD SKILL.md mandate it only for plans with external Source Contracts), so a contract-free plan validly starts at Task 1; Check 4c then sets `PREV=0` and greps the log for `task=0 type=spec-review`, which can never exist → `BLOCKED: … dispatch the spec reviewer for Task 0.` The only way past is forging a `task=0` entry — violating the exact provenance model the check protects. **(2) Module boundaries:** `transition-module.py` Step 5 truncates the live dispatch log (`open(dispatch_log,"w").close()`), so the next module's first task has no prior provenance in the live log and Check 4c BLOCKs on the (now-archived) N-1 reviews. This execution abandoned the prescribed `transition-module.py` flow for a manual manifest advance both times to dodge it. **Fix (covers both):** give Check 4c a parallel exemption — skip the PREV provenance check when `PREV < MANIFEST_TASK_START` (no prior in-scope task), preserving full enforcement for plans that *do* declare Task 0 and for non-first tasks within a module. Regression fixtures: a Task-1-start manifest PASSes; a module-2-first-task manifest PASSes; a Task-0 manifest still gets PREV provenance-checked. Same-file sibling to **N1**; pairs with **N4** (together make `transition-module.py`-driven multi-module SDD runnable without manual workarounds). The 3-stage hook's module-boundary Check 4c is a new, untested path (both prior multi-module features ran older hooks). |
| P1 | Direct-to-`writing-plans` entry mode (re-installs setup guardrails) | user-raised | friction, speed | M | done (merged e53cbaf) | **Implemented in `pipeline-flexibility`, merged to main 2026-05-31 (`e53cbaf`).** `Plan.entry_mode` field (`direct`\|`brainstorming`); `writing-plans/SKILL.md` Step 0.5 ("Resolve feature directory") with 4-branch stale-`.active-feature` conflict detection (ported from brainstorming), worktree/branch guard, optional `check-distillation.sh` when a distilled spec is supplied, and entry-mode recording in plan YAML frontmatter. Hook enforces the plan-quality half (plan-validation-gate). Entry-mode dial for the planning pipeline, sibling to `adaptive-enforcement-tiers`. |
| P2 | Reliable project closeout (lifecycle close) | user-raised | friction, quality | M–L | open | Closeout is purely manual + under-cleans, and there's no persisted "done" signal, so completed features pile up unclosed (`.active-feature` left set, feature dirs never archived). **Decisions:** archive feature dir → existing `docs/imp-plans/archive/`; trigger via SessionStart nudge + entry-time GC (no Stop-hook auto-prompt — would fire mid-smoke-test). **(a) Foundation [S]:** add `completed_at`/`status` to `SddSession`, written by `controller-checkpoint.py` at pre-completion gate PASS; gitignore `.sdd-session.json` ✓ **shipped 2026-05-30** (was un-ignored; written to the *feature dir* so the entry is no-leading-slash. One instance — the merged sdd-hook-improvements manifest — is already committed; `git rm --cached` pending user decision on whether to keep it as feature history). **(b) Closeout [M]:** extend `finishing-a-development-branch` to archive the feature dir + remove the manifest (today it only clears `.active-feature`/`.allow-main`/branch/worktree). **(c) Discovery [S–M]:** SessionStart nudge **gated on `.active-feature` present in repo root AND manifest `status: complete` AND nothing in-flight** — stays silent in BiG/outside/non-superpowers repos (no `.active-feature` → instant no-op). **(d) Backstop [M]:** entry-time GC in brainstorming/writing-plans using the signal; in-flight detection (uncommitted changes, branch≠main, unchecked tasks, pending deviations) protects active work. Pairs with P1 as "lifecycle hygiene" (clean entry + clean exit). |
| N4 | Make pre-completion gate archive-aware | N2 SSOT audit + pipeline-flexibility deviations (Module 1→2) | quality | M | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Task 1).** `controller-checkpoint.py` `find_report_file`/`find_all_report_files` now recurse into `archive-*/` — the pre-completion gate passes with archived completed-module reports. (Chosen the recurse-into-`archive-*` option, not the manifest-trust option.) **Original analysis:** `controller-checkpoint.py` `find_all_report_files`/`find_report_file` (L186–189, L121–127) glob only `reports_dir`, not `archive-<module>/`. After a `transition-module.py` archive, completed-module reports read as "missing" at the final gate. Fix: recurse into `archive-*` subdirs, or read `module_reports_archived` from the manifest and trust prior gates. **Pairs with N3** — together they make multi-module SDD runnable through the prescribed scripts (today both quirks force manual workarounds). |
| N5 | Make the fence-blind task-header regex fence-aware (BOTH sites) | N2 SSOT audit | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Fence-aware parsing at ALL sites (7+1 spec'd, plus 2 extra found: `task_zero_is_first`, pre-completion `all_task_ids`) via a single `_unfenced_content` helper, consolidated into `_report_utils.py` (SSOT, lazy pydantic import). 7 tests in `test_fence_aware_parsing.py`. Known minors → N20. **Original analysis:** `^###\s+Task\s+(\d+)` exists as TWO identically-valued regexes — `TASK_HEADER_RE` (`validate-plan.py`:48) AND `TASK_HEADER_PATTERN` (`controller-checkpoint.py`:58) — both must be fixed. Matches `### Task 91/93/94/95` **inside fenced code fixtures** in a plan, inflating the verification-ratio denominator (Check 8), the pre-completion checkbox count, AND `all_tasks_have_reports` (demanded reports for fixture task ids — confirmed false-positive that blocked the pipeline-flexibility pre-completion checkpoint; real tasks 0-9 all had reports). Fix: skip headers inside ``` fenced blocks (sibling of N9 dedup). Low risk, removes a silent miscount. |
| N6 | "Hook enforces this" framing pass on SDD SKILL.md | N2 SSOT audit (extends C6(a)) | friction | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `cbff47e`.** Doc-only, no behavior change. Apply the C6(a) treatment (§258–265, already done) to §282–286 (pre-dispatch checkpoint, enforced by Check 5c), §286/Check-6b (context summary, enforced by Check 6b), and §426–428 (report validation, enforced by Check 4b): state the hook enforces the gate automatically so the manual run is an optional early check, not required ceremony. Removes residual skip-guilt + false "controller-only honor system" impression. Pairs with any future SDD SKILL edit (file is at its word-count ceiling). |
| N7 | Pre-execution `source_contracts: FAIL` false positive | N2 SSOT audit + pipeline-flexibility deviations (Ingestion) + CLAUDE.md Hook Dev Gotchas | friction | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Pre-execution now reports OK (valid-absent) for `Source Contracts: None`/empty; PASS for non-empty; OK for absent. Kills the per-run accepted deviation; CLAUDE.md gotcha rewritten. **Original analysis:** `controller-checkpoint.py --phase pre-execution` reports `source_contracts: FAIL` on a legitimately-empty prose `Source Contracts: None` (treats "None" as present-but-malformed). Documented + accepted as a deviation **every run**. Fix: read frontmatter `source_contracts` field, or treat prose "None" as valid-absent. Recurring friction with a known fix. |
| N8 | F6 regression check is literal-substring brittle | N2 SSOT audit + pipeline-flexibility deviations (Task 6) | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `86ddb95`.** F6 now keys on intent (the presence of a Direct-entry signal) rather than an exact phrase. **Original analysis:** `validate-all-skills.py:569` greps for the literal `"invoked directly"` / `"skipping brainstorming"` to assert standalone-invocation guidance exists; a semantically-equivalent rewording silently FAILs (this execution hit it — required a minimal phrasing fix to satisfy F6). Fix: key on intent (e.g., presence of a "Direct entry" section/heading) rather than an exact phrase. |
| N9 | Extract `_task_ids_where(plan_contents, field, value)` SSOT helper | N2 SSOT audit + pipeline-flexibility deviations (Task 4) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** `_task_ids_where(plan_contents, field, value)` + `_load_all_plan_contents(manifest, git_root)` landed; all callers updated; also fixed a latent manifest-mode aggregation double-count/parent-omission. 7 tests. **Original analysis:** `controller-checkpoint.py` `_declared_minimum_task_ids` (L228) and `_verification_task_ids` (L263) are near-verbatim frontmatter parsers. Collapse into one `_task_ids_where(...) -> (set, parsed_any)`. ~20 lines, behavior-preserving (both ratio paths tested). Flagged by Task 4 quality review; mirrors architectural-principles "single source of truth for logic." |
| N10 | Make the hook's Check 5 Task-0 lookup archive-aware | pipeline-flexibility deviations (multi-module) — coined in `2026-06-01-sdd-enforcement-hardening` spec/plan | quality | S | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Task 2).** `sdd-pre-dispatch-hook.sh` Check 5's Task-0 lookup now globs `archive-*/` as well, so a Source-Contracts plan still finds an archived Task 0 at module 2. Local glob only — the shared `task_report_glob` helper is unchanged. Sibling of N4 (the checkpoint-side archive-awareness); together with N3 they make `transition-module.py`-driven multi-module SDD runnable without manual workarounds. (Coined as a feature-internal ID — went spec→plan without a prior open BACKLOG row; logged here for the audit trail.) |
| N11 | `transition-module.py` did not recompute `enforcement.context_summary_at` on module transition | discovered during `2026-06-01-sdd-enforcement-hardening` plan review | quality | S | done | **Discovered + resolved by `2026-06-01-sdd-enforcement-hardening` (Task 3).** `transition-module.py` did not recompute `enforcement.context_summary_at` on module transition — it stayed pinned to the completed module's midpoint, firing Check 6b early for later-module tasks. Fixed: `transition()` recomputes it for the next module's range. |
| N12 | Transition gate vs hook gate divergence (micro+modules) | `2026-06-01-sdd-enforcement-hardening` deviations (Task 3 spec review) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Transition now gates provenance ONLY on `enforcement.dispatch_provenance` (file-existence stays on review modes ≠ skip) — hook↔transition SSOT; micro+modules transitions PASS with self-review files and no dispatch log. SSOT-agreement truth table still green. **Original analysis:** `transition-module.py:validate_module_completion` gates provenance on `process_requirements.{spec,quality}_review_mode != "skip"`, while the hook gates on `enforcement.dispatch_provenance`. For a micro+modules plan (dispatch_provenance=False but modes=self_review≠skip), the transition would require provenance the hook never wrote (over-enforcement). Only reachable in micro+modules (which `validate-plan.py` already WARNs against). Fix: align the transition gate with `enforcement.dispatch_provenance`, or document the intent. Task 3 was spec-compliant (spec MANDATED `!= "skip"`). |
| N13 | plan.md Task-4 SSOT-test snippet un-runnable as written | `2026-06-01-sdd-enforcement-hardening` deviations (Task 4) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Backport corrected TWICE: the sprint plan's originally-prescribed backport was itself a no-op (plan defect caught by the remediated Task 3 quality review — the mkdir lines belonged at the top of `test_minimum_signal_agreement`, matching the shipped test, not in `_hook_requires_quality_prov`). Hardening plan.md now matches the shipped test. **Original analysis:** The canonical Task 4 test snippet in `docs/imp-plans/2026-06-01-sdd-enforcement-hardening/plan.md` passes nonexistent subdirs to two subprocess drivers (git-init-before-mkdir → `FileNotFoundError`); the SHIPPED test added two `mkdir()` lines. Fix: backport the 2 `mkdir` lines into the plan's verbatim block so a future re-run from plan.md doesn't re-hit the error. Plan-hygiene; not ship-blocking. Instance of C1 (plan-reference code unrun). |
| N14 | C1 pre-existing pipe bug also present in main's advisory skill-enforce hook | `2026-06-01-sdd-enforcement-hardening` deviations (Task 0) | quality | S | done | **RESOLVED by the merge to main (`e708d9b`, 2026-06-03).** The `grep`-into-`grep -q` (piped) `pipefail` SIGPIPE bug was latent in main's then-advisory `sdd-skill-enforcement-hook.sh`; the sprint's de-piped, now-blocking hook replaced that copy at merge (verified: advisory heredoc gone, `exit 2` live). |
| N15 | Hook-integrity baseline (`baseline.txt`) was stale | `2026-06-01-sdd-enforcement-hardening` deviations (Task 7 verification) | quality | S | done | **Resolved by `2026-06-01-sdd-enforcement-hardening` (Task 7 fix-now, commit 52f130f).** `tests/ARaymond-hook-baseline/baseline.txt` was last captured 2026-04-14 and never re-captured across the SDD-hook-improvements + pipeline-flexibility merges, so `check-hooks.sh` FAILed on 5 hooks even on main (registration unchanged; sha256 hash drift only). This feature's Tasks 0+2 legitimately edited 2 more hooks. Fix: `check-hooks.sh --capture` regenerated all 7 hashes (the 5 pre-existing verified byte-identical to main = deployed reality); committed alone at 52f130f. Process gap: editing a baselined hook should re-capture the baseline in the same change (like migrations+code). |
| N16 | `validate-report.py` rejects `task_type:verification` reports (empty files_changed) | `2026-06-01-sdd-enforcement-hardening` deviations (Task 7 verification) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** `ImplementerReport.task_type` field added; `files_changed_non_empty_for_done` exempts verification; CLI fixture tests prove the full validate-report.py pipeline accepts a verification report with empty files_changed and still rejects implementation+DONE+empty. Non-last verification tasks unblocked. **Original analysis:** First live verification run surfaced it: `ImplementerReport.files_changed_non_empty_for_done` (implementer_report.py:48-53) raises when status is DONE/DONE_WITH_CONCERNS AND `files_changed` is empty — but a `task_type:verification` task legitimately changes ZERO files. `validate-report.py` FAILs on a correct verification report (not gamed with a fake file). Not a mechanical blocker for the last task (no hook Check 4b on it; pre-completion runs no per-report validation), but blocks any future verification task that ISN'T last. Pipeline-flexibility introduced verification tasks but never updated the report model. Fix: add `task_type` to ImplementerReport and exempt verification, or relax the rule when files_changed empty + tests.result PASS. |
| N17 | `transition-module.py` verification-id source has no main-plan fallback | `2026-06-01-sdd-enforcement-hardening` final code review (Task 3) | quality | S | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Main-plan fallback added (mirrors the hook). Known residual: hook checks `-f` existence, transition checks truthiness — SET-but-missing module.file diverges fail-closed → N19. **Original analysis:** `validate_module_completion` reads verification task_type only from the completing module's `module.file` (`if module.file:` → empty set otherwise, transition-module.py:110), whereas the hook falls back to `MANIFEST_PLAN_FILE` when `active_module_file` is empty (sdd-pre-dispatch-hook.sh:297-298). Harmless today (multi-module manifests always set `module.file`; single-module plans never transition), but a latent asymmetry/foot-gun if a future manifest shape omits `module.file`. Fix: add the same main-plan fallback, or document why the guard is intentional. |
| C1 | Plan-reference code execution validation | skill-eval §2.1 | quality | L | open | **Highest correctness leverage.** Reference snippets in plans are approved unrun; this fork's own `compute_midpoint` bug shipped 3×. plan-document-reviewer is codebase-isolated by design and can't catch it. Needs new fixtures + design. |
| C2 | Integration-test coverage requirement | skill-eval §2.2 | quality | M | done | **Done 2026-06-10 (`sdd-cleanup-and-integration-gate`, sprint-3, 2-module SDD run — first live `transition-module.py` transition).** Full C2 shipped: `IntegrationTest` model + `Plan.integration_test`; validate-plan advisory risk-surface WARNING (+sections entry); pre-completion **Check 10** (`integration_test_present`) — declared path must `is_file()` AND be in the feature changeset (untracked ∪ diff vs NEWEST merge-base among origin/HEAD/main/master — the stale-unpushed-origin fail-open was found live and fixed; present-but-malformed declarations FAIL with shape guidance — final-review fix 911f025); writing-plans docs; e2e Step 11. 22 tests in `test_c2_integration_gate.py` incl. the BLOCKING merge-base fixture. Follow-ups → N22/N25. **Original analysis:** Unit coverage enforced, e2e isn't (a cache-poisoning vuln + unreachable endpoint passed all unit tests). Plan-time declaration for contract/route/security-touching features + hook check. |
| C3 | Migrate gameable gates from controller-invoked → PreToolUse hooks | skill-eval §10 | quality | L | open | The meta-pattern: out-of-band hooks hold; controller-invoked steps get skipped under pressure (minimum-tier abuse, forged `.dispatch-log`, honesty run only when prompted). B8 must be designed within this principle. |
| C4 | Computed tier recommendation heuristics | adaptive-tiers v1.1 (deferred) | quality | M | open | Flag high-risk tasks (auth/credentials/migrations) wrongly downgraded by task-count alone. Natural guard for B6. |
| C5 | Cross-artifact validation (Pydantic Phase 3) | pydantic Phase 3 (deferred) | quality | L | open | Detect manifest↔plan↔reports↔deviations drift (e.g., "manifest says spec-review dispatched but report is controller-written" — catches the forgery class behind C3). |
| C6 | Resolve the duplicated/manual context-budget step | adaptive-tiers v1.1 + live honesty-check evidence | friction, quality | M | (a) done 2026-05-30 · (b) open | **Reframed — not a "usability" fix.** **(a) shipped:** SKILL.md §257–269 manual prescription retired → now states the hook enforces it automatically (SDD skill 5029→4753 words; regression 145 PASS). Root cause: SKILL.md §257–269 prescribes a *manual* run of `estimate-task-tokens.py` before each dispatch, but the pre-dispatch hook Check 6 (L660–708) **already runs it automatically** and blocks `TOO_LARGE` — so the manual step is redundant ceremony, correctly skipped on judgment (live evidence: honesty-check "skipped required step"). The two have drifted (skill passes `--task-file --constraints-file`; hook passes `--plan-file --task` only), and *both under-measure*: they count task text (+constraints for the skill variant) but NOT real injected context (source files the task lists, CLAUDE.md refs, prior reports, scene-setting) — so a small ≤200-line task can still dispatch a huge prompt. Note the 200-line cap is only a `validate-plan.py` WARNING, not a blocker, so the "validated ≤200-line plan ⇒ within budget" proxy is unsound. **(a) [S, quick win]** retire the manual prescription in SKILL.md → state the hook enforces it automatically (single source of truth; removes skip-guilt). **(b) [M]** strengthen the hook auto-estimate to pass `--constraints-file` (script already accepts it) + estimate injected context. Instance of C3 (controller-invoked → hook-enforced). Strengthened estimate gives B10 a real pressure signal. |
| I1 | Per-feature pipeline too heavy for refactors | inferred (time table) | friction, speed | — | open | Structural; substantially covered by P1 + B6. Track here so it isn't re-discovered. |
| I2 | Subagent dispatch overhead dominates test/doc tasks | inferred | speed | — | open | Covered by B6 / B7. |
| I3 | Honesty check structurally defeated in autonomous runs | inferred | quality | — | open | = B8. |
| I4 | Report validators (`result ∈ {PASS,FAIL}`, `passing ≤ written`) | inferred | — | — | won't-do | Validators are correct — keep them. The pain was diagnosis, fixed by B2. Logged so they aren't loosened by mistake. |
| I5 | Dispatch-log cold-start cascade | inferred | friction | — | open | = N1. |
| N18 | Checkpoint pre-dispatch lacked the hook's N3a module-boundary skip-guard | LIVE — first `transition-module.py` transition, sdd-cleanup sprint-3 | quality | S | done | **Found + fixed in-feature 2026-06-10 (`sdd-cleanup-and-integration-gate`, commits c45f5f7 + 9d0e9c8).** First live transition archived module-1 reports; `controller-checkpoint.py --phase pre-dispatch` for module-2's first task FAILed on the previous task's archived reports (the hook had the N3a guard since hardening; the checkpoint never got it). Fix: previous-task checks SKIP when `previous_task < manifest task_range[0]`; detail distinguishes prior-module vs no-Task-0 cells via `manifest_has_prior_modules`; boundary completion re-verified by transition + terminal pre-completion backstops. 5 tests. |
| N19 | Hook↔transition `module.file` fallback divergence (set-but-missing) + transition cosmetics | sdd-cleanup Task 6 reviews | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `ae05d8a`.** `transition-module.py` now adopts the stricter AND-exists fallback (set-but-missing module.file no longer yields an empty exemption set); dead `verif_ids` initializer and line-number comment rot cleaned up in one pass over `validate_module_completion`. **Original analysis:** Hook checks `-n` AND `-f` (existence) before using module.file; transition (N17 fix) checks truthiness only — a SET-but-MISSING module.file falls back in the hook but yields an empty exemption set (fail-closed) at transition. Spec-faithful; arguably the hook should adopt the stricter semantic. Plus: dead `verif_ids = set()` initializer (both branches reassign) and line-number comment rot ("hook lines ~294-299" → name the construct). One pass over `validate_module_completion`. |
| N20 | Fence-helper edge cases: tilde fences + unclosed-fence-at-EOF | sdd-cleanup Task 3 quality review | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `a8a76cf`.** `_unfenced_content` now honors `~~~` fences and an UNCLOSED fence at EOF emits a WARNING instead of silently blanking the file; `_load_script` importlib loader hoisted (D15). **Original analysis:** `_unfenced_content` ignores `~~~` fences (CommonMark-legal; repo uses backticks only) and an UNCLOSED ``` at EOF blanks the rest of the file — fails closed in validate-plan (header/frontmatter mismatch) but fails OPEN in `all_tasks_have_reports` (missing tasks silently skipped). One characterization test + optional tilde support; single SSOT site now (`_report_utils.py`). |
| N21 | `create_manifest` test helper not tier-faithful (`context_summary_at`) | sdd-cleanup Task 5 reviews | quality | S | open | Carried into `sdd-aggregate-gate-visibility` (not scoped this sprint); still open. Helper hardcodes `context_summary_at: 2` for ALL tiers; real micro materializes `None` — micro-tier fixtures exercise the N11 recompute branch real micro skips (fixture-fidelity gap vs "fixtures must match actual system output"). Make the override tier-faithful (profile value; override only standard). |
| N22 | C2 risk-surface scan calibration (stemming + fence-awareness) | sdd-cleanup Tasks 9/11 | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `efc9204`.** `_C2_RISK_PATTERNS` now uses stem-style patterns (`auth\w*`, `migrat\w*`, …) and the scan runs over `_unfenced_content(content)` so plans quoting keywords in code fences no longer self-warn. **Original analysis:** `_C2_RISK_PATTERNS` is singular-only (`migrations`/`caches`/`routers`/`authentication` all miss) and `routes/` needs a following word-char (misses end-of-token); the scan is raw-content so plans QUOTING keywords in code fences self-warn (module-2's own plan does — quote-vs-use limitation, advisory-only). Fix: stem-style pattern (`auth\w*`, `migrat\w*`, …) + optionally scan `_unfenced_content(content)`. |
| N23 | `extract-execution-trace.py` dead `STATUS_VALUE_PATTERN` ref + fix-dispatch misattribution | sdd-cleanup Task 9 quality review + trace audit | quality | S | open | Carried into `sdd-aggregate-gate-visibility` (not scoped this sprint); still open. (a) extract-execution-trace.py:39-40 reads nonexistent `_mod.STATUS_VALUE_PATTERN` BEFORE `VALID_STATUSES`, so the AttributeError aborts the try and the hardcoded fallback set is ALWAYS used — the lazily-preserved SSOT export has zero live consumers; fix the dead ref or delete the export. (b) The extractor misattributes ad-hoc fix dispatches (task_number 1 / -1000) and lacks cross-session records — weakens the "0 anomalies" signal. |
| N24 | Python 3.9 PATH-`python3` FAILs all frontmattered plans (pydantic subprocess) | sdd-cleanup Task 9 quality review | quality | S | open | Carried into `sdd-aggregate-gate-visibility` (not scoped this sprint); still open. `sdd_session.py:33` uses PEP 604 `int \| None`; validate-plan's pydantic subprocess uses `sys.executable` — under a 3.9 PATH python3 it exits 1 → BLOCKER on every frontmattered plan (fails CLOSED). Not live here (PATH python3 = 3.14) but a portability trap. Fix: Optional[] in models, or pin the subprocess to the venv python. |
| N25 | Check 10 follow-up batch | sdd-cleanup Task 10 + fix reviews | quality | S–M | open | **Partly done 2026-06-22 (`sdd-aggregate-gate-visibility`):** (a) `c79531b` (Check 10 feature-window fallback — on-main false-block fix); (b,d,f) `077cd92` (line-anchored frontmatter scan in `_integration_test_paths` + directory/malformed diagnostics that name the source plan file); (c) `c06b230` (module-level `_git_run` SSOT across 3 git sites). **STILL OPEN, pointed at this feature:** (e) checkpoint's own git calls not config-nulled (only test fixtures hardened); (g) newest-merge-base heuristic unprobed on exotic topologies (long-lived release branches). **Original analysis:** (a) on-base-branch false-block: remoteless repo + SDD on main (`.allow-main`) → merge-base==HEAD makes COMMITTED integration tests invisible → hard FAIL despite legit work (Aaron prefers improving on-main flows); (b) `content.find("---", 3)` not line-anchored in `_integration_test_paths` (inherited from `_task_ids_where` — fix both together); (c) third near-identical git subprocess wrapper → module-level `_git_run` SSOT; (d) directory-as-declared-path says "missing on disk" (fail-closed but misleading detail); (e) checkpoint's own git calls not config-nulled (only test fixtures hardened); (f) malformed-declaration messages don't name the source plan FILE; (g) newest-merge-base heuristic unprobed on exotic topologies (long-lived release branches). |
| N26 | Dispatch-log classification gaps: fix/re-review dispatches + gate-artifact filenames | sdd-cleanup trace audit + live Check 3b block | quality | S | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `7dc7812`.** Hook now logs review-driven FIX dispatches + partner RE-review rounds (fix/re-review markers, `type=fix`/`type=fix-unattributed` lines) and Check 3b's allowed-prefix list admits the gate-required report names. Baseline recaptured in the same change. **Original analysis:** (a) Review-driven FIX dispatches and partner RE-review rounds log as `type=unknown` or not at all (reviewer-stage passthrough without task number) — the tamper-evidence backbone can't see the fix cycle (N18's fix + 2 reviews left zero entries; corroboration required session-jsonl forensics). (b) Hook Check 3b's allowed-prefix list excludes the GATE-REQUIRED report names (`honesty-check-*`, `execution-trace-audit.md`, `final-code-review.md`, `checkpoint-pre-completion.json` is exempt as .json) — benign today (those artifacts appear after the last implementer dispatch) but a latent block for any post-gate implementer dispatch in the same workspace (bit this session via `fix-n18-*` names). |
| N27 | Pre-completion AGGREGATE gates only police the final module | sdd-cleanup final review (Important #2) | quality | M | done | **Done 2026-06-22 (`sdd-aggregate-gate-visibility`) — Check 7 `c27fd79` + Check 9 `9039c97` + docs `f3cfd72` + Task-14 archive-aware e2e proof (this feature's commit).** Both aggregate inputs are now archive-aware: Check 7's `_review_tiers_per_task` globs `archive-*/` (archived module reviews stay in numerator AND denominator) and Check 9's `_merged_dispatch_times` merges archived `archive-*/.dispatch-log` files so verification windows survive live-log truncation. e2e Step 12 is the in-sprint end-to-end proof (H1 self-hosting hazard: live hooks run main's pre-fix scripts, so the e2e — which exercises THIS checkout — is the only archive-aware run this sprint). **Original analysis:** Now that transitions work: Check 7 (min-tier ratio) reads observed tiers via the intentionally-flat glob (archived module reviews leave numerator AND denominator) and Check 9 (git-reality) skips tasks absent from the truncated live dispatch log. Per-task existence/provenance IS boundary-verified by `validate_module_completion`; only cross-module POLICY aggregates lose visibility. Fix options: archive-aware ratio/git-reality inputs (archived dispatch logs exist under `archive-*/`), or transition-time aggregate snapshots. Deserves its own planned task. |
| N28 | Test-hygiene batch from sprint-3 reviews | sdd-cleanup Tasks 4/9 minors | quality | S | open | **(c) done 2026-06-22 (`sdd-aggregate-gate-visibility`) — `a8a76cf` (`_load_script` hoist, D15).** (a,b,d) carried into this feature, still open. (a) N7 test asserts `!= FAIL` — upgrade to `== OK` + blocker-absence (the actual N7 payload); (b) pre-execution three-way arms (non-empty→PASS, absent→OK) have no direct unit coverage; (c) `_load_script` importlib loader now duplicated in `test_fence_aware_parsing.py` + `test_c2_integration_gate.py` — hoist to `sdd_test_helpers.py` on next touch; (d) C2 WARNING `frontmatter=None`-with-risk + explicit-null cells got tests in Task 10 Step 0 — verify they survived (they did; listed for completeness). |
| N29 | Port upstream v6 `writing-skills` authoring additions (Match-the-Form-to-the-Failure + Micro-Test Wording) | upstream v6 sync 2026-06-22 (assessment §A3) | quality | S | open | The remaining Category-A item from the v6 sync (A1/A2/A4 already taken). Content-port two new sections into `skills/writing-skills/SKILL.md`: **"Match the Form to the Failure"** (a table for picking prohibition-vs-worked-example guidance — a flat "don't do X" works for discipline slips but backfires when the problem is output *shape*) and **"Micro-Test Wording"** (sample a phrasing a handful of times against a no-guidance control and read every result by hand before committing). Upstream commits `cbc8273` (+ `fdb0f42`, `1aa45d2`). **Do NOT `git cherry-pick`** — those commits are entangled with the vendor-neutral prose rewrite (CSO→SDO, "your agent") we deliberately decline (assessment §D3); hand-port the section content only. `writing-skills/SKILL.md` is near its word-count ceiling — offset with a `references/` extraction if needed; re-run `validate-all-skills.py` after. Aligns with `docs/prompting-best-practices.md`. |
| N30 | Adopt architecture-neutral SDD cost wins from the upstream v6 review rewrite | upstream v6 sync 2026-06-22 (assessment §B-neutral) | speed, quality | M | open | Three pieces of upstream's v6 SDD rewrite that are **independent of the one-vs-two-reviewer architecture** and so takeable regardless of N31's outcome: **(1) file-based handoff** — upstream's `task-brief` + `review-package` scripts write task text + review diff to files for the subagent to read instead of pasting into context (upstream calls the pasted diff "the single biggest reviewer cost"); extends our existing `reports/` file pattern. **(2) enforced model-naming per dispatch** — our `~/.claude/rules/workflows.md` already *wants* this; upstream made it a template requirement after an unnamed model silently inherited the session's top tier (one run put all 26 reviewers on the most expensive model). **(3) progress ledger for resume** — compare to our `context-summary.py` + checkpoint files; may be redundant or may improve. Evaluate each against our hook-enforced model; (2) likely the cheapest high-value adopt. Pairs with N31. |
| N32 | Clean up shellcheck findings surfaced by `lint-shell.sh --all` | A2 linter first `--all` run, 2026-06-23 | quality | S | open | The first full run of the A2-ported linter flagged ~5 minor warnings — **NONE of the severe SIGPIPE/`set -u` fail-open class.** In 2 baselined enforcement hooks: `handoff-acceptance/scripts/handoff-gate-hook.sh:38` (SC2044 — `for` over `find` output, fragile on whitespace paths) and `subagent-driven-development/scripts/sdd-pre-dispatch-hook.sh:236` (SC2034 — unused `label`, dead-code smell per architectural-principles). Plus `brainstorming/scripts/start-server.sh:100` (SC2164 unguarded `cd`) + `:125` & `stop-server.sh:26` (SC2034 unused `i`); and several `tests/` scripts (SC2064 trap-quoting, SC2320 `$?`-after-echo, SC2010 `ls\|grep`). Fix the 4 skill/hook scripts first — **editing the 2 baselined hooks requires a `check-hooks.sh --capture` baseline re-capture in the same change** (ship together). Test scripts lower priority. Deferred out of the v6-sync commit to keep that scope clean. |
| N31 | STRATEGIC DECISION: one-reviewer-two-verdicts (v6) vs our two-reviewer + partner + provenance model | upstream v6 sync 2026-06-22 (assessment §B-fork) | speed, quality | L | open (needs decision session) | The centerpiece of the v6 sync. Upstream collapsed two per-task reviewers → ONE `task-reviewer-prompt.md` (spec + quality verdicts in one pass, plus a "can't-verify-from-diff" verdict) + one whole-branch review on the top model at the end. Eval-claimed **~2× faster / ~50% fewer tokens**. **Confirmed collision with our enforcement:** `sdd-pre-dispatch-hook.sh` classifies dispatches into spec-review/quality-review/trace-audit/partner-review (lines 163–166) and **line 478 hard-blocks** any task dispatch missing a per-task `task-NNN-spec-review.md`; we still ship `spec-reviewer-prompt.md` + `code-quality-reviewer-prompt.md`. Adopting v6's combined prompt is **not a prompt swap — it's rearchitecting the enforcement layer** (Checks 4b/4c, dispatch provenance, partner gate). The real question: upstream brings outside eval evidence that our two-reviewer+partner+provenance model may be **over-engineered on cost**. Options: **(a)** keep our model, take only N30 cost wins; **(b)** pilot v6 one-reviewer in a throwaway branch, measure against our enforcement suite, decide on evidence; **(c)** full migration (largest; unwinds significant fork investment). Default **(a)/(b)**, not (c). Note: v6's anti-gaming rules (controller can't tell a reviewer what to ignore; reviewers read-only + skeptical of "left it unabstracted on purpose" rationales) align with our philosophy and could be ported into our *existing* two-reviewer prompts independently of this decision. |
| N33 | controller-checkpoint.py pre-dispatch not verification-aware for the PREVIOUS task | sdd-aggregate-gate-visibility Task 14 (live-discovered) | quality | S | open | Pre-dispatch FAILs `previous_spec_review`/`previous_quality_review` when the PREVIOUS task is a `task_type: verification` task (which legitimately has no reviews). The live HOOK is verification-aware (`sdd-pre-dispatch-hook.sh:504-505` skips Checks 4b/4c when `PREV_TASK_TYPE=verification`); `controller-checkpoint.py` pre-dispatch (~lines 1244-1299) is NOT — a checkpoint-script-only false-FAIL (the real gate, the hook, allows the dispatch). Direct sibling to N18 (checkpoint lacked the hook's module-boundary skip-guard). Fix: mirror the hook's PREV-verification skip in the checkpoint pre-dispatch previous-review checks. **(Renumbered N29→N33 on merge: the upstream-v6 sync independently claimed N29–N32.)** |
| N34 | Promote `_frontmatter_block` to `_report_utils.py` — codebase-wide line-anchored frontmatter SSOT | sdd-aggregate-gate-visibility Task 9 quality review + trace audit | quality | S | open | Task 9 (N25b) added a line-anchored `_frontmatter_block` helper to `controller-checkpoint.py` but it is LOCAL to that file. **7 other scripts still use the naive `content.find("---", 3)`** that the helper fixes: `validators.py:56`, `materialize-manifest.py:48`, `validate-plan.py:540`, `transition-module.py:68`, `validate-report.py:95`, `context-summary.py:165,187`. The report validator's naive split bit this very session (a `^---$` inside a Task-9 report-frontmatter value truncated the YAML). No correctness impact today (those sites haven't hit a triple-dash-in-value), but it is a real SSOT gap. Fix: promote `_frontmatter_block` into `_report_utils.py` (stdlib-only — keep validate-plan.py bare-python3 safe) and migrate all 8 sites. (Promised in the Task-9 deviation; created 2026-06-23 by the trace audit's MUST-FIX.) **(Renumbered N30→N34 on merge: the upstream-v6 sync independently claimed N29–N32.)** |
| N35 | pre-dispatch hook Check 5c remediation command + the 3 SKILL.md "Controller Health Checkpoints" commands omit `--manifest` → module-boundary checkpoints fail | live `inbox-agent-int` v1-core-pipeline (first downstream module boundary following the hook's emitted checkpoint cmd, post-N18; Module 1→2) | friction | S | done 2026-06-26 (applied to main working tree; uncommitted) | **Surfaced at the first module boundary in a real downstream SDD session that followed the hook's emitted Check-5c command verbatim.** (Sprint 3's 2026-06-10 transition — the first-ever live `transition-module.py` run — built the N18 machinery in THIS repo and drove the checkpoint with `--manifest` directly, so it never exercised the hook's emitted command. N18 also only shipped in Sprint 3, so pre-N18 no boundary-skip existed to arm.) `sdd-pre-dispatch-hook.sh:644` emitted a checkpoint command with `--plan-file <plan.md>` and **no `--manifest`**, so controller-checkpoint.py's N18 boundary-skip (needs `manifest_task_range`, set only with `--manifest`, lines 1113–1118 / 848–851) never armed → the checkpoint hunted the prior (archived) module's reports in the flat `reports/` dir and FAILed; the controller rediscovered `--manifest` by trial-and-error (exactly the transcript). Fix: hook now emits `--manifest $MANIFEST` (drops the unfilled `<plan.md>` placeholder — `--plan-file` is "Required *unless* --manifest", and the manifest resolves plan_file). Companion: the 3 SKILL.md commands (§272–292) used a **non-existent `--feature-dir` flag** (that flag is on `materialize-manifest.py`, not `controller-checkpoint.py`), omitted the *required* `--deviations-file`/`--reports-dir`, and lacked `--manifest` — reconciled to the e2e-verified `--manifest` form. Only bit at module boundaries (why it surfaced now). Baseline re-captured; unit 497 / regression 143-0-2 / e2e 13 green. Sibling to N18/N33 (hook↔checkpoint drift). |
| N36 | Post-implementation `/simplify` cleanup pass at the SDD pre-completion gate | user-raised 2026-06-26 (this session) | quality | M | open | **Design captured; revisit later.** Idea: after code is written following a plan, run the built-in `/simplify` (reuse / altitude / efficiency cleanup that *applies* fixes — distinct from the existing SDD review passes that only *report*). **Decisions reached this session:** **(1) Runner = the controller (orchestrator)** — NOT the implementer subagent (`/simplify` itself fans out to the `code-simplifier` agent, so a subagent can't reliably spawn it / invoke the Skill machinery; the implementer is also scoped to one task's write-scope, and running it there breaks fresh-agent isolation) and NOT the partner (verify/read-only role — must not mutate code). The controller is the only seat in the dispatch tree allowed to have children + invoke skills. **(2) Timing = ONCE at the Pre-Completion Gate** (`SKILL.md:120`, immediately before "Dispatch final code reviewer") — NOT per-module-boundary. The highest-value simplification is *cross-module* reuse, which only becomes visible once all modules exist; a per-boundary pass is structurally blind to it AND risks "simplifying away" a shared seam (e.g. an M1 foundation helper) that later modules import; it also costs 6× the multi-agent fan-out for zero reduction in final-review diff size (simplify edits land as commits either way). A module is an execution-ordering unit, not a shipping unit — if the end-of-feature diff feels too big, the *feature* was too big (split into 2 SDD runs). Required sequence: **simplify → re-run tests (incl. the declared `integration_test`) → final code reviewer** (the review must see the post-simplify code, else it's stale). Per-module only as an explicit opt-in for genuinely independent vertical-slice modules; off by default. **(3) Scope = source files ONLY**, computed from the union of implementers' `files_changed` (already recorded per task in `ImplementerReport`) MINUS tests, fixtures, generated/vendored files, and SDD process artifacts — NOT a raw `git diff base...HEAD`. This is the answer to "would it ever run against non-code?": YES if diff-scoped, and it is actively destructive — it would try to "simplify" golden / byte-identity fixtures, SQL lifted byte-for-byte from a spec, the `reports/` + `deviations.md` audit trail, CLAUDE.md / README prose, and tests (weakening the very guardrail validating the refactor). Only the controller at pre-completion holds the complete file inventory to compute this scope safely — a second reason for end-of-feature timing. **(4) Plan vs SKILL.md = SKILL.md.** "Always simplify after writing" is a uniform *process* rule (identical in every plan) → belongs in the skill = single source of truth; baking it into each plan re-creates the "forget it under pressure" failure mode + duplication. A frontmatter field whose value would always be identical is a smell. The ONLY per-feature signal that legitimately belongs in plan frontmatter is an **exclusion list of simplify-hostile source paths** (byte-identity SQL, golden generators) — that is per-feature *data*, not the simplify decision. **(5) Enforcement (fork philosophy: advisory SKILL.md text gets skipped under pressure):** Phase 1 = SKILL.md Pre-Completion Gate step (cheap, ships now, advisory); Phase 2 = `controller-checkpoint.py` pre-completion check requiring a `reports/simplify-pass.md` artifact (>50 bytes), tier-gated to SKIP on micro — same pattern as the honesty / trace-audit gates; opt-out via `simplify_pass: false` frontmatter that defaults to ON (safe default = on, skipping is the deliberate act). **Open decisions for the revisit:** tests in/out of simplify scope (lean out — risk of weakening assertions); non-redundancy with the existing per-task quality reviewer + final code reviewer (additive because it *applies* vs they *report*, and it's holistic/cross-file vs per-task); commit convention (its own `refactor: simplify <feature>` commit, not amending task commits). **Verify before building:** `/simplify` is a built-in Claude Code skill (not in this fork) — confirm empirically whether it dispatches subagents (the runner conclusion holds either way). Touches SDD SKILL.md + `controller-checkpoint.py` + a new report artifact + `Plan` frontmatter (exclusion list / opt-out) → M. Next step = `superpowers:brainstorming` to pin timing / exclusion-field shape / enforcement phase, then a small plan. |
| N37 | Make per-role model/effort picks executable (encode allocation defaults into dispatch templates) | user-raised 2026-07-06 (this session) — model/effort allocation analysis | friction, speed | S–M | open | **Make the per-role model/effort picks executable, not advisory.** Today `subagent-driven-development/references/model-selection.md` (rewritten 2026-07-06 with the two-dial capability/effort framing + per-role table + current-model footnote) is guidance the *controller reads and applies by judgment* at each dispatch — the kind of controller-invoked step that gets skipped under pressure (cf. C3). N37 bakes a `Recommended model / effort` line into each SDD dispatch prompt template (`implementer-prompt.md` / `spec-reviewer-prompt.md` / `code-quality-reviewer-prompt.md` / `trace-auditor-prompt.md` / `pre-execution-audit-prompt.md`) so the pick travels *with* the template. The pattern already half-exists: `controller-partner-prompt.md` header already says "**Model:** Use haiku". **Two honest constraints.** (1) *Rot:* hard-coding model IDs into N templates reverses the model-neutral-body design that keeps a model launch touching only ONE footnote (validated 3× on 2026-07-06 across the Sonnet 5 / Opus 4.8 / Fable 5 launches — only the footnote moved) — so encode *tier references* ("standard tier", pointing at `model-selection.md`), NOT literal IDs. (2) *Effort not wire-able via the dispatch surface:* SDD dispatches through the Agent/Task tool, which exposes a per-call `model` override but **no `effort` param** (effort is a Workflow-tool `opts.effort` concept / session-level setting) — so N37's *model* half is executable per-dispatch, its *effort* half stays advisory prose unless dispatch moves to the Workflow tool. **Recommended minimal version [S]:** add a one-line tier reference to the 2–3 highest-confidence/most-stable templates (implementer→cheap for well-specified tasks; reviewers→standard/capable; trace-auditor→standard+high), each pointing at `model-selection.md` for resolution — captures most of the consistency benefit, model IDs stay in the single footnote. **Full version [M]:** all templates + a controller-side tier-resolution step. Behavior-touching (changes what the controller actually dispatches) → own brainstorming→plan→SDD cycle, not a doc tweak. **Overlaps N30(2)** (upstream v6's enforced model-naming-per-dispatch): N30(2) is "name *a* model so it isn't silently the top tier" (a cost-safety guard); N37 is "name the *right* tier per role" (the allocation defaults). Do them together — N30(2) supplies the enforcement (must name a model), N37 supplies the defaults (which tier). Analysis + full rationale: `docs/process-improvement-findings/2026-07-06-model-effort-allocation-fable5.md`. Deferred pending one real SDD run that exercises the allocation. |
| N38 | Scope fences must survive spec distillation (negative contract material) | telemetry-exp stable-fact-store spec review 2026-07-07 — finding M1: spec.md §1.2 out-of-scope fence had no counterpart in spec-distilled.md | quality | S | done 2026-07-07 | **The distillation pipeline had no category for negative scope, and every layer leaned toward deleting it:** Rule 2 ("'we considered but rejected' text is stripped") pattern-matches an out-of-scope/deferred list; the distilled template had no fence section; the reviewer prompt's artifact-removal check flags alternative-language (could flag a PRESERVED fence as an artifact); `check-distillation.sh` had only negative checks. Since the distilled spec is the ONLY doc the plan writer + implementers read, a dropped fence has no downstream recovery path — scope fences are what stop an implementer from "helpfully" building deferred work mid-task. **RED evidence:** production M1 (fence fully dropped) + synthetic subagent baseline (fence demoted to one line inside Contract Facts, heading + per-item deferral targets lost — the current rules were followed *correctly*, so this was a rules defect, not distiller error). **Fix (R1–R5, TDD per writing-skills):** brainstorming SKILL.md Distillation Rule 3 "Scope fences preserved" (negative contract material; never merged/summarized/demoted; old rules 3–6 renumbered 4–7); `## Out of scope — do not build` template section preceding Contract Facts (matches the telemetry-exp remediation shape); distillation-review checklist item 6; `distillation-reviewer-prompt.md` fence-preservation check + explicit not-an-artifact carve-out + "near the top" tolerance for Contract Facts; design-presentation coverage now leads with scope boundaries (R5, so specs reliably HAVE a fence to carry); `check-distillation.sh` optional 2nd arg (full-spec path) FAILs (exit 1, `fence: MISSING` + `fence_detail`) when a heading-level out-of-scope/non-goals/do-not-build fence has no distilled counterpart — heading-level matching only (body prose doesn't count); one-arg mode unchanged (writing-plans direct entry calls it with one arg); script is NOT in the hook baseline (no re-capture). **GREEN:** same subagent scenario with updated rules preserved all 5 fence items + deferral targets; checker PASS/`fence: PRESENT`; JSON output valid in every branch (no pipes into `grep -q` — the C1 SIGPIPE class avoided by here-file greps in `if` conditions). 12 pytest cases (`tests/unit/test_check_distillation.py`) pin the one-arg contract + two-arg fence semantics. Suites: unit 509, regression 158/0/2 (pre-existing advisories), install PASS. |

---

## Recommended sequencing

Ordered by leverage; sequenced so nothing collides with the in-flight `sdd-hook-improvements` worktree.

1. **B1–B5 — ✓ DONE (merged 7030b25).** 3-stage hook classifier + `review_tier`.
2. **Quick wins — ✓ DONE 2026-05-30:** C6(a) manual token-estimate step retired from SKILL.md; `.sdd-session.json` gitignored (one already-committed instance pending untrack decision).
3. **B6, P1, N2 — ✓ DONE & merged (`pipeline-flexibility`, `e53cbaf`, 2026-05-31):** `task_type: verification` (B6, with keyword+ratio+git-reality anti-smuggling guard in place of C4), direct-`writing-plans` `entry_mode` (P1), and the SSOT audit (N2, see `2026-05-31-ssot-audit.md`).
4. **N3 + N4 + N10 + N11 — ✓ DONE & merged (`2026-06-01-sdd-enforcement-hardening`, `e708d9b`, 2026-06-03):** the multi-module hook fixes + N15 (baseline) + skill-enforce blocking (C5). `transition-module.py`-driven multi-module SDD now runs without manual workarounds. N14 resolved by the merge. **STILL OPEN:** **N1** (same file, `sdd-pre-dispatch-hook.sh` — fold into the next edit), and the sprint's own follow-ups **N12/N13/N16/N17**. **CAVEAT:** the multi-module + live `task_type:verification` paths are test-validated (unit+e2e) but NOT yet run in a real multi-module session — the next real multi-module SDD run is the first live exercise, and **N16 must land before any non-last verification task**.
5. **P2** — project closeout / lifecycle close; user-raised; shares `.active-feature` / completion-signal plumbing with the now-done P1.
6. **B8** — user-confirmed for unattended runs; design within C3's principle.
7. **C6(b)** strengthen the hook estimate; **C1** highest correctness leverage.
8. **C2 + C3** — trust-at-scale.
9. **Cheap SSOT cleanups — N5, N6, N7, N8, N9 — ✓ ALL DONE** (N5/N7/N9 in `sdd-cleanup-and-integration-gate` 2026-06-10; N6/N8 in `sdd-aggregate-gate-visibility` 2026-06-22, `cbff47e`/`86ddb95`).
10. **B10, C5** — opportunistic; B10 benefits from C6(b)'s real pressure signal.
11. **B7, B9, C4 (standalone)** — revisit; B7/B9 may prove unnecessary now that B6 has shipped.

Each open item becomes its own brainstorming → writing-plans → SDD cycle, validated by `validate-all-skills.py`, `tests/unit/`, and `tests/integration/sdd-e2e-test.sh`. C1/C2 additionally need a new fixture proving a known-bad reference snippet / missing integration path is now caught.

---

## Sprint 3 (planned 2026-06-03 — EXECUTED & merged 2026-06-10 as `sdd-cleanup-and-integration-gate`)

**Outcome:** Track 1 (N16/N5/N7/N1/N12/N13/N17 + N9) and Track 2 (C2) shipped in one 2-module SDD run; Track 3 partially proven — the multi-module path ran LIVE for the first time (clean transition; surfaced + fixed N18), but the plan declared no `task_type:verification` task, so the non-last-verification live exercise remains open. N6/N8 were NOT folded in this sprint (budget) — subsequently done 2026-06-22 in `sdd-aggregate-gate-visibility` (`cbff47e`/`86ddb95`). Follow-ups N19–N28 logged above (N19/N20/N22/N26/N27 + N25(a,b,c,d,f) + N28(c) closed by `sdd-aggregate-gate-visibility`). Original plan below for the audit trail.

Theme: **close the loop on sprint-1/2 features + raise the integration-test floor.** Anchor (Track 2) chosen by Aaron on 2026-06-03 — C2 over P2/C1/C3 because it's the only Critical-severity item that's M-sized and execution-ready, and its plan-time-declaration + hook-check fix shape is a concrete down-payment on C3's out-of-band-enforcement principle. Track-1 rows flip to `in-flight` when execution starts; this block is a planning artifact, not a status change.

### Track 1 — Cleanup bundle (one standard-tier feature; **N16 first**)

Deliberately bundled **now**, not folded in opportunistically (sequencing item #9) — opportunistic fold-in is the "I noted it but kept going" anti-pattern (`2026-05-21-skill-evaluation.md` §10.8). ~7 files (hook, checkpoint, `validate-plan.py`, `validate-all-skills.py`, `implementer_report.py`, `transition-module.py`, SDD SKILL.md) with real behavior changes ⇒ needs integration tests; the three bug-fixes carry a failing-test-first obligation.

| Item | Role in bundle |
|---|---|
| **N16** | Bug fix (test-first). Add `task_type` to `ImplementerReport`; exempt `verification` from `files_changed`-non-empty. **Unblocks Track 3.** |
| **N5** | Bug fix (test-first). Fence-aware `^### Task` regex in BOTH `validate-plan.py` + `controller-checkpoint.py`. |
| **N7** | Bug fix (test-first). Treat prose `Source Contracts: None` as valid-absent; kills a per-run accepted deviation. |
| **N1** | Hook preflight — accumulate all missing prereqs (same file as the just-merged hardening work). |
| **N12, N13, N17** | `sdd-enforcement-hardening` follow-ups: align transition gate w/ `enforcement.dispatch_provenance`; backport `mkdir` into plan snippet; add transition main-plan fallback. |
| N6, N8, N9 | Fold in if budget allows: hook-enforces-this framing pass; intent-based F6 check; `_task_ids_where` SSOT helper. |

### Track 2 — Anchor: **C2** integration-test gate

Lead with a short fixture spike (sequencing note already requires C2 to ship a fixture proving a missing integration path is now caught): design the plan-time declaration field for contract/route/security-touching features + the pre-completion hook check, validated against a fixture modeling the practerus M15 cache-poisoning miss. Then ship as a standard-tier feature.

### Track 3 — Prove it live (Phase-4 gap)

After N16 lands, run one real SDD session whose plan **deliberately includes a non-last `task_type:verification` task and crosses a module boundary** — the only exercise of the never-run-live multi-module + verification paths. (The Track-1+Track-2 work could itself be authored as a 2-module plan to serve double duty as this live exercise.)

### Cross-track sequencing

Track 1 and C2 **both** modify `sdd-pre-dispatch-hook.sh` and `controller-checkpoint.py`. Run **Track 1 before C2** so C2's new checkpoint check builds on a base where N5 (fence-blind miscount) and N7 (source-contracts false positive) are already gone — otherwise C2 inherits two known-buggy code paths.

### Deferred (not sprint 3)

- **Sprint-4 design spikes (need brainstorming first):** **C1** (plan-ref execution validation, L), **C3 + B8** (gameable-gates→hooks + async honesty, L).
- **Lower-leverage / revisit:** B7, B9 (may be unnecessary post-B6); B10 + C6(b) (paired — B10 wants C6(b)'s real pressure signal); C4; C5.

---

## Sources

- `docs/2026-05-28-sdd-session-assessment-btd-consolidation.md` — BTD consolidation SDD session post-mortem (B*/I* items).
- `docs/process-improvement-findings/2026-05-21-skill-evaluation.md` — cross-repo Critical findings (C1–C3).
- `docs/imp-plans/2026-05-17-adaptive-enforcement-tiers/` — deferred v1.1 candidates (C4, C6).
- `docs/plans/2026-04-24-pydantic-meta-design.md` — Pydantic Phase 3 candidates (C5).
- `docs/imp-plans/2026-05-28-sdd-hook-improvements/` — work addressing B1–B5 (merged 7030b25).
- Live SDD run in the `practerus-platform` repo — `docs/imp-plans/2026-05-27-marketing-cta-guard/` Task 1 pre-dispatch (2026-05-31); surfaced **N3** (no-Task-0 manifestation).
- `docs/process-improvement-findings/2026-05-31-ssot-audit.md` — SKILL.md-vs-hook SSOT audit (N2; sources N3–N9).
- `docs/imp-plans/2026-05-31-pipeline-flexibility/` — B6 + P1 implementation; `deviations.md` sources the execution-discovered hook/checkpoint gaps (N3 module-transition manifestation, N4, N5, N7, N8, N9).
- `docs/imp-plans/2026-06-01-sdd-enforcement-hardening/` — multi-module enforcement gap closure (N3/N4/N10/N11 fixed; N15 baseline fixed) + skill-bypass hook blocking (C5, incl. the C1 SIGPIPE de-pipe caught in review); **merged to main `e708d9b` 2026-06-03** (N14 resolved by the merge). `deviations.md` + `reports/final-code-review.md` + `reports/execution-trace-audit.md` source the follow-ups N12 (transition↔hook gate divergence), N13 (un-runnable plan snippet), N16 (validate-report rejects verification reports), N17 (transition verif-id no main-plan fallback).
- `docs/process-improvement-findings/2026-06-22-upstream-v6-sync-assessment.md` — upstream v6 (obra/superpowers, `896224c`) sync assessment vs our fork at merge-base `f2cbfbe`. Sources **N29** (A3 writing-skills authoring port), **N30** (architecture-neutral v6 SDD cost wins), **N31** (the one-vs-two-reviewer strategic decision). Records what was taken now (A1 ultrathink-keyword fix, A2 `lint-shell.sh`, A4 worktree global-dir removal) and what was deliberately declined (v6.0.3 `.git/` fix, new-harness support, vendor-neutral prose rewrite, evals submodule split).
